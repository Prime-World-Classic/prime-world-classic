// http_gateway_base_port
// начало диапазона портов, на которых слушают сервисы Http Gateway (в сторону соц. сервера)
// 34000 значение по умолчанию

http_gateway_base_port 34000

// Shared key for the back-end web-session push (gateway "web_session_register");
// the same keyApi the back-end signs its requests with. Empty = the push
// endpoint refuses everything.

web_session_push_key "ae2c9aThaH4493c0$!$b4990e4221b50"
