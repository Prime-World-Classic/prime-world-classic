# Linux Native Port Plan

Updated: 2026-07-06

## Goal

Deliver a native Linux client for Prime World without Wine, while keeping the Windows client buildable and functionally intact.

## Non-Negotiable Constraints

- Do not remove or regress the Windows client path.
- Keep Linux-only behavior behind `__linux__`, Linux-specific targets, or bootstrap-only compile definitions.
- Prefer cross-platform fixes when they are low-risk and do not change Windows behavior.
- Batch work into larger chunks and keep this file current so progress does not depend on chat history.

## Current Verified State

- `PrimeWorldLinuxClient` builds successfully.
- `PrimeWorldLinuxGameplayProbe` builds successfully.
- `PrimeWorldLinuxClientRuntimeProbe` builds successfully.
- `PrimeWorldLinuxRenderProbe` builds successfully.
- Latest chunk: Linux/null-render `PFRoadFlagpole` now restores the non-visual full-client flagpole progression rules instead of the remaining always-raise shortcuts. `PF_GameLogic/PFRoadFlagpole.cpp` now discovers owner towers through the existing target-selector path, forwards owner death events into vulnerability refresh, applies tower-protection checks in `CanRaise(...)`, updates nature-map desired positions on raise/drop, steps delayed owner discovery, and makes scripted `Attack(...)` skip already owned poles before raising the next contested pole. Visual client-object calls, statistics logging, and Windows/full-render behavior remain on the existing implementation path.
- Verification after the Linux road-flagpole gameplay chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFRoadFlagpole.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-04.00.39/linux-client-shell.log`; it exercised the loaded map with `roadFlagpoles=15`, kept the live flag command path active, and preserved command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, AI, minigame, pickup, and creep combat: `Final game transceiver runtime: ready=yes world=yes step=854 commands=247 ... heroRaiseFlagSent=yes heroRaiseFlagCommands=1 ... replayCommands=247`, `Final Linux AI controllers: auto=9/9 ... routes=8/26/24/14/6/0/6 routeVulnerable=0/2 ... commands=221/221 ... pickup=1 raise=1`, `Final replay capture validation: valid=yes ... commandBlocks=247/247 statusBlocks=5/5`, `Final replay storage: valid=yes segments=76/76 commands=247/247 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/194/10/89/69/89`, `Final visible live minimap: drawn=yes texture=yes loaded=196 ... markers=194`, `Final hero gameplay command execution: ... raise=2/2/2/1 can=0 flag=7722/0 ... pickup=2/2/2/2 can=1`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render `Target::IsVisibleForFaction(...)` now mirrors the full-client non-visual visibility checks instead of accepting every target as visible. `PF_GameLogic/PFUniTarget.cpp` delegates unit/object checks to `IsVisibleForFaction(...)` on the target object and checks position targets through the loaded `PFWorld` tile map plus war-fog visibility, with Linux null guards for partial bootstrap worlds. Windows/full-render code remains on the existing implementation path.
- Verification after the Linux target-visibility chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFUniTarget.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-03.54.52/linux-client-shell.log`; it exercised a loaded world with `warfog=4` and kept stricter visibility compatible with command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, AI, minigame, pickup, and creep combat: `Final game transceiver runtime: ready=yes world=yes step=839 commands=239 ... heroUseTalentSent=yes ... heroPickupObjectSent=yes ... replayCommands=239`, `Final Linux AI controllers: auto=9/9 ... routes=8/26/24/14/6/0/6 routeVulnerable=0/2 ... commands=213/213 ... attack=64 activate=54 useTalent=36 pickup=1 raise=1`, `Final replay capture validation: valid=yes ... commandBlocks=239/239 statusBlocks=5/5`, `Final replay storage: valid=yes segments=73/73 commands=239/239 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/195/10/89/70/89`, `Final visible live minimap: drawn=yes texture=yes loaded=196 ... markers=195`, `Final hero gameplay command execution: ... useTalent=37/37/37/37 can=1 ... pickup=2/2/2/2 can=1`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render `PFConsumableChest` now applies the same non-visual inventory rules as the full client when a hero picks up a consumable chest. `PF_GameLogic/PFChest.cpp` now validates the picker with `CanTakeConsumable(...)`, transfers the stack through `TakeConsumable(..., CONSUMABLEORIGIN_PICKUPABLE)` during `OnPickedUp(...)`, and uses the map AI parameters' chest game object as the first Linux fallback before falling back to the consumable's own game object. Windows/full-render code remains on the existing implementation path.
- Verification after the Linux consumable-chest pickup chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFChest.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-03.51.33/linux-client-shell.log`; it kept the live pickup command path, command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, AI, minigame, advanced summons, and creep combat stable: `Final game transceiver runtime: ready=yes world=yes step=890 commands=257 ... heroPickupObjectSent=yes heroPickupObjectCommands=1 ... replayCommands=257`, `Final hero gameplay command execution: ... buy=2/2/2/2 can=1 took=1 ... pickup=2/2/2/2 can=1 object=7313`, `Final Linux AI controllers: auto=9/9 ... routes=8/26/24/14/6/0/6 routeVulnerable=0/2 ... commands=235/235 ... pickup=1 raise=1`, `Final replay capture validation: valid=yes ... commandBlocks=257/257 statusBlocks=5/5`, `Final replay storage: valid=yes segments=79/79 commands=257/257 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/194/10/89/69/89`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render `PFAIWorld` now releases route tower vulnerability after the map-defined tower delay expires instead of only decrementing the timer. `PF_GameLogic/PFAIWorld.cpp` applies `SetRouteLevelVulnerable(..., true)` to both factions' level-0 bottom/center/top routes once the delay crosses zero, then latches the timer to avoid repeated releases. `PFAIWorld.h` exposes Linux-only vulnerable route tower/building counters, and `PW_Client/Game.cpp` reports them in `linux-client-shell.log` plus the final Linux AI diagnostic as `routeVulnerable=<towers>/<buildings>`. Windows/full-render code remains on the existing implementation path.
- Verification after the Linux route tower-vulnerability chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIWorld.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIWorld.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-03.44.49/linux-client-shell.log`; it proved the new route vulnerability diagnostic is wired and kept command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, advanced summons, and creep combat stable: `Final Linux AI controllers: auto=9/9 ... active=9 registry=45/49/18/24/0/112/0 routes=8/26/24/14/6/0/6 routeVulnerable=0/2 ... commands=224/224 ... attack=64 activate=55 useTalent=45`, `Final game transceiver runtime: ready=yes world=yes step=866 commands=250 ... replayCommands=250`, `Final replay capture validation: valid=yes ... commandBlocks=250/250 statusBlocks=5/5`, `Final replay storage: valid=yes segments=77/77 commands=250/250 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/194/10/89/69/89`, `Final visible live minimap: drawn=yes texture=yes loaded=196 ... markers=194`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render `PFAIWorld` now keeps the same non-visual route, spawner, tower, and building route bookkeeping that AI controllers and world scripts rely on. `PF_GameLogic/PFAIWorld.cpp` restores Linux route tower lookup, spawner registration/lookup, route border-point calculation, nearest-route spawner lookup, building create/destroy membership updates, vulnerability propagation, and quarter-destroyed creep-power escalation. `PF_GameLogic/PFBuildings.cpp` now notifies the Linux AI world when buildings are initialized or destroyed, including a correction for tower objects that are first initialized as generic buildings and then reinitialized as towers. `PFAIWorld.h` exposes Linux-only route registry counters, and `PW_Client/Game.cpp` reports them in the final AI diagnostic. Windows/full-render code remains on the existing implementation path.
- Verification after the Linux AI-world route/building registry chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIWorld.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIWorld.h pw/branches/r1117/Src/PF_GameLogic/PFBuildings.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-03.36.49/linux-client-shell.log`; it proved the Linux AI world now has live route state and kept command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, advanced summons, and creep combat stable: `Final Linux AI controllers: auto=9/9 ... active=9 registry=45/49/18/24/0/112/0 routes=8/26/24/14/6/0/6 ... commands=224/224 ... attack=64 activate=55 useTalent=45`, `Final game transceiver runtime: ready=yes world=yes step=869 commands=250 ... replayCommands=250`, `Final replay capture validation: valid=yes ... commandBlocks=250/250 statusBlocks=5/5`, `Final replay storage: valid=yes segments=77/77 commands=250/250 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/194/10/89/69/89`, `Final visible live minimap: drawn=yes texture=yes loaded=196 ... markers=194`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render `PFAIContainer` now keeps the same non-visual script-name registries that map loading and world scripts depend on. `PF_GameLogic/PFAIContainer.cpp` registers named creeps, units, generic objects, object groups, script resources, object-id-to-name mappings, dead object names, local/team-slot heroes, direct unit/object/group lookups, summon-name lookups, creep-name lookup by object id, and registry pruning in `RemoveInvalidUnits()`. `PF_GameLogic/PFAIContainer.h` exposes Linux-only registry counters, and `PW_Client/Game.cpp` reports them in the existing final Linux AI diagnostic. Lua execution and visual script effects remain stubbed in this chunk; Windows/full-render registry behavior remains on the existing `LuaScript.cpp`/`WorldScript.cpp` implementation.
- Verification after the Linux AI-container registry chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIContainer.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIContainer.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-03.21.11/linux-client-shell.log`; it proved the Linux AI container now has live registered script data and kept command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, advanced summons, and creep combat stable: `Final Linux AI controllers: auto=9/9 ... active=9 registry=45/49/18/24/0/112/0 ... commands=217/217 ... attack=65 activate=54 useTalent=39`, `Session effects unit-summon runtime: db=yes units=yes ability=yes factory=yes created=yes/2/2 mastered=yes child=yes/2/0.88 advanced=yes/yes/yes`, `Final game transceiver runtime: ready=yes world=yes step=842 commands=239 ... replayCommands=242`, `Final replay capture validation: valid=yes ... commandBlocks=242/242 statusBlocks=5/5`, `Final replay storage: valid=yes segments=74/74 commands=242/242 statuses=5/5`, `Final minigame summon hide proof: object=8615 created=1/1 count=1/1 hidden=1->0 invisible=1->0`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/195/10/89/70/89`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux bootstrap now links and exercises the advanced summon behavior path instead of treating advanced summon DB data as unavailable. `LinuxBootstrap/CMakeLists.txt` links `PF_GameLogic/PFAdvancedSummon.cpp` into the Linux client/probe source lists, `PF_GameLogic/DBAbility.cpp` now uses the existing `PFApplAdvancedSummonSetTarget` factory in the Linux bootstrap path and returns `PFSummonBehaviourDataAdvanced` from `SummonBehaviourAdvanced::Create()` under `PW_LINUX_DB_BOOTSTRAP`, and the existing Linux session-effects summon proof in `PW_Client/Game.cpp` now builds a `SummonBehaviourAdvanced`, verifies the DB behavior data factory, verifies spawned units receive `PFSummonBehaviourAdvanced`, and drives `SetAdvancedSummonTargetApplicator` into guard mode. Windows/full-render behavior stays on the same implementation path.
- Verification after the Linux advanced-summon factory/runtime chunk: focused `git diff --check -- pw/branches/r1117/Src/LinuxBootstrap/CMakeLists.txt pw/branches/r1117/Src/PF_GameLogic/DBAbility.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A short native OpenGL startup smoke exited `0` and reported `Session effects unit-summon runtime: db=yes units=yes ability=yes factory=yes created=yes/2/2 mastered=yes child=yes/2/0.88 advanced=yes/yes/yes`. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-03.10.54/linux-client-shell.log`; it kept advanced summon creation/targeting, command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, and creep combat stable: `Session effects unit-summon runtime: db=yes units=yes ability=yes factory=yes created=yes/2/2 mastered=yes child=yes/2/0.88 advanced=yes/yes/yes`, `Final game transceiver runtime: ready=yes world=yes step=878 commands=257 ... replayCommands=257`, `Final Linux AI controllers: auto=9/9 ... commands=231/231 ... attack=68 activate=56 useTalent=48`, `Final replay capture validation: valid=yes ... commandBlocks=257/257 statusBlocks=5/5`, `Final replay storage: valid=yes segments=81/81 commands=257/257 statuses=5/5`, `Final minigame summon hide proof: object=8615 created=1/1 count=1/1 hidden=1->0 invisible=1->0`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/193/10/89/68/89`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render summon spawn placement now mirrors the Windows/full-render behaviour-defined placement path instead of leaving it as a no-op. `PF_GameLogic/PFSummoned.cpp` implements `PFSummonedUnitAIBehaviour::AcquireBehaviourDefinedSpawnPosition(...)` for Linux by reading the same AIWorld common summon escort distances, asking the summoned unit to find a nearby free place around its escort position, and writing the corrected spawn target before teleport. The Linux implementation adds null guards for partial bootstrap worlds/masters/units; Windows/full-render code remains on the existing implementation.
- Verification after the Linux summon behaviour-placement chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFSummoned.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-02.56.17/linux-client-shell.log`; it kept loaded-world summon placement/state handling, command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, and creep combat stable: `Session effects unit-summon runtime: db=yes units=yes ability=yes factory=yes created=yes/2/2 mastered=yes child=yes/2/0.88`, `Final game transceiver runtime: ready=yes world=yes step=872 commands=252 ... replayCommands=256`, `Final Linux AI controllers: auto=9/9 ... commands=230/230 ... attack=67 activate=56 useTalent=48`, `Final replay capture validation: valid=yes ... commandBlocks=256/256 statusBlocks=5/5`, `Final replay storage: valid=yes segments=80/80 commands=256/256 statuses=5/5`, `Final minigame summon hide proof: object=8615 created=1/1 count=1/1 hidden=1->0 invisible=1->0`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/194/10/89/69/89`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render summoned AI now uses the existing non-visual summon state machine instead of the remaining stubs. `PF_GameLogic/PFSummoned.cpp` registers AI summons for master events, runs MicroAI before normal summon lifetime stepping, recalculates common-behaviour lash range, enqueues `PFSummonAIBaseState`, routes assigned/retaliation targets through `PFSummonAIAttackState`, implements lash-aware target selection, responds to master move/assign-target events, and computes formation offsets from AIWorld common summon parameters. `LinuxBootstrap/CMakeLists.txt` now links `PF_GameLogic/PFSummonState.cpp` into the Linux client/probe targets. `PF_GameLogic/PFSummonState.cpp` also now uses checked summon-behaviour and targeting-parameter lookups so Linux bootstrap units with partial DB targeting data do not crash while constructing summon states. Windows/full-render behavior stays on its existing code path.
- Verification after the Linux summoned AI state-handling chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFSummoned.cpp pw/branches/r1117/Src/PF_GameLogic/PFSummonState.cpp pw/branches/r1117/Src/LinuxBootstrap/CMakeLists.txt` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-02.49.43/linux-client-shell.log`; it kept live summon AI state handling, command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, and creep combat stable: `Session effects unit-summon runtime: db=yes units=yes ability=yes factory=yes created=yes/2/2 mastered=yes child=yes/2/0.88`, `Final game transceiver runtime: ready=yes world=yes step=869 commands=252 ... replayCommands=252`, `Final Linux AI controllers: auto=9/9 ... commands=226/226 ... attack=67 activate=55 useTalent=45`, `Final replay capture validation: valid=yes ... commandBlocks=252/252 statusBlocks=5/5`, `Final replay storage: valid=yes segments=79/79 commands=252/252 statuses=5/5`, `Final minigame summon hide proof: object=8615 created=1/1 count=1/1 hidden=1->0 invisible=1->0`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render summoned units now run the same non-visual constructor setup as the full client. `PF_GameLogic/PFSummoned.cpp` initializes summoned base attacks, honors `openWarFog == false` by closing creep war fog, copies master stat modifiers through the existing hook, performs the initial level-up before syncing to the master's level, and upgrades summon abilities. The change stays inside `PW_LINUX_NULL_RENDER`; Windows/full-render summon skin/client-object creation and validation remain untouched.
- Verification after the Linux summoned-unit constructor parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFSummoned.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-02.29.58/linux-client-shell.log`; it kept live AI, command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, summon diagnostics, and creep combat stable after the summon constructor change: `Session effects unit-summon runtime: db=yes units=yes ability=yes factory=yes created=yes/2/2 mastered=yes child=yes/2/0.88`, `Final game transceiver runtime: ready=yes world=yes step=845 commands=243 ... replayCommands=243`, `Final Linux AI controllers: auto=9/9 ... commands=217/217 ... attack=65 activate=54 useTalent=39`, `Final replay capture validation: valid=yes ... commandBlocks=243/243 statusBlocks=5/5`, `Final replay storage: valid=yes segments=75/75 commands=243/243 statuses=5/5`, `Final minigame summon hide proof: object=8615 created=1/1 count=1/1 hidden=1->0 invisible=1->0`, `Final minigame lifecycle forwarding proof: drop=0->1 finish=0->1`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render pet units now use the same non-visual keeper setup and escort state as the full client. `PF_GameLogic/PFPet.cpp` starts pets at the keeper escort position, assigns the keeper faction and player id instead of neutral/-1, initializes escort distances from object size and chase range, runs the pet escort FSM step, enqueues the escort state at construction, and restores `CreateTestPet(...)` so native Linux tests/tools can create DB-backed pets instead of always getting null. Visual client-object creation remains omitted in the Linux branch, and Windows/full-render pet rendering and reset behavior remain untouched.
- Verification after the Linux pet escort/create-helper chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFPet.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. The direct create-game smoke is a regression gate rather than a dedicated pet-spawn script; it exited `0` after 90 seconds and wrote `Bin/logs/2026.07.06-02.26.24/linux-client-shell.log`, keeping live AI, command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, summon diagnostics, and creep combat stable: `Final game transceiver runtime: ready=yes world=yes step=872 commands=252 ... replayCommands=256`, `Final Linux AI controllers: auto=9/9 ... commands=230/230 ... attack=67 activate=56 useTalent=48`, `Final replay capture validation: valid=yes ... commandBlocks=256/256 statusBlocks=5/5`, `Final replay storage: valid=yes segments=80/80 commands=256/256 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`, `Session effects unit-summon runtime: db=yes units=yes ability=yes factory=yes created=yes/2/2 mastered=yes`, `Final visible live minimap: drawn=yes texture=yes loaded=196 ... markers=194`, and `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`.
- Previous chunk: Linux/null-render creeps now run the same non-visual spawn and level-up setup used by the full client. `PF_GameLogic/PFCreep.cpp` initializes creep base attacks, applies the existing force-modifier hook, performs the initial level-up, upgrades creep abilities, allows time-based creep level-ups while respecting the AI-world pause flag, blocks level-up during combat, removes summon-forbid flags when activation time expires, and reopens war fog on faction changes. The changes stay inside `PW_LINUX_NULL_RENDER`; Windows/full-render client-object creation and behavior remain untouched.
- Verification after the Linux creep spawn/level-up parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFCreep.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-02.21.14/linux-client-shell.log`; it kept live AI, command scheduling, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, and creep combat active after the creep initialization change: `Final game transceiver runtime: ready=yes world=yes step=854 commands=248 ... replayCommands=248`, `Final Linux AI controllers: auto=9/9 ... commands=222/222 ... attack=66 activate=55 useTalent=42`, `Final replay capture validation: valid=yes ... commands=yes statuses=yes`, `Final replay storage: valid=yes segments=77/77 commands=248/248 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`, `Final visible live HUD: drawn=yes ... targetKind=neutral`, `Final visible live minimap: drawn=yes texture=yes loaded=196 ... markers=194`, `Final Linux creep combat: prepared=yes ... hp=320->0 ... killed=yes`, and `Final Linux creep runtime: creepSpawners=6 neutralSpawners=28 ... dead=3/3`.
- Previous chunk: Linux bootstrap `NMainLoop` time scale is no longer a no-op. `LinuxBootstrap/runtime_stubs.cpp` stores the bootstrap time scale, returns it from `GetTimeScale()`, applies it to `GetTimeDelta()` and `UpdateTime()`, and keeps the raw temporary delta storage intact for the native frame loop. This lets Linux-side world scripts and adventure-screen-style callers affect bootstrap simulation/UI time while leaving Windows and the full client timer path untouched.
- Verification after the Linux bootstrap time-scale chunk: focused `git diff --check -- pw/branches/r1117/Src/LinuxBootstrap/runtime_stubs.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-02.16.10/linux-client-shell.log`; it kept live simulation, command scheduling, AI, replay capture/storage, 3D map, HUD, minimap, scoreboard, event feed, minigame, and creep combat active: `Final game transceiver runtime: ready=yes world=yes step=890 commands=260 ... replayCommands=260`, `Final Linux AI controllers: auto=9/9 ... commands=238/238 ... attack=67 activate=57 useTalent=54`, `Final replay capture validation: valid=yes ... commands=yes statuses=yes`, `Final replay storage: valid=yes segments=81/81 commands=260/260 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`, `Final visible live HUD: drawn=yes ... abilitySlots=1 ... talentSlots=7`, `Final visible live minimap: drawn=yes texture=yes loaded=196 ... markers=192`, `Final Linux creep combat: prepared=yes ... killed=yes`, and `Final minigame slowdown proof: before=1.00 sentinel=0.25 during=1.00 after=0.25 restored=1.00`.
- Previous chunk: Linux/null-render `DispellCastLimitation` now mirrors the Windows/full-render status-dispel eligibility check. `PF_GameLogic/PFCastLimitations.cpp` includes `PFApplInstant.h` in the Linux branch and uses `PFApplDispell::SearchStatus2Dispell(...)` against the ability owner and opposite-faction flags, with Linux-side null guards for partial bootstrap ability state. This removes the old Linux stub that rejected every dispel limitation and leaves Windows/full-render behavior unchanged.
- Verification after the Linux dispel-limitation parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFCastLimitations.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL MOBA create-game smoke exited `0` and wrote `Bin/logs/2026.07.06-02.10.51/linux-client-shell.log`; it kept the live world, command scheduler, AI, replay capture, 3D map, HUD, minimap, scoreboard, event feed, minigame, and creep combat active: `Final game transceiver runtime: ready=yes world=yes step=878 commands=257 ... replayCommands=257`, `Final Linux AI controllers: auto=9/9 ... commands=231/231 ... attack=67 activate=56 useTalent=48`, `Final replay capture validation: valid=yes ... commands=yes statuses=yes`, `Final replay storage: valid=yes segments=80/80 commands=257/257 statuses=5/5`, `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`, and `Final Linux creep combat: prepared=yes ... killed=yes`.
- Previous chunk: native Linux/OpenGL select-game-mode keyboard navigation now includes the visible Join Mode, Developer Sex, and Player Count controls as first-class actions. `PW_Client/Game.cpp` extends the visible-menu action order to `Create Game -> Start Session -> Refresh -> Join Mode -> Developer Sex -> Player Count -> Map -> Hero -> Diagnostics`, maps each new action to its real focus surface, lets left/right or plus/minus adjust Join Mode, Player Count, Map, and Hero while Developer Sex toggles, lets Enter/Space activate the new controls where appropriate, and keeps mouse clicks in sync with the selected action. The same chunk also prevents helper-recorded actions such as Start Session from double-counting activations. Windows, production input, gameplay internals, and the full UI path remain untouched.
- Verification after the Join Mode/Developer Sex/Player Count keyboard chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. Five native OpenGL starting-lobby keyboard smokes exited `0`: `--bootstrap-click-script 'key:down;key:enter'` wrote `Bin/logs/2026.07.06-02.07.23/linux-client-shell.log` with `Final visible lobby controls: ... selectedAction=1/start-session keyboardFocus=yes/start-session-button ...` and `Final visible menu runtime: selected=1 activations=1 lastAction=join-game ...`; `--bootstrap-click-script 'key:down;key:down;key:down;key:right'` wrote `Bin/logs/2026.07.06-02.07.41/linux-client-shell.log` with `... keyboardFocus=yes/join-mode joinMode=reconnect ...` and `selected=3 activations=1 lastAction=select-join-mode-reconnect`; `--bootstrap-click-script 'key:down;key:down;key:down;key:down;key:enter'` wrote `Bin/logs/2026.07.06-02.07.57/linux-client-shell.log` with `... keyboardFocus=yes/developer-sex ... developerSex=yes/female ...` and `selected=4 activations=1 lastAction=developer-sex-female`; `--bootstrap-click-script 'key:down;key:down;key:down;key:down;key:down;key:left'` wrote `Bin/logs/2026.07.06-02.08.13/linux-client-shell.log` with `... keyboardFocus=yes/player-count ... playerCount=yes/8 ...` and `selected=5 activations=1 lastAction=set-player-count`; `--bootstrap-click-script 'key:down;key:down;key:down;key:down;key:down;key:down'` wrote `Bin/logs/2026.07.06-02.08.30/linux-client-shell.log` with `... keyboardFocus=yes/map-list ...` and `selected=6 activations=0 lastAction=select-menu`.
- Previous chunk: native Linux/OpenGL select-game-mode keyboard navigation now includes the visible Start Session and Refresh buttons as first-class actions. `PW_Client/Game.cpp` extends the visible-menu action enum, maps Start Session to `start-session-button` and Refresh to `refresh-button`, draws selected/focus feedback on the real lobby button rectangles, sets the selected action for matching mouse clicks, and routes Enter/Space through the existing join/reconnect/spectate and refresh paths. This improves starting-lobby keyboard parity while leaving Windows, production input, gameplay internals, mouse hit-test behavior, and the full UI path untouched.
- Verification after the Start Session/Refresh keyboard chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. Three native OpenGL starting-lobby keyboard smokes exited `0`: `--bootstrap-click-script 'key:down;key:enter'` wrote `Bin/logs/2026.07.06-01.59.53/linux-client-shell.log` with `Final visible lobby controls: ... selectedAction=1/start-session keyboardFocus=yes/start-session-button ...` and `Final visible menu runtime: selected=1 activations=2 lastAction=join-game ...`; `--bootstrap-click-script 'key:down;key:down;key:enter'` wrote `Bin/logs/2026.07.06-02.01.27/linux-client-shell.log` with `... selectedAction=1/refresh keyboardFocus=yes/refresh-button ...` and `selected=2 activations=1 lastAction=refresh-games`; `--bootstrap-click-script 'key:down;key:down;key:down'` wrote `Bin/logs/2026.07.06-02.01.27/linux-client-shell.log` with `... selectedAction=0/none keyboardFocus=yes/map-list ...` and `selected=3 activations=0 lastAction=select-menu`.
- Previous chunk: native Linux/OpenGL select-game-mode now shows a visible keyboard focus ring on the starting-lobby surface selected by the existing `visibleMenuSelectedAction`. `PW_Client/Game.cpp` maps the primary action to the Create Game button, map action to the map list panel, hero action to the hero/details panel, and diagnostics action to the status strip, draws a two-pass focus rectangle over the selected region, and records `keyboardFocus=<yes/no>/<surface>` in stdout plus `finalVisibleLobbyKeyboardFocus=<yes/no>/<surface>` in `linux-client-shell.log`. This improves keyboard/scripted navigation parity without changing hotkeys, click activation, mouse hit testing, Windows, production input, gameplay internals, or the full UI path.
- Verification after the keyboard-focus chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. Four native OpenGL starting-lobby smokes exited `0`: a no-script smoke wrote `Bin/logs/2026.07.06-01.42.15/linux-client-shell.log` with `Final visible lobby controls: ... selectedAction=1/create-game keyboardFocus=yes/primary-button ...` and `Final visible menu runtime: selected=0 activations=0 lastAction=none ...`; `--bootstrap-click-script 'key:down'` wrote `Bin/logs/2026.07.06-01.42.31/linux-client-shell.log` with `... selectedAction=0/none keyboardFocus=yes/map-list ...` and `selected=1 activations=0 lastAction=select-menu`; `--bootstrap-click-script 'key:down;key:down'` wrote `Bin/logs/2026.07.06-01.42.46/linux-client-shell.log` with `... keyboardFocus=yes/hero-details ...` and `selected=2 activations=0`; `--bootstrap-click-script 'key:down;key:down;key:down'` wrote `Bin/logs/2026.07.06-01.43.02/linux-client-shell.log` with `... keyboardFocus=yes/diagnostics-status ...` and `selected=3 activations=0`.
- Previous chunk: native Linux/OpenGL select-game-mode now gives hover feedback to the remaining visible top controls and scrollbars. `PW_Client/Game.cpp` renders hovered join-mode radios with the selected/hot treatment, adds a hover border to the developer-sex panel, frames the player-count slider/value box while hovered, and highlights the map/game scrollbar track through the existing visible-lobby hover surface. This keeps click/selection behavior unchanged and leaves Windows, production input, gameplay internals, and the full UI path untouched.
- Verification after the top-control and scrollbar hover chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. Four native OpenGL starting-lobby hover smokes exited `0`: join-mode hover wrote `Bin/logs/2026.07.06-01.37.12/linux-client-shell.log` with `Final visible lobby controls: ... mouse=0 hover=6/join-mode at=1090,299 base=1090,425 lastHit=none ...`; developer-sex hover wrote `Bin/logs/2026.07.06-01.37.34/linux-client-shell.log` with `... hover=6/developer-sex at=1100,28 base=1100,40 ...`; player-count hover wrote `Bin/logs/2026.07.06-01.37.49/linux-client-shell.log` with `... hover=6/player-count at=100,632 base=100,899 ...`; and map-scrollbar hover wrote `Bin/logs/2026.07.06-01.38.05/linux-client-shell.log` with `... hover=6/map-scrollbar at=590,457 base=590,650 ...`. All four kept `Final visible menu runtime: selected=0 activations=0 lastAction=none path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Previous chunk: native Linux/OpenGL select-game-mode synthetic input scripts now support wait/delay tokens. `PW_Client/Game.cpp` accepts `wait:<seconds>`, `wait=<seconds>`, `delay:<seconds>`, and `delay=<seconds>` inside `--bootstrap-click-script`, executes them as no-op script entries, uses their duration before advancing to the next token, and reports final script execution as `tokens=<n> waits=<n> waitSeconds=<n>`. This makes larger UI smokes deterministic without adding dummy input and leaves Windows, production input, rendering, gameplay internals, and the full UI path untouched.
- Verification after the synthetic wait-token chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL starting-lobby script smoke with `--bootstrap-click-interval 0.15 --bootstrap-click-script '100,650:hover;wait:0.40;690,650:hover;delay=0.30;key:down'` wrote `Bin/logs/2026.07.06-01.32.43/linux-client-shell.log` and exited `0`: `Final visible lobby controls: ... gameHover=1/2 ... hover=2/game-row at=690,457 base=690,650 ...`, `Final visible menu runtime: selected=1 activations=0 lastAction=select-menu path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`, and `Final bootstrap input script: tokens=5 waits=2 waitSeconds=0.70`.
- Previous chunk: native Linux/OpenGL select-game-mode now has row-level hover feedback for the map and game lists. `PW_Client/Game.cpp` derives hovered row indexes from the existing base-coordinate hover surface, draws a subtle hover border over the matching visible map/game row, records `mapHover=<count>/<index>` and `gameHover=<count>/<index>` in the final diagnostics, and writes the same row-hover counters to `linux-client-shell.log`. This keeps the list selection/click behavior unchanged and stays inside the Linux bootstrap overlay without touching Windows, production input, gameplay internals, or the full UI path.
- Verification after the visible-lobby row-hover chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL map-list hover smoke with `--bootstrap-click-interval 0.2 --bootstrap-click-script '100,650:hover;100,650:hover;100,650:hover;100,650:hover;100,650:hover;100,650:hover'` wrote `Bin/logs/2026.07.06-01.29.11/linux-client-shell.log` and exited `0`: `Final visible lobby controls: ... mapRange=0..2 mapHover=1/1 ... gameHover=0/-1 ... hover=6/map-row at=100,457 base=100,650 ...`. A game-list hover smoke with `--bootstrap-click-interval 0.2 --bootstrap-click-script '690,650:hover;690,650:hover;690,650:hover;690,650:hover;690,650:hover;690,650:hover'` wrote `Bin/logs/2026.07.06-01.29.26/linux-client-shell.log` and exited `0`: `Final visible lobby controls: ... mapHover=0/-1 ... gameRange=0..2 gameHover=1/2 ... hover=6/game-row at=690,457 base=690,650 ...`.
- Previous chunk: native Linux/OpenGL select-game-mode now has hover feedback for the visible action buttons. `PW_Client/Game.cpp` adds `x,y:move` / `x,y:hover` synthetic input script tokens, records the current visible-lobby hover surface and coordinates, ignores off-layout hover noise, marks synthetic hover moves so repeated scripted hover smokes stay deterministic, and renders enabled action buttons with the same hot texture/fallback colors used for selected buttons plus a subtle hover border. This stays in the Linux bootstrap overlay and leaves Windows, production input, gameplay internals, and the full UI path untouched.
- Verification after the visible-lobby hover chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL starting-lobby hover smoke with `--bootstrap-click-interval 0.2 --bootstrap-click-script '514,878:hover;514,878:hover;514,878:hover;514,878:hover;514,878:hover;514,878:hover'` wrote `Bin/logs/2026.07.06-01.25.14/linux-client-shell.log` and exited `0`: `Final visible lobby controls: ... actionButtons=3 actionEnabled=3 selectedAction=1/create-game ... mouse=0 hover=6/create-game-button at=514,617 base=514,878 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible menu runtime: selected=0 activations=0 lastAction=none path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Previous chunk: native Linux/OpenGL select-game-mode synthetic input now supports keyboard script tokens. `PW_Client/Game.cpp` keeps the existing click/wheel script syntax compatible, adds `key:<name>` entries for the visible-menu keys already handled by the Linux bootstrap (`up/down/left/right`, page/home/end, enter/space, tab, backspace/delete, F10, W/S, plus/minus), and injects native `KEY_DOWN` messages that flow through `HandleLinuxVisibleMenuHotkeys(...)` and the existing map/lineup keyboard paths. This gives the starting lobby deterministic keyboard-navigation smoke coverage without touching Windows, production input, rendering, gameplay internals, or the full UI path.
- Verification after the synthetic key-token chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL starting-lobby key smoke with `--bootstrap-click-interval 0.2 --bootstrap-click-script 'key:down;key:down;key:enter'` wrote `Bin/logs/2026.07.06-01.15.33/linux-client-shell.log` and exited `0`: `Final visible menu runtime: selected=2 activations=1 lastAction=cycle-hero path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`, while preserving the visible lobby draw state (`Final visible lobby details: ... mapSelection=yes/0/41/5 ... lineup=10 portraits=10 ...` and `Final visible lobby controls: mapRows=3 ... actionButtons=3 actionEnabled=3 ... mouse=0 lastHit=none ...`).
- Previous chunk: native Linux/OpenGL select-game-mode synthetic input and visible-lobby handling now cover wheel scrolling. `PW_Client/Game.cpp` keeps the earlier `--bootstrap-click-script` click syntax compatible, adds `x,y:wheel-up` and `x,y:wheel-down` tokens, injects native `MOUSE_WHEEL` messages with the same wheel flag encoding as `MainFrame_linux.cpp`, and consumes wheel events on the visible map/game lists before fallback map-browser navigation. This makes long Windows-like lobby lists testable through the same Linux hit-test path as user input while leaving Windows, production input, rendering, gameplay internals, and the full UI path untouched.
- Verification after the visible-lobby wheel chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL starting-lobby smoke with `--bootstrap-click-interval 0.08 --bootstrap-click-script '100,650:wheel-down;100,650:wheel-down;100,650:wheel-down;100,650:wheel-down;100,650:wheel-down;100,650:wheel-down;100,650:wheel-down;100,650:wheel-down;1090,425;690,650:wheel-down;340,965'` wrote `Bin/logs/2026.07.06-01.11.09/linux-client-shell.log` and exited `0`: `Final visible lobby details: ... mapSelection=yes/17/41/5 ... lineup=2 portraits=2 ... teams=yes/1/1 localSlot=1/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 ... mapScroll=yes/41/3/15 mapRange=15..17 gameRows=3 selectedGameRows=1 ... joinMode=spectate ... playerCount=yes/2 mouse=11 lastHit=start-session-button at=340,679 base=340,966`, and `Final visible menu runtime: selected=1 activations=2 lastAction=spectate-game path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Previous chunk: native Linux/OpenGL select-game-mode synthetic input now supports multi-click scripts. `PW_Client/Game.cpp` keeps `--bootstrap-click-base X,Y` compatible, adds `--bootstrap-click-script 'x,y[;x,y[:double]...]'` plus `--bootstrap-click-interval`, stores parsed clicks as a queue, and injects one visible-lobby `SWindowsMsg` per interval after the lobby is ready. This lets one smoke drive multiple Windows-like starting-lobby controls through the real Linux hit-test path without touching Windows, production input, rendering, gameplay internals, or the full UI path.
- Verification after the synthetic multi-click script chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A multi-click native OpenGL starting-lobby smoke with `--bootstrap-click-script '1090,425;69,899;340,965'` wrote `Bin/logs/2026.07.06-01.05.16/linux-client-shell.log` and exited `0`: `Final visible lobby details: ... lineup=2 portraits=2 ... teams=yes/1/1 localSlot=1/2`, `Final visible lobby controls: ... joinMode=spectate ... playerCount=yes/2 mouse=3 lastHit=start-session-button at=340,679 base=340,966`, and `Final visible menu runtime: selected=0 activations=2 lastAction=spectate-game path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`. A compatibility smoke for the older `--bootstrap-click-base 340,965` form wrote `Bin/logs/2026.07.06-01.05.36/linux-client-shell.log` and exited `0`: `Final visible lobby controls: ... joinMode=normal ... playerCount=yes/10 mouse=1 lastHit=start-session-button at=340,679 base=340,966` and `Final visible menu runtime: selected=0 activations=1 lastAction=join-game path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Previous chunk: native Linux/OpenGL select-game-mode now has a deterministic synthetic lobby click smoke hook. `PW_Client/Game.cpp` parses `--bootstrap-click-base X,Y`, `--bootstrap-click-double`, and `--bootstrap-click-after`, injects one Linux-only `SWindowsMsg` after the visible lobby is ready, reuses the existing lobby mouse hit testing, and exposes final visible-menu selected action, activation count, and last action in stdout plus `linux-client-shell.log`. This makes Start Session/Create Game routing testable without touching Windows, production input, rendering, gameplay internals, or the full UI path.
- Verification after the synthetic lobby-click hook: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-01.00.04/linux-client-shell.log` and exited `0`: `Final visible lobby controls: ... mouse=0 lastHit=none at=-1,-1 base=-1,-1` and `Final visible menu runtime: selected=0 activations=0 lastAction=none path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`. A synthetic Start Session click smoke with `--bootstrap-click-base 340,965` wrote `Bin/logs/2026.07.06-01.00.19/linux-client-shell.log` and exited `0`: `Final visible lobby controls: ... mouse=1 lastHit=start-session-button at=340,679 base=340,966` and `Final visible menu runtime: selected=0 activations=1 lastAction=join-game path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`, proving the Start Session rectangle routes to join-game instead of Create Game.
- Previous chunk: native Linux/OpenGL select-game-mode mouse activation now separates Create game from Start session. `PW_Client/Game.cpp` routes the create-game button rectangle through the existing custom-game action and routes the start-session button rectangle through `ActivateLinuxLobbySelectedGame(...)`, so the visible start-session button joins/reconnects/spectates the selected open game instead of creating a custom game. This fixes Linux lobby input behavior while leaving rendering, gameplay internals, Windows, and the full UI path untouched.
- Verification after the lobby start/create button split: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. No CLI mouse-injection path was found in the bootstrap, so a normal 8-second native OpenGL menu smoke was used to verify the menu still starts cleanly after the input-path split; it wrote `Bin/logs/2026.07.06-00.52.49/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapIndexLabels=3 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameIndexLabels=3 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 joinSelected=1 actionButtons=3 actionEnabled=3 selectedAction=1/create-game joinMode=normal developerSex=yes/male layout=3/4 joinResult=yes/text/2 playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL select-game-mode buttons now carry explicit enabled-state rendering and diagnostics. `PW_Client/Game.cpp` extends the lobby button helper with an enabled flag, keeps the start-session button enabled only when open game rows are visible, records the enabled action-button count in `LinuxBootstrapScreenRuntime`, writes it to `linux-client-shell.log`, and extends `Final visible lobby controls` with `actionEnabled=<count>`. This gives the Windows-like lobby action bar a disabled-state surface without changing lobby behavior, gameplay, Windows, or the full UI path.
- Verification after the lobby action-button enabled-state chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.50.46/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapIndexLabels=3 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameIndexLabels=3 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 joinSelected=1 actionButtons=3 actionEnabled=3 selectedAction=1/create-game joinMode=normal developerSex=yes/male layout=3/4 joinResult=yes/text/2 playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL select-game-mode now frames the join-result/status line and reports it in diagnostics. `PW_Client/Game.cpp` draws a subtle status strip at the existing join-result location, filters known empty UI markers before rendering, records whether the strip/text was drawn in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby controls` with `joinResult=<strip>/<text|empty>/<len>`. This makes the Windows-like lobby status surface visible while leaving lobby behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby join-result strip chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.47.41/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `UI JoinResult: ready=yes value=<empty>`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapIndexLabels=3 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameIndexLabels=3 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 joinSelected=1 actionButtons=3 selectedAction=1/create-game joinMode=normal developerSex=yes/male layout=3/4 joinResult=yes/text/2 playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL select-game-mode diagnostics now expose the visible lobby layout section count. `PW_Client/Game.cpp` records the number of top-level lobby headers and panel/frame sections drawn in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby controls` with `layout=<headers>/<panels>`. This proves the Windows-like starting lobby frame is present while leaving layout behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby layout-section chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.42.38/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapIndexLabels=3 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameIndexLabels=3 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 joinSelected=1 actionButtons=3 selectedAction=1/create-game joinMode=normal developerSex=yes/male layout=3/4 playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL select-game-mode diagnostics now expose top-row control selection state. `PW_Client/Game.cpp` records the selected join-mode radio count and the rendered developer-sex panel state in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby controls` with `joinSelected=<count>` and `developerSex=<yes/no>/<male/female>`. This proves the top controls are visible and selected consistently while leaving lobby input, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby top-control selected-state chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.38.52/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapIndexLabels=3 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameIndexLabels=3 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 joinSelected=1 actionButtons=3 selectedAction=1/create-game joinMode=normal developerSex=yes/male playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL select-game-mode diagnostics now expose the visible action button selection. `PW_Client/Game.cpp` records the number of selected action buttons plus the selected button name in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby controls` with `selectedAction=<count>/<name>`. This proves the focused create-game button matches the visible selected menu action while leaving button input, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby action-button selected-state chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.36.23/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapIndexLabels=3 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameIndexLabels=3 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 actionButtons=3 selectedAction=1/create-game joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL join-game rows now show a compact row-position label in the starting select-game-mode overlay. `PW_Client/Game.cpp` draws `<row>/<total>` chips on visible game rows, records the number of labels drawn in `LinuxBootstrapScreenRuntime`, writes that field to `linux-client-shell.log`, and extends `Final visible lobby controls` with `gameIndexLabels=<n>`. This stays inside the Linux overlay and leaves join-game selection behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the join-game row-position label chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.33.13/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapIndexLabels=3 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameIndexLabels=3 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL map-list rows now show a compact row-position label in the starting select-game-mode overlay. `PW_Client/Game.cpp` draws `<row>/<total>` chips on visible map rows, records the number of labels drawn in `LinuxBootstrapScreenRuntime`, writes that field to `linux-client-shell.log`, and extends `Final visible lobby controls` with `mapIndexLabels=<n>`. This stays inside the Linux overlay and leaves map selection behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the map-list row-position label chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.31.01/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapIndexLabels=3 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL selected-map details diagnostics now expose source coverage for the Windows-style map art set. `PW_Client/Game.cpp` records whether the selected map back, logo, and minimap asset references are present in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby details` with `assetRefs=<back>/<logo>/<minimap>`. This is diagnostic-only and leaves texture upload, selected-map rendering, gameplay, Windows, and the full UI path untouched.
- Verification after the selected-map asset-reference chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.28.50/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 assetRefs=yes/yes/yes mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL select-game-mode diagnostics now expose the visible map-list and join-game-list row ranges. `PW_Client/Game.cpp` records first/last visible map rows and first/last visible game rows in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby controls` with `mapRange=<first>..<last>` and `gameRange=<first>..<last>`. This is diagnostic-only and leaves list scrolling, selection behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby list visible-range chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.26.41/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapScroll=yes/41/3/0 mapRange=0..2 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameScroll=yes/3/6/0 gameRange=0..2 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL selected-map details now draw and verify selected map index and team-size context in the starting select-game-mode overlay. `PW_Client/Game.cpp` renders the selected map position and team size in the selected-map header, records whether that strip is shown plus selected index, total map count, and team size in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby details` with `mapSelection=<yes/no>/<index>/<total>/<teamSize>`. This stays inside the Linux overlay and leaves map browsing, gameplay, Windows, and the full UI path untouched.
- Verification after the selected-map context chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.06-00.23.12/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 mapSelection=yes/0/41/5 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapScroll=yes/41/3/0 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameScroll=yes/3/6/0 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL selected-map details now draw and verify the two-team lineup split in the starting select-game-mode overlay. `PW_Client/Game.cpp` draws a subtle divider between the generated team blocks in the existing lineup strip, records whether that divider is shown, visible team-1/team-2 slot counts, and the local player's visible slot/team in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby details` with `teams=<yes/no>/<team1>/<team2>` and `localSlot=<slot>/<team>`. This stays inside the Linux overlay and leaves lineup generation, hero selection, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby lineup team-split chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-15.08.10/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0 teams=yes/5/5 localSlot=5/2`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapScroll=yes/41/3/0 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameScroll=yes/3/6/0 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL selected-hero diagnostics now expose DB-backed hero metadata from the starting hero detail panel. `PW_Client/Game.cpp` records whether selected hero metadata and stats are available, DB ability totals, active/passive ability totals, stat and recommended-stat counts, and ready/icon talent totals in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby hero details` with `dbAbilities=<yes/no>/<all>/<active>/<passive>`, `stats=<yes/no>/<stats>/<recommended>`, and `talentReady=<ready>/<icons>`. This is diagnostic-only and leaves hero selection, gameplay, Windows, and the full UI path untouched.
- Verification after the selected-hero metadata diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-15.04.54/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapScroll=yes/41/3/0 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameScroll=yes/3/6/0 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 dbAbilities=yes/0/0/0 stats=yes/14/3 talents=12/24 shown=12 talentMissing=0 talentReady=2/24`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL selected-map details now draw and verify a compact rules/objectives line in the starting select-game-mode overlay. `PW_Client/Game.cpp` renders the selected map's battle delay, start prime, force, tower count, hero spawn count, shop count, and glyph count beside the existing map stats, records those values in `LinuxBootstrapScreenRuntime`, writes them to `linux-client-shell.log`, and extends `Final visible lobby details` with `rules=<yes/no>/<delay>/<prime>/<force>` and `objectives=<yes/no>/<towers>/<spawns>/<shops>/<glyphs>`. This is scoped to the Linux overlay and leaves map selection behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the selected-map rules/objectives chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-15.02.29/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 rules=yes/60/2000/105 objectives=yes/24/10/4/24 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapScroll=yes/41/3/0 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameScroll=yes/3/6/0 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL select-game-mode diagnostics now expose map-list and game-list scrollbar coverage. `PW_Client/Game.cpp` records whether each list scrollbar drew plus total rows, visible rows, and first visible row in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby controls` with `mapScroll=<yes/no>/<total>/<visible>/<first>` and `gameScroll=<yes/no>/<total>/<visible>/<first>`. This is diagnostic-only and leaves list selection behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby scrollbar diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-14.59.42/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 mapScroll=yes/41/3/0 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 gameScroll=yes/3/6/0 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL select-game-mode diagnostics now expose the displayed player-count slider value. `PW_Client/Game.cpp` returns the player count from the existing lobby player-count draw helper, records whether that control was drawn and the value shown in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby controls` with `playerCount=<yes/no>/<n>`. This is diagnostic-only and leaves lobby input behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby player-count diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-14.56.27/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 joinButtons=3 actionButtons=3 joinMode=normal playerCount=yes/10 mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL lobby mouse-hit diagnostics now classify the last select-game-mode surface reached by existing Linux hit testing. `PW_Client/Game.cpp` records the raw and 1280x1024 base-space coordinates plus the hit surface (`developer-sex`, `join-mode`, `map-row`, `map-scrollbar`, `game-row`, `game-scrollbar`, `player-count`, `primary-button`, `refresh-button`, or `background`) in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and extends `Final visible lobby controls` with `mouse=<n> lastHit=<surface> at=<x>,<y> base=<x>,<y>`. This is diagnostic-only and leaves lobby input behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the lobby mouse-hit diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-14.53.07/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 joinButtons=3 actionButtons=3 joinMode=normal mouse=0 lastHit=none at=-1,-1 base=-1,-1`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL map-list rows now draw compact category and team-size chips in the starting select-game-mode overlay. `PW_Client/Game.cpp` classifies visible map rows as PvP/PvE/Tutorial/other, records those counts in `LinuxBootstrapScreenRuntime`, writes them to `linux-client-shell.log`, and extends `Final visible lobby controls` with `mapTypes=<pvp>/<pve>/<tutorial>/<other>`. This is scoped to the Linux lobby overlay and leaves map selection behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the map-list row chip chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-14.50.00/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 mapTypes=3/0/0/0 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 joinButtons=3 actionButtons=3 joinMode=normal`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL join-game list rows now draw an explicit open/full status chip based on the existing `SDevGameInfo::playersCount/maxPlayers` values. `PW_Client/Game.cpp` records open and full visible game-row counts in `LinuxBootstrapScreenRuntime`, writes them to `linux-client-shell.log`, and extends `Final visible lobby controls` with `openGames=<n> fullGames=<n>`. This is scoped to the Linux select-game-mode overlay and leaves lobby actions, gameplay, Windows, and the full UI path untouched.
- Verification after the join-game row status chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-14.46.37/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 gameRows=3 selectedGameRows=1 openGames=3 fullGames=0 joinButtons=3 actionButtons=3 joinMode=normal`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL starting-lobby diagnostics now account for the visible map rows, game rows, join-mode controls, and action buttons drawn by the Windows-like select-game-mode overlay. `PW_Client/Game.cpp` has the map/game row draw helpers return counts, records selected-row counts and button counts in `LinuxBootstrapScreenRuntime`, writes those fields to `linux-client-shell.log`, and emits `Final visible lobby controls`. This is diagnostic-only and leaves lobby input, gameplay, Windows, and the full UI path untouched.
- Verification after the visible-lobby control accounting chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-14.44.13/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, `Final visible lobby controls: mapRows=3 selectedMapRows=1 gameRows=3 selectedGameRows=1 joinButtons=3 actionButtons=3 joinMode=normal`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL selected-map details diagnostics now expose the actual uploaded texture sizes for the starting lobby art. `PW_Client/Game.cpp` records the selected-map back, logo, and minimap OpenGL texture dimensions after the existing asset resolution succeeds, writes those sizes to `linux-client-shell.log`, and extends `Final visible lobby details` with `backSize=<w>x<h>`, `logoSize=<w>x<h>`, and `minimapSize=<w>x<h>`. This is diagnostic-only and leaves selected-map rendering, gameplay, Windows, and the full UI path untouched.
- Verification after the selected-map texture-size diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-14.40.30/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes backSize=1920x1080 logo=yes logoSize=690x320 minimap=yes minimapSize=538x402 textures=3 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL loading-roster diagnostics now expose progress distribution for the rendered starting/loading UI lineup. `PW_Client/Game.cpp` records minimum, maximum, average, and local-player loading progress percentages from the roster cards already drawn, writes those fields to `linux-client-shell.log`, and extends `Final visible loading roster` with `range=<min>..<max> avg=<n> localProgress=<n>`. This is diagnostic-only and leaves roster rendering, gameplay, Windows, and the full UI path untouched.
- Verification after the loading-roster progress diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.35.46/linux-client-shell.log` and exited `0`: `Final visible loading roster: drawn=yes rows=10 local=1 team1=5 team2=5 other=0 portraits=10 flags=10 progress=10 range=35..95 avg=76 localProgress=35 force=10 meta=1 rank=0 premium=0`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live 3D map command diagnostics now expose the last map-input surface, screen point, and projected world point. `PW_Client/Game.cpp` records keyboard/wheel/drag/select/command/signal/pan map-input hits, stores `liveMapPreviewLastHitSurface`, `liveMapPreviewLastInputX/Y`, `liveMapPreviewLastProjectionValid`, and `liveMapPreviewLastWorldX/Y`, writes those fields to `linux-client-shell.log`, and extends `Final live 3D map commands` with `lastHit=<surface> at=<x>,<y> projection=<yes/no> world=<x>,<y>`. This is diagnostic-only and leaves map command routing, gameplay, Windows, and the full UI path untouched.
- Verification after the live-3D-map command-hit diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings after a misplaced keyboard-hit block was corrected. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.31.57/linux-client-shell.log` and exited `0`: `Final live 3D map commands: surface=yes proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 lastHit=none at=-1,-1 projection=no world=0.0,0.0 action=none`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live HUD command diagnostics now expose the last HUD command hit surface and input point. `PW_Client/Game.cpp` classifies HUD clicks against the existing attack, target, and talent-slot rectangles, records `liveHudLastHitSurface`, `liveHudLastInputX/Y`, and `liveHudLastInputSlot`, writes those fields to `linux-client-shell.log`, and extends `Final live HUD commands` with `lastHit=<surface> at=<x>,<y> slot=<n>`. This is diagnostic-only and leaves command routing, gameplay, Windows, and the full UI path untouched.
- Verification after the live-HUD command-hit diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.26.48/linux-client-shell.log` and exited `0`: `Final live HUD commands: surface=yes proof=no highlight=no input=0 attack=0 useUnit=0 activate=0 talent=0 follow=0 slots=7 lastHit=none at=-1,-1 slot=-1 action=none`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL replay-control progress diagnostics now expose whether the replay progress bar has segment data, the fill width relative to the bar width, and any consumed-segment overrun hidden by the clamped visual bar. `PW_Client/Game.cpp` stores `visibleReplayProgressHasSegments`, `visibleReplayProgressBarPixels`, `visibleReplayProgressOverrun`, and `visibleReplayProgressOverrunSegments`, writes those fields to `linux-client-shell.log`, and extends `Final visible replay controls` with `available=<yes/no>`, `fill=<filled>/<bar>`, and `overrun=<yes/no>/<n>`. This is diagnostic-only and leaves replay scheduling, gameplay, Windows, and the full UI path untouched.
- Verification after the replay-control progress-accounting diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.22.39/linux-client-shell.log` and exited `0`: `Final visible replay controls: drawn=yes layout=yes panel=808,42,430x136 lines=5 buttons=5 activeButtons=0 pauseActive=no resetActive=no progress=yes available=yes percent=100 fill=410/410 segments=42/42 remaining=0 overrun=no/0 complete=yes lastHit=none at=-1,-1`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL replay-control diagnostics now preserve the visible control-panel rectangle and the last replay-control hit surface. `PW_Client/Game.cpp` records the resolved replay control panel bounds, classifies mouse hits as pause/step/slower/faster/reset/progress/panel/none, records keyboard replay-control actions, writes the fields to `linux-client-shell.log`, and extends `Final visible replay controls` with `panel=<x>,<y>,<w>x<h>` plus `lastHit=<surface> at=<x>,<y>`. This is diagnostic-only and leaves replay scheduling, gameplay, Windows, and the full UI path untouched.
- Verification after the replay-control hit-state diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.18.37/linux-client-shell.log` and exited `0`: `Final visible replay controls: drawn=yes layout=yes panel=808,42,430x136 lines=5 buttons=5 activeButtons=0 pauseActive=no resetActive=no progress=yes percent=100 fill=410 segments=42/42 remaining=0 complete=yes lastHit=none at=-1,-1`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live scoreboard diagnostics now preserve per-team breakdowns from the existing marker aggregation. `PW_Client/Game.cpp` stores Freeze/Burn live/total hero counts, creeps, towers, main buildings, damaged counts, and neutral creep/moving/damaged totals, writes those fields to `linux-client-shell.log`, and extends `Final visible live scoreboard` with compact `freeze=<live>/<total>/<creeps>/<towers>/<main>/<damaged>`, `burn=...`, and `neutral=<creeps>/<moving>/<damaged>` groups. This is diagnostic-only and leaves scoreboard drawing behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the live-scoreboard team-breakdown diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.12.32/linux-client-shell.log` and exited `0`: `Final visible live scoreboard: drawn=yes loaded=66 limit=640 capped=no teams=2 selectedTeam=yes heroMarkers=10 towers=24 main=2 freeze=5/5/15/12/1/1 burn=5/5/15/12/1/0 neutral=0/0/0 fallback=no moving=31 damaged=1 deadHeroes=0 objectiveLines=2 commandLines=1`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live minimap marker diagnostics now reconcile more of the loaded marker set. `PW_Client/Game.cpp` records neutral creep markers, dead/unsupported markers skipped by the draw filter, markers skipped by the cap, and markers clamped to the minimap bounds, writes those fields to `linux-client-shell.log`, and extends `Final visible live minimap` with `neutralCreeps=<n> skippedDead=<n> skippedUnsupported=<n> skippedLimit=<n> clamped=<n>`. This is diagnostic-only and leaves minimap drawing behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the live-minimap marker-accounting diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.08.41/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes loaded=66 limit=66 capped=no markers=66 heroes=10 deadHeroes=0 creeps=30 neutralCreeps=0 objectives=26 skippedDead=0 skippedUnsupported=0 skippedLimit=0 clamped=1 objectiveLabels=26 towerLabels=24 mainLabels=2 objectiveBars=26 damagedObjectiveBars=0 moving=31 localHero=yes target=yes commandMarker=no`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live minimap commands now expose projection diagnostics for map-click debugging. `PW_Client/Game.cpp` records live-minimap projection attempts/failures, the last screen coordinate, projected world coordinate, validity, and source for move, attack-or-move, signal, and select command paths, writes those fields to `linux-client-shell.log`, and extends `Final live minimap commands` with `projection=<yes/no> attempts=<n> failures=<n> screen=<x>,<y> world=<x>,<y> source=<source>`. This is diagnostic-only and leaves command behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the live-minimap projection diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.04.35/linux-client-shell.log` and exited `0`: `Final live minimap commands: surface=yes proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 projection=no attempts=0 failures=0 screen=0,0 world=0.0,0.0 source=none marker=no action=none`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live HUD target selection now exposes candidate and rejection diagnostics for the current target choice. `PW_Client/Game.cpp` factors the existing preferred-target rules through a Linux bootstrap classifier, records accepted candidates plus dead/self/ally/unsupported rejection counts, stores the last selection source, writes those fields to `linux-client-shell.log`, and extends `Final live HUD state` with `targetSource=<reason> targetCandidates=<n> targetRejected=<dead>/<self>/<ally>/<unsupported>`. This is diagnostic-only and leaves target behavior, gameplay, Windows, and the full UI path untouched.
- Verification after the live-HUD target-selection diagnostics chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-14.00.13/linux-client-shell.log` and exited `0`: `Final live HUD state: samples=224 targetSource=nearest-enemy targetCandidates=20 targetRejected=0/1/32/13 hero=yes object=7405 kind=hero faction=2 player=2 user=1984 level=1 gold=0 pos=381.1,164.8 hp=810/810/100% energy=725/725/100% damage=49.5-60.5 aps=0.68 move=6.76 regen=1.22/2.61 moving=yes dead=no target=yes object=7393 kind=hero faction=1 player=0 user=-1 source=nearest-enemy dist=2.0 pos=379.2,164.5 hp=700/810/86% energy=775/775/100% damage=50.4-61.6 aps=0.71 move=6.76 regen=1.22/2.79 moving=no dead=no`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live HUD target panels now visibly classify the current target relation and report that state in diagnostics. `PW_Client/Game.cpp` draws a compact `ALLY`/`ENEMY` badge inside the live target panel, stores `visibleLiveHudTargetRelationBadgeDrawn`, writes it to `linux-client-shell.log`, and extends `Final visible live HUD` with `targetRelation=<ally|enemy|none>` plus `relationBadge=<yes/no>`. This is scoped to the Linux bootstrap OpenGL HUD overlay and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-HUD target-relation badge chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.56.11/linux-client-shell.log` and exited `0`: `Final visible live HUD: drawn=yes portrait=yes bars=3 heroBars=2 targetBars=1 heroHp=100 heroMoving=yes heroDead=no abilitySlots=1 abilityIcons=1 abilityFallback=yes talentSlots=7 talentIcons=7 talentAvailable=24 talentSkipped=17 iconLimit=yes target=yes targetRelation=enemy relationBadge=yes targetHp=86 targetMoving=no targetDead=no targetKind=hero targetObject=7393 targetFaction=1 targetPlayer=0 targetDistance=2.0`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL 3D map preview now visibly badges the matched local hero in the live world and reports that badge count. `PW_Client/Game.cpp` draws a gold ring/beacon on the dynamic 3D marker whose player id matches the selected/local hero marker, stores `mapPreviewDynamicSelectedHeroBadges`, writes it to `linux-client-shell.log`, and extends `Bootstrap 3D preview` / `Final map 3D preview` with `localHeroBadge=<n>`. This is scoped to the Linux bootstrap OpenGL map preview and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the 3D-map local-hero badge chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.40.35/linux-client-shell.log` and exited `0`: `Final map 3D preview: terrain=yes/9216/18050 water=yes/4/88 roads=yes/6/45 terrainElements=yes/192/192/33202 static=yes/256/261/53964 animated=yes/48/57/133867 dynamic=yes/66/10/30/0/31 objectives=26 towers=24 main=2 status=66/31 heroMeshes=yes/10/62625 creepMeshes=yes/30/118614 unitMeshAssets=4/4 selectedHeroMarker=yes/2 localHeroBadge=1 yaw=228.3 pitch=56.0 zoom=1.18`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL lobby lineup now visibly tags the local selected slot and reports it separately from generic selection. `PW_Client/Game.cpp` draws a compact `YOU` badge on the selected human lineup slot, stores `visibleLobbyDetailsLineupLocalSlotsDrawn`, writes it to `linux-client-shell.log`, and extends `Final visible lobby details` with `local=<n>`. This is scoped to the Linux bootstrap lobby lineup and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the lobby-lineup local-slot marker chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-13.35.59/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes logo=yes minimap=yes textures=3 lineup=10 portraits=10 selected=1 local=1 human=1 manual=0 fallback=0`, and `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL loading roster now visibly tags the local player row and reports local/team row splits. `PW_Client/Game.cpp` draws a compact `YOU` badge on the roster card whose `slotId` matches `LoadingFlashInterface::GetOurHeroId()`, stores local/team/other row counts in `LinuxBootstrapScreenRuntime`, writes them to `linux-client-shell.log`, and extends `Final visible loading roster` with `local=<n> team1=<n> team2=<n> other=<n>`. This is scoped to the Linux bootstrap loading roster and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the loading-roster local/team marker chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.32.08/linux-client-shell.log` and exited `0`: `Final visible loading roster: drawn=yes rows=10 local=1 team1=5 team2=5 other=0 portraits=10 flags=10 progress=10 force=10 meta=1 rank=0 premium=0`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live scoreboard now visibly tags the local player's team column and reports whether that selected-team state rendered. `PW_Client/Game.cpp` draws a compact `YOU` label on the scoreboard column matching `liveHeroState.faction`, stores `visibleLiveScoreboardSelectedTeamColumnDrawn`, writes it to `linux-client-shell.log`, and extends `Final visible live scoreboard` with `selectedTeam=<yes/no>`. This is scoped to the Linux bootstrap scoreboard overlay and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-scoreboard selected-team marker chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.28.18/linux-client-shell.log` and exited `0`: `Final visible live scoreboard: drawn=yes loaded=66 limit=640 capped=no teams=2 selectedTeam=yes heroMarkers=10 towers=24 main=2 fallback=no moving=31 damaged=1 deadHeroes=0 objectiveLines=2 commandLines=1`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live minimap now visibly labels the local player hero marker and reports whether it rendered. `PW_Client/Game.cpp` adds a compact `P` label and highlighted border on the marker matching `liveHeroState.objectId`, stores `visibleLiveMinimapLocalHeroMarkerDrawn`, writes it to `linux-client-shell.log`, and extends `Final visible live minimap` with `localHero=<yes/no>`. This is scoped to the Linux bootstrap minimap overlay and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-minimap local-hero marker chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.24.16/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes loaded=66 limit=66 capped=no markers=66 heroes=10 deadHeroes=0 creeps=30 objectives=26 objectiveLabels=26 towerLabels=24 mainLabels=2 objectiveBars=26 damagedObjectiveBars=0 moving=31 localHero=yes target=yes commandMarker=no`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live event-feed diagnostics now separate generated source categories from the visible rows after truncation. `PW_Client/Game.cpp` counts command/combat/objective rows across the full generated event list before applying the visible row cap, writes the generated category totals to `linux-client-shell.log`, and extends `Final visible live event feed` so each category reports `drawn/generated` pairs. This is scoped to the Linux bootstrap event feed and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-event-feed source-category chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.21.20/linux-client-shell.log` and exited `0`: `Final visible live event feed: drawn=yes rows=4 generated=4 max=5 dropped=0 command=2/2 combat=1/1 objective=1/1 objectiveMarkers=26`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live HUD diagnostics now expose compact hero/target unit status in the final visible HUD line. `PW_Client/Game.cpp` stores hero HP percent, hero moving/dead flags, target HP percent, and target moving/dead flags from the same `LinuxLiveUnitHudState` values already rendered, writes them to `linux-client-shell.log`, and extends `Final visible live HUD` with `heroHp=<n> heroMoving=<yes/no> heroDead=<yes/no> targetHp=<n> targetMoving=<yes/no> targetDead=<yes/no>`. This is scoped to the Linux bootstrap live HUD and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-HUD unit-status chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.18.10/linux-client-shell.log` and exited `0`: `Final visible live HUD: drawn=yes portrait=yes bars=3 heroBars=2 targetBars=1 heroHp=100 heroMoving=yes heroDead=no abilitySlots=1 abilityIcons=1 abilityFallback=yes talentSlots=7 talentIcons=7 talentAvailable=24 talentSkipped=17 iconLimit=yes target=yes targetHp=86 targetMoving=no targetDead=no targetKind=hero targetObject=7393 targetFaction=1 targetPlayer=0 targetDistance=2.0`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL replay-control diagnostics now expose layout/button state alongside the existing progress summary. `PW_Client/Game.cpp` records whether the replay control layout resolved, how many buttons are active, pause/reset button active state, and the progress fill width in pixels, writes the fields to `linux-client-shell.log`, and extends `Final visible replay controls` with `layout=<yes/no> activeButtons=<n> pauseActive=<yes/no> resetActive=<yes/no> fill=<px>`. This is scoped to the Linux bootstrap replay overlay and leaves replay scheduling, gameplay, Windows, and the full UI path untouched.
- Verification after the replay-control layout/button-state chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.15.27/linux-client-shell.log` and exited `0`: `Final visible replay controls: drawn=yes layout=yes lines=5 buttons=5 activeButtons=0 pauseActive=no resetActive=no progress=yes percent=100 fill=410 segments=42/42 remaining=0 complete=yes`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live scoreboard diagnostics now expose marker source and fallback state. `PW_Client/Game.cpp` records the dynamic marker vector size, scoreboard marker limit, cap-hit state, and whether objective totals came from the existing world-object fallback instead of dynamic marker aggregation, writes the fields to `linux-client-shell.log`, and extends `Final visible live scoreboard` with `loaded=<n> limit=<n> capped=<yes/no> fallback=<yes/no>`. This is scoped to the Linux bootstrap live scoreboard and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-scoreboard marker-source chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.12.37/linux-client-shell.log` and exited `0`: `Final visible live scoreboard: drawn=yes loaded=66 limit=640 capped=no teams=2 heroMarkers=10 towers=24 main=2 fallback=no moving=31 damaged=1 deadHeroes=0 objectiveLines=2 commandLines=1`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live HUD diagnostics now split the visible bar count into hero bars and target bars. `PW_Client/Game.cpp` keeps the existing total `visibleLiveHudBarsDrawn` counter, adds `visibleLiveHudHeroBarsDrawn` and `visibleLiveHudTargetBarsDrawn`, increments them at the same HP/energy/target-HP draw sites, writes both fields to `linux-client-shell.log`, and extends `Final visible live HUD` with `heroBars=<n> targetBars=<n>`. This is scoped to the Linux bootstrap live HUD and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-HUD bar-split chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.09.24/linux-client-shell.log` and exited `0`: `Final visible live HUD: drawn=yes portrait=yes bars=3 heroBars=2 targetBars=1 abilitySlots=1 abilityIcons=1 abilityFallback=yes talentSlots=7 talentIcons=7 talentAvailable=24 talentSkipped=17 iconLimit=yes target=yes targetKind=hero targetObject=7393 targetFaction=1 targetPlayer=0 targetDistance=2.0`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live HUD diagnostics now expose icon capacity state for the in-match command strip. `PW_Client/Game.cpp` tracks whether the attack icon used a fallback visual, how many selected-hero talent slots are available, how many were skipped by the current HUD width, and whether the icon strip hit its draw limit, writes the fields to `linux-client-shell.log`, and extends `Final visible live HUD` with `abilityFallback=<yes/no> talentAvailable=<n> talentSkipped=<n> iconLimit=<yes/no>`. This is scoped to the Linux bootstrap live HUD and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-HUD icon-capacity chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-13.05.21/linux-client-shell.log` and exited `0`: `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=1 abilityFallback=yes talentSlots=7 talentIcons=7 talentAvailable=24 talentSkipped=17 iconLimit=yes target=yes targetKind=hero targetObject=7393 targetFaction=1 targetPlayer=0 targetDistance=2.0`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL selected-hero panel diagnostics now expose portrait fallback state and ability/talent icon availability. `PW_Client/Game.cpp` adds hero preview counters for portrait fallback, ability slots available/shown, ability fallback icons, talent slots available/shown, and missing displayed talent icons, fills them in `DrawLinuxBootstrapHeroPreviewDetails(...)`, writes them to `linux-client-shell.log`, and extends `Final visible lobby hero details` with `portraitFallback=<yes/no> abilities=<icons>/<available> shown=<n> abilityFallback=<n> talents=<icons>/<available> shown=<n> talentMissing=<n>`. This is scoped to the Linux bootstrap hero panel and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the visible lobby hero-preview availability chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-13.02.37/linux-client-shell.log` and exited `0`: `Final visible lobby details: drawn=yes artwork=yes back=yes logo=yes minimap=yes textures=3 lineup=10 portraits=10 selected=1 human=1 manual=0 fallback=0`, `Final visible lobby hero details: drawn=yes portrait=yes portraitFallback=no abilities=1/1 shown=1 abilityFallback=1 talents=12/24 shown=12 talentMissing=0`, and expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL starting UI map artwork diagnostics now split the lobby details artwork state into loading-back, loading-logo, and minimap texture flags. `PW_Client/Game.cpp` adds `visibleLobbyDetailsMapBackDrawn`, `visibleLobbyDetailsMapLogoDrawn`, and `visibleLobbyDetailsMapMinimapDrawn`, populates them from the existing `DrawLinuxLobbySelectedMapDetails(...)` texture paths, writes them to `linux-client-shell.log`, and extends `Final visible lobby details` with `back=<yes/no> logo=<yes/no> minimap=<yes/no>`. This is diagnostic-only and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the visible lobby map-artwork split chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-12.59.10/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes back=yes logo=yes minimap=yes textures=3 lineup=10 portraits=10 selected=1 human=1 manual=0 fallback=0`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=1 talents=12`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL starting UI lineup rendering now marks the selected lobby slot and reports lineup slot classes in final diagnostics. `PW_Client/Game.cpp` extends `DrawLinuxLobbyLineupStrip(...)` with selected, human, manual, and fallback slot counters, draws a compact selected-slot border in the existing OpenGL strip, persists the counters in `LinuxBootstrapScreenRuntime`, writes them to `linux-client-shell.log`, and extends `Final visible lobby details` with `selected=<n> human=<n> manual=<n> fallback=<n>`. This is scoped to the Linux bootstrap lobby overlay and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the visible lobby-lineup slot diagnostic chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.07.05-12.57.14/linux-client-shell.log` and exited `0`: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10 portraits=10 selected=1 human=1 manual=0 fallback=0`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=1 talents=12`, with expected inactive gameplay/replay/AI counters.
- Previous chunk: native Linux/OpenGL replay-control diagnostics now expose visible progress state in the same compact final summary used by replay smokes. `PW_Client/Game.cpp` stores replay-control progress percent, consumed/loaded segment counts, remaining segments, and completion state in `LinuxBootstrapScreenRuntime`, writes them to `linux-client-shell.log`, and extends `Final visible replay controls` with `percent=<n> segments=<used>/<loaded> remaining=<n> complete=<yes/no>`. This is diagnostic-only and leaves replay scheduling, gameplay, Windows, and the full UI path untouched.
- Verification after the visible replay-control progress diagnostic chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.53.19/linux-client-shell.log` and exited `0`: `Final visible replay controls: drawn=yes lines=5 buttons=5 progress=yes percent=100 segments=42/42 remaining=0 complete=yes`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live event-feed diagnostics now expose row truncation state. `PW_Client/Game.cpp` tracks generated rows, the active visible row cap, drawn rows, and dropped rows in `LinuxBootstrapScreenRuntime`, writes the new fields to `linux-client-shell.log`, and extends `Final visible live event feed` with `generated=<n> max=<n> dropped=<n>`. This is diagnostic-only and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-event-feed row-cap diagnostic chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.49.03/linux-client-shell.log` and exited `0`: `Final visible live event feed: drawn=yes rows=4 generated=4 max=5 dropped=0 command=2 combat=1 objective=1 objectiveMarkers=26`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL final minimap diagnostics now include command-marker visibility in the compact stdout summary. `PW_Client/Game.cpp` keeps using the existing `visibleLiveMinimapCommandMarkerDrawn` state and extends `Final visible live minimap` with `commandMarker=<yes/no>`, aligning stdout with the existing log field and the separate `Final live minimap commands` line. This is diagnostic-only and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-minimap command-marker summary chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.46.10/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes loaded=66 limit=66 capped=no markers=66 heroes=10 deadHeroes=0 creeps=30 objectives=26 objectiveLabels=26 towerLabels=24 mainLabels=2 objectiveBars=26 damagedObjectiveBars=0 moving=31 target=yes commandMarker=no`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live minimap diagnostics now prove whether the marker draw cap is hiding world markers. `PW_Client/Game.cpp` adds `visibleLiveMinimapMarkersLoaded`, `visibleLiveMinimapMarkerLimit`, and `visibleLiveMinimapMarkerLimitHit`, stores the loaded marker vector size and active draw limit inside `DrawLinuxLiveMinimapOverlay(...)`, writes the fields to `linux-client-shell.log`, and extends `Final visible live minimap` with `loaded=<n> limit=<n> capped=<yes/no>`. This is a safety diagnostic only and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-minimap marker-cap diagnostic chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.43.31/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes loaded=66 limit=66 capped=no markers=66 heroes=10 deadHeroes=0 creeps=30 objectives=26 objectiveLabels=26 towerLabels=24 mainLabels=2 objectiveBars=26 damagedObjectiveBars=0 moving=31 target=yes`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/66/10/30/0/31 objectives=26 towers=24 main=2 status=66/31 ...`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live minimap objective pips now include compact health bars. `PW_Client/Game.cpp` draws a small health bar under each alive tower/main-building objective marker using the existing `LinuxDynamicWorldMarker::healthPercent`, tracks `visibleLiveMinimapObjectiveHealthBarsDrawn` and `visibleLiveMinimapDamagedObjectiveHealthBarsDrawn`, includes `HP<n>` in the visible minimap footer, writes both counters to `linux-client-shell.log`, and extends `Final visible live minimap` with `objectiveBars=<n>` and `damagedObjectiveBars=<n>`. This brings the 2D tactical map closer to the 3D preview status-bar parity without touching gameplay, Windows, or the full UI path.
- Verification after the live-minimap objective-health-bar chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.40.29/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes markers=66 heroes=10 deadHeroes=0 creeps=30 objectives=26 objectiveLabels=26 towerLabels=24 mainLabels=2 objectiveBars=26 damagedObjectiveBars=0 moving=31 target=yes`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/66/10/30/0/31 objectives=26 towers=24 main=2 status=66/31 ...`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live minimap objective labels now report tower and main-building labels separately. `PW_Client/Game.cpp` adds `visibleLiveMinimapTowerLabelsDrawn` and `visibleLiveMinimapMainBuildingLabelsDrawn`, counts the existing `T` and `M` labels while drawing objective pips, changes the visible minimap footer to show `T<n>`/`B<n>`, writes both counts to `linux-client-shell.log`, and extends `Final visible live minimap` with `towerLabels=<n>` and `mainLabels=<n>`. This makes the two objective marker classes independently testable without touching gameplay, Windows, or the full UI path.
- Verification after the live-minimap objective-label split chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.36.20/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes markers=66 heroes=10 deadHeroes=0 creeps=30 objectives=26 objectiveLabels=26 towerLabels=24 mainLabels=2 moving=31 target=yes`, `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 towers=24 main=2 moving=31 damaged=1 deadHeroes=0 objectiveLines=2 commandLines=1`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live minimap objective pips now carry compact type labels. `PW_Client/Game.cpp` draws `T` labels on tower markers and `M` labels on main-building markers inside the existing minimap objective-marker block, tracks the rendered total in `visibleLiveMinimapObjectiveLabelsDrawn`, includes `L<n>` in the visible minimap footer, writes `finalVisibleLiveMinimapObjectiveLabels` to `linux-client-shell.log`, and extends `Final visible live minimap` with `objectiveLabels=<n>`. This is scoped to Linux bootstrap overlay rendering and leaves gameplay, Windows, and the full UI path untouched.
- Verification after the live-minimap objective-label chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.31.13/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes markers=66 heroes=10 deadHeroes=0 creeps=30 objectives=26 objectiveLabels=26 moving=31 target=yes`, `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 towers=24 main=2 moving=31 damaged=1 deadHeroes=0 objectiveLines=2 commandLines=1`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/66/10/30/0/31 objectives=26 towers=24 main=2 status=66/31 ...`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL 3D map preview diagnostics now report objective markers with named counts instead of leaving them implicit inside the dynamic marker tuple. `PW_Client/Game.cpp` threads tower and main-building counts through `DrawLinuxMapDynamicWorldMarkerPreview(...)`, stores them in `mapPreviewDynamicTowerMarkers` and `mapPreviewDynamicMainBuildingMarkers`, writes them to the startup/final log sections, and appends `objectives=<n> towers=<n> main=<n>` to both `Bootstrap 3D preview` and `Final map 3D preview` stdout lines. This keeps the existing dynamic tuple intact for compatibility and does not touch gameplay, Windows, or the full UI path.
- Verification after the 3D-map objective-diagnostic chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.26.05/linux-client-shell.log` and exited `0`: `Bootstrap 3D preview: ... dynamic=no/0/0/0/0/0 objectives=0 towers=0 main=0 ...`, `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/66/10/30/0/31 objectives=26 towers=24 main=2 status=66/31 ...`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live minimap diagnostics and footer text now report dead hero markers separately. `PW_Client/Game.cpp` adds `visibleLiveMinimapDeadHeroMarkersDrawn`, increments it inside the same marker loop that draws minimap hero markers and crossed dead markers, includes `D<n>` in the visible minimap footer, writes the count to `linux-client-shell.log`, and extends the `Final visible live minimap` stdout line with `deadHeroes=<n>`. This keeps dead-hero minimap visibility testable without touching gameplay, Windows, or the full UI path.
- Verification after the live-minimap dead-hero chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.21.23/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes markers=66 heroes=10 deadHeroes=0 creeps=30 objectives=26 moving=31 target=yes`, `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 towers=24 main=2 moving=31 damaged=1 deadHeroes=0 objectiveLines=2 commandLines=1`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live scoreboard diagnostics now expose movement, damage, and dead-hero totals from the same team marker aggregation used to draw the scoreboard overlay. `PW_Client/Game.cpp` adds `visibleLiveScoreboardMovingMarkersDrawn`, `visibleLiveScoreboardDamagedMarkersDrawn`, and `visibleLiveScoreboardDeadHeroMarkersDrawn`, resets and stores them with the scoreboard overlay counters, writes them to `linux-client-shell.log`, and extends the `Final visible live scoreboard` stdout line. This makes scoreboard state regressions easier to catch without touching gameplay, Windows, or the full UI path.
- Verification after the live-scoreboard state-counter chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.18.09/linux-client-shell.log` and exited `0`: `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 towers=24 main=2 moving=31 damaged=1 deadHeroes=0 objectiveLines=2 commandLines=1`, `Final visible live minimap: drawn=yes texture=yes markers=66 heroes=10 creeps=30 objectives=26 moving=31 target=yes`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live HUD diagnostics now identify the visible target panel in the compact final HUD summary. `PW_Client/Game.cpp` keeps using the existing `liveTargetState` sampler, adds a `finalVisibleLiveHudTargetSummary` log field, and extends `Final visible live HUD` with target kind, object id, faction, player id, and distance. This makes HUD target-panel regressions visible without changing gameplay, sampling, Windows, or the full UI path.
- Verification after the live-HUD target-summary chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.14.55/linux-client-shell.log` and exited `0`: `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=1 talentSlots=7 talentIcons=7 target=yes targetKind=hero targetObject=7393 targetFaction=1 targetPlayer=0 targetDistance=2.0`, `Final live HUD state: samples=229 ... target=yes object=7393 kind=hero faction=1 player=0 ... dist=2.0 ...`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL replay speed controls now share one rate-application helper. `PW_Client/Game.cpp` adds `ApplyLinuxReplayInputPlaybackRate(...)` so keyboard shortcuts, clickable replay buttons, and mouse-wheel replay-panel events all clamp through the same path, update `replayInputControlEvents` and `replayInputControlSource` consistently, and increment `replayInputSpeedChangeCount` only when the clamped rate actually changes. This removes duplicated speed-control branches without changing replay scheduling, gameplay, Windows, or the full UI path.
- Verification after the replay-speed helper chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.11.46/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=42 commands=123/123 statuses=3/3 calls=42 worldSteps=41 finalStep=41 stepMatch=yes commandMatch=yes statusMatch=yes ... rate=8 pacing=no/0 controls=1 speedChanges=0 source=command-line-speed error=none`, `Final visible replay controls: drawn=yes lines=5 buttons=5 progress=yes`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=123/123 ... stepMatch=yes commandMatch=yes error=none`.
- Previous chunk: native Linux/OpenGL replay input now accepts mouse-wheel speed changes over the visible replay-control panel. `PW_Client/Game.cpp` handles `MOUSE_WHEEL` only when the cursor is inside the shared replay panel rectangle, doubles or halves `replayInputPlaybackRate` through the existing clamp, records speed-change/control counters, and consumes those wheel events so they do not leak into map/loading interaction. Keyboard controls and clickable buttons stay intact, and Windows/full UI code remains untouched.
- Verification after the replay-panel wheel-speed chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.08.27/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=42 commands=123/123 statuses=3/3 calls=42 worldSteps=41 finalStep=41 stepMatch=yes commandMatch=yes statusMatch=yes ... rate=8 pacing=no/0 controls=1 source=command-line-speed error=none`, `Final visible replay controls: drawn=yes lines=5 buttons=5 progress=yes`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=123/123 ... stepMatch=yes commandMatch=yes error=none`. This smoke is a regression for the touched replay-input path; direct wheel injection remains manual/UI-test coverage.
- Previous chunk: native Linux/OpenGL live event feed now exposes objective marker totals in the visible world row and final diagnostics. `PW_Client/Game.cpp` builds the feed's world row from the live scoreboard tower/main marker counts, falls back to loaded world totals when needed, stores the combined objective count in `visibleLiveEventFeedObjectiveMarkersDrawn`, writes it to the final log, and adds `objectiveMarkers=<n>` to the `Final visible live event feed` stdout line. This keeps the event feed aligned with minimap/scoreboard objective visibility without touching gameplay, Windows, or the full UI path.
- Verification after the live-event-feed objective-marker chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.04.14/linux-client-shell.log` and exited `0`: `Final visible live event feed: drawn=yes rows=4 command=2 combat=1 objective=1 objectiveMarkers=26`, `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 towers=24 main=2 objectiveLines=2 commandLines=1`, `Final visible live minimap: drawn=yes texture=yes markers=66 heroes=10 creeps=30 objectives=26 moving=31 target=yes`, and replay input/file live PFWorld stayed valid with `commands=123/123`, `statuses=3/3`, `stepMatch=yes`, `commandMatch=yes`, and `error=none`.
- Previous chunk: native Linux/OpenGL live scoreboard diagnostics now report tower and main-building objective counts separately. `PW_Client/Game.cpp` persists the scoreboard's computed tower/main marker totals into `visibleLiveScoreboardTowerMarkersDrawn` and `visibleLiveScoreboardMainBuildingMarkersDrawn`, resets them with the scoreboard overlay counters, writes them to the final log, and adds `towers=<n> main=<n>` to the `Final visible live scoreboard` stdout line. This keeps objective scoreboard parity testable without touching gameplay, Windows, or the full UI path.
- Verification after the live-scoreboard objective-diagnostic chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-12.00.52/linux-client-shell.log` and exited `0`: `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 towers=24 main=2 objectiveLines=2 commandLines=1`, `Final visible live minimap: drawn=yes texture=yes markers=66 heroes=10 creeps=30 objectives=26 moving=31 target=yes`, `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... commands=123/123 statuses=3/3 ... stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=123/123 ... stepMatch=yes commandMatch=yes error=none`.
- Previous chunk: native Linux/OpenGL live minimap diagnostics now report objective marker visibility separately from hero and creep markers. `PW_Client/Game.cpp` persists the already-rendered minimap objective count into `visibleLiveMinimapObjectiveMarkersDrawn`, resets it with the other minimap overlay counters, writes it to the final log, and adds `objectives=<n>` to the `Final visible live minimap` stdout line. This makes tower/main-building minimap regressions visible in smokes without touching gameplay, Windows, or the full UI path.
- Verification after the live-minimap objective-diagnostic chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-11.57.39/linux-client-shell.log` and exited `0`: `Final visible live minimap: drawn=yes texture=yes markers=66 heroes=10 creeps=30 objectives=26 moving=31 target=yes`, `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... commands=123/123 statuses=3/3 ... stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=123/123 statuses=3/3`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=123/123 ... stepMatch=yes commandMatch=yes error=none`.
- Previous chunk: native Linux/OpenGL replay-control diagnostics now prove the clickable button row was rendered. `PW_Client/Game.cpp` adds `visibleReplayControlsButtonsDrawn`, resets it with the replay overlay counters, increments it for each drawn replay-control button, and writes it to both the final log and stdout. This keeps the new mouse-button overlay easier to regression-test without changing gameplay, replay playback, Windows, or the full UI path.
- Verification after the replay-control button-diagnostic chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-11.52.58/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=42 commands=123/123 statuses=3/3 calls=42 worldSteps=41 finalStep=41 stepMatch=yes commandMatch=yes statusMatch=yes ... controls=1 source=command-line-speed error=none`, `Final visible replay controls: drawn=yes lines=5 buttons=5 progress=yes`, `Final replay file storage: requested=yes valid=yes ... commands=123/123 statuses=3/3`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=123/123 ... stepMatch=yes commandMatch=yes error=none`.
- Previous chunk: native Linux/OpenGL replay input now has clickable on-screen controls instead of only keyboard shortcuts. `PW_Client/Game.cpp` shares one replay-control panel layout between input hit-testing and drawing, adds Pause/Play, Step, slower, faster, and reset buttons, consumes panel mouse clicks so they do not leak into map/loading interactions, and keeps the existing keyboard controls intact. The overlay now draws the same button rectangles it can hit-test, and the status rows report the active control source and pacing state. Windows/full UI code remains untouched.
- Verification after the replay-control mouse chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A native OpenGL replay-input smoke against `/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.07.05-11.49.45/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=42 commands=123/123 statuses=3/3 calls=42 worldSteps=41 finalStep=41 stepMatch=yes commandMatch=yes statusMatch=yes ... controls=1 source=command-line-speed error=none`, `Final visible replay controls: drawn=yes lines=5 progress=yes`, `Final replay file storage: requested=yes valid=yes ... commands=123/123 statuses=3/3`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=123/123 ... stepMatch=yes commandMatch=yes error=none`.
- Previous chunk: native Linux/OpenGL dynamic world markers now include real tower and main-building objective instances instead of only heroes and creeps. The Linux-only `LinuxDynamicWorldMarker::EKind` adds `KIND_TOWER` and `KIND_MAIN_BUILDING`, and `PFWorld::GetLinuxDynamicWorldMarkers(...)` exports `PFTower`/`PFMainBuilding` positions, factions, sizes, and health through the existing native marker stream. The OpenGL 3D map draws larger objective markers with status bars, the live minimap draws bordered objective pips and labels `O<n>` in the minimap summary, and the live scoreboard center line now uses dynamic objective marker totals with loaded-count fallback. Windows/full UI code remains untouched.
- Verification after the objective-marker chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFWorld.h pw/branches/r1117/Src/PF_GameLogic/PFWorld.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A 60-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.07.05-11.42.14/linux-client-shell.log` and exited `0`, proving objective markers are now in the live stream with `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/195/10/89/70/89 status=195/89 heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`, `Final visible live minimap: drawn=yes texture=yes markers=195 heroes=10 creeps=159 moving=89 target=yes`, and `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 objectiveLines=3 commandLines=1`. Replay capture/storage/transceiver/server-transceiver remained valid with `commands=123/123` and `statuses=3/3`, and selected-target kill/respawn proof still completed (`killed=yes hero=yes respawn=yes`).
- Previous chunk: native Linux/OpenGL live overlays now preserve dead hero visibility instead of hiding those markers until respawn. `DrawLinuxMapDynamicWorldMarkerPreview(...)` keeps dead heroes in the 3D map as muted crossed markers while still suppressing dead creep clutter and skipping dead-unit meshes/status bars. `DrawLinuxLiveMinimapOverlay(...)` mirrors that crossed dead-hero marker on the minimap, `DrawLinuxLiveScoreboardTeamColumn(...)` labels dead hero counts when present, and `DrawLinuxLiveEventFeedOverlay(...)` adds a respawn row only while a dead hero is present. Windows/full UI and gameplay state remain untouched.
- Verification after the dead-hero overlay chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.07.05-11.33.11/linux-client-shell.log` and exited `0`, preserving loaded-world rendering and UI overlays with `Final map 3D preview: terrain=yes/9216/18050 ... dynamic=yes/166/10/89/67/89 status=166/89 heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`, `Final visible live minimap: drawn=yes texture=yes markers=166 heroes=10 creeps=156 moving=89 target=yes`, `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 objectiveLines=3 commandLines=1`, and `Final visible live event feed: drawn=yes rows=5 command=5 combat=0 objective=0`. The same smoke kept replay capture/storage/transceiver/server-transceiver valid with `commands=248/248` and `statuses=5/5`, completed selected-target kill/respawn proof (`killed=yes hero=yes respawn=yes`), and covered move, stop, attack, follow, combat move, hold, cancel, minimap signal, use-unit, activate/use-talent, portal, consumable buy/use, raise-flag, pickup, and minigame init/leave.
- Previous chunk: native Linux/OpenGL loading roster cards now expose real locale metadata from the captured loading runtime instead of leaving all metadata labels hidden. `DrawLinuxLoadingRosterCard(...)` adds a compact `Loc <locale>` token to the existing metadata line when `LinuxLoadingRuntimeHeroEntry::locale` is present, so the selected local player's `en-US` metadata is visible without inventing rank, rating, or premium data. Windows/full UI code remains untouched.
- Verification after the loading-roster locale metadata chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A 30-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.07.05-11.27.12/linux-client-shell.log` and exited `0`, improving `Final visible loading roster: drawn=yes rows=10 portraits=10 flags=10 progress=10 force=10 meta=1 rank=0 premium=0` while preserving loading info/chat, loaded-world rendering, live HUD/minimap/scoreboard/event feed, replay capture/storage/transceiver/server-transceiver validity with `commands=97/97` and `statuses=3/3`, and scripted command coverage through move, stop, attack, follow, combat move, hold, cancel, minimap signal, use-unit, activate/use-talent, portal, consumable buy/use, raise-flag, and pickup.
- Previous chunk: native Linux/OpenGL live HUD attack ability diagnostics now count visible fallback ability icons instead of reporting `abilityIcons=0` when the DB texture is missing. `DrawLinuxLiveHudOverlay(...)` now distinguishes texture-backed attack icons from fallback-rendered attack slots, keeps drawing the existing real texture path when available, and increments `visibleLiveHudAbilityIconsDrawn` for either visible path. Windows/full UI code remains untouched.
- Verification after the live-HUD ability-fallback chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. A 60-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-11.16.41/linux-client-shell.log` and exited `0`, preserving loaded-world rendering and UI overlays while improving `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=1 talentSlots=7 talentIcons=7 target=yes`. The same smoke kept `Final visible live minimap: drawn=yes texture=yes markers=169 heroes=10 creeps=159 moving=89 target=yes`, `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 objectiveLines=3 commandLines=1`, replay capture/storage/transceiver/server-transceiver valid with `commands=123/123` and `statuses=3/3`, and scripted command coverage through use-unit, activate/use-talent, portal, consumable buy/use, flag, minigame init, and pickup.
- Previous chunk: native Linux/OpenGL loading artwork now shows selected-hero ability fallback slots too, so the startup/loading composite matches the visible lobby details panel more closely. `CompositeSelectedHeroAbilityIcons(...)` now reserves up to the first four featured ability slots instead of filtering out entries whose DB icon texture failed to load, keeps drawing real scaled textures when available, and draws a compact tinted fallback marker for missing ability icons. Windows/full UI code remains untouched.
- Verification after the loading-artwork ability-fallback chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. The normal 8-second native OpenGL menu regression wrote `Bin/logs/2026.07.05-11.13.49/linux-client-shell.log` and exited `0`; startup now reports `Displayed artwork source: selected map back+logo + tactical inset + lineup portraits + hero abilities + hero talents + loading ui assets`, while preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10 portraits=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=1 talents=12`.
- Previous chunk: Linux/null-render base-unit autotargeting is no longer an inert stub. `PFBaseUnit::AutoTarget(...)` now preserves the current target while the Windows-style `targetRevisionTime` is still active, drops invalid or forbidden targets through the existing lifecycle helpers, respects dead/forbid/strong-target guards, retargets through `FindTarget(...)`, clears scream targets, and keeps the Linux `GetTargetingParamsPtr()` guard because bootstrap-created units can lack targeting parameters. Linux `PFBaseUnit::Step(...)` now invokes autotargeting before the existing attack/chase block. Windows/full-render code paths remain untouched.
- Verification after the Linux base-unit autotargeting chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFBaseUnit.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0`. A 170-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-11.06.54/linux-client-shell.log` and exited `0`: `Final game transceiver runtime: ready=yes world=yes step=1499 commands=497 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=497 replayBytes=27189`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1500 active=9 bots=1/1 ... commands=464/464 fallback=0 move=16 combat=64 stop=0 follow=0 attack=75 activate=72 useTalent=229 buy=4 consumable=1 portal=1 pickup=1 raise=1 other=0`, and `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`. Replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=497/497` and `statuses=5/5`, and minigame leave proof completed (`attempted=1 completed=1`).
- A complete native OpenGL replay-input regression against that regenerated replay used the absolute replay path, wrote `Bin/logs/2026.07.05-11.09.54/linux-client-shell.log`, and exited `0`: startup adopted the replay header (`requested=yes usable=yes adoptedMap=yes adoptedLineup=yes autoCreate=yes`), `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp segments=167 commands=497/497 statuses=5/5 calls=167 worldSteps=166 finalStep=166 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=497/497 before=0 after=497 stepMatch=yes commandMatch=yes error=none`, and AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux base-unit autotargeting chunk wrote `Bin/logs/2026.07.05-11.10.35/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10 portraits=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=1 talents=12`.
- Previous chunk: native Linux/OpenGL lobby hero details now show fallback ability slots instead of hiding abilities whose DB icon texture is missing. `DrawLinuxBootstrapHeroPreviewDetails(...)` now includes all featured abilities in the visible ability panel, keeps using the real texture when one resolves, and draws a compact fallback marker (`A` for the default attack) when the texture is absent. The final `visibleLobbyHeroAbilityIconsDrawn` counter now proves that the selected hero has a visible ability surface, while Windows/full UI code remains untouched.
- Verification after the lobby ability-slot chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -- -j2` exited `0` with only existing legacy warnings. The normal 8-second native OpenGL menu regression wrote `Bin/logs/2026.07.05-11.02.41/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10 portraits=10`, and improving hero details to `Final visible lobby hero details: drawn=yes portrait=yes abilities=1 talents=12`.
- Previous chunk: two-agent parallel chunk combining gameplay target-scan parity and visible lobby parity. Linux/null-render `PFBaseUnit::FindTarget(...)` now first tries a `PFAIWorld::ForAllUnitsInRange(...)` scan with Windows-style search radius expansion, target flags, flying support, direct-sight flags, `UnitMaskingPredicate`, `CanSelectTarget(...)`, and real range checks; the previous Linux bootstrap-unit vector scan remains as fallback for partial bootstrap worlds. Windows/full-render code paths are untouched. The native Linux/OpenGL lobby lineup strip now resolves each `LinuxHeroCatalogEntry::iconRef` through the existing lobby texture cache and draws compact real hero portraits inside the ten lineup slots, with a new `visibleLobbyDetailsLineupPortraitsDrawn` diagnostic so menu smokes can prove the portrait path instead of only counting text slots.
- Verification after the AI-world target-scan plus lobby-lineup-portrait chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/PF_GameLogic/PFBaseUnit.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 170-second native OpenGL `--bootstrap-create-game` regression after the combined chunk wrote `Bin/logs/2026.07.05-10.51.33/linux-client-shell.log` and exited `0`. It proves the AI-world `FindTarget(...)` path keeps live AI, scripted command coverage, loaded-world rendering, and replay capture stable: `Final game transceiver runtime: ready=yes world=yes step=1463 commands=484 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=484 replayBytes=26348`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1464 active=9 bots=1/1 ... commands=453/453 fallback=0 move=16 combat=64 stop=0 follow=0 attack=73 activate=72 useTalent=220 buy=4 consumable=1 portal=1 pickup=1 raise=1 other=0`, and `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`. The minigame leave proof completed (`attempted=1 completed=1`) and replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=484/484` and `statuses=5/5`.
- A complete native OpenGL replay-input regression against that regenerated replay used the absolute replay path under `Bin/logs`, wrote `Bin/logs/2026.07.05-10.56.09/linux-client-shell.log`, and exited `0`: startup adopted the replay header (`requested=yes usable=yes adoptedMap=yes adoptedLineup=yes autoCreate=yes`), `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp segments=160 commands=484/484 statuses=5/5 calls=160 worldSteps=159 finalStep=159 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=484/484 before=0 after=484 stepMatch=yes commandMatch=yes error=none`, and AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the combined chunk wrote `Bin/logs/2026.07.05-10.56.50/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game` and upgrading the lobby detail counter to `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10 portraits=10`; hero details remain `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, so filling ability icons remains an open visible-parity task.
- Previous chunk: Linux/null-render base-unit target lifecycle now keeps the same attacker/target bookkeeping used by the Windows/full-render path while keeping Windows untouched. Linux `PFBaseUnit::OnBeingTargeted(...)` now registers attackers, `AssignTarget(...)` drops the old target, emits `PFBaseUnitAssignTargetEvent`, links the target back through `OnBeingTargeted(...)`, and updates `targetRevisionTime`, while `DropTarget()` removes the attacker ring link and clears target state. `StopAttackingMe(...)` now clears current attackers and drops hostile inbound dispatch targets, respecting pursue-dispatch filtering. The Linux branch uses `GetTargetingParamsPtr()` before reading `targetRevisionTime` because bootstrap-created Linux units can lack targeting parameters; this guard fixed an immediate create-game crash found during validation. Linux `AutoTarget(float)` is still stubbed, so this is target lifecycle parity, not complete retargeting parity yet.
- Verification after the Linux base-unit target-lifecycle chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFBaseUnit.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings. A short 5-second `--bootstrap-create-game` crash repro also exited `0` after the targeting-parameter guard and wrote `Bin/logs/2026.07.05-10.37.55/linux-client-shell.log`.
- A 170-second native OpenGL `--bootstrap-create-game` regression after the target-lifecycle chunk wrote `Bin/logs/2026.07.05-10.38.10/linux-client-shell.log` and exited `0`. It proves attacker bookkeeping, assignment/drop events, scripted command coverage, live AI, loaded-world rendering, and replay capture remain stable: `Final game transceiver runtime: ready=yes world=yes step=1520 commands=504 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=504 replayBytes=27682`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1521 active=9 bots=1/1 ... commands=474/474 fallback=0 move=16 combat=64 stop=0 follow=0 attack=76 activate=72 useTalent=238 buy=4 consumable=1 portal=1 pickup=1 raise=1 other=0`, and `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`. The minigame leave proof completed and replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=504/504` and `statuses=5/5`.
- A complete native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-10.41.17/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=170 commands=504/504 statuses=5/5 calls=170 worldSteps=169 finalStep=169 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=504/504 before=0 after=504 stepMatch=yes commandMatch=yes error=none`, and AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux base-unit target-lifecycle chunk wrote `Bin/logs/2026.07.05-10.41.58/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render base-unit target eligibility now mirrors the Windows/full-render checks while keeping Windows untouched. Linux `PFBaseUnit::CanAttackFlying()` now asks the active attack ability whether it can hit flying units instead of always returning true. Linux `CanAttackTarget(...)` now requires a valid, vulnerable target and rejects flying targets unless the attack can hit them. Linux `CanSelectTarget(...)` now rejects self/dead/forbidden-auto-target targets, honors optional warfog visibility, and delegates to attached behavior selection rules. This removes the old Linux shortcut where any non-null unit could be selected, improving AI scans, scripted combat proofs, and future MicroAI/talent target quality without changing DirectX or Windows code.
- Verification after the Linux base-unit target-eligibility chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFBaseUnit.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 170-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-10.26.20/linux-client-shell.log` and exited `0`. It proves the stricter Linux target eligibility keeps live AI, scripted command coverage, loaded-world rendering, and replay capture stable: `Final game transceiver runtime: ready=yes world=yes step=1505 commands=492 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=492`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1506 active=9 bots=1/1 ... commands=459/459 fallback=0 move=17 combat=63 attack=70 activate=72 useTalent=232 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, and `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`. The minigame leave proof completed (`attempted=1 completed=1`) and replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=492/492` and `statuses=5/5`.
- A complete native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-10.29.21/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=161 commands=492/492 statuses=5/5 calls=161 worldSteps=160 finalStep=160 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=492/492 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=492/492 before=0 after=492 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux base-unit target-eligibility chunk wrote `Bin/logs/2026.07.05-10.30.46/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI route progression now uses the Windows/full-render route-segment logic while keeping Windows untouched. Linux `GetNextRoutePoint(...)` no longer advances by only taking the nearest waypoint plus one; it detects already-bypassed waypoints by walking forward along the route segment direction and applies the same near-next-point skip used by Windows. Linux `CompareRoutePoints(...)` now also matches the Windows same-segment distance comparison instead of only comparing waypoint indexes. Empty and single-point route guards were kept in the Linux branch so bootstrap-only partial route data cannot read outside the vector. This feeds lane walking, appended-quarter route checks, war-front/border comparison, tower-retreat rally offsets, and respawn lane pushes.
- Verification after the Linux AI route-progress chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIHelper.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 170-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-10.15.11/linux-client-shell.log` and exited `0`. It proves the route-progress change keeps live AI movement, the scripted chain, loaded-world rendering, and replay capture stable: `Final game transceiver runtime: ready=yes world=yes step=1490 commands=486 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=486`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1491 active=9 bots=1/1 ... commands=456/456 fallback=0 move=17 combat=63 attack=70 activate=72 useTalent=229 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, and `Final map 3D preview: terrain=yes/9216/18050 ... heroMeshes=yes/10/62625 creepMeshes=yes/48/183161`. The minigame leave proof completed (`attempted=1 completed=1`) and replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=486/486` and `statuses=5/5`.
- A complete native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-10.18.15/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=160 commands=486/486 statuses=5/5 calls=160 worldSteps=159 finalStep=159 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=486/486 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=486/486 before=0 after=486 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI route-progress chunk wrote `Bin/logs/2026.07.05-10.18.57/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI target acquisition and retreat routing now match the Windows/full-render controller more closely while keeping Windows untouched. Linux `PFAIHelper::FindEnemyNear()` now scans `PFAIWorld::ForAllUnitsInRange(...)` with the same visibility, flying-target, `UnitMaskingPredicate`, and `CanSelectTarget(...)` filtering pattern used by Windows instead of taking the shortcut `FindTarget(...)` result; it keeps a fallback for missing AIWorld. Linux `PFAIController::GoToSpawnPos()` now queues `GoToOwnBase()` after the spawn move, matching the Windows retreat/spawn routing sequence so healing/respawn movement uses the route-home path instead of only a direct spawn point move.
- Verification after the Linux AI target-scan/retreat-routing chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIHelper.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 170-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-10.04.50/linux-client-shell.log` and exited `0`. It proves the stricter Linux AI target scan keeps the scripted chain, AI-originated commands, live world, and replay capture alive while reducing noisy AI command volume: `Final game transceiver runtime: ready=yes world=yes step=1535 commands=516 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=522`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1536 active=9 bots=1/1 ... commands=489/489 fallback=0 move=17 combat=70 attack=81 activate=72 useTalent=241 buy=4 consumable=1 portal=1 pickup=1 raise=1 other=0`, and the minigame leave proof completed (`attempted=1 completed=1`). Replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=522/522` and `statuses=5/5`.
- A complete native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-10.07.50/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=180 commands=522/522 statuses=5/5 calls=180 worldSteps=179 finalStep=179 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=522/522 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=522/522 before=0 after=522 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI target-scan/retreat-routing chunk wrote `Bin/logs/2026.07.05-10.08.33/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI interaction priority now treats flag raising and pickup as lower priority than nearby combat. `TryPickupObject()` no longer starts pickup searches while `FindEnemyNear()` reports a target, and Linux `AIFlagRaisingState` / `AIPickupObjectState` abandon object interactions when a nearby enemy appears so the controller can return to attack/war-front decisions. Windows/full-render branch untouched.
- Verification after the Linux AI interaction-priority chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIStates.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 170-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-09.34.44/linux-client-shell.log` and exited `0`. It proves the interaction-priority change keeps the scripted chain, AI-originated commands, and replay capture alive: `Final game transceiver runtime: ready=yes world=yes step=1547 commands=1510 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=1512`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1548 active=9 bots=1/1 ... commands=1480/1480 fallback=0 move=933 combat=67 attack=128 activate=72 useTalent=275 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, and the minigame leave proof completed (`attempted=1 completed=1`). Replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=1512/1512` and `statuses=5/5`.
- A complete native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-09.38.32/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=965 commands=1512/1512 statuses=5/5 calls=965 worldSteps=964 finalStep=964 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=1512/1512 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=1512/1512 before=0 after=1512 stepMatch=yes commandMatch=yes error=none`. The earlier 18-second replay window exited `0` but stopped before this longer replay fully drained, so this chunk uses a 30-second, 8x replay window for the complete verification. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI interaction-priority chunk wrote `Bin/logs/2026.07.05-09.39.25/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI lane setup now mirrors the Windows/full-render controller route initialization more closely while keeping the Windows branch untouched. The Linux `PFAIController` constructor now honors `BotsSettings::midOnly` by forcing the center lane, `SetLine(...)` randomizes negative line requests through `NRandom::Random(...)` instead of clamping to lane `0`, applies the same per-lane `ShiftWaypoint(...)` offsets used by Windows, appends enemy quarters that sit past the current route, and appends a final approach point in front of the enemy main building. This gives Linux bots the same shaped road used by Windows lane walking, tower pressure, war-front checks, and respawn lane pushes.
- Verification after the Linux AI lane-setup parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 170-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-09.25.51/linux-client-shell.log` and exited `0`. It proves the richer Linux road setup keeps the scripted chain and AI-originated command proofs alive: `Final game transceiver runtime: ready=yes world=yes step=1511 commands=1458 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=1463`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1512 active=9 bots=1/1 ... commands=1431/1431 fallback=0 move=897 combat=63 attack=128 activate=72 useTalent=266 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, and the minigame leave proof completed (`attempted=1 completed=1`). Replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=1463/1463` and `statuses=5/5`.
- A sequential native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-09.28.50/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=929 commands=1463/1463 statuses=5/5 calls=929 worldSteps=928 finalStep=928 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=1463/1463 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=1463/1463 before=0 after=1463 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI lane-setup parity chunk wrote `Bin/logs/2026.07.05-09.29.29/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI decision priority is closer to the Windows/full-render controller while keeping the Windows branch untouched. `PFAIController::UseTalents()` and the Linux `TalentPart::UseTalents()` helper now choose a random talent among equal-priority best candidates instead of always choosing the first one, matching the Windows AI tie-break. The helper path also applies the Linux `TalentWrapper::CanBeUsed()` guard before target search, and Linux `PFAIController::RaiseFlags()` now skips flag searches while `PFAIHelper::FindEnemyNear()` reports a nearby enemy so combat pressure is not preempted by flag interaction.
- Verification after the Linux AI decision-priority chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIHelper.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 170-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-09.03.29/linux-client-shell.log` and exited `0`. It proves the decision-priority changes keep the scripted chain and AI-originated command proofs alive: `Final game transceiver runtime: ready=yes world=yes step=1511 commands=1458 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=1463`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1512 active=9 bots=1/1 ... commands=1431/1431 fallback=0 move=897 combat=63 attack=128 activate=72 useTalent=266 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, and the minigame leave proof completed (`attempted=1 completed=1`). Replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=1463/1463` and `statuses=5/5`.
- A sequential native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-09.06.29/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=929 commands=1463/1463 statuses=5/5 calls=929 worldSteps=928 finalStep=928 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=1463/1463 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=1463/1463 before=0 after=1463 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI decision-priority chunk wrote `Bin/logs/2026.07.05-09.07.06/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render `PFAIController` now matches more of the Windows/full-render controller lifecycle while keeping the Windows branch untouched. Linux death handling now calls `Cleanup()` and resets `healingTick`, respawn immediately queues the same shop path that Windows queues before delayed lane pressure, road/spawn/shop routing now respects mounted heroes that cannot be controlled, and the Linux step loop runs `FsmStep(...)` before talent/healing/high-level decisions like the Windows loop. The Linux-only proof gates still run after the state-machine step so bootstrap command coverage remains deterministic.
- Verification after the Linux AI lifecycle/order chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 170-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-08.45.42/linux-client-shell.log` and exited `0`. It proves the Windows-like lifecycle/order change keeps the full scripted chain and later AI-originated command proofs alive: `Final game transceiver runtime: ready=yes world=yes step=1352 commands=1235 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=1241`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1353 active=9 bots=1/1 ... commands=1212/1212 fallback=0 move=736 combat=56 attack=125 activate=72 useTalent=218 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, and the minigame leave proof completed (`attempted=1 completed=1`). Replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=1241/1241` and `statuses=5/5`.
- A sequential native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-08.50.07/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=770 commands=1241/1241 statuses=5/5 calls=770 worldSteps=769 finalStep=769 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=1241/1241 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=1241/1241 before=0 after=1241 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI lifecycle/order chunk wrote `Bin/logs/2026.07.05-08.50.45/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render `PFAIController::Step(...)` now mirrors another important slice of the Windows/full-render controller flow while keeping the Windows branch untouched. The Linux controller now exits early when `PFAIWorld::WasGameFinished()` is set, waits until the configured bot `timeToGo` before issuing the post-respawn enemy-base lane push, clears the respawn marker only after that push is sent, and calls the normal `Heal(true)` path after respawn routing. `OnBecameIdle()` also no longer pushes a lane route while the controller is still in the respawn-wait window.
- Verification after the Linux AI respawn/game-finished chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 120-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-08.36.06/linux-client-shell.log` and exited `0`. It proves the respawn/game-finished controller parity change does not starve the scripted proof chain and does not flood AI commands: `Final game transceiver runtime: ready=yes world=yes step=1124 commands=612 ... heroMoveCommands=3 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=612`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1125 active=9 bots=1/1 ... commands=583/583 fallback=0 move=271 combat=51 attack=65 activate=68 useTalent=123 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, and the minigame leave proof completed (`attempted=1 completed=1`). Replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=612/612` and `statuses=5/5`.
- A sequential native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-08.38.54/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=336 commands=612/612 statuses=5/5 calls=336 worldSteps=335 finalStep=335 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=612/612 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=612/612 before=0 after=612 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI respawn/game-finished chunk wrote `Bin/logs/2026.07.05-08.39.17/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI state behavior is now closer to the Windows/full-render AI state machine while preserving the Windows branch. The Linux `AIMoveToState`, `AIGoToObjectState`, `AIAttackUnitState`, `AIPickupObjectState`, `AIUseTeleportState`, and `EscapeFromTowerState` now carry the same important production semantics: wait briefly for move/attack command execution, keep combat moves alive while a current target exists, follow moving object targets, delay pickup until movement starts, resume lane pressure after teleport, and stop tower escape only after the hero is outside tower range. Because Linux null-render `IsMoving()` can read false too early, Linux uses a longer local stuck window before treating a not-moving unit as a failed move; this prevents the Windows-style recovery logic from flooding the native scheduler.
- The same chunk makes the bootstrap command proof deterministic under real AI traffic. The selected-target proof can now top up only the Linux bootstrap controlled hero after sustained live damage has already been proven, so the activate-talent and later minigame proof chain no longer depends on incidental creep gold timing. This is bootstrap instrumentation only; Windows/full-render gameplay remains untouched.
- Verification after the Linux AI state-parity/pacing chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIStates.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 120-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-07.39.58/linux-client-shell.log` and exited `0`. It proves the state pacing no longer starves the scripted proof chain and no longer floods AI commands: `Final game transceiver runtime: ready=yes world=yes step=1118 commands=463 ... heroMoveCommands=3 ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=466`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1119 active=9 bots=1/1 ... commands=438/438 fallback=0 move=121 combat=56 attack=68 activate=68 useTalent=120 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, and the minigame leave proof completed (`attempted=1 completed=1`). Replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=466/466` and `statuses=5/5`.
- A sequential native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-07.42.07/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=215 commands=466/466 statuses=5/5 calls=215 worldSteps=214 finalStep=214 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=466/466 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=466/466 before=0 after=466 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI state-parity/pacing chunk wrote `Bin/logs/2026.07.05-07.42.27/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Latest chunk: Linux/null-render AI now has the Windows-style tower/war-front controller decisions that were still empty on Linux, while keeping the Windows/full-render branch untouched. The Linux controller can now find nearby enemy towers/main buildings through the active `PFAIWorld` voxel query, push `ATTACKINGTOWER` combat moves toward visible structures, retreat from visible/attacking towers with `EscapeFromTowerState`, compute fallback rally points with the same path-offset helper as Windows, and accumulate route-front drift so bots can push `BACKTOWARFRONT` movement toward the creep/frontier line instead of only chasing nearby units. A staged Linux-only tower proof after the portal/flag/pickup proof pushes one selected controller toward the first alive enemy route structure through the normal native `CombatMove` command path.
- Verification after the Linux AI tower/war-front chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIController.h` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 120-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-06.44.37/linux-client-shell.log` and exited `0`. It proves the new AI controller changes do not starve the scripted proof chain: `Final game transceiver runtime: ready=yes world=yes step=1058 commands=239 ... heroCombatMoveSent=yes ... heroUsePortalSent=yes ... heroUseConsumableSent=yes ... heroBuyConsumableSent=yes ... heroRaiseFlagSent=yes ... heroInitMinigameSent=yes ... heroPickupObjectSent=yes ... replayCommands=239`, while Linux AI stayed live with `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1059 active=9 bots=1/1 ... commands=214/214 fallback=0 move=3 combat=11 attack=14 activate=65 useTalent=116 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`. Replay capture/readback/storage/transceiver/server-transceiver all stayed valid with `commands=239/239` and `statuses=5/5`.
- A sequential native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-06.46.47/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=72 commands=239/239 statuses=5/5 calls=72 worldSteps=71 finalStep=71 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=239/239 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=239/239 before=0 after=239 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI tower/war-front chunk wrote `Bin/logs/2026.07.05-06.47.06/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI now has a deterministic controller-originated interaction proof for portal, flag, and pickup commands, while keeping Windows/full-render AI untouched. After the existing AI consumable proof window, one selected Linux AI controller sends `CreateCmdUsePortal(...)`, `CreateCmdRaiseFlag(...)`, and `CreateCmdPickupObject(...)` through `PFAIHelper` and the normal native transceiver/replay path. This closes the previous "controller command was wired but not observed live" proof gap for those three command kinds; the normal range-limited flag/pickup states and conservative lane-walk `TryTeleport()` hook remain in place for organic behavior. Linux `UseTalents()` now also skips talents that `TalentWrapper::CanBeUsed()` rejects, reducing rejected talent command traffic without changing the Windows AI path.
- The same chunk fixed a Linux bootstrap scheduler proof issue exposed by live AI traffic. `PW_Client/Game.cpp` now records whether each scripted bootstrap command type has executed at least once and lets later scripted proof gates advance based on that observation, instead of requiring the scripted command to remain the most recent packed command. This keeps AI-controller commands from starving the scripted acceptance chain when they interleave with the controlled hero proof stream; it is bootstrap instrumentation only.
- Verification after the Linux AI interaction-proof chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIController.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0` with only existing legacy warnings.
- A 95-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-06.28.41/linux-client-shell.log` and exited `0`. It proves the new AI-controller interaction commands and the scheduler gate fix together: `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=915 active=9 bots=1/1 ... commands=142/142 fallback=0 move=2 combat=11 attack=9 activate=57 useTalent=58 buy=1 consumable=1 portal=1 pickup=1 raise=1 other=0`, while the scripted chain still reached all later command classes (`heroActivateTalentSent=yes`, `heroUseTalentSent=yes`, `heroUsePortalSent=yes`, `heroUseConsumableSent=yes`, `heroBuyConsumableSent=yes`, `heroRaiseFlagSent=yes`, `heroInitMinigameSent=yes`, `heroPickupObjectSent=yes`) and replay capture/storage/transceiver/server-transceiver stayed valid with `commands=169/169` and `statuses=5/5`. The live map, HUD, minimap, scoreboard, event feed, minigame leave proof, creep runtime, and 3D map preview remained active.
- A sequential native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-06.30.53/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=62 commands=169/169 statuses=5/5 calls=62 worldSteps=61 finalStep=61 stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=169/169 statuses=5/5 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=169/169 before=0 after=169 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the Linux AI interaction-proof chunk wrote `Bin/logs/2026.07.05-06.31.13/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI lane walking now has the native portal decision hook that was still stubbed on Linux. The Linux `AIMoveByLineState` now calls `PFAIController::TryTeleport()` like the Windows/full-render state, and the Linux `TryTeleport()` branch mirrors the conservative production checks: wait for `BotsSettings::timeToTeleport`, pick the first alive allied route tower from `PFAIWorld`, reject nearby or wrong-direction targets, require `GetPortal()->CanBeUsed()` and no cast limitations, then push `AIUseTeleportState` so `PFAIHelper::UsePortal()` sends `CreateCmdUsePortal(...)` through the native transceiver path. A Linux-local scan delay prevents route/tower scans from running every frame. Windows/full-render AI remains on its existing implementation.
- A 210-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-06.02.44/linux-client-shell.log` and exited `0`. This run crossed the normal bot portal threshold (`tp=180`) and stayed stable with the new hook compiled and running: `Final game transceiver runtime: ... step=1808 commands=547 ... replayCommands=547 replayBytes=36752`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=1809 active=9 bots=1/1 ... commands=535/535 fallback=0 move=2 combat=11 attack=12 activate=72 useTalent=436 buy=1 consumable=1 portal=0 pickup=0 raise=0`, and replay capture/readback/storage/transceiver/server-transceiver all stayed valid with `commands=547/547` and `statuses=3/3`. The MOBA content still did not naturally send an AI portal command because late-window bots were in attack/talent states rather than lane-walking; this chunk closes the Linux stub/hook gap, but a content-forced portal proof remains open.
- Verification after the Linux AI portal-hook chunk: built `PrimeWorldLinuxClient`, `PrimeWorldLinuxGameplayProbe`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxRenderProbe` with `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2`; the final build exited `0` with only existing legacy warnings.
- A clean 90-second native OpenGL `--bootstrap-create-game` regression after the portal-hook chunk wrote `Bin/logs/2026.07.05-06.07.33/linux-client-shell.log` and exited `0`: `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=879 active=9 bots=1/1 ... commands=147/147 fallback=0 move=2 combat=11 attack=12 activate=56 useTalent=64 buy=1 consumable=1 portal=0 pickup=0 raise=0`, `Final game transceiver runtime: ... commands=159 ... replayCommands=159 replayBytes=8308`, and capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=159/159` and `statuses=3/3`.
- A sequential native OpenGL replay-input regression against that regenerated replay wrote `Bin/logs/2026.07.05-06.09.13/linux-client-shell.log` and exited `0`: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... commands=159/159 statuses=3/3 ... stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=159/159 statuses=3/3 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=159/159 before=0 after=159 stepMatch=yes commandMatch=yes error=none`.
- The normal 8-second native OpenGL menu regression after the portal-hook chunk wrote `Bin/logs/2026.07.05-06.10.00/linux-client-shell.log` and exited `0`, preserving `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`.
- Previous chunk: Linux/null-render AI now has conservative flag-raising and pickup-object state wiring that uses real native commands instead of inert stubs. `PFAIController` periodically searches for only nearby raisable flagpoles and pickupable objects, skips higher-priority combat/healing/shop/teleport states, and pushes the existing AI state machine states so commands still flow through `PFAIHelper` / the native transceiver path. `PFInteractObjectState` now performs the queued interaction action on Linux, `PFCreatureRaiseFlagState::DoAction()` calls the real flagpole raise hooks, and `PFHeroPickupObjectState::DoAction()` calls the pickup object's real `PickUp(...)` path. `PFWorld` gained nearest-object Linux helpers so controller AI does not chase the first global object on the map. Windows/full-render paths remain on their existing behavior.
- The first version of that chunk intentionally got tested before tightening: a 90-second create-game smoke passed but exposed a command flood (`Final Linux AI controllers: ... commands=7581/7581 ... combat=7479`). The implementation was then narrowed to range-limited nearest flag/pickup helpers and higher-priority state guards. The fixed 90-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-05.55.10/linux-client-shell.log` and exited `0`: `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=879 active=9 bots=1/1 ... commands=147/147 fallback=0 move=2 combat=11 attack=12 activate=56 useTalent=64 buy=1 consumable=1 portal=0 pickup=0 raise=0 other=0`, `Final game transceiver runtime: ... commands=159 ... replayCommands=159 replayBytes=8308`, and replay capture/readback/storage/transceiver/server-transceiver all stayed valid with `commands=159/159` and `statuses=3/3`.
- Verification after the Linux AI flag/pickup interaction chunk: built `PrimeWorldLinuxClient`, `PrimeWorldLinuxGameplayProbe`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxRenderProbe` with `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2`; the final build exited `0` with only existing legacy warnings.
- A native OpenGL replay-input regression against the generated replay exited `0` and validated live replay playback: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... commands=159/159 statuses=3/3 ... stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=159/159 statuses=3/3 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=159/159 before=0 after=159 stepMatch=yes commandMatch=yes error=none`. The run also kept the visible live replay HUD/minimap/scoreboard/event feed active.
- The normal 8-second native OpenGL menu regression exited `0` and keeps the starting UI visible: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, with expected inactive gameplay/replay/AI counters.
- Latest chunk: Linux replay/live-PFWorld playback now survives the real AI consumable buy/use commands generated by create-game, while preserving the Windows/full-render paths. The Linux bootstrap command resolver now resolves serialized ability targets against the live `PFWorld` before `CmdUseTalent`, `CmdUsePortal`, and `CmdUseConsumable` execute. For replayed consumables whose serialized unit target cannot be resolved from the replay `CPtr`, `CmdUseConsumable` falls back to the resolved live command hero as the target, which matches the self-potion path and prevents the Linux dispatch/applicator code from touching a stale unit target. `CmdBuyConsumable` still resolves the live hero from the encoded append-slot hero object id, so replayed buy followed by replayed use now consumes the slot item (`qty=1->0`) in a freshly loaded live PFWorld.
- The queued controller-driven AI consumable proof is now no-fallback in live create-game. The Linux AI controller keeps the shop/healing/consumable state wiring, uses the native `CmdBuyConsumable` executor through the transceiver, observes the purchased slot, lowers health for the potion proof, and sends `CmdUseConsumable` through the same native Linux command path. Windows/full-render AI remains on its existing command/state path.
- The same chunk fixed a Linux/null-render crash hit by AI `UseTalent` throw applicators. `ProbeEffectsPoolRuntime(...)` now keeps the verified `PF_Core::EffectsPool` alive for the runnable Linux client under `PW_LINUX_NULL_RENDER`, and `PFApplThrow::PrepareFlyEffect()` now guards a missing effects pool or empty fly effect before retrieving. This keeps Windows/full-render behavior on the normal initialized effects-pool path while preventing the native Linux shell from crashing when gameplay-owned applicator effects start after diagnostics.
- Verification after the latest chunk: built `PrimeWorldLinuxClient`, `PrimeWorldLinuxGameplayProbe`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxRenderProbe` with `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2`; the final build exited `0` with only existing legacy warnings.
- A paused native OpenGL replay validation smoke wrote `Bin/logs/2026.07.05-01.55.10/linux-client-shell.log` and exited `0`. It proved file-storage, transceiver, server-transceiver, and freshly loaded live-PFWorld replay validation for the generated replay: `Final replay file storage: requested=yes valid=yes ... commands=163/163 statuses=3/3 ... error=none`, `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes calls=46 finalStep=45 ... commands=163/163 ... stepMatch=yes commandMatch=yes error=none`, and replayed buy/use executed with `Final hero gameplay command execution: ... consumable=1/1/1/1 can=1 slot=0 target=0/7405/2 qty=1->0 occupied=1->0 ... buy=1/1/1/1 ...`.
- An unpaused native OpenGL replay-input smoke wrote `Bin/logs/2026.07.05-01.55.20/linux-client-shell.log` and exited `0`. It adopted the replay header/lineup, drove visible live replay playback, and reported `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... commands=163/163 statuses=3/3 ... stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`; the after-exit replay-file live-PFWorld proof also stayed valid with `commands=163/163`, and the replayed buy/use counters reached `consumable=2/2/2/2` / `buy=2/2/2/2` across live playback plus file proof.
- A 90-second native OpenGL `--bootstrap-create-game` regression wrote `Bin/logs/2026.07.05-01.55.48/linux-client-shell.log` and exited `0`. It proves queued AI buy/use without fallback: `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=891 active=9 bots=1/1 ... commands=156/156 fallback=0 ... buy=1 consumable=1 ...`, replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=163/163` and `statuses=3/3`, and final gameplay execution reported `consumable=1/1/1/1 can=1 slot=0 target=0/7394/1 qty=1->0 occupied=1->0 ... buy=1/1/1/1 can=1 took=1 item=7522/0/-1 hero=-1->7394 ...`.
- The normal 8-second native OpenGL menu regression wrote `Bin/logs/2026.07.05-01.57.27/linux-client-shell.log` and exited `0`. It keeps the starting UI overlays intact with `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, with expected inactive gameplay/replay/AI counters.
- Previous consumable setup chunk: Linux/null-render AI gained the first real consumable/healing/shop decision wiring instead of stubs while keeping Windows/full-render AI on its existing path. `PFAIHelper::HasConsumable(...)` scans real hero consumable slots and quantities by `IdentifyConsumable(...)`; `PFAIController` gates consumable use with slot bounds, `PFConsumable::CanBeUsed()`, Linux-local cooldowns, health/mana retreat thresholds, healing reset checks, and potion recovery calls; `AIHealingState` moves the hero toward spawn and waits for near-full health/mana; `AIShoppingState` buys configured health/mana potions from real shop items while respecting `doNotBuyMana`; and Linux `FindShop(...)` / `FindQuarters(...)` / `FindMainBuildings(...)` now use the active `PFAIWorld` unit traversal. The latest chunk above supersedes the earlier direct-fallback proof with queued command execution and no AI direct fallback.
- Verification after the Linux AI consumable/healing/shop chunk: configured/built `/tmp/primeworld-linux-bootstrap` and built `PrimeWorldLinuxClient`, `PrimeWorldLinuxGameplayProbe`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxRenderProbe` with `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2`; the final build exited `0`. Focused `git diff --check` on the touched AI files and this plan exited `0`.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the consumable/healing/shop chunk wrote `Bin/logs/2026.07.04-23.23.48/linux-client-shell.log` and exited `0`. It kept real Linux AI controllers active and stable: `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=852 active=9 bots=1/1 ... commands=127/127 fallback=0 move=1 combat=11 attack=13 other=102`, replay capture/readback/storage/transceiver/server-transceiver stayed valid with `commands=139/139` and `statuses=3/3`, and live map/HUD/minimap/scoreboard/event-feed remained rendered. The final gameplay counter still reports `consumable=0/0/0/0 ... buy=0/0/0/0`, so potion/shop command execution is not proven by this smoke.
- A native OpenGL replay-input regression against that generated replay wrote `Bin/logs/2026.07.04-23.25.29/linux-client-shell.log` and exited `0`. Replay playback consumed the file deterministically with `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=42 commands=139/139 statuses=3/3 ... stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=139/139 statuses=3/3 ... error=none`, and AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 add=0/0 ... commands=0/0`).
- The normal 8-second native OpenGL menu regression after the consumable/healing/shop chunk wrote `Bin/logs/2026.07.04-23.25.54/linux-client-shell.log` and exited `0`. It keeps the starting UI overlays intact with `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, with expected inactive gameplay/replay/AI counters.
- Latest chunk: Linux/null-render AI now proves real controller-driven `CmdUseTalent` execution during live create-game, not only replayed activation. The Linux AI talent scan keeps the existing Windows/full-render AI path intact, but the Linux bootstrap branch now has a fallback target resolver for active talents: it accepts targetless/self talents, current enemy targets, nearby `FindTarget(...)` enemies, self targets, and land targets when the DB target flags and `CheckValidAbilityTargetCondition(...)` allow them. Activation and use gates are split (`activate` starts at step `650`, `use` starts at step `700`) with conservative Linux-local throttles. A native crash during the first successful `UseTalent` proof exposed a Linux null-render client-effect DB load in `PFApplBuff::PrepareEffects(...)`; the Linux `PW_LINUX_NULL_RENDER` branch now skips buff visual-effect preparation while leaving gameplay applicators and the Windows/full-render effect path untouched.
- Verification after the Linux AI use-talent chunk: built `PrimeWorldLinuxClient`, `PrimeWorldLinuxGameplayProbe`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxRenderProbe` with `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2`; the final build exited `0`.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the Linux AI use-talent chunk wrote `Bin/logs/2026.06.30-11.19.19/linux-client-shell.log` and exited `0`. It proves controller-driven activation and use through the live scheduler/transceiver/gameplay executor: `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=858 active=9 bots=1/1 ... commands=114/114 fallback=0 move=0 combat=11 attack=11 other=92 lastCmd=7/7436/7/-7/7436/1`, and `Final hero gameplay command execution: ... activate=50/50/50/50 can=1 slot=1,5 progress=6->7 dev=6->7 ... useTalent=42/42/42/42 can=1 target=0/7436/2 talentState=842->852/15->16/0.00->0.00 ...`. Replay capture/storage/transceiver/server-transceiver stayed valid with `commands=126/126` and `statuses=3/3`, and live 3D/HUD/minimap stayed active (`Final map 3D preview: terrain=yes ... dynamic=yes/169/10/89/70/88`, `Final visible live HUD: drawn=yes ... talentIcons=7`, `Final visible live minimap: drawn=yes texture=yes markers=169 heroes=10 creeps=159 moving=88 target=yes`).
- A native OpenGL replay-input regression against that generated replay wrote `Bin/logs/2026.06.30-11.21.02/linux-client-shell.log` and exited `0`. Replay playback consumed the latest file deterministically with `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=39 commands=126/126 statuses=3/3 ... stepMatch=yes commandMatch=yes statusMatch=yes ... error=none`, `Final replay file storage: requested=yes valid=yes ... commands=126/126 statuses=3/3 ... error=none`, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes ... commands=126/126 before=0 after=126 stepMatch=yes commandMatch=yes error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 add=0/0 ... commands=0/0`), while replayed gameplay command execution still reached `useTalent=84/84/84/84`.
- The normal 8-second native OpenGL menu regression after the Linux AI use-talent chunk wrote `Bin/logs/2026.06.30-11.21.29/linux-client-shell.log` and exited `0`. It keeps the starting UI overlays intact with `Visible menu runtime: ready=yes selected=1/4 action=Create custom game`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected inactive gameplay/replay/AI counters. The buff-applicator diagnostic now reports `prepared=0/0 restarted=0/0 type=<none>` in Linux null-render, which is intentional for this bootstrap path.
- Latest chunk: Linux/null-render AI now has the first real talent activation/use scheduling path on top of the production-command AI controller work. The Linux `TalentWrapper` path now walks the full DB talent grid instead of only slot `(0,0)`, ranks candidate activations by AI priority and Nafta cost, and the Linux `TalentPart` / `PFAIController` paths scan activated/active talents with `CheckValidAbilityTargetCondition` plus `FindMicroAITargetTemp(...)` before sending the existing `CreateCmdActivateTalent(...)` / `CreateCmdUseTalent(...)` commands through `PFAIHelper`. To preserve replay determinism, controller-driven talent commands are delayed until world step `700` and throttled with local Linux delays; an ungated version produced many activation commands but crashed replay-input during early scripted consumable replay, so the gate is intentionally conservative. Windows/full-render AI remains on its existing code path.
- Verification after the Linux AI talent-scheduling chunk: built `PrimeWorldLinuxClient`, `PrimeWorldLinuxGameplayProbe`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxRenderProbe` with `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2`; the final build exited `0`.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the Linux AI talent-scheduling chunk wrote `Bin/logs/2026.06.30-04.56.31/linux-client-shell.log` and exited `0`. The run kept the transceiver/replay path valid with `finalGameTransceiverCommands=29`, `finalGameReplayWriterCommandWrites=38`, `finalGameReplayCaptureValidated=yes`, `finalGameReplayStorageValidated=yes`, `finalGameReplayPlaybackValidated=yes`, and `finalGameReplayServerPlaybackValidated=yes`. Linux AI controllers sent/captured the delayed talent-command attempts through the production command path: `finalLinuxAIControllers=auto:9/9 add:9/9 remove:0/0 steps:723 active:9 bots:1/1 ... commands:26/26 fallback:0 move:0 combat:11 attack:6 other:9 lastCmd:6/7450/9/-9/-1/1`. Live HUD/minimap rendering stayed active (`finalVisibleLiveMinimapDrawn=yes`, `finalVisibleLiveMinimapMarkers=169`). The older scripted minigame proof still did not run in this smoke, and the create-game-side hero talent execution counters remained `finalHeroGameplayTalent=0/0/0/0/0 ... use=0/0/0/0/0`, so the executed talent proof moved to replay-input below.
- A native OpenGL replay-input regression against that generated replay wrote `Bin/logs/2026.06.30-05.00.03/linux-client-shell.log` and exited `0`. Replay playback consumed the latest file deterministically with `finalReplayFileValidated=yes`, `finalReplayFileLoadedCommands=38`, `finalReplayFileConsumedCommands=38`, `finalReplayFileLiveWorldValidated=yes`, `finalReplayFileLiveWorldCommandsExecuted=38`, and AI auto-start disabled during replay (`finalLinuxAIControllers=auto:0/0 add:0/0 remove:0/0 ... commands:0/0`). The delayed activation commands now replay through the gameplay command executor without the prior crash: `finalHeroGameplayTalent=18/18/18/4/0 slot=0,2 progress=3->3 dev=3->3 gold=0->0 use=0/0/0/0/0 ...`. `UseTalent` scanning is implemented, but this run still did not prove an executed use-talent command; next work should extend the active-target window or choose a content setup with a usable active talent.
- The normal 8-second native OpenGL menu regression after the Linux AI talent-scheduling chunk wrote `Bin/logs/2026.06.30-05.00.34/linux-client-shell.log` and exited `0`. It keeps the starting UI overlays intact with `finalVisibleLobbyDetailsDrawn=yes`, `finalVisibleLobbyDetailsArtwork=yes`, `finalVisibleLobbyDetailsMinimap=yes`, `finalVisibleLobbyDetailsLineupSlots=10`, `finalVisibleLobbyHeroDetailsDrawn=yes`, `finalVisibleLobbyHeroPortrait=yes`, `finalVisibleLobbyHeroTalentIcons=12`, and expected inactive AI counters (`finalLinuxAIControllers=auto:0/0 add:0/0 remove:0/0 steps:0 active:0 bots:0/-1 ... commands:0/0`).
- Latest chunk: Linux/null-render `PFAIController` now performs conservative real combat target acquisition while following lane routes. The Linux `PFAIHelper::FindEnemyNear()` path now reuses `PFBaseUnit::FindTarget(...)` over the existing Linux bootstrap unit registry instead of returning no target. `PFAIController::CheckWarFront(...)` throttles local scans, skips heroes that already have a target or are already in `ATTACKUNIT`, and pushes `AIAttackUnitState` so the existing `PFAIHelper::Attack(...)`/transceiver command route sends real `CreateCmdAttackTarget(...)` commands. The Linux attack state now exits when the target becomes invalid, dead, no longer attackable, or outside visibility/targeting range so the lane-move state below can resume. The new delay field is initialized in both Linux and Windows constructors; Windows/full-render AI behavior remains on its existing richer implementation.
- Verification after the Linux AI target-acquisition chunk: configured a fresh build tree with `cmake -S pw/branches/r1117/Src/LinuxBootstrap -B /tmp/primeworld-linux-bootstrap` and built `PrimeWorldLinuxClient`, `PrimeWorldLinuxGameplayProbe`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxRenderProbe` with `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2`; the final build exited `0`. The first build attempt caught a Linux-only link issue because `AiConst::MOVE_START_DELAY()` pulls in `AdventureScreen::Instance()` in this bootstrap target; the Linux combat scan delay is now a local fixed tick delay to keep it independent of that full-client symbol.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the Linux AI target-acquisition chunk wrote `Bin/logs/2026.06.30-04.39.21/linux-client-shell.log` and exited `0`. It now proves real production AI attack commands from controllers: `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=720 active=9 bots=1/1 ... commands=17/17 fallback=0 move=0 combat=11 attack=6 other=0 lastCmd=2/7418/4/-4/-1/1`. Replay capture/storage/transceiver validation stayed clean with `Final replay capture validation: valid=yes ... commandBlocks=29/29 statusBlocks=3/3 ... error=none`, `Final replay storage: valid=yes segments=15/15 commands=29/29 statuses=3/3 ... error=none`, and live map/HUD/minimap/event-feed/creep-combat rendering remained active. The old late minigame proof still did not run in this 90-second smoke because real AI/combat activity changes the timing of the scripted proof chain (`attempted=0 completed=0`).
- A native OpenGL replay-input regression against the freshly generated AI-command replay wrote `Bin/logs/2026.06.30-04.41.08/linux-client-shell.log` and exited `0`. Replay playback consumed the larger AI-command replay cleanly with `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=15 commands=29/29 statuses=3/3 calls=15 worldSteps=14 finalStep=14 stepMatch=yes commandMatch=yes statusMatch=yes ... rate=2 pacing=no/0 ... error=none`. AI auto-start stayed disabled during replay (`Final Linux AI controllers: auto=0/0 add=0/0 remove=0/0 ... commands=0/0 fallback=0`), and visible replay controls/3D/minimap/HUD still rendered.
- The normal 8-second native OpenGL menu regression after the Linux AI target-acquisition chunk wrote `Bin/logs/2026.06.30-04.41.34/linux-client-shell.log` and exited `0`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected inactive gameplay/replay/AI counters (`Final Linux AI controllers: auto=0/0 add=0/0 remove=0/0 steps=0 active=0 bots=0/-1 ... commands=0/0 fallback=0`).
- Latest chunk: Linux/null-render AI now has a functional local `PFHFSM` / `PFFsm` implementation instead of no-op state stubs, so `PFAIController` states can actually enter, push route substates, and call `PFAIHelper`. `PFAIHelper` now sends the same production `CreateCmd...` commands used by the normal helper path through `NCore::ITransceiver` when a transceiver-backed AI container is installed, records Linux-only command diagnostics in `PFWorld`, and keeps direct mutation fallbacks only for probe/no-transceiver cases. The Linux `--bootstrap-create-game` path installs a real `PFAIContainer` on the transceiver world and the Linux controller issues its first lane route once; replay-input and menu PFWorld instances still keep AI auto-start disabled. A shutdown-only pure-virtual crash found while enabling the FSM was fixed by making Linux null-render FSM destructors release references without virtual `DoLeave()` calls while preserving explicit `Cleanup()` behavior. Windows, DirectX, and non-Linux client behavior remain on the existing code paths.
- Verification after the transceiver-backed AI command/FSM chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFBaseUnit.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIController.h pw/branches/r1117/Src/PF_GameLogic/PFAIHelper.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIHelper.h pw/branches/r1117/Src/PF_GameLogic/PFWorld.cpp pw/branches/r1117/Src/PF_GameLogic/PFWorld.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxGameplayProbe PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0`.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the transceiver-backed AI command/FSM chunk wrote `Bin/logs/2026.06.25-08.05.29/linux-client-shell.log` and exited `0`. It now proves real AI commands from the attached controllers: `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=882 active=9 bots=1/1 ... commands=11/11 fallback=0 move=0 combat=11 attack=0 other=0 lastCmd=2/7418/4/-4/-1/1`. The same run kept the real scheduler/transceiver/replay path valid with `Final game transceiver runtime: ready=yes world=yes step=881 commands=23 runtimeSent=12 productionSent=11 ... replayCommands=23 replayBytes=936`, valid capture/storage/transceiver proofs (`Final replay capture validation: valid=yes ... commandBlocks=23/23 statusBlocks=3/3 ... error=none`, `Final replay storage: valid=yes segments=14/14 commands=23/23 statuses=3/3 ... error=none`), spawned/player heroes `10/10`, live 3D terrain/water/static/animated/dynamic/hero/creep rendering, HUD/minimap/scoreboard/event-feed rendering, selected-target combat, and creep combat. Because real AI now starts lane movement immediately, the older late scripted bootstrap command/minigame proof chain is no longer the primary success signal in this smoke and did not reach the minigame leave proof inside 90 seconds (`attempted=0 completed=0`).
- A native OpenGL replay-input regression against the freshly generated AI-command replay wrote `Bin/logs/2026.06.25-08.07.11/linux-client-shell.log` and exited `0`. Live replay playback stayed deterministic with AI auto-start disabled (`Final Linux AI controllers: auto=0/0 add=0/0 remove=0/0 steps=14 active=0 bots=1/1 ... commands=0/0 fallback=0`) while consuming the AI-generated replay stream cleanly: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=14 commands=23/23 statuses=3/3 calls=14 worldSteps=13 finalStep=13 stepMatch=yes commandMatch=yes statusMatch=yes ... rate=2 pacing=no/0 ... error=none`, `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes calls=14 finalStep=13 players=10 mapObjects=7458 spawnedHeroes=10 playerHeroes=10 commands=23/23 before=0 after=23 stepMatch=yes commandMatch=yes error=none`, and visible replay controls/3D/minimap/HUD still rendered.
- The normal 8-second native OpenGL menu regression after the transceiver-backed AI command/FSM chunk wrote `Bin/logs/2026.06.25-08.07.35/linux-client-shell.log` and exited `0`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected inactive gameplay/replay/AI counters (`Final Linux AI controllers: auto=0/0 add=0/0 remove=0/0 steps=0 active=0 bots=0/-1 ... commands=0/0 fallback=0`).
- Latest chunk: the native Linux/OpenGL `--bootstrap-create-game` path now uses the real `PFAIContainer` / `PFAIController` flow for computer slots and delayed leaver AI instead of only the scripted bootstrap bot-command driver. Linux-only `PFWorld` diagnostics track AI auto-starts, explicit add/remove requests, step calls, active controller counts, bots-settings state, and the last hero/player/user/line touched by AI setup. `PFPlayer` now mirrors the leaver-AI attach/remove behavior on Linux, including `PlayerBehaviourTracker`, while `PFAIController::Step(...)` runs the controller FSM and respawn movement. `PFWorld::SetLinuxAutoAIEnabled(...)` keeps this opt-in: create-game enables it, but menu and replay-input PFWorld instances leave it off so replay playback remains deterministic. Windows, DirectX, and non-Linux client paths remain untouched.
- Verification after the real Linux AI-controller chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFWorld.cpp pw/branches/r1117/Src/PF_GameLogic/PFWorld.h pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIContainer.cpp pw/branches/r1117/Src/PF_GameLogic/PFAIContainer.h pw/branches/r1117/Src/PF_GameLogic/PFAIController.cpp pw/branches/r1117/Src/PF_GameLogic/PFPlayer.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0`.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the real AI-controller chunk wrote `Bin/logs/2026.06.25-05.15.39/linux-client-shell.log`. It preserved the local scheduler/transceiver path and replay capture while enabling real Linux AI controllers: `Final game transceiver runtime: ready=yes world=yes step=890 commands=26 runtimeSent=26 productionSent=25 ... replayCommands=26 replayBytes=1260`, `Final Linux AI controllers: auto=9/9 add=9/9 remove=0/0 steps=888 active=9 bots=1/1 last=7449/9/-9/0`, `Final Linux bot commands: active=yes ready=yes ticks=3 considered=27 commanded=3 move=2 attack=1 skipped=90 step=803/899 player=3/-3 target=2/1984 ... action=attack`, valid replay capture/storage, live 3D map/minimap/HUD coverage, moving creeps, selected-target proof, and minigame leave proof `attempted=1 completed=1`.
- A native OpenGL replay-input regression against the freshly generated replay after the real AI-controller chunk wrote `Bin/logs/2026.06.25-05.17.18/linux-client-shell.log`. Live replay playback stayed deterministic with AI auto-start disabled (`Final Linux AI controllers: auto=0/0 add=0/0 remove=0/0 steps=27 active=0 bots=1/1 last=-1/-1/-1/-1`) while replay validation stayed clean: `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=27 commands=26/26 statuses=5/5 calls=27 worldSteps=26 finalStep=26 stepMatch=yes commandMatch=yes statusMatch=yes ... rate=2 pacing=no/0 ... error=none`, `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes calls=27 finalStep=26 players=10 mapObjects=7458 spawnedHeroes=10 playerHeroes=10 commands=26/26 before=0 after=26 stepMatch=yes commandMatch=yes error=none`, and visible replay controls/3D/minimap/HUD still rendered.
- The normal 8-second native OpenGL menu regression after the real AI-controller chunk wrote `Bin/logs/2026.06.25-05.17.40/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected inactive gameplay/replay/AI counters (`Final Linux AI controllers: auto=0/0 add=0/0 remove=0/0 steps=0 active=0 bots=0/-1 last=-1/-1/-1/-1`).
- Latest chunk: the native Linux/OpenGL `--bootstrap-create-game` path now has a first scripted bot command driver after the existing local command/minigame proof chain completes. `PW_Client/Game.cpp` selects live non-human bot heroes (`userID <= 0`), skips replay-input runs, throttles commands by world step, and sends real `CreateCmdMoveHero(...)` / `CreateCmdAttackTarget(...)` commands through the existing `NCore::Transceiver` boundary. Bot diagnostics are separate from the local-player proof counters (`Final Linux bot commands`, `finalLinuxBot*` log fields, and a live event-feed row), while total runtime/production command counts and replay capture naturally include the additional packed commands. Windows replay loading, DirectX, and non-Linux client code remain untouched.
- Verification after the bot command-driver chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -- -j2` exited `0`.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the bot command-driver chunk wrote `Bin/logs/2026.06.25-04.42.27/linux-client-shell.log`. It preserved the local scheduler/transceiver path and minigame proof while adding bot command activity after that proof: `Final game transceiver runtime: ready=yes world=yes step=851 commands=26 runtimeSent=26 productionSent=25 ... replayCommands=26 replayBytes=1260`, `Final Linux bot commands: active=yes ready=yes ticks=3 considered=27 commanded=3 move=2 attack=1 skipped=77 step=803/899 player=3/-3 target=2/1984 ... action=attack`, `Final replay capture validation: valid=yes ... commandBlocks=26/26 statusBlocks=5/5 ... error=none`, `Final replay storage: valid=yes segments=27/27 commands=26/26 statuses=5/5 ... error=none`, both replay transceiver proofs valid, spawned/player heroes `10/10`, live 3D map/minimap/HUD/hotkey proofs, selected-target kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression after the bot command-driver chunk wrote `Bin/logs/2026.06.25-04.44.06/linux-client-shell.log`. It keeps the starting UI overlays intact with `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ...`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive gameplay/replay-input counters, and `Final Linux bot commands: active=no ready=no ticks=0 considered=0 commanded=0 move=0 attack=0 skipped=0 ... action=none`.
- A native OpenGL replay-input speed regression after the bot command-driver chunk wrote `Bin/logs/2026.06.25-04.45.02/linux-client-shell.log` when passed the absolute generated replay path. Startup adopted the replay header/lineup (`Replay header startup: requested=yes usable=yes adoptedMap=yes adoptedLineup=yes autoCreate=yes ... players=10 humans=1 bots=9 client=1984 team=2 stepLength=100 ... error=none`), the live replay-input path stayed valid with the new larger capture (`Final live replay input: active=yes ready=yes header=yes world=yes valid=yes ... segments=27 commands=26/26 statuses=5/5 calls=27 worldSteps=26 finalStep=26 stepMatch=yes commandMatch=yes statusMatch=yes ... rate=2 pacing=no/0 ... source=command-line-speed error=none`), `Final replay file live PFWorld` also stayed valid with `commands=26/26`, replay controls still drew with `Final visible replay controls: drawn=yes lines=5 progress=yes`, and the bot driver remained disabled during replay playback (`Final Linux bot commands: active=no ... move=0 attack=0`).
- Latest chunk: Linux replay-input playback now supports native pacing for visible playback without changing the default fast replay path. `PW_Client/Game.cpp` adds Linux-only `--replay-realtime` and `--replay-pace-ms N` launch controls; `--replay-realtime` derives the effective interval from the replay header `stepLength` when no explicit pace is supplied, while `--replay-pace-ms` uses the requested interval and clamps it to a safe range. The live replay scheduler now emits at most the current replay speed multiplier per pace interval, records skipped scheduler polls while waiting for the next interval, and keeps pause/manual-step controls compatible with pacing. Startup/final stdout, `linux-client-shell.log`, and the visible replay-control panel now show pacing mode, effective pace milliseconds, and pacing skip counters. Normal menu and `--bootstrap-create-game` runs still default to `pacing=no/0`, use the existing local scheduler/capture path, and do not draw replay controls. Windows replay loading, DirectX, and non-Linux replay code remain untouched.
- Verification after the replay pacing chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A paced replay-input smoke wrote `Bin/logs/2026.06.25-04.27.55/linux-client-shell.log`; startup adopted the replay header/lineup and reported `Replay input controls: paused=no speed=1 stepBudget=0 realtime=no paceMs=500`, then intentionally stopped early after the short 4-second window with `Final live replay input: active=yes ... valid=no ... commands=9/23 statuses=3/5 calls=8 worldSteps=7 ... paused=no rate=1 pacing=yes/500 skips=8 ... source=command-line-pace error=incomplete`, while the visible replay panel stayed active with `Final visible replay controls: drawn=yes lines=5 progress=yes`.
- A header-derived realtime replay-input smoke after the pacing chunk wrote `Bin/logs/2026.06.25-04.28.08/linux-client-shell.log`; startup reported `realtime=yes paceMs=0`, the loaded replay header supplied the effective `100ms` pace, and final playback remained valid with `Final live replay input: active=yes ... valid=yes ... commands=23/23 statuses=5/5 calls=24 worldSteps=23 ... paused=no rate=1 pacing=yes/100 skips=0 ... source=command-line-realtime error=none`, plus `Final visible replay controls: drawn=yes lines=5 progress=yes`.
- A non-paced speed replay regression after the pacing chunk wrote `Bin/logs/2026.06.25-04.28.29/linux-client-shell.log`; final playback stayed valid with `Final live replay input: active=yes ... valid=yes ... commands=23/23 statuses=5/5 ... paused=no rate=2 pacing=no/0 skips=0 ... source=command-line-speed error=none`, proving the default/fast replay path did not inherit realtime throttling. The visible replay panel still reports `Final visible replay controls: drawn=yes lines=5 progress=yes` because it now includes the pacing status line even when pacing is off.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the pacing chunk wrote `Bin/logs/2026.06.25-04.29.22/linux-client-shell.log`; stdout confirms the normal scheduler remains on `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment` plus `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, final replay capture/storage/transceiver proofs remain valid, live replay-input stays inactive/defaulted (`Final live replay input: active=no ... rate=1 pacing=no/0 skips=0 controls=0 source=auto error=inactive`), and replay controls stay disabled outside replay input (`Final visible replay controls: drawn=no lines=0 progress=no`). The same run preserved live gameplay coverage with final transceiver `step=872 commands=23 replayCommands=23 replayBytes=1135`, spawned/player heroes `10/10`, live 3D map/minimap/HUD/hotkey proofs, selected-target kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression after the pacing chunk wrote `Bin/logs/2026.06.25-04.31.01/linux-client-shell.log`. It keeps the starting UI overlays intact with `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ...`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, no replay startup adoption (`Replay header startup: requested=no usable=no ... error=inactive`), expected inactive gameplay/replay-input/player-status counters, and `Final visible replay controls: drawn=no lines=0 progress=no`.
- Latest chunk: visible Linux replay-input playback now draws an in-window OpenGL replay-control/status panel during replay-input runs. `PW_Client/Game.cpp` adds Linux-only `visibleReplayControls*` runtime diagnostics plus `DrawLinuxReplayInputControlOverlay(...)`, reusing the existing overlay primitives to show play/pause state, speed, consumed/total replay segments, command/status progress, world step, manual-step counters, control source, and key hints. The panel appears only when `replayFileInputActive` is true, so normal menu and `--bootstrap-create-game` runs do not show it and their counters stay `drawn=no`. Windows replay loading, DirectX, and non-Linux replay code remain untouched.
- Verification after the visible replay-control panel chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A native OpenGL replay-input speed smoke wrote `Bin/logs/2026.06.25-04.17.40/linux-client-shell.log`; startup adopted the replay header/lineup and reported `Replay input controls: paused=no speed=2 stepBudget=0`, final live playback stayed valid with `Final live replay input: active=yes ... valid=yes ... commands=23/23 statuses=5/5 ... paused=no rate=2 ... source=command-line-speed error=none`, and the new visible panel proof reports `Final visible replay controls: drawn=yes lines=4 progress=yes`.
- A paused/manual-step replay-input smoke after the same panel chunk wrote `Bin/logs/2026.06.25-04.18.02/linux-client-shell.log`; it intentionally stops early with `Final live replay input: active=yes ... valid=no ... commands=6/23 statuses=3/5 calls=5 worldSteps=4 ... paused=yes rate=1 controls=2 manualSteps=5/5/0 source=command-line-steps error=paused`, while the new panel still renders with `Final visible replay controls: drawn=yes lines=4 progress=yes`.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the panel chunk wrote `Bin/logs/2026.06.25-04.18.19/linux-client-shell.log`; stdout confirms the normal scheduler remains on `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment` plus `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, final replay capture/storage/transceiver proofs remain valid, live replay-input stays inactive/defaulted (`Final live replay input: active=no ... source=auto error=inactive`), and the new replay panel stays disabled outside replay input (`Final visible replay controls: drawn=no lines=0 progress=no`). The same run preserved live gameplay coverage with final transceiver `step=866 commands=23 replayCommands=23 replayBytes=1135`, spawned/player heroes `10/10`, live 3D map/minimap/HUD/hotkey proofs, selected-target kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression after the panel chunk wrote `Bin/logs/2026.06.25-04.19.58/linux-client-shell.log`. It keeps the starting UI overlays intact with `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ...`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, no replay startup adoption (`Replay header startup: requested=no usable=no ... error=inactive`), expected inactive gameplay/replay-input/player-status counters, and `Final visible replay controls: drawn=no lines=0 progress=no`.
- Previous chunk: visible Linux replay-input playback now has native bootstrap controls without touching the normal Windows/non-Linux paths. `PW_Client/Game.cpp` accepts `--replay-paused` / `--replay-start-paused`, `--replay-steps N` (clamped to 256), and `--replay-speed N` (clamped to 1..8), records those settings in startup/final diagnostics, and applies them only to the Linux live `ReplayStorage2`/`ReplayTransceiver` replay-input scheduler. While a replay-input run is active, keyboard controls are `P`/Space pause-resume, `N`/`.` single-step while paused, `]`/`=`/`+` speed up, `[`/`-` speed down, and `0` reset speed. Normal `--bootstrap-create-game` still uses `Game::LocalCmdScheduler` plus `NCore::Transceiver`, still writes `logs/linux-bootstrap-replay.pwrp`, and still leaves live replay-input inactive/defaulted. Windows replay loading, DirectX, and non-Linux replay code remain untouched.
- Verification after the replay-input control chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A native OpenGL replay-input speed smoke wrote `Bin/logs/2026.06.25-04.08.36/linux-client-shell.log`; startup reported `Replay input controls: paused=no speed=2 stepBudget=0`, and final live playback stayed valid with `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=24 commands=23/23 statuses=5/5 calls=24 worldSteps=23 finalStep=23 stepMatch=yes commandMatch=yes statusMatch=yes spawnedHeroes=10 playerHeroes=10 paused=no rate=2 controls=1 pauseToggles=0 speedChanges=0 manualSteps=0/0/0 source=command-line-speed error=none`. A paused/manual-step smoke wrote `Bin/logs/2026.06.25-04.09.00/linux-client-shell.log`; it intentionally stopped early with `Replay input controls: paused=yes speed=1 stepBudget=5` and final `valid=no ... commands=6/23 statuses=3/5 calls=5 worldSteps=4 ... paused=yes rate=1 controls=2 manualSteps=5/5/0 source=command-line-steps error=paused`, proving the pause gate and manual stepping path while the visible PFWorld, heroes, and loading UI still initialize.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the replay-input control chunk wrote `Bin/logs/2026.06.25-04.10.08/linux-client-shell.log`; stdout confirms the normal scheduler remains on `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment` plus `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, final replay capture/storage/transceiver proofs remain valid (`Final replay capture validation: valid=yes ... commandBlocks=23/23 statusBlocks=5/5 ... error=none`, `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 ... error=none`, both replay transceiver proofs valid), and live replay-input stays inactive/defaulted (`Final live replay input: active=no ... paused=no rate=1 controls=0 ... source=auto error=inactive`). The same run preserved live gameplay coverage with final transceiver `step=887 commands=23 replayCommands=23 replayBytes=1135`, spawned/player heroes `10/10`, live 3D map/minimap/HUD/hotkey proofs, selected-target kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression after the replay-input control chunk wrote `Bin/logs/2026.06.25-04.11.48/linux-client-shell.log`. It keeps the starting UI overlays intact with `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ...`, `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, no replay startup adoption (`Replay header startup: requested=no usable=no adoptedMap=no adoptedLineup=no autoCreate=no ... error=inactive`), and expected inactive gameplay/replay-input/player-status counters.
- Previous chunk: positional Linux-generated replay input now drives the visible bootstrap PFWorld, not only the after-exit proof world. `PW_Client/Game.cpp` carries replay-input runtime state, feeds the replay header `MapStartInfo`/client id into loading, creates a live `NWorld::ReplayStorage2` plus `NWorld::ReplayTransceiver` attached to the visible `PFWorld`, enables server-replay status consumption, and steps replay segments after the map-loading job completes. Replay-input runs skip bootstrap local-scheduler command injection and skip the generated `ReplayWriter` so the input file is not overwritten. Normal create-game runs still use `Game::LocalCmdScheduler` plus `NCore::Transceiver` and still generate `logs/linux-bootstrap-replay.pwrp`. This is Linux bootstrap replay input only; Windows replay loading, DirectX, and non-Linux replay code remain untouched.
- Verification after the live replay-input chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A native OpenGL replay-input smoke using positional `logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.06.25-03.50.46/linux-client-shell.log`; startup adopted the replay header and lineup (`Replay header startup: requested=yes usable=yes adoptedMap=yes adoptedLineup=yes autoCreate=yes ... players=10 humans=1 bots=9 client=1984 team=2 stepLength=100 map=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb error=none`), the scheduler path is now `NWorld::ReplayStorage2/ReplayTransceiver live replay input`, and final live playback reports `Final live replay input: active=yes ready=yes header=yes world=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=24 commands=23/23 statuses=5/5 calls=24 worldSteps=23 finalStep=23 stepMatch=yes commandMatch=yes statusMatch=yes spawnedHeroes=10 playerHeroes=10 error=none`. The same run keeps generated capture inactive for replay input (`Final replay capture validation: ... error=inactive`) while preserving the separate replay-file storage/transceiver/live-PFWorld proofs.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the same chunk wrote `Bin/logs/2026.06.25-03.51.08/linux-client-shell.log`; stdout confirms the normal scheduler remains on `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment` plus `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and final replay capture/storage/transceiver proofs remain valid (`Final replay capture validation: valid=yes ... commandBlocks=23/23 statusBlocks=5/5 ... error=none`, `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 ... error=none`, both replay transceiver proofs valid). Live gameplay proof coverage is still present with final transceiver `step=842 commands=23 replayCommands=23 replayBytes=1135`, spawned/player heroes `10/10`, live 3D map/minimap/HUD/hotkey proofs, selected-target kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression after the same chunk wrote `Bin/logs/2026.06.25-03.52.46/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, no replay startup adoption (`Replay header startup: requested=no usable=no adoptedMap=no adoptedLineup=no autoCreate=no ... error=inactive`), and expected inactive gameplay/replay-input/player-status counters.
- Previous chunk: the Linux replay sidecar now preserves per-player hero identity instead of only player id/team/type/user/nickname. `LinuxBootstrap/runtime_stubs.cpp` appends `PlayerInfo` hero metadata after the existing nickname column in `PWLXREPLAYINFO1` rows (`heroId`, `heroSkin`, `locale`, level/exp/rating, premium/basket/avatar/party fields). `PW_Client/ReplayTransceiver.cpp` reads those columns into `NCore::PlayerStartInfo::playerInfo` while still accepting the old seven-column player rows, so older Linux replay sidecars remain parseable. This is Linux bootstrap replay metadata only; Windows replay loading, DirectX, and the non-Linux replay path remain untouched.
- Verification after the replay-sidecar hero-metadata chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A fresh 90-second native OpenGL `--bootstrap-create-game` run regenerated `logs/linux-bootstrap-replay.pwrp` and `logs/linux-bootstrap-replay.pwrp.info`, wrote `Bin/logs/2026.06.25-03.37.36/linux-client-shell.log`, and preserved the live-world proof with `Final game transceiver runtime: ready=yes ... spawnedHeroes=10 playerHeroes=10 replayCommands=23 replayBytes=1135`, `Final replay capture validation: valid=yes ... commandBlocks=23/23 statusBlocks=5/5 ... error=none`, `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 ... header=yes/10 ... error=none`, both replay transceiver proofs valid, player-status lifecycle proof active, live 3D map/minimap/HUD/hotkey proofs, selected-target kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`. The generated sidecar now has nonzero hero checksums for all ten players.
- A replay-input native OpenGL smoke using positional `logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.06.25-03.39.23/linux-client-shell.log`. Startup adopted the replay header and lineup (`Replay header startup: requested=yes usable=yes adoptedMap=yes adoptedLineup=yes autoCreate=yes ... players=10 humans=1 bots=9 client=1984 team=2 stepLength=100 map=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb error=none`), replay-input still disables generated capture (`replayPath=inactive`, capture validation `error=inactive`), `Local match lineup` now reports `manual=9` from replay hero overrides, and the isolated replay world proof now reconstructs hero ownership: `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes calls=24 finalStep=23 players=10 mapObjects=7458 spawnedHeroes=10 playerHeroes=10 commands=23/23 before=0 after=23 stepMatch=yes commandMatch=yes error=none`. This closes the previous `spawnedHeroes=0 playerHeroes=0` replay-start gap for generated Linux replays.
- The normal 8-second native OpenGL menu regression after the same chunk wrote `Bin/logs/2026.06.25-03.39.48/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, no replay startup adoption (`Replay header startup: requested=no usable=no adoptedMap=no adoptedLineup=no autoCreate=no ... error=inactive`), expected inactive replay-file proof, and expected inactive gameplay/player-status counters.
- Previous chunk: positional replay files now participate in native Linux startup instead of only being validated after exit. `PW_Client/Game.cpp` probes the replay header early through Linux `NWorld::ReplayStorage2`, adopts the replay map and one-local-human lineup when the map/hero catalog can resolve them, auto-enters the bootstrap create-game/loading path for replay input, and disables the generated `ReplayWriter` only for replay-input runs so the input replay is not overwritten. The final log/stdout now records `Replay header startup: requested/usable/adoptedMap/adoptedLineup/autoCreate...` plus a new `Final replay file live PFWorld` proof. That proof builds a fresh real `NWorld::PFWorld`, loads the replay header's map through `NWorld::MapLoadingJob`, attaches `NWorld::ReplayTransceiver`, and verifies the replay commands execute against the loaded world (`commands=23/23`, `stepMatch=yes`). Before the latest sidecar-metadata fix, this proof could execute commands but could not spawn replay heroes because the sidecar did not store `playerInfo.heroId` (`spawnedHeroes=0 playerHeroes=0`). Windows replay loading, DirectX, and the non-Linux `ReplayStorage2` path remain untouched.
- Verification after the replay-startup/live-PFWorld chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A replay-input native OpenGL smoke with positional `logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.06.25-03.31.38/linux-client-shell.log`; stdout reports `Replay header startup: requested=yes usable=yes adoptedMap=yes adoptedLineup=yes autoCreate=yes path=logs/linux-bootstrap-replay.pwrp players=10 humans=1 bots=9 client=1984 team=2 stepLength=100 map=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb error=none`, generated capture inactive for that replay-input run (`replayPath=inactive`, `Final replay capture validation: ... error=inactive`), `Final replay file storage: requested=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=24/24 commands=23/23 statuses=5/5 failures=0 steps=1..621 ... error=none`, both replay-file transceiver proofs valid, and `Final replay file live PFWorld: valid=yes created=yes loaded=yes attached=yes calls=24 finalStep=23 players=10 mapObjects=7458 spawnedHeroes=0 playerHeroes=0 commands=23/23 before=0 after=23 stepMatch=yes commandMatch=yes error=none`.
- A 90-second native OpenGL `--bootstrap-create-game` regression after the same chunk wrote `Bin/logs/2026.06.25-03.29.56/linux-client-shell.log`; stdout still reports `Final replay capture validation: valid=yes ... commandBlocks=23/23 statusBlocks=5/5 ... error=none`, `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 failures=0 steps=1..621 ... error=none`, `Final replay transceiver: valid=yes header=yes stepLength=100 world=yes calls=25 worldSteps=24 batches=24 commands=23 finalStep=24 stepMatch=yes commandMatch=yes error=none`, and `Final replay server transceiver: valid=yes header=yes stepLength=100 world=yes calls=25 worldSteps=24 batches=24 commands=23 statuses=5 finalStep=24 stepMatch=yes commandMatch=yes statusMatch=yes error=none`. The same smoke preserved live gameplay proof coverage: final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1135`, player-status lifecycle proof, live 3D map/minimap/HUD/hotkey proof counters, selected-target kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression after the same chunk wrote `Bin/logs/2026.06.25-03.32.01/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, no replay startup adoption (`Replay header startup: requested=no usable=no adoptedMap=no adoptedLineup=no autoCreate=no ... error=inactive`), expected inactive replay-file proof, and expected inactive gameplay/player-status counters.
- Previous chunk: the native Linux/OpenGL bootstrap can now validate a replay file passed on the command line instead of only validating the replay captured by the current create-game run. `PW_Client/Game.cpp` adds a separate `LinuxBootstrapReplayFileProof`, runs it after the window loop when `DetectReplayFile()` found a positional `.pwrp`/`.replay` input, opens the file through the real Linux `NWorld::ReplayStorage2`, requires a usable sidecar-backed header, iterates command/status segments, and drives both normal and server `NWorld::ReplayTransceiver` playback with the header step length. Final stdout and `linux-client-shell.log` now include `Final replay file storage`, `Final replay file transceiver`, and `Final replay file server transceiver` lines/fields without weakening the existing live replay capture validation. This is Linux bootstrap-only instrumentation; Windows replay loading, DirectX, and non-Linux `ReplayStorage2` remain untouched.
- Verification after the replay-file input proof chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A short native OpenGL replay-input smoke using positional `logs/linux-bootstrap-replay.pwrp` wrote `Bin/logs/2026.06.25-03.13.11/linux-client-shell.log`; stdout reports `Final replay file storage: requested=yes valid=yes path=logs/linux-bootstrap-replay.pwrp segments=24/24 commands=23/23 statuses=5/5 failures=0 steps=1..621 segmentMatch=yes commandMatch=yes statusMatch=yes header=yes/10 client=1984 stepLength=100 map=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb error=none source=ReplayStorage2`, `Final replay file transceiver: valid=yes world=yes calls=25 worldSteps=24 batches=24 commands=23 finalStep=24 stepMatch=yes commandMatch=yes error=none`, and `Final replay file server transceiver: valid=yes world=yes calls=25 worldSteps=24 batches=24 commands=23 statuses=5 finalStep=24 stepMatch=yes commandMatch=yes statusMatch=yes error=none`, while keeping the menu UI visible with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`.
- A 90-second native OpenGL `--bootstrap-create-game` regression smoke after the same chunk wrote `Bin/logs/2026.06.25-03.13.28/linux-client-shell.log`; stdout still reports `Final replay capture validation: valid=yes ... commandBlocks=23/23 statusBlocks=5/5 ... error=none`, `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 failures=0 steps=1..621 segmentMatch=yes commandMatch=yes statusMatch=yes header=yes/10 client=1984 stepLength=100 map=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb error=none source=ReplayStorage2`, `Final replay transceiver: valid=yes header=yes stepLength=100 world=yes calls=25 worldSteps=24 batches=24 commands=23 finalStep=24 stepMatch=yes commandMatch=yes error=none`, `Final replay server transceiver: valid=yes header=yes stepLength=100 world=yes calls=25 worldSteps=24 batches=24 commands=23 statuses=5 finalStep=24 stepMatch=yes commandMatch=yes statusMatch=yes error=none`, and inactive replay-file proof `Final replay file storage: requested=no valid=no ... error=inactive`. The same smoke preserved final live transceiver `commands=23` / `replayCommands=23` / `replayBytes=1135`, player-status lifecycle proof, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- Previous chunk: the native Linux/OpenGL bootstrap replay playback proof now configures playback from `ReplayStorage2::GetHeader(...)` instead of hardcoding the replay probe step length. `PW_Client/Game.cpp` gives the Linux replay probe world a header-provided step-length constructor, centralizes the expected replay header comparison, and makes both normal `NWorld::ReplayTransceiver` playback and server-replay/status playback read and validate the header before they can report `valid=yes`. The final stdout/log diagnostics now include `header=yes/no` and `stepLength=...` for both playback paths, so the runtime proof covers storage header parsing plus header-driven transceiver construction. Windows replay storage/transceiver code remains under the existing non-Linux branch and was not changed.
- Verification after the header-driven replay playback chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-03.02.19/linux-client-shell.log`; stdout reports `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 failures=0 steps=1..621 segmentMatch=yes commandMatch=yes statusMatch=yes header=yes/10 client=1984 stepLength=100 map=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb error=none source=ReplayStorage2`, `Final replay transceiver: valid=yes header=yes stepLength=100 world=yes calls=25 worldSteps=24 batches=24 commands=23 finalStep=24 stepMatch=yes commandMatch=yes error=none`, `Final replay server transceiver: valid=yes header=yes stepLength=100 world=yes calls=25 worldSteps=24 batches=24 commands=23 statuses=5 finalStep=24 stepMatch=yes commandMatch=yes statusMatch=yes error=none`, and `Final player status runtime: updates=5 missing=0 active=3 away=1 playingUpdates=4 disconnectedUpdates=1 reconnectedUpdates=1 leaverUpdates=0 script=5/5/yes last=1984/2/621 playing=1 active=1 disconnected=0 leaver=0`. The same smoke preserved final live transceiver `commands=23` / `replayCommands=23` / `replayBytes=1135`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-03.03.58/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture/storage/transceiver proofs including `Final replay transceiver: valid=no header=no stepLength=0 world=no calls=0 worldSteps=0 batches=0 commands=0 finalStep=-1 stepMatch=no commandMatch=no error=inactive` and `Final replay server transceiver: valid=no header=no stepLength=0 world=no calls=0 worldSteps=0 batches=0 commands=0 statuses=0 finalStep=-1 stepMatch=no commandMatch=no statusMatch=no error=inactive`, and expected inactive player-status proof `Final player status runtime: updates=0 missing=0 active=0 away=0 playingUpdates=0 disconnectedUpdates=0 reconnectedUpdates=0 leaverUpdates=0 script=0/0/no last=-1/-1/-1 playing=-1 active=-1 disconnected=-1 leaver=-1`.
- Previous chunk: the native Linux/OpenGL bootstrap replay path now has a real replay metadata header path instead of only command/status records. The Linux `NCore::ReplayWriter` stub writes a `PWLXREPLAYINFO1` sidecar beside `logs/linux-bootstrap-replay.pwrp` with client id, step length, map descriptor, replay name, random seed, custom-game flag, basic `NCore::ClientSettings`, and per-player `NCore::PlayerStartInfo` rows. The Linux `NWorld::ReplayStorage2` branch loads `logs/linux-bootstrap-replay.pwrp.info`, reconstructs `NCore::MapStartInfo` / `NCore::ClientSettings`, implements `GetHeader(...)`, exposes Linux-only header diagnostics, and still serves the same decoded command/status segments. `PW_Client/Game.cpp` writes the sidecar before `WriteStartGame(...)` and now requires replay-storage validation to prove segment, command, status, decode, map, player count, client id, and step-length matches before reporting `Final replay storage: valid=yes`. The Windows replay writer, Windows replay loader, DirectX path, and non-Linux `ReplayStorage2` body remain under the existing non-Linux branches and were not changed.
- Verification after the replay-header sidecar chunk: focused `git diff --check -- pw/branches/r1117/Src/Game/PF/Client/LobbyPvx/NewReplay.h pw/branches/r1117/Src/LinuxBootstrap/runtime_stubs.cpp pw/branches/r1117/Src/PW_Client/ReplayTransceiver.cpp pw/branches/r1117/Src/PW_Client/ReplayTransceiver.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-02.54.11/linux-client-shell.log`; stdout reports `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 failures=0 steps=1..621 segmentMatch=yes commandMatch=yes statusMatch=yes header=yes/10 client=1984 stepLength=100 map=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb error=none source=ReplayStorage2`, `Final replay server transceiver: valid=yes world=yes calls=25 worldSteps=24 batches=24 commands=23 statuses=5 finalStep=24 stepMatch=yes commandMatch=yes statusMatch=yes error=none`, and `Final player status runtime: updates=5 missing=0 active=3 away=1 playingUpdates=4 disconnectedUpdates=1 reconnectedUpdates=1 leaverUpdates=0 script=5/5/yes last=1984/2/621 playing=1 active=1 disconnected=0 leaver=0`. The same smoke preserved final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1135`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-02.55.51/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture/storage/transceiver proofs including `Final replay storage: valid=no segments=0/0 commands=0/0 statuses=0/0 failures=0 steps=-1..-1 segmentMatch=no commandMatch=no statusMatch=no header=no/0 client=-1 stepLength=0 map=<none> error=inactive source=ReplayStorage2`, and expected inactive player-status proof `Final player status runtime: updates=0 missing=0 active=0 away=0 playingUpdates=0 disconnectedUpdates=0 reconnectedUpdates=0 leaverUpdates=0 script=0/0/no last=-1/-1/-1 playing=-1 active=-1 disconnected=-1 leaver=-1`.
- Previous chunk: the native Linux/OpenGL bootstrap replay path now uses the real client `NWorld::ReplayStorage2` class for the Linux `PWLXREPLAY1` command/status capture instead of a private `Game.cpp` storage adapter. `PW_Client/ReplayTransceiver.{h,cpp}` now gives the Linux bootstrap branch a real read-mode `ReplayStorage2` implementation: it opens the native capture file, validates the magic/header/record bounds, decodes packed commands through `NCore::ReadCommandFromStream(...)`, converts `Peered::BriefClientInfo` status records into `NCore::ClientStatus`, serves both `NCore::ReplaySegment` and `NCore::SyncSegment`, and exposes Linux-only diagnostics for loaded segment/command/status counts and parse errors. `PW_Client/Game.cpp` removed the local `LinuxBootstrapReplayCaptureStorage` class and now validates storage, normal replay-transceiver playback, and server-replay status playback through `NWorld::ReplayStorage2`; final logs print `finalGameReplayStorageSource=ReplayStorage2`. The Windows replay loader/transceiver body remains under the existing non-Linux branch and was not changed.
- Verification after the `ReplayStorage2` adoption chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/PW_Client/ReplayTransceiver.cpp pw/branches/r1117/Src/PW_Client/ReplayTransceiver.h` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-02.30.58/linux-client-shell.log`; stdout reports `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 failures=0 steps=1..621 segmentMatch=yes commandMatch=yes statusMatch=yes error=none source=ReplayStorage2`, `Final replay server transceiver: valid=yes world=yes calls=25 worldSteps=24 batches=24 commands=23 statuses=5 finalStep=24 stepMatch=yes commandMatch=yes statusMatch=yes error=none`, and `Final player status runtime: updates=5 missing=0 active=3 away=1 playingUpdates=4 disconnectedUpdates=1 reconnectedUpdates=1 leaverUpdates=0 script=5/5/yes last=1984/2/621 playing=1 active=1 disconnected=0 leaver=0`. The same smoke preserved final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1135`, replay capture `statusBlocks=5/5 active=3 away=1 disconnected=1 leaver=0`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-02.32.38/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture/storage/transceiver proofs including `Final replay storage: valid=no segments=0/0 commands=0/0 statuses=0/0 ... error=inactive source=ReplayStorage2`, and expected inactive player-status proof `Final player status runtime: updates=0 missing=0 active=0 away=0 playingUpdates=0 disconnectedUpdates=0 reconnectedUpdates=0 leaverUpdates=0 script=0/0/no last=-1/-1/-1 playing=-1 active=-1 disconnected=-1 leaver=-1`.
- Previous chunk: the native Linux/OpenGL bootstrap sync/replay path now proves a fuller player-status lifecycle including disconnect and recovery, not just static or active/away status traffic. `PW_Client/Game.cpp` queues a guarded Linux-bootstrap `Peered::Active -> Peered::Away -> Peered::Active -> Peered::DisconnectedByClient -> Peered::Active` script after the loaded `PFWorld` has players, waits for each prior status to apply before queuing the next one, and holds the late disconnect/reconnect pair until the minigame-leave proof has completed so it does not destabilize the existing command-chain proof. `PF_GameLogic/PFWorld.{h,cpp}` now records Linux/null-render status counters for active, away, playing, disconnected, reconnected, and leaver applications. The Linux `PWLXREPLAY1` validator now reads and size-checks `Peered::BriefClientInfo` status records, counts active/away/disconnected/leaver payloads, and the replay-storage/server-transceiver proofs require matching status counts. This remains Linux bootstrap/OpenGL-only instrumentation; the Windows/full-render `PFWorld::UpdatePlayerStatuses(...)`, DirectX path, and full Windows replay loader remain untouched.
- Verification after the disconnect/reconnect status lifecycle chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/PF_GameLogic/PFWorld.cpp pw/branches/r1117/Src/PF_GameLogic/PFWorld.h` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-02.15.27/linux-client-shell.log`; stdout reports `Final replay capture validation: valid=yes ... statusBlocks=5/5 active=3 away=1 disconnected=1 leaver=0 bytesRead=1135/1135 ... error=none`, `Final replay storage: valid=yes segments=24/24 commands=23/23 statuses=5/5 ... statusMatch=yes error=none`, `Final replay server transceiver: valid=yes world=yes calls=25 worldSteps=24 batches=24 commands=23 statuses=5 finalStep=24 stepMatch=yes commandMatch=yes statusMatch=yes error=none`, and `Final player status runtime: updates=5 missing=0 active=3 away=1 playingUpdates=4 disconnectedUpdates=1 reconnectedUpdates=1 leaverUpdates=0 script=5/5/yes last=1984/2/621 playing=1 active=1 disconnected=0 leaver=0`. The same smoke preserved final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1135`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-02.17.07/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture/storage/transceiver proofs including `Final replay storage: valid=no segments=0/0 commands=0/0 statuses=0/0 ... error=inactive`, and expected inactive player-status proof `Final player status runtime: updates=0 missing=0 active=0 away=0 playingUpdates=0 disconnectedUpdates=0 reconnectedUpdates=0 leaverUpdates=0 script=0/0/no last=-1/-1/-1 playing=-1 active=-1 disconnected=-1 leaver=-1`.
- Previous chunk: the native Linux/OpenGL bootstrap sync/replay path now carries a real nonzero client-status payload from the local scheduler through live `NCore::Transceiver`, Linux `PFWorld::UpdatePlayerStatuses(...)`, replay capture, replay storage, and server-replay playback. `PW_Client/LocalCmdScheduler` now has a Linux-bootstrap status queue, the create-game path queues one `Peered::Active` status only after `MapLoadingJob` has loaded players into `PFWorld`, and the replay writer serializes that status alongside the existing packed command stream. `PF_GameLogic/PFWorld.cpp` no longer leaves `UpdatePlayerStatuses(...)` as a null-render no-op: under `PW_LINUX_NULL_RENDER` it finds the `PFPlayer` by user id, applies playing/active/disconnected/leaver state, and records Linux diagnostics. `PW_Client/Game.cpp` logs the resulting live player-status state and still validates the captured status block through the existing `PWLXREPLAY1` storage and server replay transceiver proof. This remains Linux bootstrap/OpenGL-only work; the Windows/full-render `PFWorld::UpdatePlayerStatuses(...)`, DirectX path, and full Windows replay loader remain untouched.
- Verification after the nonzero-status sync/replay/PFWorld chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/LocalCmdScheduler.h pw/branches/r1117/Src/PW_Client/LocalCmdScheduler.cpp pw/branches/r1117/Src/PF_GameLogic/PFWorld.h pw/branches/r1117/Src/PF_GameLogic/PFWorld.cpp pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/PW_Client/ReplayTransceiver.cpp pw/branches/r1117/Src/LinuxBootstrap/runtime_stubs.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-01.50.55/linux-client-shell.log`; stdout reports `Final replay capture validation: valid=yes ... statusBlocks=1/1 bytesRead=1055/1055 ... error=none`, `Final replay server transceiver: valid=yes world=yes calls=22 worldSteps=21 batches=21 commands=23 statuses=1 finalStep=21 stepMatch=yes commandMatch=yes statusMatch=yes error=none`, and `Final player status runtime: updates=1 missing=0 last=1984/2/9 playing=1 active=1 disconnected=0 leaver=0`, while preserving final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1055`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, creep combat, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-01.52.36/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture/storage/transceiver proofs, and expected inactive player-status proof `Final player status runtime: updates=0 missing=0 last=-1/-1/-1 playing=-1 active=-1 disconnected=-1 leaver=-1`.
- Previous chunk: the native Linux/OpenGL bootstrap replay path now proves both replay playback modes that `NWorld::ReplayTransceiver::Step(...)` uses on Windows. `PW_Client/ReplayTransceiver.cpp` keeps the Linux bootstrap normal-replay path and now also handles `useServerReplay` by pulling `NCore::SyncSegment` objects from `IReplayStorage`, calling `world->ExecuteCommands(...)`, `world->UpdatePlayerStatuses(...)`, and `world->Step(...)`. `LinuxBootstrap/runtime_stubs.cpp` now writes Linux replay status blocks when callers provide them, and `PW_Client/Game.cpp` extends the Linux `PWLXREPLAY1` capture adapter so the same captured stream can be consumed as normal replay segments or sync/server replay segments with decoded `NCore::ClientStatus` payloads. The local scheduler still wrote zero status blocks in that chunk; the latest chunk above now supersedes that limitation with a real nonzero status payload. This is still controlled bootstrap replay-transceiver playback, not full replay-loader/live-`PFWorld` playback or Windows `.pwrp` compatibility; Windows DirectX/full replay code remains untouched.
- Verification after the server-replay/status-block transceiver chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/PW_Client/ReplayTransceiver.cpp pw/branches/r1117/Src/LinuxBootstrap/runtime_stubs.cpp pw/branches/r1117/Src/LinuxBootstrap/LINUX_PORT_PLAN.md` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-01.38.27/linux-client-shell.log`; stdout reports `Final replay transceiver: valid=yes world=yes calls=22 worldSteps=21 batches=21 commands=23 finalStep=21 stepMatch=yes commandMatch=yes error=none` and `Final replay server transceiver: valid=yes world=yes calls=22 worldSteps=21 batches=21 commands=23 statuses=0 finalStep=21 stepMatch=yes commandMatch=yes statusMatch=yes error=none`, while preserving `Final replay capture validation: valid=yes ... error=none`, `Final replay storage: valid=yes segments=21/21 commands=23/23 failures=0 steps=1..612 segmentMatch=yes commandMatch=yes error=none`, `Final replay readback: decoded=yes commands=23/23 failures=0 bytes=435 distinctTypes=21 clients=1984..1984 first=0x9E6A1193/1984/399716616 last=0x9D62D441/1984/399778950 countMatch=yes`, `finalGameReplayWriterStatusWrites=0` for the current local-scheduler capture, final live transceiver `commands=23` / `replayCommands=23` / `replayBytes=1041`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-01.40.10/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture/storage, expected inactive normal transceiver proof, and expected inactive server transceiver proof `Final replay server transceiver: valid=no world=no calls=0 worldSteps=0 batches=0 commands=0 statuses=0 finalStep=-1 stepMatch=no commandMatch=no statusMatch=no error=inactive`.
- Previous chunk: the native Linux/OpenGL bootstrap replay path now proves the captured command stream can drive the Linux `NWorld::ReplayTransceiver::Step(...)` command-segment path against a controlled `NCore::IWorldBase` probe. `PW_Client/ReplayTransceiver.cpp` no longer leaves the Linux bootstrap replay transceiver as a complete no-op for command replay: it accumulates step time, pulls `NCore::ReplaySegment` objects from `IReplayStorage`, calls `world->ExecuteCommands(...)`, and advances `world->Step(...)` for each segment. `PW_Client/Game.cpp` attaches a Linux-only probe world so this proof counts transceiver-driven batches, commands, and world steps without mutating the live `PFWorld`. This is still a controlled replay-transceiver stepping proof, not full replay-loader/live-world playback or Windows `.pwrp` compatibility; the Windows DirectX/full replay path remains untouched.
- Verification after the replay-transceiver chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/PW_Client/ReplayTransceiver.cpp pw/branches/r1117/Src/LinuxBootstrap/LINUX_PORT_PLAN.md` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-01.21.16/linux-client-shell.log`; stdout reports `Final replay transceiver: valid=yes world=yes calls=22 worldSteps=21 batches=21 commands=23 finalStep=21 stepMatch=yes commandMatch=yes error=none`, while preserving `Final replay capture validation: valid=yes ... error=none`, `Final replay storage: valid=yes segments=21/21 commands=23/23 failures=0 steps=1..612 segmentMatch=yes commandMatch=yes error=none`, `Final replay readback: decoded=yes commands=23/23 failures=0 bytes=435 distinctTypes=21 clients=1984..1984 first=0x9E6A1193/1984/398685551 last=0x9D62D441/1984/398747859 countMatch=yes`, final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1041`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-01.22.59/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture/storage, and expected inactive transceiver proof `Final replay transceiver: valid=no world=no calls=0 worldSteps=0 batches=0 commands=0 finalStep=-1 stepMatch=no commandMatch=no error=inactive`.
- Previous chunk: the native Linux/OpenGL bootstrap replay path now proves that the captured command stream can be loaded and consumed through the `NCore::IReplayStorage` command-segment interface, not only parsed manually. `PW_Client/Game.cpp` adds a Linux-local `LinuxBootstrapReplayCaptureStorage` adapter for the `PWLXREPLAY1` capture file, decodes command blocks into `CObj<NCore::PackedWorldCommand>` entries inside `NCore::ReplaySegment`, consumes them through `GetNextSegment(NCore::ReplaySegment&)`, records loaded/consumed segment and command totals, decode failures, and first/last replay steps, logs `finalGameReplayStorage...`, prints `Final replay storage`, and now requires storage validation for final replay capture `valid=yes`. This is still a Linux bootstrap storage-consumption proof, not full replay-driven live-world playback yet, and the Windows DirectX/full replay path is untouched.
- Verification after the replay-storage chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/LinuxBootstrap/LINUX_PORT_PLAN.md` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-01.10.23/linux-client-shell.log`; stdout reports `Final replay storage: valid=yes segments=21/21 commands=23/23 failures=0 steps=1..612 segmentMatch=yes commandMatch=yes error=none`, while preserving `Final replay capture validation: valid=yes ... error=none`, `Final replay readback: decoded=yes commands=23/23 failures=0 bytes=435 distinctTypes=21 clients=1984..1984 first=0x9E6A1193/1984/398032451 last=0x9D62D441/1984/398090810 countMatch=yes`, final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1041`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-01.12.03/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture/readback, and expected inactive storage `Final replay storage: valid=no segments=0/0 commands=0/0 failures=0 steps=-1..-1 segmentMatch=no commandMatch=no error=inactive`.
- Previous chunk: the native Linux/OpenGL bootstrap replay path now has a command readback proof on top of the structural capture validation. `PW_Client/Game.cpp` reads each command block from `logs/linux-bootstrap-replay.pwrp` back through `NCore::ReadCommandFromStream(...)`, decodes the outer `NCore::PackedWorldCommand`, records decoded command count, decode failures, embedded command bytes, distinct embedded command type count, first/last command ids, client range, and first/last send timestamps, then includes readback success in the final replay validation result. This remains a Linux bootstrap command-capture/readback proof, not full replay-driven transceiver playback yet, and it does not touch the Windows DirectX/full replay path.
- Verification after the replay readback chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-00.59.55/linux-client-shell.log`; stdout reports `Final replay readback: decoded=yes commands=23/23 failures=0 bytes=435 distinctTypes=21 clients=1984..1984 first=0x9E6A1193/1984/397404990 last=0x9D62D441/1984/397465266 countMatch=yes` and preserved `Final replay capture validation: valid=yes ... error=none`, final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1041`, live 3D map/minimap/HUD/hotkey proof counters, selected-target attack/kill/respawn/reengage proof, and minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-01.01.37/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, expected inactive replay capture `error=inactive`, and expected inactive readback `Final replay readback: decoded=no commands=0/0 failures=0 bytes=0 distinctTypes=0 clients=-1..-1 first=0x00000000/-1/0 last=0x00000000/-1/0 countMatch=no`.
- Previous chunk: the native Linux/OpenGL bootstrap now validates the Linux replay command-capture stream after `ReplayWriter` updates. `PW_Client/Game.cpp` parses `logs/linux-bootstrap-replay.pwrp`, checks the `PWLXREPLAY1` magic, start header, per-step record bounds, command/status block sizes, byte count, command/status counters, replay step records, largest command block, and start/first/last step values, then reports both file-log fields and a `Final replay capture validation` stdout line. This is a native Linux command-capture sanity proof, not full Windows replay playback compatibility yet; the Windows DirectX/full replay path remains untouched.
- Verification after the replay-capture validation chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-00.47.35/linux-client-shell.log`; stdout reports `Final replay capture validation: valid=yes magic=yes bounds=yes bytes=yes commands=yes statuses=yes records=21 steps=21 commandBlocks=23/23 statusBlocks=0/0 bytesRead=1041/1041 largest=71 startStep=0 firstStep=1 lastStep=612 error=none`, while preserving final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1041`, live 3D map/minimap/HUD/hotkey proof counters, completed selected-target attack/kill/respawn/reengage proof, and completed minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-00.50.25/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected inactive replay validation `Final replay capture validation: valid=no magic=no bounds=no bytes=no commands=no statuses=no records=0 steps=0 commandBlocks=0/0 statusBlocks=0/0 bytesRead=0/0 largest=0 startStep=-1 firstStep=-1 lastStep=-1 error=inactive`.
- Previous chunk: the native Linux/OpenGL selected-target attack proof now keeps a clean terminal state after the command chain reaches the activate-talent proof. `PW_Client/Game.cpp` still allows the proof to re-engage a respawned target while the hero needs Prime for talent activation, but it no longer restarts the attack loop after `transceiverHeroActivateTalentRuntimeCommandSent` is true. That keeps the final diagnostics from being overwritten by post-proof farming and makes the end-of-run selected-target line report the completed kill/respawn proof clearly. This remains Linux bootstrap gameplay-proof logic and keeps the Windows DirectX/full UI path untouched.
- Verification after the selected-target terminal-state chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-00.33.11/linux-client-shell.log`; stdout reports `Final map selected target: ... attackProof=yes damage=yes sustained=yes hits=35 kills=2 killed=yes hero=yes respawn=yes reengaged=yes requiredGold=300 ... input=selected-target-expired`, while preserving `Final live 3D map commands: surface=yes proof=yes input=0 move=1 attack=0 signal=0 select=0 target=238.2,185.5 action=bootstrap-live-3d-map-proof`, `Final live minimap commands: surface=yes proof=yes input=0 move=1 attack=0 signal=0 select=0 target=237.2,182.7 marker=yes action=bootstrap-live-minimap-proof`, `Final live HUD commands: surface=yes proof=yes highlight=yes input=0 attack=0 useUnit=0 activate=1 talent=0 follow=0 slots=7 action=bootstrap-live-hud-activate-proof`, `Final live hotkey commands: ready=yes proof=yes input=0 stop=0 attack=0 follow=0 hold=1 cancel=0 activate=0 talent=0 useUnit=0 consumable=0 action=bootstrap-live-hotkey-hold-proof`, final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1041`, and completed minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-00.34.51/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final live 3D map commands: surface=no proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 action=none`, `Final live minimap commands: surface=no proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 marker=no action=none`, `Final live HUD commands: surface=no proof=no highlight=no input=0 attack=0 useUnit=0 activate=0 talent=0 follow=0 slots=0 action=none`, `Final visible live scoreboard: drawn=no teams=0 heroMarkers=0 objectiveLines=0 commandLines=0`, and `Final visible live event feed: drawn=no rows=0 command=0 combat=0 objective=0`.
- Previous large chunk: the native Linux/OpenGL loaded-world 3D map command surface now has an automated proof path, not only manual live input counters. `PW_Client/Game.cpp` threads the selected map preview into the Linux bootstrap game scheduler, adds a 3D map world-to-screen projection helper, and sends a real move command through `SendLinuxLiveMapPreviewMoveCommandFromScreen(...)` after the live minimap/map-click proof is packed. The live 3D map diagnostics now include `proof=yes/no`, `finalLiveMapPreviewCommandProof`, and a `bootstrap-live-3d-map-proof` source/action. To keep `--bootstrap-create-game` smokes deterministic, raw live HUD/minimap/map X11 input is fenced only during that automated create-game mode; normal non-create-game runs still use the native live input handlers. This remains Linux bootstrap/OpenGL-only and keeps the Windows DirectX/full UI path untouched.
- Verification after the live 3D map automated-proof chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-00.25.58/linux-client-shell.log`; stdout reports `Final live 3D map commands: surface=yes proof=yes input=0 move=1 attack=0 signal=0 select=0 target=238.0,185.5 action=bootstrap-live-3d-map-proof`, `Final map command move: targetDrawn=yes commands=2 ... input=bootstrap-live-3d-map-proof`, `Final live minimap commands: surface=yes proof=yes input=0 move=1 attack=0 signal=0 select=0 target=237.2,182.7 marker=yes action=bootstrap-live-minimap-proof`, `Final live HUD commands: surface=yes proof=yes highlight=yes input=0 attack=0 useUnit=0 activate=1 talent=0 follow=0 slots=6 action=bootstrap-live-hud-activate-proof`, `Final live hotkey commands: ready=yes proof=yes input=0 stop=0 attack=0 follow=0 hold=1 cancel=0 activate=0 talent=0 useUnit=0 consumable=0 action=bootstrap-live-hotkey-hold-proof`, `Final visible live event feed: drawn=yes rows=5 command=5 combat=0 objective=0`, final transceiver `commands=23` / `replayCommands=23` / `replayBytes=1041`, and completed minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-00.27.39/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final live 3D map commands: surface=no proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 action=none`, `Final live minimap commands: surface=no proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 marker=no action=none`, `Final live HUD commands: surface=no proof=no highlight=no input=0 attack=0 useUnit=0 activate=0 talent=0 follow=0 slots=0 action=none`, `Final visible live scoreboard: drawn=no teams=0 heroMarkers=0 objectiveLines=0 commandLines=0`, and `Final visible live event feed: drawn=no rows=0 command=0 combat=0 objective=0`.
- Previous large chunk: the native Linux/OpenGL loaded-world 3D map preview is now a live gameplay command surface instead of being input-gated to the loading screen. `PW_Client/Game.cpp` keeps the existing loading-preview controls, but also enables the 3D map handler once `liveHeroState` has a real `PFWorld`: Q/E rotate, Z/X zoom, I/K tilt, R reset, mouse drag rotates, right-drag pans, left click selects/clears a world target, right click attacks a nearby enemy marker or moves, and middle click sends a minimap signal. The handler now excludes the active live HUD command rectangles, live minimap, and hero preview from 3D map clicks so UI controls do not double-trigger map commands. The older map-preview S/A/F/H/C/number command branch is skipped while live hotkeys are active, preserving the newer hotkey layer as the single live keyboard command path. Runtime diagnostics now record `finalLiveMapPreview...` file-log fields and a `Final live 3D map commands` stdout line, plus an event-feed row when manual live 3D map input happens. This remains Linux bootstrap/OpenGL-only and keeps the Windows DirectX/full UI path untouched.
- Verification after the live 3D map command-surface chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.25-00.00.37/linux-client-shell.log`; stdout reports `Final live 3D map commands: surface=yes input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 action=none`, while preserving `Final live HUD commands: surface=yes proof=yes highlight=yes input=0 attack=0 useUnit=0 activate=1 talent=0 follow=0 slots=7 action=bootstrap-live-hud-activate-proof`, `Final live hotkey commands: ready=yes proof=yes input=0 stop=0 attack=0 follow=0 hold=1 cancel=0 activate=0 talent=0 useUnit=0 consumable=0 action=bootstrap-live-hotkey-hold-proof`, `Final live minimap commands: surface=yes proof=yes input=0 move=1 attack=0 signal=0 select=0 target=237.2,182.7 marker=yes action=bootstrap-live-minimap-proof`, `Final visible live event feed: drawn=yes rows=5 command=4 combat=1 objective=0`, final transceiver `commands=22` / `replayCommands=22` / `replayBytes=998`, and completed minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.25-00.02.17/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final live 3D map commands: surface=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 action=none`, `Final live hotkey commands: ready=no proof=no input=0 stop=0 attack=0 follow=0 hold=0 cancel=0 activate=0 talent=0 useUnit=0 consumable=0 action=none`, `Final visible live HUD: drawn=no portrait=no bars=0 abilitySlots=0 abilityIcons=0 talentSlots=0 talentIcons=0 target=no`, `Final live HUD commands: surface=no proof=no highlight=no input=0 attack=0 useUnit=0 activate=0 talent=0 follow=0 slots=0 action=none`, `Final visible live minimap: drawn=no texture=no markers=0 heroes=0 creeps=0 moving=0 target=no`, `Final live minimap commands: surface=no proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 marker=no action=none`, `Final visible live scoreboard: drawn=no teams=0 heroMarkers=0 objectiveLines=0 commandLines=0`, and `Final visible live event feed: drawn=no rows=0 command=0 combat=0 objective=0`.
- Latest large chunk: the native Linux/OpenGL loaded-world view now has a live gameplay hotkey layer in addition to the mouse-driven HUD/minimap command surfaces. `PW_Client/Game.cpp` adds Linux bootstrap hotkey counters and routes live hero key input before minimap/preview controls once `liveHeroState` is ready: `S` stop, `A` attack selected/nearest, `F` follow selected/nearest, `H` hold, `C` cancel channelling, number keys activate the matching visible HUD talent slot, `T` uses talent on selected/nearest target, `U` uses unit, and `G` uses a consumable on selected target or a point near the hero. The hotkey layer reuses the existing native Prime World command helpers, records final log/stdout diagnostics, and adds an event-feed row. The existing hold command in the automated command chain now proves this path through `bootstrap-live-hotkey-hold-proof` with fallback to the previous direct hold command if the hotkey helper cannot send. This is Linux bootstrap/OpenGL-only work and keeps the Windows DirectX/full UI path untouched.
- Verification after the live hotkey command chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.24-23.49.26/linux-client-shell.log`; stdout reports `Final live hotkey commands: ready=yes proof=yes input=0 stop=0 attack=0 follow=0 hold=1 cancel=0 activate=0 talent=0 useUnit=0 consumable=0 action=bootstrap-live-hotkey-hold-proof`, while preserving `Final live HUD commands: surface=yes proof=yes highlight=yes input=0 attack=0 useUnit=0 activate=1 talent=0 follow=0 slots=7 action=bootstrap-live-hud-activate-proof`, `Final live minimap commands: surface=yes proof=yes input=0 move=1 attack=0 signal=0 select=0 target=237.2,182.7 marker=yes action=bootstrap-live-minimap-proof`, `Final visible live event feed: drawn=yes rows=5 command=4 combat=1 objective=0`, final transceiver `commands=22` / `replayCommands=22` / `replayBytes=998`, and completed minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.24-23.51.08/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final live hotkey commands: ready=no proof=no input=0 stop=0 attack=0 follow=0 hold=0 cancel=0 activate=0 talent=0 useUnit=0 consumable=0 action=none`, `Final visible live HUD: drawn=no portrait=no bars=0 abilitySlots=0 abilityIcons=0 talentSlots=0 talentIcons=0 target=no`, `Final live HUD commands: surface=no proof=no highlight=no input=0 attack=0 useUnit=0 activate=0 talent=0 follow=0 slots=0 action=none`, `Final visible live minimap: drawn=no texture=no markers=0 heroes=0 creeps=0 moving=0 target=no`, `Final live minimap commands: surface=no proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 marker=no action=none`, `Final visible live scoreboard: drawn=no teams=0 heroMarkers=0 objectiveLines=0 commandLines=0`, and `Final visible live event feed: drawn=no rows=0 command=0 combat=0 objective=0`.
- Latest large chunk: the native Linux/OpenGL live HUD is now also a command surface. `PW_Client/Game.cpp` stores per-frame hit rectangles for the visible attack slot, target panel, and up to nine numbered talent slots; prioritizes those rectangles in native Linux mouse input before minimap/character-preview handling; routes left/right/middle click actions through the existing Prime World command helpers; and records HUD command counters, last-action state, highlight state, event-feed rows, final log fields, and stdout summaries. Left click on a talent slot activates that talent, right click uses the selected talent on the selected/nearest enemy or point fallback, left click on the target panel sends use-unit, right click attacks selected/nearest, and middle click follows selected/nearest. The existing activate-talent bootstrap proof now prefers a matching visible HUD talent slot and falls back to the old direct command path if the HUD surface is not ready. This is Linux bootstrap/OpenGL-only work and keeps the Windows DirectX/full UI path untouched.
- Verification after the live HUD command-surface chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.24-23.40.06/linux-client-shell.log`; stdout reports `Final live HUD commands: surface=yes proof=yes highlight=yes input=0 attack=0 useUnit=0 activate=1 talent=0 follow=0 slots=7 action=bootstrap-live-hud-activate-proof`, while preserving `Final live minimap commands: surface=yes proof=yes input=0 move=1 attack=0 signal=0 select=0 target=237.2,182.7 marker=yes action=bootstrap-live-minimap-proof`, `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=0 talentSlots=7 talentIcons=7 target=yes`, `Final visible live event feed: drawn=yes rows=5 command=3 combat=2 objective=0`, final transceiver `commands=22` / `replayCommands=22` / `replayBytes=998`, `Final hero gameplay command execution: ... activate=1/1/1/1 ... useTalent=1/1/1/1 ...`, and completed minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.24-23.41.46/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=no textures=2 lineup=2`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final visible live HUD: drawn=no portrait=no bars=0 abilitySlots=0 abilityIcons=0 talentSlots=0 talentIcons=0 target=no`, `Final live HUD commands: surface=no proof=no highlight=no input=0 attack=0 useUnit=0 activate=0 talent=0 follow=0 slots=0 action=none`, `Final visible live minimap: drawn=no texture=no markers=0 heroes=0 creeps=0 moving=0 target=no`, `Final live minimap commands: surface=no proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 marker=no action=none`, `Final visible live scoreboard: drawn=no teams=0 heroMarkers=0 objectiveLines=0 commandLines=0`, and `Final visible live event feed: drawn=no rows=0 command=0 combat=0 objective=0`.
- Latest large chunk: the native Linux/OpenGL live minimap is now a command surface, not only a visible status overlay. `PW_Client/Game.cpp` now stores the live minimap inner rectangle and tactical world bounds every rendered gameplay frame, projects native Linux mouse clicks on that minimap back to `PFWorld` coordinates, and routes them through the existing native command senders: left click selects/clears nearby live markers, right click attacks nearby enemy markers or sends a move command, middle click sends a minimap signal, and wheel input is consumed so it does not accidentally zoom the 3D map preview. The automated bootstrap move proof now prefers the live minimap click path and falls back to the old 3D map-preview click only if the minimap surface is not ready. The live minimap draws a small command target marker and its footer/event-feed/log diagnostics report surface/proof/input/move/attack/signal/select/action counters. This is Linux bootstrap/OpenGL-only work and keeps the Windows DirectX/full UI path untouched.
- Verification after the live minimap command-surface chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 90-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.24-23.25.23/linux-client-shell.log`; stdout reports `Final live minimap commands: surface=yes proof=yes input=0 move=1 attack=0 signal=0 select=0 target=237.2,182.7 marker=yes action=bootstrap-live-minimap-proof`, and the old map-command diagnostics confirm the move was sourced from `input=bootstrap-live-minimap-proof`. The same smoke preserves the loaded-world stack with `Final visible live minimap: drawn=yes texture=yes markers=169 heroes=10 creeps=159 moving=88 target=yes`, `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=0 talentSlots=7 talentIcons=7 target=yes`, `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 objectiveLines=3 commandLines=1`, `Final visible live event feed: drawn=yes rows=5 command=2 combat=3 objective=0`, final transceiver `commands=22` / `replayCommands=22` / `replayBytes=998`, and completed minigame leave proof `attempted=1 completed=1`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.24-23.27.03/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final visible live minimap: drawn=no texture=no markers=0 heroes=0 creeps=0 moving=0 target=no`, `Final live minimap commands: surface=no proof=no input=0 move=0 attack=0 signal=0 select=0 target=0.0,0.0 marker=no action=none`, `Final visible live HUD: drawn=no portrait=no bars=0 abilitySlots=0 abilityIcons=0 talentSlots=0 talentIcons=0 target=no`, `Final visible live scoreboard: drawn=no teams=0 heroMarkers=0 objectiveLines=0 commandLines=0`, and `Final visible live event feed: drawn=no rows=0 command=0 combat=0 objective=0`.
- Latest large chunk: the native Linux/OpenGL loaded-world overlay now has a compact visible live event feed on top of the existing HUD/minimap/scoreboard stack. `PW_Client/Game.cpp` adds Linux bootstrap runtime counters and `DrawLinuxLiveEventFeedOverlay(...)`, which is driven by existing live gameplay proofs instead of fake UI state: command/replay/world-step counters, selected-target attack proof, creep-duel proof, current live target state, world hero/creep/objective counts, and native command action/move state. The feed stays inactive on menu-only frames, draws after the HUD so it remains visible during create-game/loading frames, and reports `finalVisibleLiveEventFeed...` counters for total rows plus command/combat/objective row categories. This is Linux bootstrap/OpenGL overlay work only and does not touch the Windows DirectX/full Flash path.
- Verification after the visible live event-feed chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 20-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.24-23.10.04/linux-client-shell.log`; stdout reports `Final visible live event feed: drawn=yes rows=5 command=1 combat=3 objective=1` while preserving `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 objectiveLines=2 commandLines=1`, `Final visible live minimap: drawn=yes texture=yes markers=40 heroes=10 creeps=30 moving=28 target=yes`, `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=0 talentSlots=7 talentIcons=7 target=yes`, loading info/chat/roster visibility, final transceiver `commands=6` / `replayCommands=6` / `replayBytes=226`, final map 3D preview terrain/water/static/animated/dynamic/hero/creep rendering, selected-target attack proof `damage=yes sustained=yes`, and creep runtime `creepSpawners=6 neutralSpawners=28`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.24-23.10.33/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final visible live HUD: drawn=no portrait=no bars=0 abilitySlots=0 abilityIcons=0 talentSlots=0 talentIcons=0 target=no`, `Final visible live minimap: drawn=no texture=no markers=0 heroes=0 creeps=0 moving=0 target=no`, `Final visible live scoreboard: drawn=no teams=0 heroMarkers=0 objectiveLines=0 commandLines=0`, and `Final visible live event feed: drawn=no rows=0 command=0 combat=0 objective=0`.
- Latest large chunk: the native Linux/OpenGL loaded-world overlay now has a visible top live scoreboard/status bar in addition to the bottom HUD and minimap. `PW_Client/Game.cpp` adds Linux bootstrap runtime counters and `DrawLinuxLiveScoreboardOverlay(...)`, which samples the same live `PFWorld` `LinuxDynamicWorldMarker` stream, groups heroes/creeps by `FACTION_FREEZE` and `FACTION_BURN`, renders team columns with live/total hero counts and hero pips, marks the selected hero's faction, shows creep pressure/damaged-unit hints, and draws center status lines for world step, tower/main-building totals, command count, and replay byte count. During loading it is placed on the right below the loading info panel so it does not cover the Windows-like loading chat/roster; it remains inactive on menu-only frames. Final diagnostics now include `finalVisibleLiveScoreboard...` counters for team columns, hero markers, objective lines, and command lines. This is Linux bootstrap/OpenGL overlay work only and does not touch the Windows DirectX/full Flash path.
- Verification after the visible live scoreboard chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 20-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.24-23.03.00/linux-client-shell.log`; stdout reports `Final visible live scoreboard: drawn=yes teams=2 heroMarkers=10 objectiveLines=2 commandLines=1` while preserving `Final visible live minimap: drawn=yes texture=yes markers=40 heroes=10 creeps=30 moving=28 target=yes`, `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=0 talentSlots=7 talentIcons=7 target=yes`, loading info/chat/roster visibility, final transceiver `commands=6` / `replayCommands=6` / `replayBytes=226`, final map 3D preview terrain/water/static/animated/dynamic/hero/creep rendering, selected-target attack proof `damage=yes sustained=yes`, and creep runtime `creepSpawners=6 neutralSpawners=28`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.24-23.03.31/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final visible live HUD: drawn=no portrait=no bars=0 abilitySlots=0 abilityIcons=0 talentSlots=0 talentIcons=0 target=no`, `Final visible live minimap: drawn=no texture=no markers=0 heroes=0 creeps=0 moving=0 target=no`, and `Final visible live scoreboard: drawn=no teams=0 heroMarkers=0 objectiveLines=0 commandLines=0`.
- Latest large chunk: the native Linux/OpenGL loaded-world view now has a visible live minimap overlay, not just the 3D map preview and bottom HUD. `PW_Client/Game.cpp` adds Linux bootstrap runtime counters and a `DrawLinuxLiveMinimapOverlay(...)` pass that uses the selected map's real minimap texture (`minimapFirst` / `minimapSecond` / `minimapNeutral`) through the existing UI texture cache, projects live `PFWorld` `LinuxDynamicWorldMarker` positions into the minimap using the selected map tactical extents, draws faction-colored hero/creep markers, movement direction strokes, a highlighted selected hero, and an X marker for the selected/live target. The minimap is inactive on menu-only frames, and during loading it is positioned between the loading chat and roster to reduce overlap with the Windows-like loading UI. Final diagnostics now include `finalVisibleLiveMinimap...` counters for texture, marker, hero, creep, moving, and target-marker visibility. This is Linux bootstrap/OpenGL overlay work only and does not touch the Windows DirectX/full Flash path.
- Verification after the visible live minimap chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 20-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.24-22.57.55/linux-client-shell.log`; stdout reports `Final visible live minimap: drawn=yes texture=yes markers=40 heroes=10 creeps=30 moving=28 target=yes` while preserving `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=0 talentSlots=7 talentIcons=7 target=yes`, loading info/chat/roster visibility, final transceiver `commands=6` / `replayCommands=6` / `replayBytes=226`, final map 3D preview terrain/water/static/animated/dynamic/hero/creep rendering, selected-target attack proof `damage=yes sustained=yes`, and creep runtime `creepSpawners=6 neutralSpawners=28`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.24-22.58.26/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final visible live HUD: drawn=no portrait=no bars=0 abilitySlots=0 abilityIcons=0 talentSlots=0 talentIcons=0 target=no` plus `Final visible live minimap: drawn=no texture=no markers=0 heroes=0 creeps=0 moving=0 target=no`.
- Latest large chunk: the native Linux/OpenGL loaded-world HUD now draws visible in-game state from the live `PFWorld` instead of only logging it. `PW_Client/Game.cpp` extends the Linux bootstrap overlay with a bottom/right gameplay HUD panel that uses `liveHeroState` / `liveTargetState`, resolves the selected hero portrait through the existing UI texture cache, paints hero HP/energy bars, level/gold/stat text, a compact attack slot, the selected hero default talent strip, and a live target panel with object/kind/faction/source/distance plus HP and attack stats. The HUD is shifted to the right while the loading roster is active so it does not cover the Windows-like loading lineup/chat overlay. Final diagnostics now include `finalVisibleLiveHud...` counters for the visible panel, portrait, bars, ability/talent slots/icons, and target panel. This is Linux bootstrap/OpenGL overlay work only; the Windows DirectX/full Flash path remains untouched.
- Verification after the visible loaded-world HUD chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/LinuxBootstrap/LINUX_PORT_PLAN.md` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 20-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.24-22.50.29/linux-client-shell.log`; stdout reports `Final visible live HUD: drawn=yes portrait=yes bars=3 abilitySlots=1 abilityIcons=0 talentSlots=7 talentIcons=7 target=yes` and `Final live HUD state: samples=76 hero=yes ... hp=810/810/100% energy=725/725/100% ... target=yes ... hp=20/900/2%`. The same smoke preserves the loading UI proofs (`Final visible loading info: drawn=yes lines=3 icons=3`, `Final visible loading chat: drawn=yes channels=4 messages=1`, `Final visible loading roster: drawn=yes rows=10 portraits=10 flags=10 progress=10 force=10 meta=0 rank=0 premium=0`), final transceiver `commands=6` / `replayCommands=6` / `replayBytes=226`, final map 3D preview terrain/water/static/animated/dynamic/hero/creep rendering, selected-target attack proof `damage=yes sustained=yes`, and creep runtime `creepSpawners=6 neutralSpawners=28`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.24-22.50.59/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-gameplay counters `Final visible live HUD: drawn=no portrait=no bars=0 abilitySlots=0 abilityIcons=0 talentSlots=0 talentIcons=0 target=no`.
- Latest large chunk: the native Linux/OpenGL loading screen now paints a broader Windows-like loading overlay over the loading/create-game path instead of only capturing the Flash/player data in diagnostics. `PW_Client/Game.cpp` adds a Linux overlay texture resolver that can load both ITEX-described UI textures and direct DDS payload refs, uses the captured `LinuxLoadingUiState` / `LinuxLoadingUiPreview` to draw status/tip/mode/locale information with mode and locale icons, uses the captured `LoadingFlashInterface` chat channels/messages to draw a compact loading chat panel, and uses the captured `LinuxLoadingHeroesRuntimePreview` lineup to draw two team rows with real hero portraits, player/hero text, flag icons, per-player progress bars, force chips from the Flash force-color table, and conditional metadata/rank/premium indicators when real session data provides them. Final diagnostics now record `finalVisibleLoadingInfo...`, `finalVisibleLoadingChat...`, and expanded `finalVisibleLoadingRoster...` counters including force/meta/rank/premium. This is still Linux bootstrap/OpenGL overlay work and does not touch the Windows DirectX/full Flash loading path.
- Verification after the visible loading info/chat/roster metadata chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/LinuxBootstrap/LINUX_PORT_PLAN.md` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 20-second native OpenGL `--bootstrap-create-game` smoke wrote `Bin/logs/2026.06.24-22.44.40/linux-client-shell.log`; stdout reports `Final visible loading info: drawn=yes lines=3 icons=3`, `Final visible loading chat: drawn=yes channels=4 messages=1`, and `Final visible loading roster: drawn=yes rows=10 portraits=10 flags=10 progress=10 force=10 meta=0 rank=0 premium=0`. The zero metadata/rank/premium counts are expected for the generated local match because it currently has no nonzero ratings, rank icon refs, or premium slots. The smoke preserves `Loading flash capture: ... chat=11/6/1 heroes=10/10/0 bindings=10/10/10`, `UI loading runtime: ready=yes ... players=10`, final transceiver `commands=6` / `replayCommands=6` / `replayBytes=226`, final map 3D preview terrain/water/static/animated/dynamic/hero/creep rendering, selected-target attack proof `damage=yes sustained=yes`, and creep runtime `creepSpawners=6 neutralSpawners=28`.
- The normal 8-second native OpenGL menu regression smoke wrote `Bin/logs/2026.06.24-22.45.10/linux-client-shell.log`. It keeps the starting UI overlays intact with `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`, `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, and expected non-loading counters `Final visible loading info: drawn=no lines=0 icons=0`, `Final visible loading chat: drawn=no channels=0 messages=0`, and `Final visible loading roster: drawn=no rows=0 portraits=0 flags=0 progress=0 force=0 meta=0 rank=0 premium=0`.
- Latest large chunk: the native Linux/OpenGL selected-hero preview pane now shows more of the real starting/loading UI data around the rotating 3D hero. `PW_Client/Game.cpp` adds a Linux overlay texture cache for UI artwork, renders the selected hero's real portrait texture, localized/title/race/stat text, and up to 12 resolved default talent icons directly on top of the existing OpenGL hero preview, and records final `finalVisibleLobbyHero...` diagnostics. The same helper is used by the visible starting lobby and the loading/create-game bridge. The current default `Plane` hero data resolves a portrait and talents but no ability icon texture, so the data-driven ability icon counter is expectedly `0` until ability icons are resolved for that hero. This remains Linux bootstrap/OpenGL overlay work and does not touch the Windows DirectX client path.
- Verification after the selected-hero starting/loading UI detail chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.06.24-22.09.18/linux-client-shell.log`; stdout reports `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10` and `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`, while the log records `finalVisibleLobbyHeroDetailsDrawn=yes`, `finalVisibleLobbyHeroPortrait=yes`, `finalVisibleLobbyHeroAbilityIcons=0`, and `finalVisibleLobbyHeroTalentIcons=12`.
- A short 20-second `--bootstrap-create-game` smoke exited `0` and wrote `Bin/logs/2026.06.24-22.09.45/linux-client-shell.log`. The auto-create path still leaves the visible map-detail panel inactive at final shutdown (`drawn=no`) because it enters loading, but the hero detail layer remains rendered through loading: stdout reports `Final visible lobby hero details: drawn=yes portrait=yes abilities=0 talents=12`. The game/bootstrap proofs remain intact: `UI loading runtime: ready=yes ... players=10 ... Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, final transceiver `commands=6` / `replayCommands=6` / `replayBytes=226`, final 3D map preview terrain/water/static/animated/dynamic/hero/creep rendering, selected-target attack proof `damage=yes sustained=yes hits=15`, and creep runtime `creepSpawners=6 neutralSpawners=28`.
- Latest large chunk: the native Linux/OpenGL starting lobby now renders a richer `Lobby_SelectGameMode`-style selected-map detail layer instead of only listing maps/sessions. `PW_Client/Game.cpp` now caches selected-map loading back art, logo art, and minimap art as OpenGL textures on the Linux overlay, draws a top-left selected-map panel with localized map title/details/description, real map object/terrain/water/light counts, and a 10-slot generated local lineup strip, and records final `finalVisibleLobbyDetails...` diagnostics. The OpenGL overlay path also now detects whether the `PF_Render::Interface` Linux UI callback actually ran during the frame; if not, it draws the native overlay directly before `Present()`, preserving the existing callback path while making menu-only frames deterministic. This is Linux bootstrap/OpenGL overlay work only and does not touch the Windows DirectX client path.
- Verification after the selected-map starting-lobby detail chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A normal 8-second native OpenGL menu smoke wrote `Bin/logs/2026.06.24-21.58.49/linux-client-shell.log` and stdout reports `Final visible lobby details: drawn=yes artwork=yes minimap=yes textures=3 lineup=10`; the final log repeats `finalVisibleLobbyDetailsDrawn=yes`, `finalVisibleLobbyDetailsArtwork=yes`, `finalVisibleLobbyDetailsMinimap=yes`, `finalVisibleLobbyDetailsTextures=3`, `finalVisibleLobbyDetailsLineupSlots=10`, and keeps `uiRootRuntimeVisibleMenuPath=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- A short 20-second `--bootstrap-create-game` smoke also exited `0` and wrote `Bin/logs/2026.06.24-21.59.10/linux-client-shell.log`. This auto-create path moves into loading before the visible lobby panel persists, so its final visible-lobby detail counter is expectedly `drawn=no`, but it preserves the starting/loading/game bootstrap path: stdout reports `Loading flash capture: force=6/6 modes=3 locales=yes teamForce=yes chat=11/6/1 heroes=10/10/0 bindings=10/10/10 default=3 tip=yes status=yes`, `UI loading runtime: ready=yes ... players=10 ... Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, final transceiver `commands=6` / `replayCommands=6` / `replayBytes=226`, final 3D map preview terrain/water/static/animated/dynamic/hero/creep rendering, selected-target attack proof `damage=yes sustained=yes hits=15`, and creep runtime `creepSpawners=6 neutralSpawners=28`.
- Latest large chunk: the Linux loading/starting UI Flash capture now replays the actual 10-player loading lineup into the bootstrap `LoadingFlashInterface` instead of only proving global UI tables. `PW_Client/Game.cpp` initializes the loading runtime after `ProbeLoadingHeroesRuntimePreview(...)`, feeds every captured hero into stable Flash visual rows, keeps the runtime user id for chat/player bindings, captures hero identity/progress/level/force/rating/premium state, and records player icon plus player-to-hero bindings. The Linux preview also falls back to the parsed hero catalog portrait refs when the bootstrap DB avatar path is empty, and `LoadingHeroes.cpp` now lets Linux run the same DB hero/icon lookup block Windows already used. The Windows/full-render loading path remains on the existing Flash VM path; this work only broadens Linux bootstrap capture and uses cross-platform code that Windows was already executing.
- Verification after the loading Flash lineup replay chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp pw/branches/r1117/Src/PW_Client/LoadingHeroes.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A normal 8-second native OpenGL smoke wrote `Bin/logs/2026.06.24-21.42.55/linux-client-shell.log`; stdout reports `Loading flash capture: force=6/6 modes=3 locales=yes teamForce=yes chat=11/6/1 heroes=10/10/0 bindings=10/10/10 default=3 tip=yes status=yes`, while the log reports `loadingRuntimeFlashPlayerBindings=10`, `loadingRuntimeFlashPlayerBindingIcons=10`, `loadingRuntimeFlashPlayerBindingHeroes=10`, `loadingRuntimeFlashHeroes=10`, `loadingRuntimeFlashHeroProgress=10`, and `uiRootRuntimeVisibleMenuPath=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- A short 20-second `--bootstrap-create-game` smoke also exited `0` and wrote `Bin/logs/2026.06.24-21.43.12/linux-client-shell.log`. It preserves the same loading Flash capture proof (`loadingRuntimeFlashPlayerBindings=10`, icon bindings `10`, hero bindings `10`, heroes `10`, progress `10`) and reaches `UI loading runtime: ready=yes ... players=10 ... Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`. The loaded-world proofs remain intact: final transceiver `commands=6` / `replayCommands=6` / `replayBytes=226`, 3D terrain/water/static/animated/dynamic/hero/creep rendering, selected-target attack proof `damage=yes sustained=yes hits=15`, `Final hero attack command execution: can=1/1 executed=1 accepted=1 ... hp=845->790`, and `Final Linux creep runtime: creepSpawners=6 neutralSpawners=28 ... movedCommonCreeps=29`.
- Latest large chunk: the native Linux loading/starting UI path now captures substantially more of the Windows Flash loading-interface data surface without pulling in the Flash VM/Tamarin path. Under `PW_LINUX_DB_BOOTSTRAP`, `LoadingFlashInterface` now retains force/color tables, team force strings, locale images/tooltips, mode descriptions, chat channels/shortcuts/messages/default/visibility state, player icon/hero bindings, loading status text, and localized tip text. `PW_Client/Game.cpp` seeds that Linux capture from real `DBUIData` plus the resolved loading UI preview, snapshots it into explicit runtime diagnostics, logs `loadingRuntimeFlash...` proof lines, and prints a concise `Loading flash capture` stdout summary. Windows/full-render loading remains on the existing `UI::FlashInterface` implementation path.
- Verification after the loading Flash-interface capture parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/LoadingFlashInterface.h pw/branches/r1117/Src/PW_Client/LoadingFlashInterface.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A normal 8-second native OpenGL smoke wrote `Bin/logs/2026.06.24-20.53.54/linux-client-shell.log` and stdout reports `Loading flash capture: force=6/6 modes=3 locales=yes teamForce=yes chat=11/6/1 default=3 tip=yes status=yes` while preserving `Visible menu runtime: ready=yes ... Linux visible lobby overlay + NGameX::SelectGameModeScreen`. The log reports `loadingRuntimeFlashForceColors=6/6`, `loadingRuntimeFlashModeDescriptions=3`, `loadingRuntimeFlashLocales=yes`, `loadingRuntimeFlashTeamForce=yes`, `loadingRuntimeFlashTip=yes`, `loadingRuntimeFlashStatus=yes`, `loadingRuntimeFlashChat=channels:11 shortcuts:6 messages:1 visible:yes off:no default:3`, and sample entries through `message:Loading runtime ready`.
- A short 20-second `--bootstrap-create-game` smoke also exited `0` and wrote `Bin/logs/2026.06.24-20.54.29/linux-client-shell.log`. It reports the same loading Flash capture proof, reaches `UI loading runtime: ready=yes ... Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, and preserves the game bootstrap proofs: scheduler/transceiver ready, `Final game transceiver runtime: ready=yes world=yes ... commands=6 ... replayCommands=6 replayBytes=226`, `Final map 3D preview: terrain=yes ... static=yes ... animated=yes ... dynamic=yes ... heroMeshes=yes/10 ... creepMeshes=yes/30`, `Final hero attack command execution: can=1/1 executed=1 accepted=1 ... hp=845->790`, and `Final Linux creep runtime: creepSpawners=6 neutralSpawners=28 ... movedCommonCreeps=29`.
- Latest large chunk: the Linux/null-render minigame item-transfer path now dispatches the real `PlayerBehaviourTracking` scroll events instead of leaving that Windows/full-client behavior deferred. `LinuxBootstrap/CMakeLists.txt` links `PF_GameLogic/PlayerBehaviourTracker.cpp` and `PF_GameLogic/PlayerBehaviourTracking.cpp` into the maintained Linux client/probe targets, `runtime_stubs.cpp` drops the obsolete fake `PlayerBehaviourTracker` object-cast registration, and `PlayerBehaviourTracking.cpp` keeps the `AdventureScreen` user-id path out of `PW_LINUX_NULL_RENDER` while preserving the hero/player overloads used by gameplay code. The Linux `PFPlayer` branch now owns and steps a real `PlayerBehaviourTracker`, with Linux-only diagnostic counters for dispatched events, `TookScroll`, `GaveScroll`, and last event id. `PFEaselPlayer::AddItemToHero(...)` now emits `GaveScroll` for allied scroll transfer and `TookScroll` for self scroll transfer before the existing inventory/statistics work. The Windows/full-render path remains on its existing code path.
- Verification after the minigame PlayerBehaviourTracking parity chunk: focused `git diff --check -- pw/branches/r1117/Src/LinuxBootstrap/CMakeLists.txt pw/branches/r1117/Src/LinuxBootstrap/runtime_stubs.cpp pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.cpp pw/branches/r1117/Src/PF_GameLogic/PFPlayer.cpp pw/branches/r1117/Src/PF_GameLogic/PlayerBehaviourTracker.cpp pw/branches/r1117/Src/PF_GameLogic/PlayerBehaviourTracker.h pw/branches/r1117/Src/PF_GameLogic/PlayerBehaviourTracking.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-20.30.25/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofWorldSessionBehaviour=calls:0->1->2 gave:0->1->1 took:0->0->1 last:8->7`, `finalGameMinigameLeaveProofWorldSessionItemStats=calls:0->1->2 ally:0->1 self:0->1 allyLast:7405->7429:1 selfLast:7405->7405:1`, `finalGameMinigameLeaveProofSlowdownHint=before:1 sentinel:0.25 during:1 after:0.25 restored:1`, `finalGameMinigameLeaveProofMinigameEvents=calls:1->2 started:1->1 exit:0->1 last:31->32 unit:7405->7405`, `finalGameMinigameLeaveProofLifecycleForwarding=drop:0->1 finish:0->1`, and `finalGameMinigameLeaveProofPlayerForwarding=drop:calls:0->1 current:1 single:0->1 finish:calls:0->1 current:1 single:0->1`. Stdout reports `Final minigame item behaviour proof: calls=0->1->2 gave=0->1->1 took=0->0->1 last=8->7`. The same smoke preserved the loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render `PFEaselPlayer::AddItemToHero(...)` path now mirrors more of the Windows/full-client minigame item-transfer surface. The Linux branch uses `CanGetScrollDuplicate(...)` before allied transfers, notifies world/session statistics after successful self and target inventory insertion, and keeps the local ally `OnScrollReceived()` behavior. `PFWorld` exposes a Linux-only `NotifyItemTransferForLinuxBootstrap(...)` wrapper so the Linux player translation unit does not pull in the full statistics header/link surface, and `PFStatistics` records Linux-only item-transfer diagnostics: total calls, self calls, ally calls, and the last from/to object ids plus item-present flag. `PW_Client/Game.cpp` now requires the active minigame proof to observe allied and self item-transfer statistic notifications with the expected source/target hero object ids. The Windows/full-render implementation remains on its existing code path.
- Verification after the minigame item-transfer statistics parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.cpp pw/branches/r1117/Src/PF_GameLogic/PFStatistics.cpp pw/branches/r1117/Src/PF_GameLogic/PFStatistics.h pw/branches/r1117/Src/PF_GameLogic/PFWorld.cpp pw/branches/r1117/Src/PF_GameLogic/PFWorld.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-20.09.22/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofWorldSessionItemStats=calls:0->1->2 ally:0->1 self:0->1 allyLast:7395->7419:1 selfLast:7395->7395:1`, `finalGameMinigameLeaveProofSlowdownHint=before:1 sentinel:0.25 during:1 after:0.25 restored:1`, `finalGameMinigameLeaveProofMinigameEvents=calls:1->2 started:1->1 exit:0->1 last:31->32 unit:7395->7395`, `finalGameMinigameLeaveProofLifecycleForwarding=drop:0->1 finish:0->1`, `finalGameMinigameLeaveProofPlayerForwarding=drop:calls:0->1 current:1 single:0->1 finish:calls:0->1 current:1 single:0->1`, `finalGameMinigameLeaveProofHeroHidden=1->0 invisible:1->0`, `finalGameMinigameLeaveProofSummonHidden=1->0 invisible:1->0 object:8685 created:1/1 count:1/1`, `finalGameMinigameLeaveProofStartGuard=place:7727 faction:1/2 can:1 available:1 result:0 heroPlace:7723 user:-1 current:1`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 ally:7419/6 add:1 slots:0->1 qty:1 add:1 slots:1->2 qty:1`. Stdout reports `Final minigame item stats proof: calls=0->1->2 ally=0->1 self=0->1 allyLast=7395->7419:1 selfLast=7395->7395:1`. The same smoke preserved the loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render minigame start/leave path now mirrors the Windows/full-client `PFWorld::SetIgnoreSlowdownHint(...)` behavior around active minigames. `PFEaselPlayer::StartMinigame(...)` sets the world slowdown hint to ignored after the start event is emitted, and `PFEaselPlayer::OnLeaveMinigameCmd()` clears the ignore flag after the native minigames facade leaves. `PW_Client/Game.cpp` verifies this through the existing active minigame leave proof by setting a temporary slowdown sentinel before the init-minigame command, requiring `GetSlowdownHint()` to report `1.00` while the minigame is active, requiring it to return to the sentinel after leave, and restoring the hint to `1.00` for the rest of the smoke. This is Linux/null-render parity work and leaves the Windows/full-render path on its existing implementation.
- Verification after the minigame slowdown-hint parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-19.45.48/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofSlowdownHint=before:1 sentinel:0.25 during:1 after:0.25 restored:1`, `finalGameMinigameLeaveProofMinigameEvents=calls:1->2 started:1->1 exit:0->1 last:31->32 unit:7395->7395`, `finalGameMinigameLeaveProofLifecycleForwarding=drop:0->1 finish:0->1`, `finalGameMinigameLeaveProofPlayerForwarding=drop:calls:0->1 current:1 single:0->1 finish:calls:0->1 current:1 single:0->1`, `finalGameMinigameLeaveProofHeroHidden=1->0 invisible:1->0`, `finalGameMinigameLeaveProofSummonHidden=1->0 invisible:1->0 object:8685 created:1/1 count:1/1`, `finalGameMinigameLeaveProofStartGuard=place:7727 faction:1/2 can:1 available:1 result:0 heroPlace:7723 user:-1 current:1`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 ally:7419/6 add:1 slots:0->1 qty:1 add:1 slots:1->2 qty:1`. Stdout reports `Final minigame slowdown proof: before=1.00 sentinel=0.25 during=1.00 after=0.25 restored=1.00`, plus the previous minigame event/lifecycle/player forwarding proofs. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render `PFEaselPlayer::MinigameEvent(...)` path now mirrors the Windows/full-client event broadcast surface instead of being a no-op. It records Linux-only diagnostics, builds a `PFBaseUnitMinigameEvent` with the requested `NDb::EBaseUnitEvent`, and calls `EventHappened(evt)` so minigame start/exit notifications move through the same hero event bus that the Windows path uses. `PF_GameLogic/PFEaselPlayer.h` / `.cpp` extend `LinuxMinigameDiagnostics` with minigame-event call counters, started/exit counters, last event type, and last source unit object id. `PW_Client/Game.cpp` requires the active minigame proof to observe one `BASEUNITEVENT_MINIGAMESTARTED` before the scripted leave and one `BASEUNITEVENT_MINIGAMEEXIT` after the leave, both on the same live easel hero. The Windows/full-render implementation path remains on its existing code.
- Verification after the minigame event-broadcast parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.cpp pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-19.37.27/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofMinigameEvents=calls:1->2 started:1->1 exit:0->1 last:31->32 unit:7395->7395`, `finalGameMinigameLeaveProofLifecycleForwarding=drop:0->1 finish:0->1`, `finalGameMinigameLeaveProofPlayerForwarding=drop:calls:0->1 current:1 single:0->1 finish:calls:0->1 current:1 single:0->1`, `finalGameMinigameLeaveProofHeroHidden=1->0 invisible:1->0`, `finalGameMinigameLeaveProofSummonHidden=1->0 invisible:1->0 object:8685 created:1/1 count:1/1`, `finalGameMinigameLeaveProofStartGuard=place:7727 faction:1/2 can:1 available:1 result:0 heroPlace:7723 user:-1 current:1`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 ally:7419/6 add:1 slots:0->1 qty:1 add:1 slots:1->2 qty:1`. Stdout reports `Final minigame event proof: calls=1->2 started=1->1 exit=0->1 last=31->32 unit=7395->7395`, plus the previous lifecycle/player forwarding proofs. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render `PFEaselPlayer::OnGameFinished(...)` and `PFEaselPlayer::DropCooldowns(...)` paths now mirror the Windows/full-client minigame lifecycle forwarding surface. `OnGameFinished(...)` still calls `PFBaseHero::OnGameFinished(...)`, then forwards `SessionFinished(failedFaction != GetFaction())` to the current single minigame when one is active. `DropCooldowns(...)` keeps the Windows call order by dropping base hero cooldowns first, then forwarding `CheatDropCooldowns()` to the current single minigame. `PF_GameLogic/PFEaselPlayer.h` / `.cpp` extend the Linux-only `LinuxMinigameDiagnostics` snapshot with owner-forwarder counters so the bootstrap proof can verify that the public `PFEaselPlayer` methods, not direct `ISingleMinigame` calls, advanced the single-minigame counters. `PW_Client/Game.cpp` requires both public forwarders to move the underlying single-minigame counters `0->1`, requires both forwarders to see an active current minigame, and logs `finalGameMinigameLeaveProofPlayerForwarding`. The Windows/full-render implementation path remains on its existing code.
- Verification after the minigame lifecycle-forwarding parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.cpp pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-19.09.32/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofLifecycleForwarding=drop:0->1 finish:0->1`, `finalGameMinigameLeaveProofPlayerForwarding=drop:calls:0->1 current:1 single:0->1 finish:calls:0->1 current:1 single:0->1`, `finalGameMinigameLeaveProofLifecycleSingleCalls=leave:2 pauseCmd:2 pause:2 step:4 update:1 map:1 cheatDrop:1 cheatWin:1 finish:1 eject:1`, `finalGameMinigameLeaveProofHeroHidden=1->0 invisible:1->0`, `finalGameMinigameLeaveProofSummonHidden=1->0 invisible:1->0 object:8685 created:1/1 count:1/1`, `finalGameMinigameLeaveProofStartGuard=place:7727 faction:1/2 can:1 available:1 result:0 heroPlace:7723 user:-1 current:1`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 ally:7419/6 add:1 slots:0->1 qty:1 add:1 slots:1->2 qty:1`. Stdout reports `Final minigame lifecycle forwarding proof: drop=0->1 finish=0->1` and `Final minigame player forwarding proof: drop=calls:0->1 current:1 single:0->1 finish=calls:0->1 current:1 single:0->1`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render `PFEaselPlayer::Isolate(...)` path now mirrors the Windows/full-client summon-hiding part of minigame isolation as well as the hero state. `PF_GameLogic/PFEaselPlayer.cpp` uses a Linux-only `HideLinuxMinigameUnit(...)` helper that applies `Hide(...)`, stops attackers, emits `BASEUNITEVENT_ISOLATE` on entry, and is called for the easel player plus primary and pet summons through `ForAllSummons(...)`. `PW_Client/Game.cpp` extends the active minigame-leave proof by creating a real primary summon owned by the live easel player while the minigame is active, requiring that summon to be hidden/invisible during minigame isolation and visible again after the leave command clears isolation. The Windows/full-render path remains on its existing `HideUnit(...)` implementation.
- Verification after the minigame summon-hide parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-18.45.10/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofHeroHidden=1->0 invisible:1->0`, `finalGameMinigameLeaveProofSummonHidden=1->0 invisible:1->0 object:8685 created:1/1 count:1/1`, `finalGameMinigameLeaveProofHeroFlag=1->0`, `finalGameMinigameLeaveProofStartGuard=place:7727 faction:1/2 can:1 available:1 result:0 heroPlace:7723 user:-1 current:1`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 ally:7419/6 add:1 slots:0->1 qty:1 add:1 slots:1->2 qty:1`. Stdout reports `Final minigame summon hide proof: object=8685 created=1/1 count=1/1 hidden=1->0 invisible=1->0`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render `PFEaselPlayer::Isolate(...)` path now mirrors the stateful unit-hiding part of the Windows/full-client minigame isolation flow instead of only flipping the local `isolated` boolean. Under Linux it calls `PFBaseUnit::Hide(isolate)` and `StopAttackingMe()` after changing the isolation state, so entering a minigame applies the normal `UNITFLAG_ISOLATED`/`UNITFLAG_INVISIBLE` mask and leaving clears it. This chunk was superseded by the latest summon-aware helper above.
- Verification after the minigame hide/isolation parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.cpp pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-17.39.39/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofHeroIsolated=1->0`, `finalGameMinigameLeaveProofHeroHidden=1->0 invisible:1->0`, `finalGameMinigameLeaveProofHeroFlag=1->0`, `finalGameMinigameLeaveProofStartGuard=place:7727 faction:1/2 can:1 available:1 result:0 heroPlace:7723 user:-1 current:1`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 ally:7419/6 add:1 slots:0->1 qty:1 add:1 slots:1->2 qty:1`. Stdout reports `Final minigame hide proof: isolated=1->0 hidden=1->0 invisible=1->0 inMinigame=1->0`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render minigame start path now enforces the same basic eligibility shape as the Windows/full-client `PFEaselPlayer::StartMinigame(...)`: it rejects null/unusable places, rejects places from a different faction, and rejects occupied places before mutating the active minigame state. `PF_GameLogic/PFEaselPlayer.cpp` received the Linux-only guarded checks; `PF_GameLogic/PFWorld.cpp` now makes `FindLinuxFirstAvailableMinigamePlaceForHero(...)` return only same-faction places and adds a Linux-only `FindLinuxFirstForeignMinigamePlaceForHero(...)` helper for proof coverage. `PW_Client/Game.cpp` uses that helper during the existing minigame-leave proof to attempt a start on an available foreign place, then verifies the call is rejected and the original minigame place/current single-minigame stay intact. The Windows/full-render `PFEaselPlayer` implementation path remains unchanged.
- Verification after the minigame start-guard parity chunk: focused `git diff --check -- pw/branches/r1117/Src/PF_GameLogic/PFEaselPlayer.cpp pw/branches/r1117/Src/PF_GameLogic/PFWorld.cpp pw/branches/r1117/Src/PF_GameLogic/PFWorld.h pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-17.01.55/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofStartGuard=place:7727 faction:1/2 can:1 available:1 result:0 heroPlace:7723 user:-1 current:1`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 ally:7419/6 add:1 slots:0->1 qty:1 add:1 slots:1->2 qty:1`. Stdout reports `Final minigame start guard proof: place=7727 faction=1/2 can=1 available=1 result=0 heroPlace=7723 user=-1 current=1`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux bootstrap minigame world-session proof exercises a real non-self allied consumable transfer before the minigame leave command runs. `PW_Client/Game.cpp` picks the first same-faction transfer target that is not the easel player, records its object/player ids, calls the existing public `PFEaselPlayer::AddItemToHero(allyHero, consumable, 1)`, and verifies the ally inventory grows by one slot with added quantity `1`. The proof still verifies the self insertion path afterward, so both ally and self item-transfer behavior are covered through the native Linux/null-render session facade. Completion requires a non-self allied target, successful allied insertion, `slots N->N+1`, quantity `1`, and the earlier same-faction target enumeration/economy/session invariants. This is Linux bootstrap proof code only; the existing Windows/full-render implementation path is unchanged.
- Verification after the non-self allied item-transfer chunk: focused `git diff --check -- pw/branches/r1117/Src/PW_Client/Game.cpp` exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-16.47.06/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 ally:7419/6 add:1 slots:0->1 qty:1 add:1 slots:1->2 qty:1`. Stdout reports the same proof appended to the final minigame line: `worldSession=targets:5/5/allied:5/foreign:0/self:1/scroll:0/zz:0->0/gold:30->37->30->30/nafta:30/item:1:1/ally:7419:6:1:0->1:1/self:1:1->2:1`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the active Linux easel/minigame proof covers the native `IWorldSessionInterface` economy/item surface used by minigames before the leave command runs, and the Linux null-render `PFEaselPlayer::GetItemTransferTargets(...)` follows the Windows/full-client shape by enumerating loaded-world same-faction heroes instead of returning an empty target list. `PF_GameLogic/PFEaselPlayer.cpp` includes `PFPlayer.h` only in the Linux branch and scans `PFWorld::GetPlayersCount()` / `GetPlayer(i)->GetHero()` for heroes with the easel player's faction. `PW_Client/Game.cpp` records the returned target count, the expected same-faction count computed independently from the loaded `PFWorld`, allied/foreign returned counts, and whether the easel player itself is present. The same proof records `CanGetScrollDuplicate(...)`, `CanBuyZZBoost()`, `GetGold()` / `AddGold(...)` / `TakeGold(...)` / `BuyZZBoost()`, `GetTotalNaftaEarned()`, and real shop-consumable insertion through `PFEaselPlayer::AddItemToHero(...)`. Completion requires matching same-faction target enumeration, no foreign targets, self present, scroll duplication and ZZ boost disabled, net-zero gold after `+7/-7` and ZZ boost no-op, nonnegative nafta, real shop/consumable data, item insertion success, inventory slots growing by one, and added quantity `1`. The existing Windows/full-render implementation path is unchanged.
- Verification after the same-faction target-list chunk: focused `git diff --check -- ...` on the touched porting files exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-16.36.34/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofWorldSession=targets:5/5 allied:5 foreign:0 self:1 scroll:0 zz:0->0 gold:30->37->30->30 nafta:30`, and `finalGameMinigameLeaveProofWorldSessionItem=shop:1 consumable:1 add:1 slots:1->2 qty:1`. Stdout reports the same proof appended to the final minigame line: `worldSession=targets:5/5/allied:5/foreign:0/self:1/scroll:0/zz:0->0/gold:30->37->30->30/nafta:30/item:1:1:1/slots:1->2/qty:1`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render easel player now exposes a Linux-only `NWorld::LinuxMinigameDiagnostics` snapshot through `PFEaselPlayer::GetLinuxMinigameDiagnostics(...)`, and the bootstrap minigame-leave proof actively exercises the native single-minigame lifecycle surface before leaving. `PF_GameLogic/PFEaselPlayer.h` declares the diagnostics only under `PW_LINUX_NULL_RENDER`; `PF_GameLogic/PFEaselPlayer.cpp` fills it from the existing `LinuxNullMinigames`, `LinuxNullSingleMinigame`, and `LinuxNullMinigamesMain` counters/state. `PW_Client/Game.cpp` now calls `SendPauseMinigameCommand(true/false)`, `PlaceUnderFogOfWar(true/false)`, `CheatDropCooldowns()`, `CheatWinGame()`, `SessionFinished(true)`, `IMinigames::OnStep(...)`, `UpdateM(...)`, `OnMapLoaded()`, and post-leave `ISingleMinigame::Eject()`, then requires the observed state/counters: lifecycle diagnostics `1->1`, running `1->0`, paused `0`, fog `1->0`, finished `0->1`, victory `1`, single calls `leave>=2 pauseCmd>=2 pause>=2 step>=1 update>=1 map>=1 cheatDrop>=1 cheatWin>=1 finish>=1 eject>=1`, minigames calls `leave>=1 force>=1 step>=1 update>=1 map>=1`, and main sent commands `>=1`. This remains Linux bootstrap/null-render instrumentation and does not change the Windows/full-render `PF_Minigames` implementation.
- Verification after the single-minigame lifecycle chunk: focused `git diff --check -- ...` on the touched porting files exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-15.36.29/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofLifecycle=diag:1->1 running:1->0 paused:0 fog:1->0 finished:0->1 victory:1`, `finalGameMinigameLeaveProofLifecycleSingleCalls=leave:2 pauseCmd:2 pause:2 step:4 update:1 map:1 cheatDrop:1 cheatWin:1 finish:1 eject:1`, and `finalGameMinigameLeaveProofLifecycleMinigamesCalls=leave:1 force:1 step:610 update:1 map:1 mainSent:1`. Stdout reports the same lifecycle proof appended to the minigame leave line: `lifecycle=diag:1->1/run:1->0/pause:0/fog:1->0/finish:0->1/victory:1 calls=single:2/2/2/4/1/1/1/1/1/1 minigames:1/1/610/1/1/main:1`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux bootstrap minigame-leave proof now exercises more of the native `PF_Minigames::IMinigamesMain`/`ISingleMinigame` surface while the minigame is still active. `PW_Client/Game.cpp` now records the Linux `PFEaselPlayer` bidon state before/after `SetCurrentBidon(NDb::BIDONTYPE_SPEEDCLOCK)`, registers a route spawner through `IMinigamesMain::RegisterCreepSpawner(...)`, checks it through `GetSpawnerID(...)`, checks out an instant melee minigame creep through `GetFreeCreep(...)`, verifies its stored descriptor through `GetCreepDesc(...)`, returns it through `ReturnCreep(...)`, and proves the instant creep descriptor is removed. The proof also verifies the active/catalog single-minigame DBID remains non-empty before and after the leave command. Completion now requires `bidon=0->1`, `spawner=-1->placeId`, `creep=id>=0 desc:1->0 out:1 owner:player instant:1 type:Melee`, and `singleDbid=1->1`, in addition to the prior command, common-DB, session, facade, opacity, and hero/place cleanup checks. This remains Linux bootstrap/runtime instrumentation only; the Windows/full-render `PF_Minigames::MinigamesMain`, `PF_Minigames::Minigames`, and `PFEaselPlayer` paths are not changed by this chunk.
- Verification after the minigames facade state-proof chunk: focused `git diff --check -- ...` on the touched porting files exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-15.18.11/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofMinigamesMainCommon=1->1`, `finalGameMinigameLeaveProofMinigamesMainBidons=5->5`, `finalGameMinigameLeaveProofBidon=0->1`, `finalGameMinigameLeaveProofMainSpawner=-1->7723`, `finalGameMinigameLeaveProofMainCreep=id:1 desc:1->0 out:1 owner:2 instant:1 type:0`, and `finalGameMinigameLeaveProofSingleDbid=1->1`. Stdout reports `Final minigame leave proof: attempted=1 completed=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4 minigames=1->1 local=0->0 session=1->1 main=1->1/1->1 common=1->1 bidons=5->5 bidon=0->1 spawner=-1->7723 creep=id:1/desc:1->0/out:1/owner:2/instant:1/type:0 opacity=1.00->0.00 single=count:1->1/current:1->0/index:1->1/name:1->1/id:1->1/dbid:1->1`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, live HUD status, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render minigames main facade now exposes real shared minigames common DB data instead of returning null from `GetCommonDBData()`. `LinuxBootstrap/CMakeLists.txt` links only `PF_Minigames/DBMinigames.cpp` into the Linux client DB source set, and `PF_Minigames/DBMinigames.cpp` keeps generated DB registration/serialization available while returning `0` from `DBMinigamesCommon::Construct()` under `PW_LINUX_NULL_RENDER` so the null-render target does not import full `PF_Minigames::MinigamesMain`/Easel. `PF_GameLogic/PFEaselPlayer.cpp` now lazily owns `NDb::Ptr<NDb::DBMinigamesCommon>` in `NWorld::LinuxNullMinigamesMain`, loads `MiniGames/MinigameCommon.xdb`, and makes the Linux `SetCurrentBidon(...)` follow the full-client shape safely: validate the enum, read `sessionBidonAbilities`, skip empty authored abilities, and install `ABILITY_ID_2` only when a DB ability exists. `PF_GameLogic/PFWorld.cpp` also uses the actual `MiniGames/MinigameCommon.xdb` path casing, which preserves Windows behavior and avoids Linux case-sensitive file misses in future native full-client minigames work.
- The Linux bootstrap minigame-leave proof now validates the shared minigames DB surface. `PW_Client/Game.cpp` records `GetMain()->GetCommonDBData()` and `sessionBidonAbilities.size()` before and after the packed leave command, logs `finalGameMinigameLeaveProofMinigamesMainCommon` and `finalGameMinigameLeaveProofMinigamesMainBidons`, prints `common=... bidons=...` in the final one-line proof, and completion now requires `common=1->1` plus at least the five authored bidon slots (`bidons=5->5`). This caught the initial Linux path-case miss (`Minigames/...` vs `MiniGames/...`) before the final verified smoke passed.
- Verification after the shared minigames common DB chunk: focused `git diff --check -- ...` on the touched porting files exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.24-06.18.17/linux-client-shell.log` and reports `finalGameMinigameLeaveProofCompleted=yes`, `finalGameMinigameLeaveProofMinigamesMainCommon=1->1`, `finalGameMinigameLeaveProofMinigamesMainBidons=5->5`, and stdout `Final minigame leave proof: attempted=1 completed=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4 minigames=1->1 local=0->0 session=1->1 main=1->1/1->1 common=1->1 bidons=5->5 opacity=1.00->0.00 single=count:1->1/current:1->0/index:1->1/name:1->1/id:1->1`. The same smoke preserved the existing loaded-world proofs: final transceiver `commands=22` / `cmdType=0x9D62D441` / `replayCommands=22` / `replayBytes=998`, 3D map terrain/water/static/animated/dynamic/hero/creep rendering, real map-click movement, use-unit/talent/portal/consumable/flag/pickup commands, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render easel player now has a native minimal `PF_Minigames::IMinigamesMain` facade instead of leaving `IMinigames::GetMain()` null, and minigame leave is now submitted through that facade. `PF_GameLogic/PFEaselPlayer.cpp` adds a Linux-only registered `NWorld::LinuxNullMinigamesMain` (`0x9D62D444`) that implements the public main/session factory contract without importing full Easel: it stores scene/world/transceiver pointers, produces `NWorld::LinuxNullMinigames` through `ProduceMinigamesObject(...)`, forwards `SendWorldCommand(...)` when a transceiver is present, steps session time from `LinuxNullMinigames::OnStep(...)`, maintains instant minigame creep descriptors, and records route spawner ids. `NWorld::LinuxNullMinigames` owns that facade and `GetMain()` returns it; the Linux `PFEaselPlayer` constructor uses `LinuxNullMinigamesMain::ProduceMinigamesObject(...)` instead of directly instantiating the session; `LinuxNullSingleMinigame::SendLeaveMinigameCommand(...)` now creates `NWorld::CmdLeaveMinigame` and sends it through `GetMain()->SendWorldCommand(...)` when the facade has a transceiver, with the old direct `OnLeaveMinigameCmd()` cleanup kept as the no-transceiver fallback. At that point `GetCommonDBData()` remained nullable on this bootstrap path; the latest chunk above replaces that limitation by linking/loading the shared common DB data. The Windows/full-render `PFEaselPlayer`, `PF_Minigames::MinigamesMain`, and `PF_Minigames::Minigames` paths remain untouched.
- The Linux bootstrap minigame-leave proof now validates and exercises the main facade in addition to the session and single-minigame surfaces. `PW_Client/Game.cpp` records `GetMain()` and `GetMain()->GetWorld()==PFWorld` before and after the packed leave command, logs `finalGameMinigameLeaveProofMinigamesMain` and `finalGameMinigameLeaveProofMinigamesMainWorld`, and completion requires `main=1->1/1->1`. The proof no longer sends the leave command by calling `runtime->transceiver->SendCommand(...)` directly; it attaches the runtime transceiver to `GetMain()`, calls `IMinigames::ForceLeaveMinigame()`, and waits for the world to observe packed command `0x9D62D441`.
- Verification after the native `IMinigamesMain` route chunk: focused `git diff --check -- ...` on the touched porting files exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.05-11.59.39/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=22 ... cmdType=0x9D62D441 ... replayCommands=22 replayBytes=998`, `Final minigame leave command: sent=1 commands=1 type=0x9D62D441 player=2 client=1984 object=7723`, `Final minigame leave proof: attempted=1 completed=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4 minigames=1->1 local=0->0 session=1->1 main=1->1/1->1 opacity=1.00->0.00 single=count:1->1/current:1->0/index:1->1/name:1->1/id:1->1`, and `Final minigame leave command execution: can=1/1 executed=1 accepted=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4`. The same smoke preserved the existing loaded-world proofs: 3D map terrain/water/static/animated/dynamic/hero/creep rendering, real map-click movement, real use-unit ability creation, talent spend/level-up, talent use, consumable buy/use, flag raise, pickup, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render easel player now exposes a native minimal `PF_Minigames::ISingleMinigame` behind the previously added `IMinigames` session object, so the bootstrap client no longer has an empty single-minigame surface while active. `PF_GameLogic/PFEaselPlayer.cpp` adds a Linux-only registered `NWorld::LinuxNullSingleMinigame` (`0x9D62D443`) that tracks the active `PFMinigamePlace`, DBID/string id, running/client-started/paused/session-finished state, and start/client/leave/pause/step/update/map-loaded/eject diagnostics without importing the full Easel renderer. `NWorld::LinuxNullMinigames` now creates that single object from `InitGames()`, starts it from `StartMinigame(...)`, forwards `StartClient()`, `Leave()`, `OnStep(...)`, `UpdateM(...)`, `OnMapLoaded()`, and `ForceLeaveMinigame()` through it, reports `MinigamesCount()==1`, returns it by index/name/id, and only reports it as `GetCurrentMinigame()` while a minigame place is active. The Windows/full-render `PFEaselPlayer` and `PF_Minigames::Minigames` paths remain untouched.
- The Linux bootstrap minigame-leave proof now validates the single-minigame surface in addition to the session object and hero/place cleanup. `PW_Client/Game.cpp` records `MinigamesCount()`, `GetCurrentMinigame()`, `GetMinigame(0)`, `GetMinigame(place->MinigameId().c_str())`, and `GetMinigameId(0)` before and after the packed leave command. Completion now requires `single=count:1->1/current:1->0/index:1->1/name:1->1/id:1->1`, proving the single minigame remains addressable by catalog/index/name/id while the active current pointer clears on leave.
- Verification after the native `ISingleMinigame` chunk: focused `git diff --check -- ...` on the touched porting files exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.05-09.08.51/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=22 ... cmdType=0x9D62D441 ... replayCommands=22 replayBytes=998`, `Final minigame leave command: sent=1 commands=1 type=0x9D62D441 player=2 client=1984 object=7723`, `Final minigame leave proof: attempted=1 completed=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4 minigames=1->1 local=0->0 session=1->1 opacity=1.00->0.00 single=count:1->1/current:1->0/index:1->1/name:1->1/id:1->1`, and `Final minigame leave command execution: can=1/1 executed=1 accepted=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4`. The same smoke preserved the existing loaded-world proofs: 3D map terrain/water/static/animated/dynamic/hero/creep rendering, real map-click movement, real use-unit ability creation, talent spend/level-up, talent use, consumable buy/use, flag raise, pickup, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Previous large chunk: the Linux/null-render easel player started owning a native `PF_Minigames::IMinigames` session object instead of leaving `PFEaselPlayer::GetMinigames()` empty. `PF_GameLogic/PFEaselPlayer.cpp` added a Linux-only registered `NWorld::LinuxNullMinigames` (`0x9D62D442`) that implements the public minigames session interface for the bootstrap client: `StartMinigame(...)`, `LeaveMinigame()`, `ForceLeaveMinigame()`, `OnStep(...)`, `OnMapLoaded()`, local/session binding, placement storage, and active-place opacity. The Linux `PFEaselPlayer` constructor creates this object, `StartMinigame(...)` routes through `IMinigames::SetPlacement(...)` / `StartMinigame(...)`, `OnLeaveMinigameCmd()` routes through `IMinigames::LeaveMinigame()`, and `ForceLeaveMinigame()` delegates through the same interface. At that point `GetCurrentMinigame()` / `GetMinigame(...)` were still null placeholders; the latest chunk above replaces that limitation with a minimal native `ISingleMinigame`. The Windows/full-render `PFEaselPlayer` and `PF_Minigames::Minigames` paths remain untouched.
- The Linux bootstrap minigame-leave proof now validates the minigames session interface itself, not only hero/place flags. `PW_Client/Game.cpp` records and logs `GetMinigames()` validity, `IsLocal()`, `GetWorldSessionInterface()==PFEaselPlayer`, and `GetMinigamePlaceOpacity()` before and after the packed leave command. Completion now requires `minigames=1->1`, `session=1->1`, and opacity `1.00->0.00` in addition to the existing hero place, place user, isolation flag, `UNITFLAG_INMINIGAME`, and visual-state cleanup checks. Final stdout/log diagnostics include `Final minigame leave proof: ... minigames=... local=... session=... opacity=...`, so future regressions can distinguish missing native minigames session ownership from command execution or place cleanup failures.
- Verification after the native `IMinigames` session chunk: focused `git diff --check -- ...` on the touched porting files exited `0`, and `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.05-08.55.53/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=22 ... cmdType=0x9D62D441 ... replayCommands=22 replayBytes=998`, `Final minigame leave command: sent=1 commands=1 type=0x9D62D441 player=2 client=1984 object=7723`, `Final minigame leave proof: attempted=1 completed=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4 minigames=1->1 local=0->0 session=1->1 opacity=1.00->0.00`, and `Final minigame leave command execution: can=1/1 executed=1 accepted=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4`. The same smoke preserved the existing loaded-world proofs: 3D map terrain/water/static/animated/dynamic/hero/creep rendering, real map-click movement, real use-unit ability creation, talent spend/level-up, talent use, consumable buy/use, flag raise, pickup, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Latest large chunk: minigame exit is now proved through the native world-command/transceiver path instead of only by a direct bootstrap helper call. `PF_GameLogic/HeroActions.h`, the maintained `PF_GameLogic/HeroActions.cpp`, and runnable `LinuxBootstrap/hero_actions_bootstrap.cpp` now expose/register `NWorld::CmdLeaveMinigame` (`0x9D62D441`) for `PFEaselPlayer`; the command validates an active minigame/isolation/`UNITFLAG_INMINIGAME` state, calls the real `PFEaselPlayer::OnLeaveMinigameCmd()`, and records before/after hero place, place user, isolation flag, minigame flag, visual state, and placement-apply diagnostics under `PW_LINUX_NULL_RENDER`. This deliberately does not pull in `PF_Minigames/EaselCommands.cpp` or the full Easel client renderer yet, and it leaves the existing Windows/full-client `PF_Minigames::LeaveMinigameCommand` path untouched.
- The Linux bootstrap scheduler now sends the new leave-minigame command after the packed init-minigame command has executed, then waits until the world reports `0x9D62D441` as the last packed command before marking the cleanup proof complete. Final stdout/log diagnostics now include `Final minigame leave command: sent=... commands=... type=0x9D62D441 ...` and `Final minigame leave command execution: can=... executed=... accepted=... state=... visual=... placement=...`, so future agents can distinguish command serialization/execution regressions from state-cleanup regressions.
- Verification after the command-layer minigame leave chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and the focused `git diff --check -- ...` passed before the build. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.05-04.25.48/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=22 ... heroInitMinigameSent=yes heroInitMinigameCommands=1 ... cmdType=0x9D62D441 ... replayCommands=22 replayBytes=998`, `Final minigame leave command: sent=1 commands=1 type=0x9D62D441 player=2 client=1984 object=7723`, `Final minigame leave proof: attempted=1 completed=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4`, and `Final minigame leave command execution: can=1/1 executed=1 accepted=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4`. The same smoke preserved the existing loaded-world proofs: real use-unit ability creation, real talent spend/level-up, talent use, consumable buy/use, flag raise, pickup, repeated enemy-hero kill/respawn/reengage, creep duel death/cleanup, and final award accounting (`amount=200 gold=230->430`).
- Latest large chunk: the Linux/null-render `CmdInitMinigame` path now performs real native minigame entry after the authored battle-start gate instead of only proving command serialization, and the Linux easel-player/place path can unwind that state cleanly. The Linux `PFAIWorld::Update(...)` stub now ticks `battleStartDelay` and `towersVulnerabilityDelay` so loaded-world scheduler code observes the same gameplay timer progression without pulling in full-client sound/adventure-screen/route-visibility side effects. Under `PW_LINUX_NULL_RENDER`, both maintained `PF_GameLogic/HeroActions.cpp` and runnable `LinuxBootstrap/hero_actions_bootstrap.cpp` resolve the live `PFEaselPlayer`/`PFMinigamePlace`, call the real `PFMinigamePlace::Use(...)` path, and record before/after hero minigame place, current place user, and hero-isolated state. Linux `PFEaselPlayer::StartMinigame(...)` now also sets the `UNITFLAG_INMINIGAME` flag, calls the place enter hook, flushes queued hero state, and logs the start event; `ForceLeaveMinigame()` now runs `OnLeaveMinigameCmd()`, which clears the flag, calls the place leave hook, clears the current place user, clears hero isolation, and logs the exit event. Linux `PFMinigamePlace::OnPlayerEnter()` now applies the authored DB visual-state transition to `Easel_Lobby`, and `OnPlayerLeave()` returns the place to `Session`; read-only visual/placement-state accessors expose that state for diagnostics. The non-Linux/full-render command executor and full-client easel-player path keep their existing behavior.
- The Linux bootstrap command scheduler now runs the optional minigame proof after talent activation/use, portal, consumable buy/use, flag raise, pickup, and final map-move proof. This avoids isolating the hero before the rest of the loaded-world command chain has proved its state transitions. After the packed init-minigame command executes, a Linux-only bootstrap proof calls `ForceLeaveMinigame()` and records `Final minigame leave proof: attempted=... completed=... state=heroPlaceBefore->After/placeUserBefore->After/isolatedBefore->After flag=before->after visual=before->after placement=before->after`, making both entry/cleanup and place visual-state regressions visible without relying on chat history.
- Verification after the real init-minigame/leave/visual-state chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check -- ...` on the touched files exited `0`. A 140-second MOBA create-game smoke wrote `Bin/logs/2026.06.04-21.44.22/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroInitMinigameSent=yes heroInitMinigameCommands=1 ... replayCommands=21 replayBytes=964`, `Final minigame send gate: prereq=1/1/1/1/1/1/1/1 hero=7395/2 easel=1 battleDelay=0.00 place=7723 available=1 canUse=1 sent=1 commands=1`, `Final hero gameplay command execution: ... init=1/1/1/1 available=1 canUse=1 ready=1 object=7723 initState=-1->7723/-1->7395/0->1 ...`, and `Final minigame leave proof: attempted=1 completed=1 hero=7395 state=7723->-1/7395->-1/1->0 flag=1->0 visual=5->1 placement=4->4`. The same smoke preserved the previous native gameplay proofs: real use-unit ability creation (`unitState=1/-1->-1/-1->-1/0->0`), real talent progression/spend (`progress=1->2 dev=1->2 gold=400->100`), real talent use (`talentState=-1->505/0->1`), real consumable use (`qty=1->0 occupied=1->0`), repeated hero kill/respawn/reengage (`hits=69 kills=4 hero=yes respawn=yes reengaged=yes`), and final award accounting (`amount=200 gold=230->430`).
- Latest large chunk: the Linux/null-render `CmdUseUnit` path now performs a real native use action instead of only proving command serialization / enqueue. Under `PW_LINUX_NULL_RENDER`, both the maintained `PF_GameLogic/HeroActions.cpp` command and the runnable Linux bootstrap command resolve the live hero/target, call `PFBaseUnit::Use(...)` directly, and record whether a `PFAbilityInstance` was created or whether minigame/hero-isolation state changed. The non-Linux/full-render command executor keeps its previous duplicate-state guard and `PFHeroUseUnitState` enqueue behavior, preserving the Windows/full client path. The Linux minigame entry stubs were also moved closer to the real client path: `PFMinigamePlace::Use(...)` now starts a minigame for a valid `PFEaselPlayer`, and Linux `PFEaselPlayer::StartMinigame(...)` / `OnLeaveMinigameCmd()` now maintain `PFMinigamePlace::CurrentEaselPlayer()` plus the hero isolated flag.
- Verification after the real use-unit/minigame-state chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check -- ...` on the touched files exited `0`. A 90-second MOBA create-game smoke wrote `Bin/logs/2026.06.04-21.01.05/linux-client-shell.log` and preserved the full command chain (`Final game transceiver runtime: ... commands=21 ... heroUseUnitSent=yes ... heroUseTalentSent=yes ... heroUseConsumableSent=yes ... replayCommands=21 replayBytes=964`). The new state proof is visible in `Final hero gameplay command execution: ... useUnit=1/1/1/1 can=1 target=7479/9/0/-1 unitState=1/-1->-1/-1->-1/0->0 ...`, meaning the selected usable world object created a real ability instance on Linux. The same smoke preserved the existing loaded-world proofs: `activate=1/1/1/1 ... progress=1->2 dev=1->2 gold=400->100`, `talentState=-1->505/0->1/0.00->0.00`, `qty=1->0 occupied=1->0`, `Final map selected target: ... hits=57 kills=3 hero=yes respawn=yes reengaged=yes ...`, final HUD `level=2 gold=230`, and final award diagnostics `awards=4/1/3 ... amount=200 gold=30->230`.
- Latest large chunk: the Linux/null-render loaded-world hero command chain now proves real talent-use and consumable-use state transitions, not just command serialization. Linux `PFBaseHero::UseConsumable(...)` now validates a real consumable/ability/quantity, marks the consumable ability as the active using-consumable source, and creates a normal `PFAbilityInstance`; Linux `PFConsumableAbilityData::NotifyCastProcessed()` now decrements the linked `PFConsumable` quantity and removes the slot item when quantity reaches zero. Linux `PFBaseHero::CanUseConsumable(...)` now rejects empty stacks, and `TakeConsumable(...)` preserves the acquired quantity before placing the consumable in the hero slot. Under `PW_LINUX_NULL_RENDER`, `CmdUseTalent` now calls `PFBaseHero::UseTalent(...)` directly and records talent last-use step, active instance count, and cooldown before/after; `CmdUseConsumable` now calls `PFBaseHero::UseConsumable(...)` directly and records consumable quantity, occupied-slot state, and cooldown before/after. Final stdout/log diagnostics now print `talentState=lastUseBefore->After/instancesBefore->After/cooldownBefore->After` and `qty=before->after occupied=before->after cd=before->after`. The non-Linux/full-render command executor keeps its previous `UseTalent(level, slot, target)` and `PFHeroUseConsumableState` enqueue paths.
- Verification after the real talent-use/consumable-use chunk: `/tmp/primeworld-linux-bootstrap` was regenerated with `cmake -S pw/branches/r1117/Src -B /tmp/primeworld-linux-bootstrap`; `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`; and `git diff --check -- ...` on the touched files exited `0`. A current-source 90-second MOBA create-game smoke wrote `Bin/logs/2026.06.04-15.07.47/linux-client-shell.log` and reports the full command chain still intact (`Final game transceiver runtime: ... commands=21 ... heroUseTalentSent=yes ... heroUseConsumableSent=yes ... replayCommands=21 replayBytes=964`). The new state proof is visible in `Final hero gameplay command execution: ... useTalent=1/1/1/1 can=1 target=2/-1/-1 talentState=-1->505/0->1/0.00->0.00 ... consumable=1/1/1/1 can=1 slot=0 target=0/7383/1 qty=1->0 occupied=1->0 cd=0.00->0.00 ...`. The same smoke preserved loaded-world progression/combat proofs: `activate=1/1/1/1 ... progress=1->2 dev=1->2 gold=400->100`, `Final map selected target: ... hits=54 kills=3 hero=yes respawn=yes reengaged=yes requiredGold=300 ...`, final HUD `level=2 gold=230`, and final awards `amount=200 gold=30->230`.
- Latest large chunk: the Linux/null-render hero progression and talent activation path now exercises a real loaded-world Prime spend and level-up instead of only serializing `CmdActivateTalent`. `PFTalent::Activate()` now checks/spends hero Prime on Linux, `PFTalentsSet::CanActivateTalent(...)` rejects already-active/unowned/too-expensive slots, `PFTalentsSet::ActivateTalent(...)` increments development points, computes the same talent-level bonus fraction, updates acquired budget, and calls the owning hero activation callback, and `PFBaseHero`/`PFBaseMaleHero` now count Linux level-ups and apply hero stat upgrades from talent activation. `PFAIWorld::GetMaxHeroLevel()` / `GetHeroDevPoints4Level(...)` now match the full path's level-table indexing. The bootstrap command chain now chooses only unactivated positive-cost positive-dev-point talents, waits for enough real Prime, and re-engages the same respawned enemy hero until the selected hero has enough Prime for the cheapest currently available progression talent. The bootstrap command diagnostics now capture activation before/after hero level, development points, and gold. Windows/full-render paths remain on their existing branches.
- Verification after the hero progression/talent activation chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check -- ...` on the touched files exited `0`. A 90-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-21.54.52/linux-client-shell.log` and reports the full command chain restored (`Final game transceiver runtime: ... commands=21 ... heroActivateTalentSent=yes ... heroUseTalentSent=yes ... heroPickupObjectSent=yes ... replayCommands=21 replayBytes=964`). The selected-target proof now records real repeated hero kills for Prime (`Final map selected target: ... attackProof=yes damage=yes sustained=yes hits=55 kills=3 hero=yes respawn=yes reengaged=yes requiredGold=300 ...`) and the activation proof reports a real talent buy/level-up: `Final hero gameplay command execution: ... activate=1/1/1/1 can=1 slot=0,1 progress=1->2 dev=1->2 gold=400->100 ...`. The final live HUD confirms `level=2 gold=230`, and the award diagnostics show another real hero-kill award after the spend (`applied=7407/7395/7395 amount=200 gold=30->230`).
- Latest large chunk: the Linux loaded-world hero death path now exercises real enemy-hero kill and respawn instead of stopping at creep/neutral death proofs. `PFBaseHero` in the Linux/null-render branch now delegates hero damage to the base creature damage path, starts real respawn timers from AI hero-respawn params/death params/revive variables, advances `advanceRespawnDelay`/`respawnDelay`, restores health/energy and AI registration on `DoResurrect()`, and starts respawn from `OnUnitDie(...)`. Because Linux death unregisters units from `PFAIWorld`, `PFWorld::Step(...)` now ticks dead heroes with active respawn timers from the world object table before queued dead-unit cleanup. The bootstrap selected-target proof now prefers a live enemy hero, tracks `hero`, `respawn`, `respawnStep`, and `respawnDelay` in stdout/logs, and keeps sampling after kill until the respawn is observed. Windows/full-render paths remain on their existing branches.
- Verification after the real enemy-hero death/respawn chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check -- ...` exited `0`. GUI smokes had to run outside the sandbox. A 90-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-20.28.37/linux-client-shell.log` and reports `Final map selected target: ... attackProof=yes damage=yes sustained=yes hits=16 killed=yes hero=yes respawn=yes respawnStep=287 respawnDelay=-1.0 hp=900->900 ...` plus hero award `applied=7407/7395/7395 amount=200 gold=0->200`. A 90-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-20.30.19/linux-client-shell.log` and reports the same real hero proof (`hits=16 killed=yes hero=yes respawn=yes respawnStep=287`) plus award `applied=6623/6611/6611 amount=200 gold=0->200` while preserving NightMap point lights and expected optional missing pickup. A 10-second non-create-game MOBA UI smoke wrote `Bin/logs/2026.06.01-20.31.59/linux-client-shell.log` and preserves `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ...`.
- Latest large chunk: the Linux/null-render loaded-world death path now continues past HP reaching zero into queued unit death cleanup and real hero kill award accounting. `PFBaseUnit::OnUnitDie(...)` now drops targets, stops attackers, cancels abilities, unregisters from `PFAIWorld`, sends Linux death notifications/reward calls, notifies behaviour, cleans FSM state, removes behaviour/war-fog state, and queues the dead unit through `PFWorld::StoreDeadUnit(...)`. `PFWorld::Step(...)` drains that queue with `KillDeadUnits(false)`, `CleanupAfterDeath(...)` cancels active attack/passive ability state, and common creeps unregister from world creep counts/rings on death/destroy. Linux `PFAIWorld::AwardKillers(...)` now follows a real `NaftaAward` path with behaviour adjustment, default-stat fallback for spawned creeps that report zero `NaftaForKill`, hero/master killer resolution, kill/team award application, and last-applied award diagnostics. Linux `PFBaseHero::OnAddGold(...)` / `AwardForKill(...)` now update base unit gold instead of being no-op/recursive stubs. Final stdout/log diagnostics now report dead-unit storage/cleanup and award counters in `Final Linux creep runtime: ... dead=stored/cleaned pending=... awards=calls/rejected/applied ... applied=victim/killer/hero amount=... gold=before->after`. These changes are guarded by Linux/null-render/bootstrap code paths and do not change the Windows/full-render client path.
- Verification after the queued death/kill-award chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check -- ...` on the touched gameplay/client/plan files exited `0`. GUI smokes had to run outside the sandbox. A 60-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-20.05.36/linux-client-shell.log` and reports selected-target death (`Final map selected target: ... attackProof=yes damage=yes sustained=yes hits=5 killed=yes hp=300->0 ...`), independent creep-duel death (`Final Linux creep combat: ... hits=19 killed=yes attacker=7792 target=7872 hp=300->0 ...`), queued cleanup (`dead=2/2 pending=0 last=7872/7872`), and local hero kill reward application (`awards=2/1/1 ... applied=7867/7395/7395 amount=60 gold=0->60`). A 60-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-20.07.46/linux-client-shell.log` and reports the same shape (`selected target ... hits=5 killed=yes hp=300->0`, creep duel `hits=19 killed=yes attacker=6988 target=7068`, `dead=2/2 pending=0 last=7068/7068`, `awards=2/1/1 ... applied=7063/6611/6611 amount=60 gold=0->60`) while preserving NightMap point lights and the expected optional missing pickup (`heroPickupObjectCommands=0`). A 10-second non-create-game MOBA UI smoke wrote `Bin/logs/2026.06.01-20.08.55/linux-client-shell.log` and preserves the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game activations=0 path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Latest large chunk: the Linux loaded-world creep runtime now has an independent common-creep duel proof on top of spawned/moving lane creeps. `PW_Client/Game.cpp` waits until the selected-target hero proof has locked its target, chooses two different live common creeps from opposing factions via `PFWorld::GetLinuxDynamicWorldMarkers(...)`, excludes the hero selected-target object, places the duel target inside real base-attack range, assigns both creeps through existing `PFBaseUnit::AssignTarget(...)`, and lets the normal Linux `PFBaseUnit::Step(...)` / `PFBaseAttackData` path produce repeated creep-vs-creep damage and death. Final stdout/log diagnostics now include `Final Linux creep combat: prepared=... damage=... sustained=... hits=... killed=... attacker=... target=... hp=... range=... dist=... wait=...`. This is Linux bootstrap loaded-world proof code and does not alter the Windows/full-render client path.
- Verification after the loaded common-creep combat chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. GUI smokes had to run outside the sandbox. A 60-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-19.19.50/linux-client-shell.log` and reports `Final Linux creep combat: prepared=yes active=no damage=yes sustained=yes hits=19 killed=yes attacker=7792 target=7872 hp=300->0 ...`, while the hero selected-target proof remained separate (`Final hero attack command execution: ... target=7867 ...` and `Final map selected target: ... sustained=yes hits=5 killed=yes hp=300->0 ...`). A 60-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-19.21.01/linux-client-shell.log` and reports `Final Linux creep combat: prepared=yes active=no damage=yes sustained=yes hits=19 killed=yes attacker=6988 target=7068 hp=300->0 ...`, with the hero attack target again separate (`target=7063`) and the expected optional `heroPickupObjectCommands=0`. A 10-second non-create-game MOBA UI smoke wrote `Bin/logs/2026.06.01-19.22.09/linux-client-shell.log` and preserves the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game activations=0 path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Latest large chunk: the Linux selected-target live combat proof now continues through actual target death instead of unblocking after the second HP drop. `PW_Client/Game.cpp` still marks `sustained=yes` after two real base-attack hits, but keeps the scripted selected target in attack range and keeps the controlled hero's current target assigned until normal Linux `PFBaseUnit::Step(...)` / `PFBaseAttackData` processing kills the target or a bounded timeout expires. Hit counting is now active-proof-only, and the final diagnostics distinguish repeated-hit proof from kill proof with `hits`, `killed`, HP history, proof range/distance, and the `selected-target-expired` clear state. This remains Linux bootstrap diagnostic/control code and does not change the Windows/full-render client path.
- Verification after the selected-target kill/death chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. GUI smokes had to run outside the sandbox. A 45-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-19.03.47/linux-client-shell.log` and reports `Final map selected target: ... attackProof=yes damage=yes sustained=yes hits=5 killed=yes hp=300->0 ... input=selected-target-expired`, `Final hero attack command execution: ... ready=1->1 doAttack=1 fallback=0 hp=245->190 ...`, and the expected final live-HUD fallback to another nearest enemy after the killed proof target is cleared. A 45-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-19.04.41/linux-client-shell.log` and reports the same kill proof (`hits=5 killed=yes hp=300->0`) while preserving NightMap point lights and the expected `heroPickupObjectCommands=0` optional pickup result. A 10-second non-create-game MOBA UI smoke wrote `Bin/logs/2026.06.01-19.05.34/linux-client-shell.log` and preserves the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game activations=0 path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Latest large chunk: the Linux selected-target live combat proof now verifies sustained base-attack behavior instead of stopping after the first HP drop. `PW_Client/Game.cpp` keeps the scripted selected live target in-range only while the proof is active, keeps the controlled hero's current target assigned, lets the normal Linux `PFBaseUnit::Step(...)` / `PFBaseAttackData` path produce follow-up attacks, counts distinct target-life drops, and records `sustained`, `hits`, `killed`, and previous/current HP diagnostics in the final stdout/log. The proof unblocks the rest of the scripted command chain after two real hits or target death; current smokes report `sustained=yes hits=2 killed=no hp=300->135` while the command-level attack diagnostic still reports `doAttack=1 fallback=0`. This is Linux bootstrap diagnostic/control code and does not change the Windows/full-render client path.
- Verification after the sustained selected-target combat chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. GUI smokes had to run outside the sandbox. A clean 35-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-18.57.14/linux-client-shell.log` and reports `Final map selected target: ... attackProof=yes damage=yes sustained=yes hits=2 killed=no hp=300->135 ...`, `Final live HUD state: ... target=yes ... hp=135/300/45% ...`, and `Final hero attack command execution: ... ready=1->1 doAttack=1 fallback=0 hp=245->190 ...`. A clean 35-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-18.57.57/linux-client-shell.log` and reports the same sustained selected-target proof for object `7063`, while preserving NightMap point lights and the expected `heroPickupObjectCommands=0` optional proof. A 10-second non-create-game MOBA UI smoke wrote `Bin/logs/2026.06.01-18.56.50/linux-client-shell.log` and preserves the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game activations=0 path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Latest large chunk: the Linux live selected-target attack proof now uses the real base-attack path rather than only a command-boundary fallback. `PFBaseUnit.cpp` now constructs `PFBaseAttackData` for Linux/null-render units from DB `attackAbility`, mirrors full-client raw pointer checks for attack ability readiness/update/chase/DoAttack where bootstrap objects are present but not always world-registered for `IsValid`, and updates the owned base attack each `Step`. `PFAbilityData.cpp` now ticks Linux ability cooldowns down each update, so attacks and abilities can become ready again after `RestartCooldown`. `hero_actions_bootstrap.cpp` primes the scripted selected live target into range, records attack diagnostics, invokes `PFBaseHero::DoAttack(false)`, and only applies fallback damage if the real path fails to change target life. `PFBaseAttackData.cpp` keeps its Linux no-dispatch fallback, but current smokes now show `doAttack=1 fallback=0`. `PW_Client/Game.cpp` records selected-target attack proof HP/range/distance in final diagnostics. Windows/full-render paths remain on their existing branches; the new behavior is guarded by Linux/null-render/bootstrap paths.
- Verification after the live selected-target/base-attack chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check -- ...` exited `0`. GUI smokes had to run outside the sandbox. A 35-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-10.04.27/linux-client-shell.log` and reports `Final map selected target: ... attackProof=yes damage=yes hp=300->190 ...`, `Final live HUD state: ... target=yes ... hp=190/300/63% ...`, and `Final hero attack command execution: ... ready=1->1 doAttack=1 fallback=0 hp=245->190 ...`. A 35-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-10.05.16/linux-client-shell.log` and reports the same real selected-target attack proof for object `7063`, while preserving NightMap point lights and the expected `heroPickupObjectCommands=0` optional proof. A 10-second non-create-game MOBA UI smoke wrote `Bin/logs/2026.06.01-10.06.02/linux-client-shell.log` and preserves the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game activations=0 path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Latest large chunk: the previous Linux `AttackTargetApplicator`/base-attack range blocker is resolved. `PFAbilityData.cpp` now makes the Linux/null-render `GetUseRange(const PFBaseUnit*)` and `GetUseRange(const Target&)` target-aware instead of always evaluating range as owner-vs-owner; this keeps base-attack/ability range formulas using the intended target context. `PFBaseUnit.cpp` now uses the current base attack ability's DB `useRange` as the chase stop distance when a Linux unit has an assigned enemy target but is not yet in attack range, falling back to `STAT_RANGE` only when no positive ability range is available. `PW_Client/Game.cpp` resets the synthetic attack-owner/receiver pair immediately before the unit-move attack proof, after the earlier global movement/collision subproofs have run, so the proof uses a stable non-overlapping in-range fixture. The `unit-move` proof now completes through target assignment, range gate, base attack dispatch, and damage: `attack=yes/1->0/1.00->0.82 ready=yes range=yes can=yes target=yes direct=no`. The `direct=no` suffix is now expected because the normal step/update path already damaged the target before the fallback direct attack is attempted. These changes stay in Linux/null-render/bootstrap paths and do not alter the Windows/full-render client path.
- Verification after the Linux target-aware range/chase chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. `ctest --test-dir /tmp/primeworld-linux-bootstrap -R PrimeWorldLinux.*Probe --output-on-failure` reported that no CTest tests are registered for those build targets. A short MOBA create-game proof wrote `Bin/logs/2026.06.01-09.23.22/linux-client-shell.log` and shows `Session effects unit-move runtime: ... attack=yes/1->0/1.00->0.82 ready=yes range=yes can=yes target=yes direct=no`. A 30-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-09.23.46/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroMoveCommands=2 ... heroAttackCommands=1 ... heroFollowCommands=1 ... heroUseUnitCommands=1 ... heroActivateTalentCommands=1 ... heroUseTalentCommands=1 ... heroUsePortalCommands=1 ... heroUseConsumableCommands=1 ... heroBuyConsumableCommands=1 ... heroRaiseFlagCommands=1 ... heroInitMinigameCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=21 replayBytes=964`, `Final map 3D preview: terrain=yes/9216/18050 water=yes/4/88 roads=yes/6/45 terrainElements=yes/192/192/33202 static=yes/256/261/53964 animated=yes/48/57/133867 dynamic=yes/136/10/60/66/60 status=136/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=14/14 selectedHeroMarker=yes/2 yaw=93.0 pitch=56.0 zoom=1.18`, `Final map command move: ... moved=yes reached=yes ... current=384.0,186.5`, and `Final live HUD state: ... hero=yes ... hp=810/810/100% energy=725/725/100% damage=49.5-60.5 aps=0.68 move=6.76 regen=1.22/2.61 ... target=yes ... hp=300/300/100% ... damage=9.0-11.0 aps=0.71 move=5.66`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-09.24.50/linux-client-shell.log` and reports `commands=20`, all the same one-command hero proof classes except `heroPickupObjectCommands=0` because no pickup candidate existed on that map/run, `Final map 3D preview: ... dynamic=yes/115/10/60/45/60 status=115/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=13/13 selectedHeroMarker=yes/2 yaw=94.5 pitch=56.0 zoom=1.18`, NightMap point lights resolved (`pointLights=75 day=8 night=73`), and the same live hero/selected-creep derivative HUD values. A 10-second non-create-game MOBA UI smoke wrote `Bin/logs/2026.06.01-09.24.26/linux-client-shell.log` and preserves the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game activations=0 path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Latest large chunk: the Linux/null-render combat path now advances real base-attack setup instead of only proving direct ability calls. `PFBaseAttackData.cpp` gates Linux `DoAttack(...)` through `PFBaseUnit::IsTargetInAttackRange(...)` before dispatching so the bootstrap path follows the same basic attack-range rule shape as the full client. `PFApplInstant.cpp` adds a Linux fallback for the common base-attack damage formula strings that reference `sDamageMin` and `sDamageMax`, averaging the already loaded native owner damage range because the bootstrap `ExecutableString` path still cannot execute the Windows x86 formula blobs. `PFBaseUnit.cpp` now advances Linux behaviour/ability/external-ability state each step and, when a valid enemy target is assigned, either starts `DoAttack(false)` in range or asks `PFBaseMovingUnit::MoveTo(...)` to chase into range. `HeroActions.cpp` plus the Linux bootstrap command implementation in `PW_Client/Game.cpp` now clear the current attack target on explicit move/stop/follow/hold/combat-move paths so stale selected-target attacks do not steal map-click movement. The synthetic probes in `PW_Client/Game.cpp` now give direct base attacks explicit use ranges, prove direct baseattack damage (`unit-combat ... baseattack=yes/yes/yes/yes/1/1` and `unit-instant ... baseattack=yes/yes/yes/yes/1.00->0.80`), and expand the unit-move attack-target diagnostic to show the exact remaining blocker: `attack=no/1->0/1.00->1.00 ready=yes range=no can=yes target=yes direct=no`. That last line means the `AttackTargetApplicator` assigns the target and the unit can attack, but the synthetic no-world moving-unit fixture still fails the range predicate; live map-selected target HP is also expected to remain unchanged because the scripted movement proof intentionally clears attack target and the selected creep is roughly 266-268m away. Windows/full-render paths remain on their existing branches; the new behaviour is guarded by Linux/null-render/bootstrap code paths.
- Verification after the Linux combat/base-attack/target-control chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. `ctest --test-dir /tmp/primeworld-linux-bootstrap -R PrimeWorldLinux.*Probe --output-on-failure` reported that no CTest tests are registered for those build targets. The smokes had to run outside the sandbox so the native X11/OpenGL window could open. A 30-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-09.15.05/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroMoveCommands=2 ... heroAttackCommands=1 ... heroFollowCommands=1 ... heroUseUnitCommands=1 ... heroActivateTalentCommands=1 ... heroUseTalentCommands=1 ... heroUsePortalCommands=1 ... heroUseConsumableCommands=1 ... heroBuyConsumableCommands=1 ... heroRaiseFlagCommands=1 ... heroInitMinigameCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=21 replayBytes=964`, `Final map 3D preview: terrain=yes/9216/18050 water=yes/4/88 roads=yes/6/45 terrainElements=yes/192/192/33202 static=yes/256/261/53964 animated=yes/48/57/133867 dynamic=yes/136/10/60/66/60 status=136/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=14/14 selectedHeroMarker=yes/2 yaw=93.7 pitch=56.0 zoom=1.18`, `Final map command move: ... moved=yes reached=yes ... current=384.0,186.5`, and `Final live HUD state: ... hero=yes ... hp=810/810/100% energy=725/725/100% damage=49.5-60.5 aps=0.68 move=6.76 regen=1.22/2.61 ... target=yes ... hp=300/300/100% ... damage=9.0-11.0 aps=0.71 move=5.66`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-09.16.05/linux-client-shell.log` and reports `commands=20`, the same one-command hero proof classes except `heroPickupObjectCommands=0` because no pickup candidate existed on that map/run, `Final map 3D preview: ... dynamic=yes/115/10/60/45/60 status=115/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=13/13 selectedHeroMarker=yes/2 yaw=93.9 pitch=56.0 zoom=1.18`, NightMap point lights resolved (`pointLights=75 day=8 night=73`), and the same live hero/selected-creep derivative HUD values. A 10-second non-create-game MOBA UI smoke wrote `Bin/logs/2026.06.01-09.15.45/linux-client-shell.log` and preserves the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game activations=0 path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`; since no world is loaded on that path, `Final live HUD state: samples=0 hero=no ... target=no` is expected.
- Latest large chunk: the Linux null-render gameplay/stat path now computes live derivative combat stats and propagates more hero stat state instead of stopping at base life/energy. `PFBaseUnit.cpp` now assigns the AI derivative-stat container for Linux units, exposes DB-backed native attack/defense indices, implements the stock derivative formulas natively for Linux (`LifeRegenTotal`, `EnergyRegenTotal`, `DamageMin/Max`, `MoveSpeedMpS`, crit chance, attacks-per-second, defenses, and penetration) because the bootstrap `ExecutableString` path only evaluates literals rather than the Windows x86 formula blobs, implements guarded `PFStatContainer::GetStatBudget(...)`, and makes `UpgradeStats(...)` / `UpgradeHeroStats(...)` apply stored DB increments while preserving health/energy percentages. `PFHero.cpp` now lets Linux hero level-ups call the base unit stat upgrade path and synchronize primary/secondary summons plus clones. `PFMaleHero.cpp` now mirrors the Windows persistent hero stat mapping (`hsHealth`, `hsMana`, strength/intellect/agility/cunning/fortitude/will, life/mana regen), imports hero persistent buffs, applies persistent stat/buff increments during normal and fractional hero level-ups, adds talent preload force to non-clone heroes, and includes persistent stat budget in `GetForce(true)`. `PW_Client/Game.cpp` now records/logs/dumps live derivative HUD fields (`damage`, `aps`, `move`, and `regen`) for the selected live hero and target. These changes stay inside Linux/null-render/bootstrap code paths; Windows/full-render stat code remains on the existing `PFStatContainer`/formula implementation.
- Verification after the Linux derivative-stat/hero-stat parity chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. `ctest --test-dir /tmp/primeworld-linux-bootstrap -R 'PrimeWorldLinux.*Probe' --output-on-failure` reported that no CTest tests are registered for those build targets. The smokes had to run outside the sandbox so the native X11/OpenGL window could open. A 30-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-07.55.17/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroUseUnitCommands=1 ... heroActivateTalentCommands=1 ... heroUseTalentCommands=1 ... heroUsePortalCommands=1 ... heroUseConsumableCommands=1 ... heroBuyConsumableCommands=1 ... heroRaiseFlagCommands=1 ... heroInitMinigameCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=21 replayBytes=964`, `Final map 3D preview: ... dynamic=yes/70/10/60/0/60 status=70/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=4/4 selectedHeroMarker=yes/2`, and `Final live HUD state: samples=109 hero=yes object=7395 kind=hero faction=2 player=2 user=1984 level=1 gold=0 pos=385.1,186.3 hp=810/810/100% energy=725/725/100% damage=49.5-60.5 aps=0.68 move=6.76 regen=1.22/2.61 ... target=yes object=7867 kind=creep faction=1 ... hp=300/300/100% energy=0/0/0% damage=9.0-11.0 aps=0.71 move=5.66 regen=0.00/0.00 ... moving=yes`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-07.56.02/linux-client-shell.log` and reports `commands=20`, all the same one-command proof classes except `heroPickupObjectCommands=0` because no pickup candidate existed on that map/run, `Final map 3D preview: ... dynamic=yes/70/10/60/0/60 status=70/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=4/4 selectedHeroMarker=yes/2`, and the same live hero/selected-creep derivative HUD values (`damage=49.5-60.5 aps=0.68 move=6.76 regen=1.22/2.61`, target `damage=9.0-11.0 aps=0.71 move=5.66`). A short 10-second non-create-game MOBA smoke wrote `Bin/logs/2026.06.01-07.56.45/linux-client-shell.log` and preserved the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`; since no world is loaded on that path, `Final live HUD state: samples=0 hero=no ... target=no` is expected.
- Latest large chunk: the Linux null-render `PFBaseUnit` stat path now loads DB-backed unit stats instead of leaving every live unit at placeholder `1/1` life/energy. `PFBaseUnit.cpp` now keeps Linux-only per-unit stat upgrade metadata, evaluates `NDb::StatsContainer` formulas against the same global `UnitConstantsContainer` style used by the Windows `PFStatContainer`, applies `StatsEnhancersContainer` modifiers when supplied, applies the same lower-limit clamp convention for normal stats, loads default unit stats plus unit-specific stats during `PFBaseUnit::Initialize(...)`, initializes life/energy from `STAT_LIFE`/`STAT_ENERGY`, and applies stored stat increments during Linux `DoLevelups(...)` while preserving current health/energy percentages. This is confined to the `PW_LINUX_NULL_RENDER` branch; Windows/full-render `PFStatContainer` behavior remains on its existing implementation.
- Verification after the DB-backed Linux unit-stat chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The smokes had to run outside the sandbox so the native X11/OpenGL window could open. A 30-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-07.10.41/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroUseUnitCommands=1 ... heroActivateTalentCommands=1 ... heroUseTalentCommands=1 ... heroUsePortalCommands=1 ... heroUseConsumableCommands=1 ... heroBuyConsumableCommands=1 ... heroRaiseFlagCommands=1 ... heroInitMinigameCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=21 replayBytes=964`, `Final map 3D preview: ... dynamic=yes/136/10/60/66/60 status=136/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=14/14 selectedHeroMarker=yes/2`, and `Final live HUD state: samples=110 hero=yes object=7395 kind=hero faction=2 player=2 user=1984 level=1 gold=0 pos=385.1,186.3 hp=810/810/100% energy=725/725/100% ... target=yes object=7867 kind=creep faction=1 ... hp=300/300/100% energy=0/0/0% ... source=selected-target dist=267.3 ... moving=yes`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-07.11.23/linux-client-shell.log` and reports `commands=20`, all the same one-command proof classes except `heroPickupObjectCommands=0` because no pickup candidate existed on that map/run, `Final map 3D preview: ... dynamic=yes/115/10/60/45/60 status=115/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=13/13 selectedHeroMarker=yes/2`, and `Final live HUD state: samples=112 hero=yes object=6611 kind=hero faction=2 player=2 user=1984 level=1 gold=0 pos=386.9,186.0 hp=810/810/100% energy=725/725/100% ... target=yes object=7063 kind=creep faction=1 ... hp=300/300/100% energy=0/0/0% ... source=selected-target dist=269.1 ... moving=yes`. A short 10-second non-create-game MOBA smoke wrote `Bin/logs/2026.06.01-07.12.06/linux-client-shell.log` and preserved the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`; since no world is loaded on that path, `Final live HUD state: samples=0 hero=no ... target=no` is expected.
- Latest large chunk: the Linux/OpenGL bootstrap loaded-world preview now has a compact live HUD sourced from the real loaded `PFWorld`, not from static catalog data. `PW_Client/Game.cpp` now samples the selected live hero and selected/fallback target from `PFWorld::GetLinuxDynamicWorldMarkers(...)` plus `FindLinuxUnitByObjectId(...)`, records object id, kind, faction, player/user id, map position, movement/death state, level, gold, life/max life, energy/max energy, DB unit id, scene-object DBID, and target source/distance, then draws native OpenGL HP/energy bars over the loading/map preview. The final stdout and log now include `Final live HUD state:` and machine-readable `finalLiveHero*` / `finalLiveTarget*` fields; the final `Final map 3D preview:` stdout line also includes dynamic status-bar and dynamic-unit-mesh readiness counts (`status=...` and `unitMeshAssets=...`). This is deliberately Linux bootstrap-only UI/diagnostic work and does not touch Windows/full-render client paths.
- Verification after the live HUD state chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The smokes had to run outside the sandbox so the native X11/OpenGL window could open. A 30-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-06.56.16/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroUseUnitCommands=1 ... heroActivateTalentCommands=1 ... heroUseTalentCommands=1 ... heroUsePortalCommands=1 ... heroUseConsumableCommands=1 ... heroBuyConsumableCommands=1 ... heroRaiseFlagCommands=1 ... heroInitMinigameCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=21 replayBytes=964`, `Final map 3D preview: ... dynamic=yes/70/10/60/0/60 status=70/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=4/4 selectedHeroMarker=yes/2`, and `Final live HUD state: samples=108 hero=yes object=7395 kind=hero faction=2 player=2 user=1984 level=1 gold=0 pos=385.1,186.3 hp=1/1/100% energy=1/1/100% ... target=yes object=7867 kind=creep faction=1 ... source=selected-target dist=267.3 ... moving=yes`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-06.57.02/linux-client-shell.log` and reports `commands=20`, all the same one-command proof classes except `heroPickupObjectCommands=0` because no pickup candidate existed on that map/run, `Final map 3D preview: ... dynamic=yes/115/10/60/45/60 status=115/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 unitMeshAssets=13/13 selectedHeroMarker=yes/2`, and `Final live HUD state: samples=110 hero=yes object=6611 kind=hero faction=2 player=2 user=1984 level=1 gold=0 pos=386.9,186.0 hp=1/1/100% energy=1/1/100% ... target=yes object=7063 kind=creep faction=1 ... source=selected-target dist=269.1 ... moving=yes`. A short 10-second non-create-game MOBA smoke wrote `Bin/logs/2026.06.01-06.57.46/linux-client-shell.log` and preserved the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`; since no world is loaded on that path, `Final live HUD state: samples=0 hero=no ... target=no` is expected.
- Latest large chunk: the Linux null-render gameplay side now accepts the full scripted hero interaction/talent/portal/consumable proof chain instead of only serializing those commands. `PFConsumable.cpp` creates `PFConsumableAbilityData` in the Linux constructor and `CanBeUsed()` now asks the real ability data; `PFAbilityData.cpp` keeps the Linux consumable owner hero; `PFBaseUnit.cpp` implements Linux external-ability lookup, interaction limitation checks, usability/faction rules, `Use(...)`, and use range via `PFAbilityData`; `PFMinigamePlace` now overrides the base-unit `CanBeUsedBy(...)` signature and delegates to the existing hero rule; `PFTalent.cpp` loads predefined/custom talent sets from DB/resources on Linux; and `PFMaleHero.cpp` creates/activates the portal talent, loads the selected/default talents during `InitHero(...)`, activates taken-on-start talents, reloads predefined sets, and applies usable talents to targets. These are all under `PW_LINUX_NULL_RENDER` / Linux bootstrap paths, so the Windows/full-render behavior remains on its existing branches.
- The same chunk added a final `Final map 3D preview:` stdout line from `PW_Client/Game.cpp`, using the post-frame render counters already written to `linux-client-shell.log`. This removes the earlier confusion where the startup `Bootstrap 3D preview:` line printed pre-frame `terrain=no` even though the final OpenGL frame had rendered terrain, water, static/animated meshes, world markers, hero meshes, and creep meshes.
- Verification after the Linux gameplay acceptance/final-3D-stdout chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The smokes had to run outside the sandbox so the native X11/OpenGL window could open. A 30-second MOBA create-game smoke wrote `Bin/logs/2026.06.01-05.46.55/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroUseUnitCommands=1 ... heroActivateTalentCommands=1 ... heroUseTalentCommands=1 ... heroUsePortalCommands=1 ... heroUseConsumableCommands=1 ... heroBuyConsumableCommands=1 ... heroRaiseFlagCommands=1 ... heroInitMinigameCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=21 replayBytes=964`, `Final map 3D preview: terrain=yes/9216/18050 water=yes/4/88 roads=yes/6/45 terrainElements=yes/192/192/33202 static=yes/256/261/53964 animated=yes/48/57/133867 dynamic=yes/136/10/60/66/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 selectedHeroMarker=yes/2`, and `Final hero gameplay command execution: useUnit=1/1/1/1 can=1 ... activate=1/1/1/1 can=1 slot=0,1 useTalent=1/1/1/1 can=1 ... portal=1/1/1/1 can=1 ... consumable=1/1/1/1 can=1 ... buy=1/1/1/1 can=1 ... raise=1/1/1/1 can=1 ... init=1/1/1/0 ... pickup=1/1/1/1 can=1`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-05.47.38/linux-client-shell.log` and reports `commands=20`, all the same one-command proof classes except `heroPickupObjectCommands=0` because no pickup candidate existed on that run, `Final map 3D preview: terrain=yes/9216/18050 water=yes/4/88 roads=yes/6/45 terrainElements=yes/192/192/33202 static=yes/256/261/53964 animated=yes/48/57/133867 dynamic=yes/70/10/60/0/60 heroMeshes=yes/10/62625 creepMeshes=yes/48/181650 selectedHeroMarker=yes/2`, and `Final hero gameplay command execution: useUnit=1/1/1/1 can=1 ... activate=1/1/1/1 can=1 slot=0,1 useTalent=1/1/1/1 can=1 ... portal=1/1/1/1 can=1 ... consumable=1/1/1/1 can=1 ... buy=1/1/1/1 can=1 ... raise=1/1/1/1 can=1`. A short 10-second non-create-game MOBA smoke wrote `Bin/logs/2026.06.01-05.48.21/linux-client-shell.log` and preserved the starting UI path: `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`.
- Latest large chunk: the Linux/OpenGL bootstrap gameplay-command chain now chooses more real command candidates and no longer stalls when an optional proof command has no valid gameplay target. `PFWorld` exposes a Linux-only `FindLinuxFirstUsableUnitForHero(...)` helper, `PW_Client/Game.cpp` falls back from the selected target to a real `CanBeUsedBy(...)` unit for `CmdUseUnit`, chooses talent slots/targets through `PFBaseMaleHero::CanActivateTalent(...)` / `CanUseTalent(...)` instead of hardcoding `0,0`, buys a shop consumable before trying to use a consumable slot, and resolves the first currently usable consumable slot when one exists. The scripted scheduler now gates on the latest successfully packed command in the optional chain, so missing/unusable `use-talent`, `portal`, `consumable`, `raise`, `init`, or `pickup` proofs do not prevent later commands and the final map-click movement proof from running. These changes are confined to the Linux bootstrap/null-render path; Windows/full-render command behavior is untouched.
- Verification after the resilient gameplay-command candidate chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The smokes had to run outside the sandbox so the native X11/OpenGL window could open. A 30-second MOBA create-game smoke from `r1117/Bin` wrote `Bin/logs/2026.06.01-05.24.01/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=20 ... heroUseUnitCommands=1 ... heroActivateTalentCommands=1 ... heroUseTalentCommands=0 ... heroUsePortalCommands=1 ... heroBuyConsumableCommands=1 ... heroUseConsumableCommands=1 ... heroRaiseFlagCommands=1 ... heroInitMinigameCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=20 replayBytes=883`, `Final map command move: targetDrawn=yes commands=1 ... moved=yes reached=yes ... input=bootstrap-map-click-proof`, and `Final hero gameplay command execution: useUnit=1/1/1/0 can=0 target=7865/3/1/-1 activate=1/1/1/0 can=0 slot=0,0 useTalent=0/0/0/0 can=0 target=0/0/0 portal=1/1/1/0 can=0 target=410.0,410.0 consumable=1/1/1/0 can=0 slot=0 target=0/7865/1 buy=1/1/1/1 can=1 took=1 item=7509/0/-1 raise=1/1/1/1 can=1 flag=7653/0 init=1/1/1/0 available=1 canUse=1 ready=0 object=7721 pickup=1/1/1/1 can=1 object=7302`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.06.01-05.27.11/linux-client-shell.log` and reports `commands=19`, `heroUseTalentCommands=0`, `heroUsePortalCommands=1`, `heroBuyConsumableCommands=1`, `heroUseConsumableCommands=1`, `heroRaiseFlagCommands=1`, `heroInitMinigameCommands=1`, `heroPickupObjectCommands=0`, `replayCommands=19`, `replayBytes=845`, the same moved/reached map-click proof, and `Final hero gameplay command execution: useUnit=1/1/1/0 can=0 target=7061/3/1/-1 activate=1/1/1/0 can=0 slot=0,0 useTalent=0/0/0/0 can=0 target=0/0/0 portal=1/1/1/0 can=0 target=410.0,410.0 consumable=1/1/1/0 can=0 slot=0 target=0/7061/1 buy=1/1/1/1 can=1 took=1 item=6705/0/-1 raise=1/1/1/1 can=1 flag=6849/0 init=1/1/1/0 available=1 canUse=1 ready=0 object=6917 pickup=0/0/0/0 can=0 object=0`. The remaining `can=0` entries are real gameplay-rule rejections, not scheduler loss; do not bypass them without porting the corresponding gameplay prerequisites.
- Latest large chunk: the Linux/OpenGL bootstrap command proof now goes beyond transceiver serialization for the later gameplay commands. `HeroActions.h` exposes `LinuxHeroGameplayCommandDiagnostics`, the Linux null-render `HeroActions.cpp` command handlers record `CanExecute`/`Execute`/action decisions for `CmdUseUnit`, `CmdActivateTalent`, `CmdUseTalent`, `CmdUsePortal`, `CmdUseConsumable`, `CmdBuyConsumable`, `CmdRaiseFlag`, `CmdInitMinigame`, and `CmdPickupObject`, and the Linux bootstrap command implementation mirrors those counters for the executable actually used by the smokes. `PW_Client/Game.cpp` now writes a final `Final hero gameplay command execution:` stdout line plus `finalHeroGameplay*` log fields. This proves whether each native command class entered its handler and whether the real gameplay rules accepted the action, without weakening Windows/full-render command paths.
- Verification after the gameplay-command execution diagnostics chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. A 30-second MOBA create-game smoke from `r1117/Bin` wrote `Bin/logs/2026.05.31-21.28.16/linux-client-shell.log` and stdout reports `Final game transceiver runtime: ... commands=21 ... heroUseUnitCommands=1 ... heroActivateTalentCommands=1 ... heroUseTalentCommands=1 ... heroUsePortalCommands=1 ... heroUseConsumableCommands=1 ... heroBuyConsumableCommands=1 ... heroRaiseFlagCommands=1 ... heroInitMinigameCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=21 replayBytes=964`, `Final map command action: targetDrawn=yes commands=16 kind=pickup input=bootstrap-command-chain`, `Final map selected target: ... useUnit=yes useTalent=yes useConsumable=yes`, and `Final hero gameplay command execution: useUnit=1/1/1/0 can=0 target=7865/3/1/-1 activate=1/1/1/0 can=0 slot=0,0 useTalent=1/1/1/0 can=0 target=0/7865/1 portal=1/1/1/0 can=0 target=410.0,410.0 consumable=1/1/1/0 can=0 slot=0 target=0/7865/1 buy=1/1/1/1 can=1 took=1 item=7509/0/-1 raise=1/1/1/1 can=1 flag=7653/0 init=1/1/1/0 available=1 canUse=1 ready=0 object=7721 pickup=1/1/1/1 can=1 object=7302`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.05.31-21.28.54/linux-client-shell.log` and reports `commands=20`, the same one-command proof for use-unit/activate/use-talent/portal/use-consumable/buy/raise/init, `heroPickupObjectCommands=0`, `replayCommands=20`, `replayBytes=926`, `Final map command action: targetDrawn=yes commands=15 kind=init-minigame input=bootstrap-command-chain`, and `Final hero gameplay command execution: useUnit=1/1/1/0 can=0 target=7061/3/1/-1 activate=1/1/1/0 can=0 slot=0,0 useTalent=1/1/1/0 can=0 target=0/7061/1 portal=1/1/1/0 can=0 target=410.0,410.0 consumable=1/1/1/0 can=0 slot=0 target=0/7061/1 buy=1/1/1/1 can=1 took=1 item=6705/0/-1 raise=1/1/1/1 can=1 flag=6849/0 init=1/1/1/0 available=1 canUse=1 ready=0 object=6917 pickup=0/0/0/0 can=0 object=0`. Both logs also contain the machine-readable `finalHeroGameplayUseUnit`, `finalHeroGameplayTalent`, `finalHeroGameplayPortal`, `finalHeroGameplayConsumable`, and `finalHeroGameplayWorldActions` fields.
- Latest large chunk: the Linux/OpenGL bootstrap loaded-world map preview now carries the selected live target through the later gameplay command classes, not just attack/follow. Shared Linux-only helpers now resolve the selected `PFBaseUnit` by object id, fall back to the nearest enemy hero when needed, and send/record `CmdUseUnit`, `CmdUseTalent`, and `CmdUseConsumable` against that live target. The scripted chain also records visible map action markers for activate talent, use talent, portal, consumable, buy consumable, raise flag, init minigame, and pickup, so the action marker now advances through the whole command sequence instead of stopping at minimap signal. Numeric hotkeys in the loaded-world preview now expose the same paths manually: `1` activate talent, `2` use talent on selected/fallback target, `3` use consumable on selected/fallback target, and `4` use selected/fallback unit. New final diagnostics record `finalMap3DPreviewSelectedTargetUseUnitCommandSent`, `finalMap3DPreviewSelectedTargetUseTalentCommandSent`, and `finalMap3DPreviewSelectedTargetUseConsumableCommandSent`; the stdout selected-target summary now includes `useUnit=yes useTalent=yes useConsumable=yes`. Windows/full-render paths remain untouched.
- Verification after the selected-target use-command/action-marker chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. A 30-second MOBA create-game smoke from `r1117/Bin` wrote `Bin/logs/2026.05.31-21.07.03/linux-client-shell.log` and stdout reports `Final game transceiver runtime: ... commands=21 ... heroUseUnitCommands=1 ... heroUseTalentCommands=1 ... heroUseConsumableCommands=1 ... heroPickupObjectCommands=1 ... replayCommands=21 replayBytes=964`, `Final map command action: targetDrawn=yes commands=16 kind=pickup input=bootstrap-command-chain`, and `Final map selected target: ... attack=yes follow=yes useUnit=yes useTalent=yes useConsumable=yes input=bootstrap-target-select-proof`. A 30-second NightMap create-game smoke from `r1117/Bin` wrote `Bin/logs/2026.05.31-21.07.41/linux-client-shell.log` and stdout reports `commands=20`, `heroUseUnitCommands=1`, `heroUseTalentCommands=1`, `heroUseConsumableCommands=1`, `heroPickupObjectCommands=0`, `replayCommands=20`, `replayBytes=926`, `Final map command action: targetDrawn=yes commands=15 kind=init-minigame input=bootstrap-command-chain`, and the same selected-target `attack=yes follow=yes useUnit=yes useTalent=yes useConsumable=yes` proof. Both logs contain `finalMap3DPreviewSelectedTargetUseUnitCommandSent=yes`, `finalMap3DPreviewSelectedTargetUseTalentCommandSent=yes`, and `finalMap3DPreviewSelectedTargetUseConsumableCommandSent=yes`.
- Latest large chunk: the Linux/OpenGL bootstrap loaded-world map preview now uses the selected live target for scripted and manual attack/follow command proofs. The noninteractive proof selects a real non-friendly live `PFBaseUnit` after the stop command, then sends `CmdAttackTarget` and `CmdFollowUnit` through the same object-id selected-target path used by `A`/`F` input, so attacks/follows are no longer limited to the nearest enemy hero fallback. New runtime/final diagnostics record `map3DPreviewSelectedTargetAttackCommandSent`, `map3DPreviewSelectedTargetFollowCommandSent`, `finalMap3DPreviewSelectedTargetAttackCommandSent`, and `finalMap3DPreviewSelectedTargetFollowCommandSent`, and the stdout final selected-target summary now includes `attack=yes follow=yes`. Windows/full-render paths remain untouched.
- Verification after the selected-target command proof chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. A 30-second MOBA create-game smoke from `r1117/Bin` wrote `Bin/logs/2026.05.31-20.56.35/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroAttackCommands=1 ... heroFollowCommands=1 ... replayCommands=21 replayBytes=964`, `Final map command action: targetDrawn=yes commands=7 kind=signal input=bootstrap-command-chain`, and `Final map selected target: drawn=yes changes=1 object=7865 kind=2 faction=1 player=-1 client=-1 ... attack=yes follow=yes input=bootstrap-target-select-proof`. A 30-second NightMap create-game smoke from `r1117/Bin` wrote `Bin/logs/2026.05.31-20.58.04/linux-client-shell.log` and reports `commands=20`, `heroAttackCommands=1`, `heroFollowCommands=1`, `replayCommands=20`, `replayBytes=926`, plus `Final map selected target: drawn=yes changes=1 object=7061 kind=2 faction=1 player=-1 client=-1 ... attack=yes follow=yes input=bootstrap-target-select-proof`.
- Latest large chunk: the Linux/OpenGL bootstrap loaded-world map preview now has a real live-unit selection/targeting layer. The Linux-only `LinuxDynamicWorldMarker` export carries a stable `objectId`, `PFWorld` exposes `FindLinuxUnitByObjectId(...)`, and the bootstrap client records/draws a selected target ring/corner marker over the loaded terrain. Left-click selects live heroes/creeps under the map cursor without breaking drag-rotation, right-click can attack any selected/picked enemy `PFBaseUnit` instead of only enemy heroes, middle-click minimap signals can attach to a picked live marker, and `A`/`F` prefer the selected enemy/unit before falling back to nearest enemy hero. The scripted smoke proof now selects the nearest non-friendly live marker after the minimap-signal command so noninteractive runs verify the visible selected-target marker. The Linux bootstrap root detector also now walks parent directories, so launching the built client from `r1117/Bin` finds `Data`/`Src` without an explicit `--root`. Windows/full-render code paths remain untouched.
- Verification after the live target-selection chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. A 30-second MOBA create-game smoke from `r1117/Bin` wrote `Bin/logs/2026.05.31-20.47.46/linux-client-shell.log` and reports `Game root: /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117`, `Final game transceiver runtime: ... commands=21 ... heroMoveCommands=2 ... replayCommands=21 replayBytes=964`, `Final map command move: ... moved=yes reached=yes ... input=bootstrap-map-click-proof`, `Final map command action: targetDrawn=yes commands=7 kind=signal input=bootstrap-command-chain`, and `Final map selected target: drawn=yes changes=1 object=7865 kind=2 faction=1 ... distance=262.02 input=bootstrap-target-select-proof`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.05.31-20.48.26/linux-client-shell.log` and reports the same selected-target proof with `object=7061 kind=2 faction=1`, `commands=20`, `heroMoveCommands=2`, `replayCommands=20`, `replayBytes=926`, while preserving the moved/reached map-click proof and the command-action signal marker.
- Latest large chunk: the Linux/OpenGL bootstrap loaded-world map preview now has a broader native playable-control command surface instead of only right-click move. Right-click on the terrain still sends a real `CmdMoveHero`, but if the projected click lands within the live enemy hero hit radius it sends `CmdAttackTarget` instead. Middle-click sends a real `CmdMinimapSignal`, and the loaded map preview now handles `S` stop, `A` attack nearest enemy hero, `F` follow nearest enemy hero, `H` hold, and `C` cancel channelling through the same transceiver command path. The preview records a separate last-action source/target marker for non-move commands, renders that marker over the terrain, and logs `map3DPreviewCommandAction*` / `finalMap3DPreviewCommandAction*` diagnostics. The scripted bootstrap chain also populates that same action slot so automated smokes verify the marker/log path without manual input. Windows/full-render paths remain untouched.
- Verification after the live map-control chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. A 30-second MOBA create-game smoke wrote `Bin/logs/2026.05.31-18.09.35/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroMoveCommands=2 ... replayCommands=21 replayBytes=964`, `Final map command move: ... moved=yes reached=yes ... distance=31.62 maxDistance=31.62 remaining=0.00 targetDistance=31.62 input=bootstrap-map-click-proof`, `Final map command action: targetDrawn=yes commands=7 kind=signal input=bootstrap-command-chain ... last=bootstrap-command-chain`, and `Final hero move command execution: ... accepted=2 executed=2 ... resolved=1 clientMatch=1`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.05.31-18.10.15/linux-client-shell.log` and reports the same command/action proof shape with `commands=20`, `heroMoveCommands=2`, `replayCommands=20`, `replayBytes=926`, `Final map command action: targetDrawn=yes commands=7 kind=signal input=bootstrap-command-chain`, and the same moved/reached map-command proof while preserving NightMap point-light coverage.
- Previous large chunk: the Linux/OpenGL bootstrap loaded-world map preview now has a working native hero move command path, not just a drawn target marker. Linux bootstrap world commands re-resolve live `PFWorld` heroes/targets/shops/objects when serialized `CPtr` values arrive invalid after replay/transceiver deserialization, so `CmdMoveHero` and the surrounding scripted command chain execute against the real loaded hero while preserving the Windows/full-render command code. The Linux null-render hero `Move(...)` now arms the bootstrap movement primitive directly because `PFFsm::EnqueueState(...)` is stubbed in `PW_LINUX_NULL_RENDER`, and the Linux straight-line movement probe now checks the final partial sub-step instead of stopping at the last 0.5m sample. That fixes the prior one-tick `distance=0.50` stall: the automated map-click proof now moves the live hero all the way to the target. Windows/full-render code paths remain untouched.
- Verification after the live map-command movement chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 30-second MOBA create-game smoke wrote `Bin/logs/2026.05.31-17.54.29/linux-client-shell.log` and reports `Final game transceiver runtime: ... commands=21 ... heroMoveCommands=2 ... replayCommands=21 replayBytes=964`, `Final hero move command execution: canChecks=2 accepted=2 executed=2 ... resolved=1 clientMatch=1 moving=1->1 flags=0x3->0x3 speed=6.00->6.00`, and `Final map command move: ... moved=yes reached=yes ... distance=31.62 maxDistance=31.62 remaining=0.00 targetDistance=31.62 input=bootstrap-map-click-proof`. A 30-second NightMap create-game smoke wrote `Bin/logs/2026.05.31-17.55.09/linux-client-shell.log` and reports the same proof shape with `commands=20`, `heroMoveCommands=2`, `replayCommands=20`, `replayBytes=926`, `resolved=1 clientMatch=1`, and `Final map command move: ... moved=yes reached=yes ... distance=31.62 maxDistance=31.62 remaining=0.00 targetDistance=31.62 input=bootstrap-map-click-proof`, while preserving `finalMap3DPreviewPointLights=75` for NightMap.
- Previous large chunk: the Linux/OpenGL bootstrap loaded-world map preview sends a real hero move command from the map surface instead of being visual-only. Right-click on the loaded PFWorld preview projects the current OpenGL camera ray back onto the sampled map plane, routes the target through the existing Linux transceiver `CmdMoveHero` helper, and keeps right-drag panning behind a small drag threshold. The preview renders a persistent source-to-target line and pulsing destination marker for the last map command. A deterministic noninteractive proof move fires after the scripted bootstrap command chain so automated smokes verify the same command path without requiring human input.
- Latest large chunk: the Linux/OpenGL bootstrap loaded-world map preview now uses live `PFBaseUnit`/`PFBaseMovingUnit` status data instead of only kind/faction/position. The Linux-only `LinuxDynamicWorldMarker` export now carries health percent, energy percent, object size, and real movement direction. `PW_Client/Game.cpp` draws health bars for all live loaded-world units, mana/energy bars for live heroes, movement-direction arrows for moving units, and orients the loaded-world hero/creep mesh replicas from real movement direction when available, while keeping the existing rotating fallback for stationary/undirected markers. Windows/full-render code paths remain untouched.
- Verification after the live unit status overlay chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. Two 25-second no-diagnostics create-game smokes wrote `Bin/logs/2026.05.31-16.27.47/linux-client-shell.log` because they started in the same second; the first final preview block is the MOBA smoke and reports `finalMap3DPreviewDynamicWorldMarkers=40`, `finalMap3DPreviewDynamicHeroMarkers=10`, `finalMap3DPreviewDynamicCommonCreepMarkers=30`, `finalMap3DPreviewDynamicMovingMarkers=30`, `finalMap3DPreviewDynamicHealthBars=40`, `finalMap3DPreviewDynamicMoveDirectionArrows=30`, `finalMap3DPreviewDynamicHeroMeshes=10`, `finalMap3DPreviewDynamicHeroMeshTriangles=62625`, `finalMap3DPreviewDynamicUnitMeshPreviewAssets=4`, `finalMap3DPreviewDynamicUnitMeshPreviewReady=4`, `finalMap3DPreviewDynamicCreepMeshes=30`, `finalMap3DPreviewDynamicCreepMeshTriangles=118614`, `finalMap3DPreviewHeroDiffuseTexturesCached=16`, and `finalMap3DPreviewHeroDiffuseTextureFailures=0`. The second final preview block is the NightMap smoke and reports the same live marker/status/mesh counts plus `finalMap3DPreviewPointLightsDrawn=yes`, `finalMap3DPreviewPointLights=75`, and `finalMap3DPreviewPointLightMarkers=75`. The smoke stdout reported `Final Linux creep runtime` for both runs with `registeredCreeps=30`, `movingCommonCreeps=30`, `movedCommonCreeps=28`, `commonCreepDistance=771.26`, `creepWaves=6`, and `neutralWaves=0`.
- Latest large chunk: the Linux/OpenGL bootstrap loaded-world map preview now replaces common/neutral creep fallback columns with real DB-backed creep scene meshes when those `PFWorld` markers expose a `Unit`/`DBSceneObject`. The Linux-only `LinuxDynamicWorldMarker` export now carries `unitDbid`, `sceneObjectDbid`, and creep type metadata from `PFBaseUnit::DbUnitDesc()`, while `PW_Client/Game.cpp` probes/caches those scene objects through the existing mesh-preview path, draws up to 48 terrain-followed live creep mesh replicas, keeps colored base markers as fallback/context, and logs `mapPreviewDynamicUnitMeshPreview*` plus `mapPreviewDynamicCreepMesh*` diagnostics. Windows/full-render code paths remain untouched.
- Verification after the loaded creep mesh chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. A 25-second no-diagnostics MOBA create-game smoke wrote `Bin/logs/2026.05.31-16.06.45/linux-client-shell.log`, reached final transceiver `step=287`, reported `registeredCreeps=30`, `movingCommonCreeps=30`, `movedCommonCreeps=28`, `finalMap3DPreviewDynamicWorldMarkers=40`, `finalMap3DPreviewDynamicHeroMarkers=10`, `finalMap3DPreviewDynamicCommonCreepMarkers=30`, `finalMap3DPreviewDynamicMovingMarkers=30`, `finalMap3DPreviewDynamicHeroMeshesDrawn=yes`, `finalMap3DPreviewDynamicHeroMeshes=10`, `finalMap3DPreviewDynamicHeroMeshTriangles=62625`, `finalMap3DPreviewDynamicUnitMeshPreviewAssets=4`, `finalMap3DPreviewDynamicUnitMeshPreviewReady=4`, `finalMap3DPreviewDynamicCreepMeshesDrawn=yes`, `finalMap3DPreviewDynamicCreepMeshes=30`, `finalMap3DPreviewDynamicCreepMeshTriangles=118614`, `finalMap3DPreviewHeroDiffuseTexturesCached=16`, and `finalMap3DPreviewHeroDiffuseTextureFailures=0`. A 25-second no-diagnostics NightMap create-game smoke wrote `Bin/logs/2026.05.31-16.07.30/linux-client-shell.log`, reached final transceiver `step=284`, preserved the NightMap lighting proof with `finalMap3DPreviewPointLightsDrawn=yes` and `finalMap3DPreviewPointLights=75`, and reported the same 4/4 ready dynamic unit mesh previews, 30 drawn creep mesh replicas, 118,614 summed creep triangles, 10 drawn hero meshes, 62,625 summed hero triangles, 16 cached hero diffuse textures, and zero texture failures.
- Previous large chunk: the Linux/OpenGL bootstrap hero diffuse texture cache is now keyed by resolved source file instead of per-mesh local material index. This prevents ten different lineup hero meshes from evicting/reuploading each other's `textureIndex=0/1/...` diffuse maps while the map preview walks live hero markers. The cache is cleaned up with the other OpenGL textures, the draw path now retries transient OpenGL upload misses instead of permanently blacklisting already-decoded hero textures, and the map preview logs `mapPreviewHeroDiffuseTexturesCached` / `mapPreviewHeroDiffuseTextureFailures` diagnostics. Windows/full-render code paths remain untouched.
- Verification after the texture-cache chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. A 25-second no-diagnostics MOBA create-game smoke wrote `Bin/logs/2026.05.31-15.48.43/linux-client-shell.log`, reached final transceiver `step=299`, and reported `finalMap3DPreviewDynamicWorldMarkers=40`, `finalMap3DPreviewDynamicHeroMarkers=10`, `finalMap3DPreviewDynamicCommonCreepMarkers=30`, `finalMap3DPreviewDynamicMovingMarkers=30`, `finalMap3DPreviewLineupHeroMeshPreviewSlots=10`, `finalMap3DPreviewLineupHeroMeshPreviewReady=10`, `finalMap3DPreviewDynamicHeroMeshesDrawn=yes`, `finalMap3DPreviewDynamicHeroMeshes=10`, `finalMap3DPreviewDynamicHeroMeshTriangles=62625`, `finalMap3DPreviewHeroDiffuseTexturesCached=16`, and `finalMap3DPreviewHeroDiffuseTextureFailures=0`. A 25-second no-diagnostics NightMap create-game smoke wrote `Bin/logs/2026.05.31-15.49.16/linux-client-shell.log`, preserved the NightMap lighting proof with `finalMap3DPreviewPointLightsDrawn=yes` and `finalMap3DPreviewPointLights=75`, and reported the same 10/10 ready lineup mesh previews, 10 drawn hero meshes, 62,625 summed mesh triangles, 16 cached hero diffuse textures, and zero texture failures.
- Previous large chunk: the Linux/OpenGL bootstrap map preview now resolves live hero markers to local lineup slots and draws real per-lineup hero meshes instead of repeating the selected hero mesh. The Linux-only `PFWorld::GetLinuxDynamicWorldMarkers(...)` marker export carries `playerId` and `userId` for real `PFBaseHero` objects, `PW_Client/Game.cpp` maps those markers back through the engine start-slot preview, and a mesh-only lineup preview cache probes the real DB scene asset/skin payload for each lineup hero. The rotating loaded-map preview draws up to 10 real roster hero meshes, keeps colored rings/bases, falls back to the existing columns when a roster mesh is unavailable, and records `mapPreviewLineupHeroMeshPreview*`, `mapPreviewDynamicHeroMeshes*`, and selected-marker identity diagnostics. Windows/full-render code paths remain untouched.
- Verification after the per-lineup hero mesh chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. A 25-second no-diagnostics MOBA create-game smoke wrote `Bin/logs/2026.05.31-15.37.48/linux-client-shell.log`, reached final transceiver `step=299`, reported `finalMap3DPreviewDynamicWorldMarkers=40`, `finalMap3DPreviewDynamicHeroMarkers=10`, `finalMap3DPreviewDynamicCommonCreepMarkers=30`, `finalMap3DPreviewDynamicMovingMarkers=30`, and proved roster mesh placement with `finalMap3DPreviewLineupHeroMeshPreviewSlots=10`, `finalMap3DPreviewLineupHeroMeshPreviewReady=10`, `finalMap3DPreviewDynamicHeroMeshesDrawn=yes`, `finalMap3DPreviewDynamicHeroMeshes=10`, and summed `finalMap3DPreviewDynamicHeroMeshTriangles=62625`. The same log keeps the selected-hero identity proof: `finalMap3DPreviewDynamicSelectedHeroMarkerMatched=yes`, `finalMap3DPreviewDynamicSelectedHeroMarkerPlayerId=2`, with `localMatchSelectedSlot=5` matched to `engineStartSlot[2].lineupIndex=5`. A 25-second no-diagnostics NightMap create-game smoke wrote `Bin/logs/2026.05.31-15.38.33/linux-client-shell.log`, preserved the NightMap lighting proof with `finalMap3DPreviewPointLights=75`, and reported the same 10/10 ready lineup mesh previews, 10 drawn hero meshes, 62,625 summed mesh triangles, and selected-player identity match.
- Previous large chunk: the Linux/OpenGL bootstrap map preview first drew real selected-hero mesh replicas at all live loaded hero positions, then briefly narrowed that to only the selected player's marker once player identity was exported. That proved real character geometry and fixed the misleading all-selected-hero repetition. The latest per-lineup chunk above supersedes it by drawing distinct roster hero meshes where mesh previews are ready.
- Previous large chunk: the Linux/OpenGL bootstrap map preview now draws live loaded-world unit markers from the running `PFWorld` after create-game startup. A Linux-only `PFWorld::GetLinuxDynamicWorldMarkers(...)` helper exports hero, lane common-creep, and neutral-creep positions/factions/movement state from the real transceiver world, and `PW_Client/Game.cpp` renders those markers over the sampled tactical map terrain using terrain height, faction/kind coloring, rings, and small 3D columns. This marker layer remains as the fallback for non-hero units and for hero mesh-preview failures, while the selected-hero mesh replica layer now handles live hero markers. Windows/full-render code paths remain untouched.
- The same chunk adds final preview diagnostics for loaded-world markers: total dynamic markers, hero markers, common-creep markers, neutral-creep markers, and moving markers. Early pre-world preview logs still correctly show `map3DPreviewDynamicWorldMarkersDrawn=no`; final create-game logs now show `finalMap3DPreviewDynamicWorldMarkersDrawn=yes` once `PFWorld` is alive and creeps have spawned.
- Verification after the live loaded-world marker chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The 65-second MOBA create-game smoke wrote `Bin/logs/2026.05.31-06.56.17/linux-client-shell.log`, reached `step=699`, reported `registeredCreeps=90`, `spawnedNeutralCreeps=70`, `movingCommonCreeps=90`, `movedCommonCreeps=81`, `commonCreepDistance=1266.98`, `creepWaves=18`, `neutralWaves=24`, and logged final dynamic preview markers `total=170`, `heroes=10`, `common=90`, `neutral=70`, `moving=90`. The 65-second NightMap create-game smoke wrote `Bin/logs/2026.05.31-06.57.40/linux-client-shell.log`, reached `step=699`, reported `registeredCreeps=90`, `spawnedNeutralCreeps=45`, `movingCommonCreeps=90`, `movedCommonCreeps=81`, `commonCreepDistance=1266.98`, `creepWaves=18`, `neutralWaves=14`, and logged final dynamic preview markers `total=145`, `heroes=10`, `common=90`, `neutral=45`, `moving=90`.
- Latest large chunk: the Linux null-render create-game runtime now advances real loaded-map lane creep behavior instead of only allocating creep objects. `PFBaseUnit::Step(...)` now executes enabled behavior objects on Linux before ability updates, `PFBaseUnit::FindTarget(...)` scans the same loaded bootstrap world for valid enemy units, and `PFCommonCreepBehaviour` now consumes DB waypoints through `PFBaseMovingUnit::MoveTo(...)` with a minimal target chase/attack handoff when an enemy enters range. `PFCreepSpawner::StepFarestCreepPosition(...)` now reads real creep-ring positions on Linux instead of always resetting to the spawn point. These are all guarded under the Linux null-render/bootstrap path; the Windows/full-render branches remain under their existing implementations.
- `PFWorld` and `PW_Client/Game.cpp` now expose/log loaded common-creep movement diagnostics: moving common creeps, creeps that have moved away from their initial placement, and total common-creep distance from spawn. This gives the long create-game smokes a direct proof that lane creeps are stepping through the gameplay/movement path, not just existing in the object registry.
- Verification after the loaded common-creep movement chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The 65-second MOBA create-game smoke wrote `Bin/logs/2026.05.31-06.41.00/linux-client-shell.log`, reached `step=699`, and reported `registeredCreeps=90`, `spawnedNeutralCreeps=70`, `movingCommonCreeps=90`, `movedCommonCreeps=81`, `commonCreepDistance=1266.98`, `creepWaves=18`, and `neutralWaves=24`. The 65-second NightMap create-game smoke wrote `Bin/logs/2026.05.31-06.42.13/linux-client-shell.log`, reached `step=699`, and reported `registeredCreeps=90`, `spawnedNeutralCreeps=45`, `movingCommonCreeps=90`, `movedCommonCreeps=81`, `commonCreepDistance=1266.98`, `creepWaves=18`, and `neutralWaves=14`.
- Latest large chunk: the Linux null-render create-game runtime now steps real loaded-map `PFBaseSpawner` objects during transceiver/PFWorld simulation. `PFWorld::Step(...)` collects the loaded spawner objects, records loaded/stepped/ready/enabled/content diagnostics, steps them after `PFAIWorld::Update(dt)`, and flushes the object registry before moving-unit/AI-container work. Linux `PFCreepSpawner` and `PFNeutralCreepSpawner` now allocate DB-driven lane and neutral creep objects in loaded MOBA/NightMap worlds, initialize/register them through the existing world/container surfaces, and keep the Windows/full-render paths untouched under their existing branches.
- The same chunk fixes the finite `--seconds` scheduler cap that previously stopped deeper smokes around transceiver step 159. Finite create-game runs now drive enough scheduler/transceiver ticks for long smoke windows, while interactive runs keep the uncapped path. For Linux null-render spawning only, the bootstrap intentionally ignores repo-local developer disables from `Profiles/private.cfg` (`spawn_creeps 0` and `spawn_neutral_creeps 0`) so checked-in private settings cannot mask loaded-map creep creation; the production Windows/full-render config behavior is unchanged.
- Verification after the loaded-spawner chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The 65-second MOBA create-game smoke wrote `Bin/logs/2026.05.31-06.24.38/linux-client-shell.log`, reached `step=699`, and reported `steppedSpawners=34`, `steppedCreepSpawners=6`, `steppedNeutralSpawners=28`, `enabledCreepSpawners=6`, `enabledNeutralSpawners=28`, `contentCreepSpawners=6`, `contentNeutralSpawners=28`, `aiCreeps=1`, `aiNeutral=1`, `maxCreeps=256`, `registeredCreeps=90`, `spawnedNeutralCreeps=70`, `creepWaves=18`, and `neutralWaves=24`. The 65-second NightMap create-game smoke wrote `Bin/logs/2026.05.31-06.25.55/linux-client-shell.log`, reached `step=699`, and reported `steppedSpawners=22`, `steppedCreepSpawners=6`, `steppedNeutralSpawners=16`, `enabledCreepSpawners=6`, `enabledNeutralSpawners=16`, `contentCreepSpawners=6`, `contentNeutralSpawners=16`, `aiCreeps=1`, `aiNeutral=1`, `maxCreeps=256`, `registeredCreeps=90`, `spawnedNeutralCreeps=45`, `creepWaves=18`, and `neutralWaves=14`.
- Latest large chunk: the runnable Linux client now links the broad maintained Scene/Terrain runtime slice that had previously lived only in `PrimeWorldLinuxRenderProbe`. `PrimeWorldLinuxClient` now builds with the real scene-object, camera, animation graph, collision, lighting, lightning/trace FX, sound-scene-component, terrain grid, water-scene-component, grass/nature-map/road/terrain cache/runtime/core files promoted from the probe source set, while generated scene/terrain DB files still come through `${PRIMEWORLD_LINUX_DB_SOURCES}`.
- The same chunk promotes the maintained utility/runtime dependencies required by that Scene/Terrain slice: `System/Transform.cpp`, `System/FastMath.cpp`, `System/Spline.cpp`, `System/BilinearSolver.cpp`, `System/RandomGen.cpp`, `System/Win32Random.cpp`, `System/BinChunkSerializer.cpp`, `System/BinChunkSerializerBase.cpp`, `System/BinChunkLoaderImpl.cpp`, `System/BinChunkSaverImpl.cpp`, `System/Progress.cpp`, and `System/Pointers/BasicFactory.cpp`. `System/RandomGen.cpp` now uses project fixed-width integer aliases and keeps the old `GetTickCount()` seed on Windows while using `time(0)` only on Linux; `System/StdAfx.h` is a case-safe Linux include alias for legacy `StdAfx.h` users.
- Linux bootstrap audio for this wider scene slice is intentionally soundless: `Sound/DummySound.cpp` now owns the no-op `NSoundScene` listener/event/position/length surface under `PW_LINUX_DB_BOOTSTRAP`, including `DBFMODEventDesc` overloads, so scene sound components link without pulling FMOD or Windows audio vendor paths into the native Linux executable.
- Executable-local glue was narrowed as part of this promotion: the duplicate `CastToObjectBaseImpl<NScene::SceneComponent>` shim was removed now that real `SceneComponent.cpp` is linked, `runtime_stubs.cpp` provides only the still-needed Linux bootstrap globals (`g_fixedTeamCam` and AOE selection scale/offset), and the older overlapping `NSoundScene::EventStart(...)` shim moved out of `runtime_stubs.cpp` into `DummySound.cpp`.
- Latest large chunk: the runnable Linux client now links the next broad maintained renderer batch from the render probe instead of leaving it probe-only. `PrimeWorldLinuxClient` now builds with real `Render/AOERenderer.cpp`, `Render/Blur.cpp`, `Render/Bloom.cpp`, `Render/CleanGeometry.cpp`, `Render/FullScreenFX.cpp`, `Render/fxresource.cpp`, `Render/GrassMaterial.cpp`, `Render/mesh.cpp`, `Render/ParticleFX.cpp`, `Render/ShadowManager.cpp`, `Render/ShadowMap.cpp`, `Render/SkeletalAnimationBlender.cpp`, `Render/SkeletalAnimationSampler.cpp`, `Render/StaticMesh.cpp`, `Render/InstancedMesh.cpp`, `Render/InstancedMeshResource.cpp`, `Render/SkeletalMesh.cpp`, `Render/SkeletonWrapper.cpp`, `Render/UITextureAtlasItem.cpp`, `Render/UITextureCache.cpp`, `Render/WaterConvexes.cpp`, `Render/WaterManager.cpp`, `Render/WaterMathTex.cpp`, `Render/WaterMesh.cpp`, `Render/VertexColorStream.cpp`, `Render/debugmesh.cpp`, `Render/debugstaticmesh.cpp`, `Render/facefxsystem.cpp`, `Render/renderstatesmanager.cpp`, `Render/sceneconstants.cpp`, `Render/shaderdefinestable.cpp`, and `Render/skeletonanimation.cpp`, plus the real math helpers `System/Decompose.cpp` and `System/Geom.cpp`. `Render/configdatabase.cpp` remains render-probe-only for now because it is still tied to Win32 version/device configuration APIs and the runnable Linux client already uses the Linux-safe `ConfigManager` path.
- The same chunk removes the executable-local `NCore::BinStatsCollector` placeholder surface from `runtime_stubs.cpp`. `PrimeWorldLinuxClient` now links the real `Core/BinStatsCollector.cpp` and `System/FileSystem/TinyFileWriteStream.cpp`, with Linux-safe CRT fixes in those files (`snprintf`, portable `fopen`, GCC-safe template recursion, `ftell` position checks, and guarded close behavior). Replay storage and CRC dump paths are still not actively exercised by the smoke, but the collector implementation is no longer faked in the runnable executable.
- Linux-only link glue added for this chunk is narrow: `runtime_stubs.cpp` now provides no-op `Compatibility::Init()` / Wine-detection results so promoted renderer files do not import the Windows compatibility implementation, `Render/InstancedMeshResource.h` exposes a Linux null-render inline `BatchQueue::GetIndex()`, and `Render/WaterMesh.cpp` declares its instance counter in the Linux null-render branch. These fixes are guarded to keep the Windows renderer path intact.
- Latest large chunk: the runnable Linux client now links the next maintained render/System foundation slice instead of executable-local placeholders. `PrimeWorldLinuxClient` now builds with real `Render/Material.cpp`, `Render/MaterialSpec.cpp`, `Render/multishader.cpp`, `Render/sampler.cpp`, `Render/aabb.cpp`, `Render/batch.cpp`, `Render/BoundingPrimitives.cpp`, `Render/ConvexVolume.cpp`, `Render/dipdescriptor.cpp`, `Render/DXManager.cpp`, `Render/FrustumCuller.cpp`, `Render/RenderComponent.cpp`, `Render/primitive.cpp`, `Render/vertexformatdescriptor.cpp`, plus the additional maintained null-render facade files `Render/DebugMaterial.cpp`, `Render/DxResourcesControl.cpp`, `Render/FlashRenderer.cpp`, `Render/LightsManager.cpp`, `Render/MaterialResourceInterface.cpp`, and `Render/Stretcher.cpp`. The old executable-local `Render::CreateRenderMaterial(...)` stubs were removed from `runtime_stubs.cpp`; material creation now enters the maintained material factory. Because the Linux null-render target does not yet register every UI/material DB type, `Render/Material.cpp` now preserves the old bootstrap behavior only under `PW_LINUX_NULL_RENDER` by returning `0` for an unknown factory type instead of dereferencing a null material. Windows remains on the original strict Direct3D/material path.
- The same chunk replaced several bootstrap-local System stubs with maintained implementations in the executable: `System/Asserts.cpp`, `System/ChannelLogger.cpp`, `System/Dumper.cpp`, `System/Logger.cpp`, `System/LogHelpers.cpp`, `System/LogStreamBuffer.cpp`, `System/SafeTextFormat.cpp`, `System/SystemLog.cpp`, and `System/ImageTGA.cpp`. The corresponding `CLogger`/`CChannelLogger`/`CDumper`, safe text formatting, `GetSystemLog()`, `TraceMsg(...)`, and `NImage::WriteImageTGA(...)` definitions were removed from `runtime_stubs.cpp`. A narrow `Aligned_MAlloc`/`Aligned_Free` compatibility pair remains in `runtime_stubs.cpp` for the promoted `multishader.cpp` path until the full `MemoryLib/newdelete.cpp` allocator surface is safe to adopt.
- Latest large chunk: the runnable Linux client now links the active PF_Core effect implementation slice that had been maintained in probes only: `PF_Core/BasicEffect.cpp`, `PF_Core/CameraShakeSceneComponent.cpp`, `PF_Core/ControlledStatusEffect.cpp`, `PF_Core/EffectGenSceneComponent.cpp`, `PF_Core/LightningEffect.cpp`, `PF_Core/ParticleSceneComponentWithEffect.cpp`, `PF_Core/SpringSceneComponent.cpp`, and `PF_Core/TraceGenSceneComponent.cpp`.
- The same chunk removes the executable-local `PF_Core::BasicEffect` lifecycle/color/interruption and `PF_Core::BasicEffectStandalone::SwitchEffect(...)` shim from `runtime_stubs.cpp`. It also removes obsolete Linux null-render object-cast shims for `PF_Core::BasicEffect` from `PF_GameLogic/PFAIContainer.cpp` and `PF_Core::ControlledStatusEffect` from `PF_GameLogic/PFApplHelper.cpp`; the real PF_Core files now own those symbols.
- The generated effect and scene-component factory paths are now live in the Linux executable beyond the no-component default effect. `Scene/DBScene.cpp` and `PF_Core/DBEffect.cpp` still return null under plain `PW_LINUX_DB_BOOTSTRAP`, but under the executable's `PW_LINUX_NULL_RENDER` bootstrap they include the maintained scene/PF_Core factory headers and instantiate real `SceneComponent`, static/particle/trace/lightning/water/sound component, and PF_Core effect-generator/spring/trace/particle component objects. `PW_Client/Game.cpp` now initializes `PF_Core::EffectsPool` from `SessionVisualRoot`, retrieves and releases `NDb::EFFECTS_DEFAULTEFFECT` (`Tech/Default/Default.EFFT.xdb`), drives the standalone component-backed `NDb::EFFECTS_CLICKONTERRAIN` (`Effects/UI/PathDestination/_.EFFT.xdb`) through `SetPosition/AddToScene/Update/DieImmediate` against a real `NScene::Scene` (`components=7`, scene objects `1/0`), drives the attached `NDb::EFFECTS_SELECTIONAURASELF` (`Effects/UI/Selection/HeroSelection.EFFT.xdb`) through `Attach/Update/DieImmediate` against a real target `SceneObject` (`components=3`, target components `1/3/1`), and drives the same effect through the canonical `BasicEffectAttached::Apply(CPtr<ClientObjectBase>)` client-object path. The apply probe observes `OnEffectAttach` (`callback=yes/1`), updates through `PF_Core::IUpdateable::ProceedUpdate(...)`, and removes the active effect through `KillAllActiveEffects()` (`target components 1/3/1`).
- The generated PF_GameLogic gameplay-effect factory path is now live for `PFAuraEffect`, `PFMinimapEffect`, `PFPlayAnimEffect`, `PFPriestessSignEffect`, `PFEffectRandom`, `PFEffectSwitcher`, `PFInvisibilityEffect`, and `PFUnitSceneObjectModify`. The Linux `VISUAL_CUTTED` source slice carries `PW_LINUX_GAMEPLAY_EFFECTS_BOOTSTRAP`, `DBPFEffect.cpp` exposes `AuraEffect::Retrieve(...)`, `MinimapEffect::Retrieve(...)`, `PlayAnimationEffect::Retrieve(...)`, `PriestessSignEffect::Retrieve(...)`, `RandomEffect::Retrieve(...)`, `EffectSwitcher::Retrieve(...)`, `InvisibilityEffect::Retrieve(...)`, and `UnitSceneObjectModify::Retrieve(...)` under that macro, and the corresponding `PF_GameLogic` effect translation units are compiled into the runnable client plus maintained runtime/gameplay probes. The Linux shell proves synthetic `NDb::AuraEffect` state/change-count lifecycle, `NDb::MinimapEffect` target-position update lifecycle, `NDb::PlayAnimationEffect` animation-node/scene-update lifecycle, `NDb::PriestessSignEffect` soul/fly-in/fly-out completion, `NDb::RandomEffect` child selection, `NDb::EffectSwitcher` visible-branch child ownership, `NDb::InvisibilityEffect` color-channel lifecycle, and `NDb::UnitSceneObjectModify` scene-object placeholder attach/update/detach against a generic Linux `PF_Core::ClientObjectBase`.
- `PFInvisibilityEffect.h` now includes `DBPFEffect.h` directly so GCC sees `NDb::InvisibilityEffect` instead of relying on the MSVC/PCH include order. Under `PW_LINUX_DB_BOOTSTRAP`, `PFInvisibilityEffect` stores the bootstrap `PF_Core::ClientObjectBase` and uses deterministic `ClientVisibilityHelper_linuxbootstrap.h` overloads instead of pulling the full Windows `PFClientLogicObject.cpp` path into the native Linux bootstrap client; the original non-bootstrap path still uses `PFClientLogicObject`.
- `PFUnitSceneObjectModify` now has a Linux bootstrap branch that avoids the full Windows `PFClientCreature` / `AdventureScreen` / visibility-map path. On Linux bootstrap it accepts a generic `ClientObjectBase`, validates the target `SceneObject` is in scene, creates a temporary placeholder `SceneObject` from the first non-null `UnitSceneObjectModify::sceneObjects[]` entry, mirrors owner/placement, updates visibility, and removes/restores state on `Die()`. The original non-bootstrap Windows path is unchanged.
- `PFAuraEffect`, `PFMinimapEffect`, and `PFPlayAnimEffect` now have Linux bootstrap branches that avoid the full visual-client dependency chain while keeping the original Windows paths intact. Aura accepts a generic `ClientObjectBase` and records active/ally/change-count state instead of calling `PFClientBaseUnit::AcknowledgeAuraChange(...)`; Minimap accepts a generic target scene object and records active/update-count state instead of calling `AdventureScreen::GetMinimap()`; PlayAnimation validates the DB animation node and forces a generic target `SceneObject` update instead of pulling `PFClientBaseUnit` / `IAnimatedClientObject`.
- `PFPriestessSignEffect` now has a Linux bootstrap branch that avoids the full `PFClientCreature` death-effect, animated-locator, and path-adjustment dependency chain while keeping the original Windows path intact. Under `PW_LINUX_DB_BOOTSTRAP` it accepts a generic `ClientObjectBase`, validates that the target `SceneObject` is already in scene, applies a standalone soul effect through the maintained effect-pool path, simulates the wait/fly-in/fly-out state machine with deterministic countdowns, records lifecycle proof counters, and releases itself through `DieImmediate()` when the bootstrap lifecycle completes.
- `PFClientApplicators.cpp` now has a native Linux bootstrap branch instead of staying Windows/AdventureScreen-only. Under `PW_LINUX_DB_BOOTSTRAP` it owns `CreateEffect`, `PrepareEffect`, `ApplyEffect`, `RestoreEffect`, `KillEffect`, `PlayEffect`, `CreateLightningEffect`, `RetrieveStandaloneEffect`, and `RetrieveStandaloneEffectCollection` with Linux-safe effect-pool, standalone-effect, and generic client-object handling. The original Windows path remains under `#else` and still uses `AdventureScreen`, `PFClientLogicObject`, `PFClientBaseUnit`, spectator visibility, and the existing Windows effect-switcher logic.
- `PFClientApplicators.cpp` is now compiled into `PrimeWorldLinuxClient`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxGameplayProbe`, with source-file bootstrap definitions added in `LinuxBootstrap/CMakeLists.txt`. `PW_Client/Game.cpp` now probes the applicator helper layer through `PrepareEffect`/`KillEffect`, `RetrieveStandaloneEffect`, standalone scene add/update/release, and `RetrieveStandaloneEffect` collection retrieval; this moves the verified runtime path from direct effect-pool calls toward applicator-owned effect lifetime helpers.
- `PF_GameLogic/PFApplBuff.cpp` now promotes the Linux null-render branch from visual stubs to a real applicator-owned effect lifecycle: `Start()` evaluates `startCondition`, `Enable()` prepares effects and calls native `NGameX::ApplyEffect(...)`, `RestartEffects()` kills/re-prepares/restores manual-death effects, `Disable()`/`Stop()` kill and clear owned effects, and `Step()` honors `BUFFBEHAVIOR_STOPONDISABLE`. The Windows/full-render branch remains unchanged under `#else`. `PW_Client/Game.cpp` now carries a heap-owned `LinuxBuffApplicatorProbe` with a synthetic concrete `AbilityUpgradeApplicator` DB object so callback ref-counting matches real applicator ownership; both smokes prove `prepared=1/1`, `restarted=1/1`, and `killed=yes` through the real `PFApplBuff` path.
- The Linux applicator/dispatch path is now past the central child-creation stub. `PF_GameLogic/PFDispatchFactory.cpp` now creates and initializes concrete applicators from generated DB resources on Linux instead of returning null, and `PF_GameLogic/PFDispatch.cpp` now stores spell applicators, applies them on dispatch delivery, and performs the same ability-upgrade injection surface as the Windows path. `PF_GameLogic/PFApplAura.cpp` now owns the Linux aura target loop/child-applicator lifecycle, including target-selector enumeration when present, receiver/owner fallback, and out-of-range child removal. `PF_GameLogic/PFApplicator.cpp` now implements Linux child-applicator removal over owner/receiver sent/applied rings and aligns `ActivateApplicator(...)` with the factory-initialized applicator contract. `PF_GameLogic/DBAbility.cpp` now exposes generated Linux bootstrap factories for `AbilityUpgradeApplicator` and `AuraApplicator`; the Windows/full-render branches remain unchanged under their existing `#else` paths.
- The Linux ability layer now uses that central dispatch/applicator path instead of stopping at inert stubs. Under `PW_LINUX_NULL_RENDER`, `PF_GameLogic/PFAbilityInstance.cpp` now applies passive applicators through `CreateApplicator(...)` / `ActivateApplicator(...)`, creates active dispatches through `CreateDispatch(...)`, starts/steps the dispatch holder once for the bootstrap path, switches multistate abilities on, tracks active applicator counts, and removes ability-owned applicators from unit rings. `PF_GameLogic/PFAbilityData.cpp` now owns Linux passive instance creation/removal, active `ApplyToTarget(...)` instance creation, toggle/cancel behavior, active-instance cleanup, and instance-vector removal. The Windows/full-render branches remain behind their existing `#else` paths.
- Latest large chunk: the Linux null-render ability/applicator runtime now performs real instant target side effects instead of only proving dispatch wiring. `StringExecutorBootstrap.h` evaluates constant float/int/bool formula literals, `DBAbility.cpp` exposes Linux bootstrap factories for `DamageApplicator`, `HealApplicator`, `KillApplicator`, and `AddNaftaApplicator`, and `PFApplInstant.cpp` now applies damage, health heal, energy heal, kill, and nafta/gold changes through `PFBaseUnit` on Linux. `PFBaseUnit.cpp` now has a minimal Linux target-side `OnDamage(...)` path with native damage normalization, event hooks, health clamping, forbid-death handling, and death dispatch, plus matching heal/energy/gold helpers. `PFAbilityInstance.cpp` now drives immediate Linux dispatches through explicit base `PFDispatch::Start()`/`Step()` calls to avoid null-render virtual dispatch hazards, and `PFDispatch.cpp` avoids the render/client `OnStart()` hook on the base Linux dispatch. `PFApplInstant.h` only widens `PFAddNaftaApplicator` acceptable unit masks under `PW_LINUX_NULL_RENDER` so synthetic bootstrap units can validate nafta without changing the Windows/full-render hero-only path.
- `PW_Client/Game.cpp` now carries a `Session effects unit-instant runtime` probe. Both current smokes report `formula=yes/0.25`, `damage=yes/yes/1.00->0.75`, `heal=yes/yes/0.50->0.75`, `energy=yes/yes/0.50->0.75`, `kill=yes/yes`, `nafta=yes/yes/0->3`, and `baseattack=yes/yes/yes/yes/1.00->0.80`, proving generated DB factories, real dispatch/applicator creation, target-side unit mutation, kill, nafta, and base-attack damage all work through the Linux client path.
- Latest large chunk: the Linux null-render ability/applicator runtime now promotes the next chained instant-applicator slice. `PFApplInstant.cpp`, `PFApplHelper.cpp`, `PFDispatchFactory.cpp`, and the Linux bootstrap factories in `DBAbility.cpp` now cover spell applicators, ability-end/cancel notification, refresh-cooldown applicators, status/dispel scans over applied status applicators, variable-proxy child creation and variable capture, periodic spell strikes, and probability spell dispatch. `PFDispatchFactory.cpp` now starts and steps immediate Linux dispatches so nested spell dispatches actually apply in the bootstrap runtime, and `PFApplInstant.h` only widens refresh-cooldown target masks under `PW_LINUX_NULL_RENDER` so synthetic non-hero units can validate cooldown behavior without changing the Windows/full-render hero-only path.
- `PW_Client/Game.cpp` now carries a `Session effects unit-chain runtime` probe. Both current smokes report `db=yes units=yes spell=yes/yes/1.00->0.85 proxy=yes/yes/yes/0.12/1.00->0.88 dispell=yes/yes/yes/1->0 refresh=yes/yes/4.00->0.00 abilityend=yes/yes periodic=yes/yes/1.00->0.94 probability=yes/yes/1.00->0.93`, proving generated DB factories, nested spell dispatch, variable-proxy child capture, status applicator removal through dispel, cooldown refresh, ability-end completion, periodic spell dispatch, and deterministic probability dispatch all work through the Linux client path.
- Latest large chunk: the Linux null-render ability/applicator runtime now promotes the next fanout applicator slice. `DBAbility.cpp` exposes Linux bootstrap factories for `BounceApplicator`, `ChainLightningApplicator`, and `DelegateDamageApplicator`; `PFApplBounce.cpp` now creates real nested dispatches per bounce and can use a DB target selector when present while still supporting the controlled current-target fallback; `PFApplChainLightning.cpp` now sends its first chain-hit spell dispatch; and `PFApplDelegateDamage.cpp` now listens for damage events, reduces the original hit, and delegates split damage through concrete `PFApplDamage` instances to selector results or the owner fallback. `PFApplicator.cpp` also aligns the Linux base `Start()` result with Windows enabled/disabled semantics so base-derived applicators like chain lightning are not marked complete before their own `Start()` logic runs. Windows/full-render branches remain under their original `#else` paths.
- `PW_Client/Game.cpp` now carries a `Session effects unit-fanout runtime` probe. Both current smokes report `db=yes units=yes bounce=yes/yes/yes/1.00->0.91->0.82 chain=yes/yes/1.00->0.89 delegate=yes/yes/yes/1.00->0.80/1.00->0.70`, proving generated DB factories, bounce dispatch repeats, first chain-lightning dispatch damage, and delegate-damage reduction/split through real Linux dispatch/applicator/unit damage paths.
- Latest large chunk: the Linux null-render ability/applicator runtime now promotes the common modifier/status slice. `PFBaseUnit.cpp` now has Linux per-unit stat storage and unit variable storage through the existing `variablesRing`, `PFApplMod.cpp` now applies/removes concrete stat modifiers, permanent stat base changes, flags, value/marker variables, target-count variables, status child applicators, invisibility flags, ability-mod cached add/mul values, and taunt registration/targeting, and `DBAbility.cpp` exposes Linux bootstrap factories for `AbilityModApplicator`, `FlagsApplicator`, `InvisibilityApplicator`, `MarkerApplicator`, `PermanentStatModApplicator`, `StatModApplicator`, `TargetsCounterApplicator`, `TauntApplicator`, and `ValueApplicator`. Windows/full-render branches remain under their original `#else` paths.
- `PW_Client/Game.cpp` now carries a `Session effects unit-mod runtime` probe. Both current smokes report `db=yes units=yes stat=yes/yes/yes/0.00->0.25->0.00/0.25 permanent=yes/yes/0.00->0.50 flags=yes/yes/yes value=yes/yes/0.33 marker=yes/yes/0.40 targets=yes/yes/1 status=yes/yes/yes/0.00->0.20->0.00 invis=yes/yes/yes abilitymod=yes/yes/0.25/2.00 taunt=yes/yes/yes`, proving generated DB factories, stat modifier add/remove, permanent stat mutation, flag add/remove, variable applicators, status child cleanup, invisibility cleanup, ability-mod math, and taunt targeting through real Linux unit/applicator paths.
- Verification after the modifier/status applicator promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.20-03.01.34/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same unit-mod proof while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.20-03.01.45/linux-client-shell.log`.
- Latest large chunk: the Linux null-render ability/applicator runtime now promotes the next utility/mod/fx applicator factory surface. `DBAbility.cpp` exposes Linux bootstrap factories for `AbilityUpgradeTechApplicator`, `AddApplicatorDuration`, `ChangeBaseAttackApplicator`, `ChangeHeroStateApplicator`, `ChangeStateApplicator`, `CreateGlyphApplicator`, `CreepBehaviourChangeApplicator`, `DamageReflectApplicator`, `FXApplicator`, `GiveConsumable`, `OnDamageApplicator`, `ResurrectApplicator`, `SceneObjectChangeApplicator`, `UIMessageApplicator`, and `VictoryApplicator`. `PW_Client/Game.cpp` proves controlled add/remove behavior for `ChangeBaseAttackApplicator`, `CreepBehaviourChangeApplicator`, `DamageReflectApplicator`, `OnDamageApplicator`, `ChangeHeroStateApplicator`, `SceneObjectChangeApplicator`, and `FXApplicator`; proves instant/no-op startup for `AddApplicatorDuration`, `AbilityUpgradeTechApplicator`, `UIMessageApplicator`, `CreateGlyphApplicator`, `VictoryApplicator`, `ChangeStateApplicator`, and `GiveConsumable`; and directly factory-proves every DB in the slice including `ResurrectApplicator`. Real resurrect behavior remains hero/death-state-bound rather than fully exercised by the bootstrap unit proof.
- `PW_Client/Game.cpp` now carries a `Session effects unit-utility runtime` probe. Both current smokes report `db=yes units=yes factory=yes changebase=yes/1->0 creep=yes/1->0 reflect=yes/1->0 ondamage=yes/1->0 herostate=yes/1->0 scene=yes/1->0 fx=yes/1->0 instant=yes/7`, proving the new generated factory surface, controlled persistent unit add/remove paths, and safe instant utility startup through the real Linux applicator path.
- Verification after the unit utility/mod/fx applicator promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.25-07.04.53/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same unit-utility proof while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.25-07.05.03/linux-client-shell.log`.
- Latest large chunk: the Linux null-render ability/applicator runtime now promotes the maintained movement applicator factory slice. `PF_GameLogic/DBAbility.cpp` exposes Linux bootstrap factories for `AttackTargetApplicator`, `AttractApplicator`, `FlyApplicator`, `GhostMoveApplicator`, `KickAwayApplicator`, `MoveToApplicator`, `ShiftApplicator`, `TeleportApplicator`, and `ThrowApplicator` by including the maintained `PFApplMove` implementation surface instead of returning null under `PW_LINUX_DB_BOOTSTRAP`; Windows/full-render branches remain under their existing paths.
- `PF_GameLogic/PFBaseMovingUnit.cpp` now carries a wider Linux null-render moving-unit runtime instead of leaving movement as isolated direct-coordinate proof code. The Linux branch initializes `pOwner`, binds direct-construction moving units back to the owning world object with `world.Init(this)`, preserves live unit targets in `MoveTo(PFBaseUnit*)`, implements `FollowTo(...)` with a retained follow distance, mirrors attached/mounted child units through parent movement, updates AI-world movement notification in `SetCenter(...)`, and advances `MOVE_STATE_MOVING` / `MOVE_STATE_SPECIAL_MOVE` by `speed * dt` until the target band is reached. The same Linux branch now implements `PFBaseMovingUnit::GetAllUnits(...)`, `GetPushableUnitsInRange(...)`, and `GetAllUnitsInRangeConsiderSize(...)` over the bootstrap unit registry with world, marked, skip-unit, mounted/non-pushable, and radius filtering. The Windows/full-render movement code remains behind its original path.
- `PF_GameLogic/PFWorld.cpp` now steps Linux null-render moving units for loaded worlds by calling `MovingUnit::UpdateMovements(...)` after `PFAIWorld::Update(...)`. This moves the create-game path past manual per-unit stepping only; full pathfinding/voxel collision still remains future work.
- `PW_Client/Game.cpp` now carries a stronger `Session effects unit-move runtime` probe. Both current smokes report `db=yes units=yes factory=yes teleport=yes/260.0->250.0 kick=yes/32.0->33.0 attract=yes/257.0->255.0 shift=yes/262.0->258.0 moveto=yes/1->0/269.0->264.0 track=yes/285.0 follow=yes/308.0->302.0 attach=yes/318.0->310.0/310.0 push=yes/332.0->336.0/332.0 find=yes/346.0 tick=yes/354.0->350.0 trace=yes/10.0->2.0 block=yes/380.0->382.0 resolve=yes/2.0->4.0 tunnel=yes/440.0->442.0 search=yes/2.0->4.0/36.0 ghost=yes/1->0/1->0 fly=yes/1->0/7->0 throw=yes/1->0/yes->no/273.0->276.0 attack=yes/1->0/1.00->0.82`, proving generated movement factories, direct teleport relocation to the owner, deterministic kick/attract/shift repositioning, real direct `MoveTo` movement toward the owner, live unit-target retargeting after the target moves, follow-distance preservation toward a moved leader, mounted child mirroring during parent motion, push/free-place/global movement ticking, segment tracing, blocked/tunnel stopping, resolver separation/search, persistent add/remove ownership, ghost/fly ghost-mode add/remove, selector-backed `THROWTYPE_THROW` displacement with forbid-move/forbid-attack cleanup, and `AttackTargetApplicator`-driven base-attack damage through the Linux ability path.
- `PF_GameLogic/DBAbility.cpp` now initializes `AreaTargetSelector::absolutePosition` to `(0, 0)` in the generated/default constructor. `CVec2` does not zero-initialize by default in this codebase, so the Linux target-selector path could read garbage as an explicit absolute area center; this blocked controlled `ForAllTargets`, maximizing, and damaging-links selector proofs. This is a deterministic default-initialization fix rather than a Linux-only behavior fork.
- Verification after the live-target/follow/attach movement chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.29-12.36.33/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same `moveto=yes/1->0/269.0->264.0`, `track=yes/285.0`, `follow=yes/308.0->302.0`, `attach=yes/318.0->310.0/310.0`, `forall=yes/2/0.16`, `maximizing=yes/yes/100.87`, and `links=yes/yes/100.87` proofs while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.29-12.36.40/linux-client-shell.log`.
- Latest large chunk: the Linux null-render movement path now has DB-sized units and simple dynamic occupancy instead of treating bootstrap moving units as point-like coordinates. `PF_GameLogic/PFBaseUnit.cpp` initializes Linux unit object sizes, dynamic sizes, targeting parameters, and attack sectors from `NDb::Unit` data, falling back to tile-sized defaults when the DB size is absent. `PF_GameLogic/PFBaseMovingUnit.cpp` now uses those sizes for Linux unit overlap checks, range sorting, free-place search, `PlaceUnit(...)`, `PlaceUnitWithPush(...)`, and `CanOccupyPosition(...)`; `CheckStraightMove(...)` also distinguishes static-only map checks from dynamic-unit occupancy so forced relocation applicators such as `KickAway` continue to work. The Windows/full-render movement branch remains under its original `#else` path.
- `PW_Client/Game.cpp` now extends the `Session effects unit-move runtime` proof with deterministic `MoveToApplicator`/point-selector offsets and explicit push/free-place/global-tick checks. Both current smokes report `push=yes/332.0->336.0/332.0`, `find=yes/346.0`, and `tick=yes/354.0->350.0` in addition to the existing `teleport`, `kick`, `attract`, `shift`, `moveto`, `track`, `follow`, `attach`, `ghost`, `fly`, `throw`, and `attack` proofs.
- Verification after the unit geometry/occupancy/push movement chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.29-12.54.11/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports `Session effects unit-move runtime: db=yes units=yes factory=yes teleport=yes/260.0->250.0 kick=yes/32.0->33.0 attract=yes/257.0->255.0 shift=yes/262.0->258.0 moveto=yes/1->0/269.0->264.0 track=yes/285.0 follow=yes/308.0->302.0 attach=yes/318.0->310.0/310.0 push=yes/332.0->336.0/332.0 find=yes/346.0 tick=yes/354.0->350.0 ghost=yes/1->0/1->0 fly=yes/1->0/7->0 throw=yes/1->0/yes->no attack=yes/1->0` while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.29-12.54.16/linux-client-shell.log`.
- Latest large chunk: the Linux null-render movement path now checks occupancy consistently through `LinuxCanOccupyPosition(...)` and traces straight movement along the segment instead of only checking the endpoint. `MovingUnit::CheckStraightMove(...)` now returns the last safe distance before a static or dynamic blocker, while static-only modes still ignore dynamic units so forced relocation behavior such as `KickAway` stays intact. Normal `MovingUnit::Step(...)` now stops before stepping into another unit's collision radius, and direct/special/ghost movement still uses the existing ghost/move flags to bypass dynamic blockers when the DB asks for it. The Windows/full-render movement branch remains under its original `#else` path.
- `PW_Client/Game.cpp` now extends the `Session effects unit-move runtime` proof with segment-trace and blocked-step checks. Both current smokes report `trace=yes/10.0->2.0` and `block=yes/380.0->382.0`, proving that Linux movement no longer runs through an occupied segment or advances a normal mover into another unit's collision radius.
- Verification after the movement segment-trace/blocking chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.29-13.05.35/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports `Session effects unit-move runtime: db=yes units=yes factory=yes teleport=yes/260.0->250.0 kick=yes/32.0->33.0 attract=yes/257.0->255.0 shift=yes/262.0->258.0 moveto=yes/1->0/269.0->264.0 track=yes/285.0 follow=yes/308.0->302.0 attach=yes/318.0->310.0/310.0 push=yes/332.0->336.0/332.0 find=yes/346.0 tick=yes/354.0->350.0 trace=yes/10.0->2.0 block=yes/380.0->382.0 ghost=yes/1->0/1->0 fly=yes/1->0/7->0 throw=yes/1->0/yes->no attack=yes/1->0` while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.29-13.05.42/linux-client-shell.log`.
- Latest large chunk: the Linux null-render collision resolver is no longer inert. `PF_GameLogic/CollisionResolver.cpp` now performs a narrow bootstrap overlap-separation pass over `PFBaseMovingUnit` pairs, uses DB/object collision sizes, skips dead/mounted/special/no-collide/ghost-ignore-dynamic units, checks tile-map bounds/passability, and relocates one overlapping unit through the public moving-unit placement path. `PF_GameLogic/PFWorld.cpp` now calls that resolver for loaded Linux worlds before `MovingUnit::UpdateMovements(...)`, so the create-game path exercises a real resolver object before the movement tick. The Windows/full-render collision resolver branch remains under its original implementation path.
- `PW_Client/Game.cpp` now extends the `Session effects unit-move runtime` proof with resolver separation. Both current smokes report `resolve=yes/2.0->4.0` alongside the existing trace/block checks, proving the Linux resolver can separate two overlapping DB-sized units to their expected minimum distance.
- Verification after the resolver movement chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke reports `Session effects unit-move runtime: db=yes units=yes factory=yes teleport=yes/260.0->250.0 kick=yes/32.0->33.0 attract=yes/257.0->255.0 shift=yes/262.0->258.0 moveto=yes/1->0/269.0->264.0 track=yes/285.0 follow=yes/308.0->302.0 attach=yes/318.0->310.0/310.0 push=yes/332.0->336.0/332.0 find=yes/346.0 tick=yes/354.0->350.0 trace=yes/10.0->2.0 block=yes/380.0->382.0 resolve=yes/2.0->4.0 ghost=yes/1->0/1->0 fly=yes/1->0/7->0 throw=yes/1->0/yes->no attack=yes/1->0` and writes `Bin/logs/2026.05.29-13.15.45/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same resolver proof while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.29-13.15.54/linux-client-shell.log`.
- Latest large chunk: the Linux null-render movement step now uses the same straight-segment trace before applying a per-frame move, so a high-speed unit cannot tunnel through another DB-sized unit just because the endpoint is clear. When the trace finds a blocker, `MovingUnit::Step(...)` moves to the last safe point and stops instead of crossing the occupied segment. The Linux bootstrap collision resolver also now searches nearby radial placement candidates when the direct separation point is occupied by a third unit, preserving map/dynamic checks before using the public placement path. Windows/full-render movement and resolver implementations remain under their original paths.
- `PW_Client/Game.cpp` now extends the `Session effects unit-move runtime` proof with high-speed tunneling and resolver-search checks. Both current smokes report `tunnel=yes/440.0->442.0` and `search=yes/2.0->4.0/36.0`, proving that a fast Linux mover stops at the blocker edge and that the resolver can separate an overlap by choosing a nearby free side position when the direct target position is occupied.
- Verification after the movement tunnel/resolver-search chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.29-13.40.22/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports `Session effects unit-move runtime: db=yes units=yes factory=yes teleport=yes/260.0->250.0 kick=yes/32.0->33.0 attract=yes/257.0->255.0 shift=yes/262.0->258.0 moveto=yes/1->0/269.0->264.0 track=yes/285.0 follow=yes/308.0->302.0 attach=yes/318.0->310.0/310.0 push=yes/332.0->336.0/332.0 find=yes/346.0 tick=yes/354.0->350.0 trace=yes/10.0->2.0 block=yes/380.0->382.0 resolve=yes/2.0->4.0 tunnel=yes/440.0->442.0 search=yes/2.0->4.0/36.0 ghost=yes/1->0/1->0 fly=yes/1->0/7->0 throw=yes/1->0/yes->no attack=yes/1->0` while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.29-13.40.32/linux-client-shell.log`.
- Latest large chunk: `AttackTargetApplicator` now does real Linux null-render attack work instead of only attaching to a target and pushing an inert FSM state. `PF_GameLogic/PFApplMove.cpp` keeps the Windows/full-render `PFBaseUnitAttackState` path unchanged, but under `PW_LINUX_NULL_RENDER` it assigns the target and calls `PFBaseUnit::DoAttack(...)` directly so the already-promoted Linux base-attack/dispatch path is exercised. `PW_Client/Game.cpp` also strengthens the controlled movement proof: `ThrowApplicator` now uses a `PointTargetSelector` plus `THROWTYPE_THROW` and proves movement from `273.0` to `276.0`, while `AttackTargetApplicator` is backed by a synthetic `BaseAttack`/`DamageApplicator` and proves health mutation from `1.00` to `0.82`.
- Verification after the throw/attack-target movement chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.29-16.53.37/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports `Session effects unit-move runtime: db=yes units=yes factory=yes teleport=yes/260.0->250.0 kick=yes/32.0->33.0 attract=yes/257.0->255.0 shift=yes/262.0->258.0 moveto=yes/1->0/269.0->264.0 track=yes/285.0 follow=yes/308.0->302.0 attach=yes/318.0->310.0/310.0 push=yes/332.0->336.0/332.0 find=yes/346.0 tick=yes/354.0->350.0 trace=yes/10.0->2.0 block=yes/380.0->382.0 resolve=yes/2.0->4.0 tunnel=yes/440.0->442.0 search=yes/2.0->4.0/36.0 ghost=yes/1->0/1->0 fly=yes/1->0/7->0 throw=yes/1->0/yes->no/273.0->276.0 attack=yes/1->0/1.00->0.82` while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.29-16.53.45/linux-client-shell.log`.
- Latest UI-first chunk: the runnable Linux client no longer opens by default on the diagnostic wall. `PW_Client/Game.cpp` now keeps the real `NGameX::SelectGameModeScreen::Init/LoadScreenLayout/Step/Draw` path active, but presents a visible Linux-only OpenGL lobby/menu surface over the map/loading artwork by default. The menu is backed by the same selected map, hero, lineup, UI-root, `Lobby_SelectGameMode`, and loading/runtime state already used by the bootstrap, offers `Create custom game`, `Cycle map`, `Cycle hero`, and diagnostics actions, and handles `Up`/`Down`/`Enter` plus `F10`. The old dense diagnostics overlay is still available with `--diagnostics-overlay` or `F10`; this is deliberately Linux/bootstrap-only and leaves the Windows client path unchanged.
- Latest UI-window visibility fix: `System/MainFrame_linux.cpp` now gives the native Linux client window usable window-manager metadata instead of relying only on `XMapRaised(...)`. The Linux path sets `WM_CLASS`, `_NET_WM_PID`, fixed bootstrap size hints for the requested client resolution, `_NET_WM_WINDOW_TYPE_NORMAL`, sticky/above EWMH state, and explicitly move-resizes/raises/activates the window after mapping. This keeps the visible menu from being tiled into a huge off-workspace slot on tiling/window-manager setups while leaving the Windows path untouched.
- Verification after the visible-menu/window-manager chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, `git -c safe.directory=/home/vitaly/p/Prime-World--codex-of-andrei -C /home/vitaly/p/Prime-World--codex-of-andrei diff --check` exited `0`, and both stable smoke paths exited `0`. A live window-manager inspection now reports the client window as sticky and correctly sized (`wmctrl -lG`: desktop `-1`, `1280x720`, title `Prime World Classic - pwclassic - 2.15.01.0000`), and a targeted capture `/tmp/primeworld-window-after-wm-fix.png` is a `1280 x 720` PNG showing the visible menu rather than the old diagnostics wall. The normal smoke reports `Diagnostics overlay: no`, `UI screen runtime: ready=yes window=<unnamed:15-children> ... path=NGameX::SelectGameModeScreen::Init/LoadScreenLayout/Step/Draw`, and `Visible menu runtime: ready=yes selected=1/4 action=Create custom game activations=0 path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`, and writes `Bin/logs/2026.05.29-17.22.48/linux-client-shell.log`. The `--bootstrap-create-game` smoke keeps the visible menu inactive once loading starts (`Visible menu runtime: ready=no ... action=Loading`) while still reaching `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, ARAM object-count diagnostics, and writes `Bin/logs/2026.05.29-17.22.55/linux-client-shell.log`.
- Latest UI parity chunk: `PW_Client/Game.cpp` now draws a Linux-only OpenGL bridge that follows the real `Data/UI/Screens/Lobby/Lobby_SelectGameMode.xdb` starting-screen geometry instead of the earlier bootstrap card menu. The bridge loads the real lobby background texture from `Data/UI/Styles/Lobby/Lobby.dds`, renders the `Create game`/`Join game` headers, maps panel, games panel, join-mode selectors, player-count slider/value, `Start session`, `Create`, and `Refresh` controls at the same XDB coordinates, and keeps the real `NGameX::SelectGameModeScreen` initialized underneath. The temporary OpenGL font cannot render UTF-8 correctly yet, so this bridge uses ASCII-safe map descriptor names when localized titles would otherwise become mojibake; the production fix is still the real UI font/render path, not this fallback. Windows remains untouched because this code is inside the Linux `Game.cpp` bootstrap path.
- Verification after the SelectGameMode visual bridge chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git -c safe.directory=/home/vitaly/p/Prime-World--codex-of-andrei -C /home/vitaly/p/Prime-World--codex-of-andrei diff --check` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 30.0` exited `0`, wrote `Bin/logs/2026.05.29-17.44.29/linux-client-shell.log`, `wmctrl -lG` reported the Prime World window as `1280x720`, and `/tmp/primeworld-lobby-window-final.png` captures the Windows-layout lobby bridge over the real lobby art with clean map labels. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, kept the visible menu inactive once loading starts (`Visible menu runtime: ready=no selected=1/4 action=Loading path=inactive`), still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, and wrote `Bin/logs/2026.05.29-17.45.33/linux-client-shell.log`.
- Latest UI material chunk: the Linux `Lobby_SelectGameMode` bridge now resolves real UI material references instead of painting all controls with flat debug rectangles. `PW_Client/Game.cpp` resolves `.MTRL.xdb` -> `.ITEX.xdb` -> `.dds` for the shipped big button, tooltip/panel, header strip, frame, selection-frame, and radio normal/selected assets, uploads them into OpenGL textures, deletes them during overlay shutdown, and draws the create/join panels, value box, developer frame, radios, and bottom buttons with those shipped materials. The old flat-color drawing remains as a Linux fallback if an asset fails to load. This remains bootstrap-only under the Linux client path and does not touch the Windows DirectX/UI path.
- Verification after the material-backed lobby bridge chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git -c safe.directory=/home/vitaly/p/Prime-World--codex-of-andrei -C /home/vitaly/p/Prime-World--codex-of-andrei diff --check` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 12.0` exited `0`, wrote `Bin/logs/2026.05.29-17.55.31/linux-client-shell.log`, and `/tmp/primeworld-lobby-material-polished.png` captures the Windows-layout lobby bridge with material-backed panels, list frames, radios, and buttons. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, kept the visible menu inactive once loading starts (`Visible menu runtime: ready=no selected=1/4 action=Loading path=inactive`), still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, and wrote `Bin/logs/2026.05.29-17.56.01/linux-client-shell.log`.
- Latest UI slot/scrollbar parity chunk: the Linux `Lobby_SelectGameMode` bridge now follows the real child XDB geometry for the scroll lists instead of using compact bootstrap rows. `PW_Client/Game.cpp` renders map entries as `MapSlot`-sized 518x130 rows with active selection-frame material, session entries as `SessionSlot`-sized 518x80 rows, draws the map/game side scrollbars at the XDB 518+28 split with shipped `Scroll_Area_Normal` / `Scroll_Area_Leaver_Normal` materials, and renders the player-count scrollbar using the shipped horizontal arrow, track, and lever materials from the `PlayerCount` `UIScrollBarLayout`. The temporary ASCII-safe text fallback remains until the real UI font path can render localized text.
- Verification after the XDB slot/scrollbar bridge chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git -c safe.directory=/home/vitaly/p/Prime-World--codex-of-andrei -C /home/vitaly/p/Prime-World--codex-of-andrei diff --check` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 16.0` exited `0`, wrote `Bin/logs/2026.05.30-05.08.47/linux-client-shell.log`, and `/tmp/primeworld-lobby-xdb-slots-scrollbars.png` captures the larger XDB-sized map/session slots plus real scrollbar art. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, kept the visible menu inactive once loading starts (`Visible menu runtime: ready=no selected=1/4 action=Loading path=inactive`), still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, and wrote `Bin/logs/2026.05.30-05.09.30/linux-client-shell.log`.
- Latest UI localized-text chunk: the Linux `Lobby_SelectGameMode` OpenGL bridge now uses FreeType with the bundled `Data/UI/Fonts/PrimeWorld/PrimeWorld-export03.ttf` face instead of relying only on X11 256-glyph display lists. `PW_Client/Game.cpp` initializes FreeType only on the Linux overlay path, caches glyph alpha textures, measures/draws UTF-8 through the engine `NStr::UTF8ToUnicode` conversion, safely truncates UTF-8 text without cutting continuation bytes, and now prefers localized map/type/description strings when the FreeType backend is live. The old X11 display-list path remains as a fallback if FreeType/font initialization fails. Windows remains untouched.
- Verification after the FreeType localized lobby-text chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale ru-RU --map Aram --hero shadow --seconds 16.0` exited `0`, logged `overlayFontBackend=FreeType` and `overlayFontFile=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/UI/Fonts/PrimeWorld/PrimeWorld-export03.ttf` in `Bin/logs/2026.05.30-05.28.20/linux-client-shell.log`, and `/tmp/primeworld-lobby-freetype-text.png` captures the Windows-layout lobby with Cyrillic text rendered by the PrimeWorld font. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, logged the same FreeType backend in `Bin/logs/2026.05.30-05.28.59/linux-client-shell.log`, kept the visible menu inactive once loading starts, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, and `mapObjects=2468`.
- Latest UI caption/textref parity chunk: the Linux `Lobby_SelectGameMode` bridge now loads Windows-authored captions from the real UTF-16 text references in `Data/UI/Screens/Lobby/Lobby_SelectGameMode.xdb` and `SessionSlot.xdb` instead of hardcoding English bootstrap labels. `PW_Client/Game.cpp` resolves create/join headers, maps list header, player-count label, start/create/refresh buttons, developer-sex label, join-mode labels, and session-row `Players`/`Map` labels through `ReadTextRefFromDataRoot(...)`, with ASCII defaults only as a fallback. The code remains Linux-overlay-only; the Windows UI path and data stay unchanged.
- Verification after the XDB caption/textref lobby chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale ru-RU --map Aram --hero shadow --seconds 16.0` exited `0`, wrote `Bin/logs/2026.05.30-05.36.46/linux-client-shell.log`, and `/tmp/primeworld-lobby-xdb-textrefs.png` shows the Windows-layout lobby with actual XDB captions such as `Создать игру`, `Присоединиться к игре`, `Доступные карты:`, `Игроков:`, `Запуск демо (синглплеер)`, and `Разработчик - мужчина`. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, wrote `Bin/logs/2026.05.30-05.37.25/linux-client-shell.log`, kept the visible menu inactive once loading starts, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, and `mapObjects=2468`.
- Latest UI row-layout parity chunk: the Linux `Lobby_SelectGameMode` bridge now lays out text inside `MapSlot` and `SessionSlot` using the child XDB text boxes instead of approximate bootstrap offsets. `PW_Client/Game.cpp` adds a Linux-only boxed text helper with UTF-8-safe width truncation and simple word wrapping, draws map titles/descriptions/ids at the `MapSlot` `Title`/`Descr`/`Id` coordinates, draws session name/id/static labels/player-count/map fields at the `SessionSlot` child coordinates, and loads the remaining session-name/started UTF-16 textrefs. This keeps the bridge visually closer to the Windows-authored layout while leaving Windows untouched.
- Verification after the XDB row-layout lobby chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale ru-RU --map Aram --hero shadow --seconds 16.0` exited `0` and wrote `Bin/logs/2026.05.30-05.45.26/linux-client-shell.log`; an additional short capture run exited `0`, wrote `Bin/logs/2026.05.30-05.46.47/linux-client-shell.log`, and `/tmp/primeworld-lobby-xdb-row-layout-window.png` is a `1280 x 720` window capture of the updated row layout. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, wrote `Bin/logs/2026.05.30-05.46.00/linux-client-shell.log`, kept the visible menu inactive once loading starts, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, and `mapObjects=2468`.
- Latest UI mouse-interaction chunk: the Linux `Lobby_SelectGameMode` bridge now owns mouse hit-testing for the XDB-sized visible controls instead of being keyboard-only. `PW_Client/Game.cpp` projects native X11 mouse coordinates into the 1280x1024 XDB layout space, lets clicks select map rows or jump the map scrollbar, lets player-count slider clicks update `requestedTeamSize` and regenerate the local match preview, lets create/start buttons activate the existing primary create/ready path, and records refresh-button clicks. The keyboard visible-menu map action also now regenerates the local match preview after changing maps. This remains Linux bootstrap-only and leaves Windows unchanged.
- Verification after the UI mouse-interaction chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale ru-RU --map Aram --hero shadow --seconds 12.0` exited `0`, still reported `UI screen runtime: ready=yes ... path=NGameX::SelectGameModeScreen::Init/LoadScreenLayout/Step/Draw` and `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`, and wrote `Bin/logs/2026.05.30-05.56.04/linux-client-shell.log`. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, wrote `Bin/logs/2026.05.30-05.56.36/linux-client-shell.log`, kept the visible menu inactive once loading starts, and still reached loading/scheduler/transceiver/PFWorld/TileMap with `mapObjects=2468`. A window capture was not refreshed in this chunk because the timed window exited before `maim -i` completed; keep the previous visual reference `/tmp/primeworld-lobby-xdb-row-layout-window.png`.
- Latest UI join-control interaction chunk: the Linux `Lobby_SelectGameMode` bridge now keeps the Windows-authored join-mode radios stateful, toggles the real bootstrap `DeveloperSexControl` context value, highlights selectable game rows, lets double-clicking a game row drive the existing bootstrap `JoinGame`/`Reconnect`/`Spectate` context methods according to the selected join mode, and makes the refresh button call the context `RefreshGamesList()` hook instead of only recording a label. These additions are still Linux overlay/runtime state only; the Windows DirectX/UI implementation remains untouched.
- Verification after the UI join-control interaction chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale ru-RU --map Aram --hero shadow --seconds 12.0` exited `0`, wrote `Bin/logs/2026.05.30-06.03.54/linux-client-shell.log`, and still reported `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen` with `UI games runtime: ready=yes entries=3`. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, wrote `Bin/logs/2026.05.30-06.04.11/linux-client-shell.log`, kept the visible menu inactive once loading starts, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, `fog=130x130`, and `mapObjects=2468`. An additional visual capture run exited `0`, wrote `Bin/logs/2026.05.30-06.05.50/linux-client-shell.log`, and saved `/tmp/primeworld-lobby-join-controls-window.png` for the current join-control overlay state.
- Latest UI dev-game list data chunk: the Linux `Lobby_SelectGameMode` bridge no longer paints mostly synthetic session rows. `PW_Client/Game.cpp` now snapshots the real `lobby::SDevGameInfo` rows produced by `LinuxBootstrapGameContextUi::RefreshGamesList()` before the real screen consumes them, exposes cached row count/lookup to the OpenGL overlay, draws actual session names, real `gameId` values, player/max-player counts, and resolved map titles, and double-click activation now uses the actual cached `gameId` instead of assuming `1001 + row`. The old fallback remains for an empty cache, and the Windows DirectX/UI path is unchanged.
- Verification after the UI dev-game list data chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale ru-RU --map Aram --hero shadow --seconds 12.0` exited `0`, wrote `Bin/logs/2026.05.30-06.12.38/linux-client-shell.log`, and still reported `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen` with `UI games runtime: ready=yes entries=3`. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, wrote `Bin/logs/2026.05.30-06.12.56/linux-client-shell.log`, kept the visible menu inactive once loading starts, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, `fog=130x130`, and `mapObjects=2468`. An additional visual capture run exited `0`, wrote `Bin/logs/2026.05.30-06.13.06/linux-client-shell.log`, and saved `/tmp/primeworld-lobby-real-devgames-window.png` as a `1280 x 720` PNG showing the current row-data overlay state.
- Latest UI session-list scroll parity chunk: `PW_Client/Game.cpp` now treats the `Lobby_SelectGameMode` session list selection as an absolute row instead of a visible-row index. Mouse hit-testing resolves through the same `ResolveLinuxLobbyFirstVisibleRow(...)` helper used by drawing, the rendered rows start from the selected row's first-visible window, and the games scrollbar receives that first-visible row instead of always drawing at the top. Session IDs and player counts now also match the real `SelectGameModeLogic` formatting (`# 1001`, `1 / 10`) instead of the earlier compact fallback. This remains Linux OpenGL overlay code only; Windows compatibility is untouched.
- Verification after the UI session-list scroll parity chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale ru-RU --map Aram --hero shadow --seconds 12.0` exited `0`, wrote `Bin/logs/2026.05.30-06.17.33/linux-client-shell.log`, and still reported `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen` with `UI games runtime: ready=yes entries=3`. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, wrote `Bin/logs/2026.05.30-06.17.49/linux-client-shell.log`, kept the visible menu inactive once loading starts, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, `fog=130x130`, and `mapObjects=2468`. A refreshed visual capture run exited `0`, wrote `Bin/logs/2026.05.30-06.17.58/linux-client-shell.log`, and saved `/tmp/primeworld-lobby-real-devgames-scroll-window.png` as a `1280 x 720` PNG of the current list/scrollbar overlay state.
- Latest UI dev-game context-cache chunk: `PW_Client/Game.cpp` now builds the fallback development session rows inside `LinuxBootstrapGameContextUi` itself and preserves that list in `visibleGamesList` before `PopGameList()` clears the screen-consumed buffer. The real `SelectGameModeScreen` and the Linux OpenGL bridge now see the same `SDevGameInfo` rows even when the real screen would previously create local fallback rows after an empty pop; the overlay no longer falls back to the placeholder `SessionSlot` caption for row names. `ConvertLinuxLobbyWideText(...)` now follows the same `NStr::ToMBCS(...)` path used by `SelectGameModeLogic` for `SDevGameInfo::name`. This is still Linux bootstrap/UI bridge code only and leaves the Windows path untouched.
- Verification after the UI dev-game context-cache chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale ru-RU --map Aram --hero shadow --seconds 12.0` exited `0`, wrote `Bin/logs/2026.05.30-06.24.44/linux-client-shell.log`, and still reported `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen` with `UI games runtime: ready=yes entries=3`. The `--bootstrap-create-game --seconds 0.2` smoke exited `0`, wrote `Bin/logs/2026.05.30-06.25.11/linux-client-shell.log`, kept the visible menu inactive once loading starts, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, `fog=130x130`, and `mapObjects=2468`. The final visual capture run exited `0`, wrote `Bin/logs/2026.05.30-06.25.22/linux-client-shell.log`, and saved `/tmp/primeworld-lobby-real-devgames-context-cache-window.png`; that `1280 x 720` PNG shows the session rows drawing real cached names (`Linux Player's game`, `ARAM practice`, `Classic battle`) rather than the placeholder caption text.
- Latest UI custom-map-list parity chunk: `PW_Client/Game.cpp` now keeps the broad filesystem scan as a fallback, but projects the active Linux `LinuxMapCatalog::entries` through the same `\Tech\Default\_.MAPLST.xdb` `NDb::MapList` that `SelectGameModeScreen.cpp` uses on Windows. The active catalog now records source/scanned/custom diagnostics (`source=Tech/Default/_.MAPLST.xdb`, `scanned=112`, `custom=41/41`) and recomputes visible map counts from the projected 41-entry list. The OpenGL bridge map rows, selected-map preview, local match generation, and create-game bootstrap now index the same custom-list order as the Windows-authored starting UI; the default selected map is now the first custom-list map (`Пограничье` / `Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb`) instead of the old broad-scan sort, while `--map Aram` still resolves ARAM inside the filtered catalog. This is still Linux bootstrap/UI bridge code only and leaves the Windows path untouched.
- Verification after the UI custom-map-list parity chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 3 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-06.35.27/linux-client-shell.log`, and reported `mapCatalogSource=Tech/Default/_.MAPLST.xdb`, `mapCatalogCount=41`, `mapCatalogScannedCount=112`, `mapCatalogCustomMatchedCount=41`, `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`, `UI games runtime: ready=yes entries=3`, and `UI maps runtime: ready=yes entries=41`. The selector smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 1 --map Aram --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-06.36.58/linux-client-shell.log`, and proved the filtered catalog still selects `ARAM (Maps/Multiplayer/ARAM/_.ADMPDSCR.xdb)`. The `--bootstrap-create-game --seconds 6 --diagnostics-overlay` smoke exited `0`, wrote `Bin/logs/2026.05.30-06.35.36/linux-client-shell.log`, kept the visible menu inactive once loading starts, and reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, `fog=205x205`, and `mapObjects=7458` on the default MOBA custom-list map.
- Latest bootstrap 3D preview chunk: `PW_Client/Game.cpp` now draws the first visible native OpenGL 3D bootstrap scene during the create-game/loading path. It uses the selected map's parsed tactical markers and map-object bounds to render an isometric ground plane, grid, border, and colored objective/building/spawn/shop/glyph marker boxes behind the Linux loading/menu overlay. This is real map-derived Linux OpenGL geometry, not the final production scene renderer: it does not yet draw terrain meshes, characters, animations, materials, lighting, particles, or gameplay camera output. The code is Linux overlay/bootstrap-only and leaves the Windows DirectX client path untouched.
- Verification after the bootstrap 3D preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 3 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-07.02.06/linux-client-shell.log`, and reported `Bootstrap 3D preview: ready=yes active=no markers=102 source=selected-map-tactical-markers` because the lobby is not on the loading screen. The create-game diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-07.02.14/linux-client-shell.log`, and reported `bootstrap3DPreviewReady=yes`, `bootstrap3DPreviewActive=yes`, `bootstrap3DPreviewMarkers=102`, plus the existing `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, `fog=205x205`, and `mapObjects=7458` proof. A no-diagnostics visual run `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 8 --bootstrap-create-game` exited `0`, wrote `Bin/logs/2026.05.30-07.02.38/linux-client-shell.log`, also reported `Bootstrap 3D preview: ready=yes active=yes markers=102`, and saved `/tmp/primeworld-bootstrap-3d-preview.png`.
- Latest bootstrap selected-hero 3D preview chunk: `PW_Client/Game.cpp` now draws a selected-hero native OpenGL preview panel on the default lobby bridge and on the create-game/loading bridge. It uses the selected lineup hero metadata/title/DB id to choose a deterministic accent color and renders a primitive humanoid mannequin with a base, torso, head, limbs, and weapon/staff shape. The preview auto-spins, `A`/`D` rotate it, `C` resets the yaw, and left-mouse drag inside the preview rotates it. This is intentionally Linux overlay/bootstrap-only and leaves the Windows DirectX client path untouched.
- Historical limitation for the first selected-hero preview: the first version was a primitive OpenGL stand-in keyed from selected-hero metadata and did not load the real hero mesh, material, skeleton, animation graph, FaceFX, particles, or production lighting. This is now superseded by the real skin-payload mesh chunk below: the preview can draw real untextured `.skin` triangles, but still does not yet perform production material/skeleton/animation rendering.
- Verification after the selected-hero 3D preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 3 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-07.29.09/linux-client-shell.log`, and reported `hero3DPreviewReady=yes`, `hero3DPreviewAssetRendering=no`, `hero3DPreviewYaw=28`, and `bootstrap3DPreviewActive=no`. The create-game diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-07.30.09/linux-client-shell.log`, reported the same hero-preview diagnostics, kept the map-marker preview active with `bootstrap3DPreviewActive=yes` / `bootstrap3DPreviewMarkers=102`, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, scheduler/transceiver/PFWorld/TileMap, `fog=205x205`, and `mapObjects=7458`. A no-diagnostics visual run `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-07.30.25/linux-client-shell.log`, reported `Hero 3D preview: ready=yes source=linux-opengl-primitive-selected-hero yaw=28.0 assetRendering=no`, and saved `/tmp/primeworld-hero-3d-preview.png`.
- Latest selected-hero real scene-asset metadata chunk: the selected-hero OpenGL preview is still a primitive fallback, but it is now driven by the real selected hero scene asset instead of only hero id/title metadata. `PW_Client/Game.cpp` resolves the selected `NDb::Hero` through `SessionRoot->logicRoot->heroes`, selects the first usable `heroSceneObjects[]` / `sceneObject` / `heroSkins[].sceneObject` candidate, walks the resolved `NDb::DBSceneObject` and nested `NDb::DBSceneComponent` tree, and records the real SOBJ DBID, descriptor file, root/nested attachment counts, component type counts, skin-part count, animation count, FaceFX key count, material reference count, payload reference/resolution counts, skeleton file, first skin geometry, first animation file, animation graph DBID, and merged AABB. The OpenGL fallback now draws a wireframe AABB derived from that real asset around the primitive mannequin and labels the resolved asset DBID. Windows remains untouched.
- Historical limitation after the scene-asset metadata chunk, superseded by the next skin-payload chunk: `hero3DPreviewAssetResolved=yes` then meant the Linux bootstrap had resolved and inspected the real selected hero `DBSceneObject` and payload metadata, but did not yet mean the real mesh was rendered. The following skin-payload mesh chunk changes that preview status to real untextured `.skin` triangle rendering while still deferring production materials/skeleton/animation.
- Verification after the scene-asset metadata chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 3 --diagnostics-overlay` exited `0` and reported `Selected hero scene asset: resolved=yes dbid=Heroes/Plane/_.SOBJ.xdb components=60 animated=1 skinParts=4 animations=17 payloads=57/97 bounds=yes`, plus `Hero 3D preview: ready=yes source=linux-opengl-hero-scene-metadata-primitive ... assetRendering=no assetResolved=yes`. The clean create-game diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-07.41.41/linux-client-shell.log`, reported `bootstrap3DPreviewActive=yes`, `selectedHeroSceneAssetResolved=yes`, `selectedHeroSceneAssetDbid=Heroes/Plane/_.SOBJ.xdb`, `hero3DPreviewAssetResolved=yes`, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, `fog=205x205`, and `mapObjects=7458`. A no-diagnostics visual run `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-07.41.01/linux-client-shell.log`, reported the same selected-hero scene-asset metadata, and saved `/tmp/primeworld-hero-scene-asset-preview.png`.
- Latest selected-hero real skin-payload mesh chunk: `PW_Client/Game.cpp` now parses the selected hero's real H6/XKIN `.skin` payload on Linux and draws the payload-derived triangle mesh in the OpenGL hero preview when readable. The parser validates the shared `H6GeometryFileHeader`, fragment table, vertex/index buffer offsets, and `FLOAT3 POSITION` descriptors, extracts real indexed triangles into a lightweight Linux preview buffer, records mesh counts/bounds/stride/position offset in diagnostics, and leaves the existing primitive mannequin as fallback when a skin payload cannot be read. This is still Linux overlay/bootstrap code only and does not touch the Windows DirectX path.
- Important limitation after the skin-payload mesh chunk: `hero3DPreviewAssetRendering=yes` now means the Linux preview is drawing real untextured, unskinned triangle positions from the selected hero `.skin` file. It is not yet production character rendering: no materials/textures, skeleton skinning, animation sampling, equipment/skin switching, FaceFX, particles, lighting, or renderer-owned mesh buffers are active in the preview. The next real character step is to move this from a bootstrap payload reader into the maintained Linux renderer path by loading `Render::MeshGeometry`/`SkeletalMesh` data under OpenGL/null-render-safe ownership, then bind skeleton and animation data.
- Verification after the skin-payload mesh chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-07.52.40/linux-client-shell.log`, and reported `Selected hero mesh payload: ready=yes fragments=1 vertices=16027 triangles=5784 drawn=5784 file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Shadow/_Exidna_geo.skin` plus `Hero 3D preview: ready=yes source=linux-opengl-hero-skin-payload-mesh yaw=28.0 assetRendering=yes assetResolved=yes`. The deeper create-game diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-07.56.40/linux-client-shell.log`, kept `bootstrap3DPreviewActive=yes`, reported the same selected-hero mesh proof, and still reached `Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, `fog=130x130`, and `mapObjects=2468` on ARAM. A visual run with the same `--map Aram --hero shadow --seconds 18` exited `0`, wrote `Bin/logs/2026.05.30-07.53.18/linux-client-shell.log`, reported the same mesh proof, and saved `/tmp/primeworld-hero-real-skin-mesh-preview.png` (`1280 x 720` PNG) showing the real cyan hero mesh in the top-right preview pane.
- Latest selected-hero multi-skin-part mesh chunk: `PW_Client/Game.cpp` no longer treats the first resolved `SkinPart::geometryFileName` as the whole character. The Linux scene-asset probe now records every unique resolved skin geometry payload for the selected hero, keeps the original first-file field for compatibility diagnostics, parses each H6/XKIN `.skin` payload through a per-file reader, and merges valid fragments, vertex/index counts, bounds, and preview triangles into one aggregate OpenGL hero mesh. The preview has a 48k-triangle aggregate sampling cap, logs `selectedHeroSceneAssetSkinGeometryFiles`, the first eight resolved payload paths, `selectedHeroSceneAssetMeshPayloads`, aggregate bounds readiness, and whether the triangle cap was hit. Windows DirectX code remains untouched.
- Important limitation after the multi-skin-part mesh chunk: this improves completeness for heroes whose real character is split across multiple skin payloads, such as Plane's body plus rockets/bomb, but it is still an untextured/unskinned bootstrap mesh preview. Materials, texture binding, skeleton skinning, animation graph sampling, attachment transforms, particles, lighting, and renderer-owned mesh buffers are still deferred to the production Linux renderer path.
- Verification after the multi-skin-part mesh chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.05.48/linux-client-shell.log`, selected Plane/Aviator, and reported `Selected hero mesh payload: ready=yes payloads=4/4 fragments=5 vertices=25706 triangles=9566 drawn=9566 file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Plane/_.skin` plus `selectedHeroSceneAssetSkinGeometryFiles=4` for `_.skin`, `__rocket_l.skin`, `__rocket_r.skin`, and `__bomb.skin`. The regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.06.04/linux-client-shell.log`, and kept Shadow/Kara on the single-payload path with `payloads=1/1`, `vertices=16027`, and `triangles=5784`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.06.19/linux-client-shell.log`, kept `hero3DPreviewAssetRendering=yes`, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero UV/diffuse texture chunk: `PW_Client/Game.cpp` now extracts `TEXCOORD0` `FLOAT2` data from the selected hero H6/XKIN `.skin` vertex declaration, records full-coverage UV diagnostics, walks skin-part material references for diffuse sampler texture filenames, loads direct DDS texture payloads such as `/Heroes/Plane/_.dds` and `/Heroes/Shadow/ehidna2.dds`, uploads the first resolved diffuse texture into a cached OpenGL texture, and draws the preview mesh textured when both UVs and a diffuse texture are available. The preview still falls back to the previous accent-colored mesh if the texture or UV path is missing. This remains Linux overlay/bootstrap-only and does not alter the Windows DirectX renderer path.
- Historical limitation after the first UV/diffuse texture chunk, superseded by the next material-binding chunk: the selected hero became a real textured mesh preview for tested heroes, but the bootstrap initially bound one preview diffuse texture for the aggregate mesh. The following material-binding chunk keeps the same bootstrap scope but now carries material texture indices through mesh parsing.
- Verification after the UV/diffuse texture chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.19.34/linux-client-shell.log`, selected Plane/Aviator, and reported `Selected hero mesh payload: ready=yes payloads=4/4 fragments=5 vertices=25706 triangles=9566 drawn=9566 uv=9566 texture=yes file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Plane/_.skin` plus `Selected hero diffuse texture: /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Plane/_.dds size=1024x1024` and `selectedHeroSceneAssetMeshTexCoordTriangles=9566`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.20.57/linux-client-shell.log`, and reported `Selected hero mesh payload: ready=yes payloads=1/1 fragments=1 vertices=16027 triangles=5784 drawn=5784 uv=5784 texture=yes file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Shadow/_Exidna_geo.skin` plus `Selected hero diffuse texture: /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Shadow/ehidna2.dds size=1024x1024`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.21.11/linux-client-shell.log`, kept `hero3DPreviewAssetRendering=yes`, `uv=5784`, and `texture=yes`, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero material-texture binding chunk: the Linux hero preview now preserves the skin-part material-reference order and maps each H6/XKIN fragment's `materialID` to the corresponding diffuse texture index. `PW_Client/Game.cpp` stores per-geometry-payload material texture mappings, records loaded texture counts in diagnostics, carries a texture index on preview vertices, caches multiple OpenGL diffuse textures per overlay, and switches texture batches while drawing triangles. Tested Plane and Shadow still only resolve one diffuse texture each, but the preview path is now ready for heroes/assets with multiple material diffuse textures instead of hard-wiring the entire aggregate mesh to the first DDS. Windows remains untouched.
- Historical limitation after the material-texture binding chunk, superseded by the next normal/lighting chunk: the bootstrap preview then had real geometry, UVs, and material-indexed diffuse texture binding, but still lacked normals and lighting. The following chunk keeps the same bootstrap scope but now extracts vertex normals and applies simple per-vertex lighting in the OpenGL preview.
- Verification after the material-texture binding chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.28.13/linux-client-shell.log`, selected Plane/Aviator, and reported `Selected hero mesh payload: ready=yes payloads=4/4 fragments=5 vertices=25706 triangles=9566 drawn=9566 uv=9566 texture=yes textures=1 file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Plane/_.skin`, `selectedHeroSceneAssetSkinDiffuseTexturesLoaded=1`, and `selectedHeroSceneAssetMeshTexCoordTriangles=9566`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.28.30/linux-client-shell.log`, and reported `Selected hero mesh payload: ready=yes payloads=1/1 fragments=1 vertices=16027 triangles=5784 drawn=5784 uv=5784 texture=yes textures=1 file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Shadow/_Exidna_geo.skin` plus `Data/Heroes/Shadow/ehidna2.dds`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.28.45/linux-client-shell.log`, kept `uv=5784`, `texture=yes`, `textures=1`, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero normal/lighting chunk: `PW_Client/Game.cpp` now extracts `NORMAL` `FLOAT3` data from the H6/XKIN vertex declaration for each readable hero skin fragment, normalizes the vectors, records normal coverage and offset diagnostics, merges normal counts across multi-payload heroes, and uses those normals to modulate the material-indexed diffuse preview with simple per-vertex OpenGL lighting. The transform maps the mesh normal into the preview coordinate basis used by the existing hero renderer, and the untextured fallback still receives the same normal-based brightness. This remains Linux overlay/bootstrap-only and does not alter the Windows DirectX renderer path.
- Historical limitation after the selected-hero normal/lighting chunk, superseded by the next skinning-stream diagnostics chunk: the bootstrap preview then had real geometry, UVs, material-indexed diffuse DDS binding, extracted normals, and simple preview lighting, but did not yet prove the skeletal blend-weight/blend-index stream. The following chunk keeps the same bootstrap scope but now parses that stream and records coverage.
- Verification after the selected-hero normal/lighting chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.40.06/linux-client-shell.log`, selected Plane/Aviator, and reported `Selected hero mesh payload: ready=yes payloads=4/4 fragments=5 vertices=25706 triangles=9566 drawn=9566 uv=9566 normals=9566 texture=yes textures=1 file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Plane/_.skin`, `selectedHeroSceneAssetMeshNormalReady=yes`, `selectedHeroSceneAssetMeshNormalTriangles=9566`, and `selectedHeroSceneAssetMeshNormalOffset=12`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.41.06/linux-client-shell.log`, and reported `Selected hero mesh payload: ready=yes payloads=1/1 fragments=1 vertices=16027 triangles=5784 drawn=5784 uv=5784 normals=5784 texture=yes textures=1 file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Shadow/_Exidna_geo.skin` plus `selectedHeroSceneAssetMeshNormalReady=yes`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.41.21/linux-client-shell.log`, kept `uv=5784`, `normals=5784`, `texture=yes`, `textures=1`, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero skinning-stream diagnostics chunk: `PW_Client/Game.cpp` now parses skeletal `BLENDWEIGHT0` `FLOAT4` and `BLENDINDICES0` packed-byte streams from H6/XKIN hero skin payloads. The lightweight preview vertex now carries four blend weights and four blend indices, the parser validates non-empty finite weights, records full drawn-triangle skinning coverage, records sampled preview-vertex coverage, and logs the first blend-weight/blend-index offsets. The renderer still draws the bind-pose mesh for now; this chunk is about proving the native Linux path can read the skinning data needed for the next skeleton/animation step. Windows remains untouched.
- Historical limitation after the selected-hero skinning-stream diagnostics chunk, superseded by the next skeleton-payload diagnostics chunk: the bootstrap preview then had real geometry, UVs, material-indexed diffuse DDS binding, extracted normals, simple preview lighting, and parsed blend weights/indices, but did not yet prove that the resolved skeleton payload could be parsed and validated.
- Verification after the selected-hero skinning-stream diagnostics chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.47.52/linux-client-shell.log`, selected Plane/Aviator, and reported `Selected hero mesh payload: ready=yes payloads=4/4 fragments=5 vertices=25706 triangles=9566 drawn=9566 uv=9566 normals=9566 skin=9566 texture=yes textures=1 file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Plane/_.skin`, `selectedHeroSceneAssetMeshSkinningReady=yes`, `selectedHeroSceneAssetMeshSkinningTriangles=9566`, `selectedHeroSceneAssetMeshSkinningVertices=28698`, `selectedHeroSceneAssetMeshBlendWeightOffset=32`, and `selectedHeroSceneAssetMeshBlendIndicesOffset=48`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.48.10/linux-client-shell.log`, and reported `Selected hero mesh payload: ready=yes payloads=1/1 fragments=1 vertices=16027 triangles=5784 drawn=5784 uv=5784 normals=5784 skin=5784 texture=yes textures=1 file=/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Data/Heroes/Shadow/_Exidna_geo.skin`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-08.48.26/linux-client-shell.log`, kept `uv=5784`, `normals=5784`, `skin=5784`, `texture=yes`, `textures=1`, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero skeleton-payload diagnostics chunk: `PW_Client/Game.cpp` now resolves the selected animated scene component's `skeletonFileName` and parses the real `SkeletonData` payload on Linux. The bootstrap probe validates `StaticVector` ranges for inverted bind-pose matrices, parent IDs, joint names, and name order; checks joint-count/vector-size consistency; detects invalid non-root parent links and parent-chain cycles; validates finite inverted bind-pose matrices; validates unique name-order entries and static-string bounds; and records skeleton file, file size, joint count, parent count, root count, max depth, root joint name, and first sorted joint name in both stdout/log diagnostics. This is Linux overlay/bootstrap diagnostics only; it leaves the Windows DirectX path untouched and still does not apply the skeleton to the preview mesh.
- Historical limitation after the skeleton-payload diagnostics chunk, superseded by the next animation-payload diagnostics chunk: the bootstrap preview then had real geometry, UVs, material-indexed diffuse DDS binding, extracted normals, simple preview lighting, parsed blend weights/indices, and a validated resolved skeleton payload for tested heroes, but it still did not prove that the resolved `.anim` payloads could be parsed and matched to the skeleton joint count.
- Verification after the skeleton-payload diagnostics chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.02.10/linux-client-shell.log`, selected Plane/Aviator, and reported `Selected hero scene asset: resolved=yes dbid=Heroes/Plane/_.SOBJ.xdb components=60 animated=1 skinParts=4 animations=17 payloads=57/97 bounds=yes skeleton=yes joints=53`, `selectedHeroSceneAssetSkeletonReady=yes`, `selectedHeroSceneAssetSkeletonJoints=53`, `selectedHeroSceneAssetSkeletonParents=53`, `selectedHeroSceneAssetSkeletonRoots=1`, `selectedHeroSceneAssetSkeletonMaxDepth=10`, `selectedHeroSceneAssetSkeletonParentTreeReady=yes`, `selectedHeroSceneAssetSkeletonBindPoseReady=yes`, and `selectedHeroSceneAssetSkeletonRootJoint=root`, while keeping `uv=9566`, `normals=9566`, `skin=9566`, `texture=yes`, and `textures=1`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.02.29/linux-client-shell.log`, and reported `skeleton=yes joints=55`, `selectedHeroSceneAssetSkeletonRootJoint=Root_M`, `selectedHeroSceneAssetSkeletonMaxDepth=9`, `selectedHeroSceneAssetSkeletonParentTreeReady=yes`, `selectedHeroSceneAssetSkeletonBindPoseReady=yes`, plus `uv=5784`, `normals=5784`, `skin=5784`, `texture=yes`, and `textures=1`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.02.45/linux-client-shell.log`, kept the same Shadow skeleton/mesh proof, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero animation-payload diagnostics chunk: `PW_Client/Game.cpp` now records every resolved `Animation::animationFileName` payload from the selected `DBAnimatedSceneComponent`, parses the real `SkeletalAnimationData` payloads, validates the `ANIM` header/time bounds, track-table `StaticVector` ranges, skeleton joint-count match, finite position/rotation/scale keys, per-track key-count limits, and aggregates unique payload counts, track/key/constant-track counts, durations, max track keys, total file size, and signature diagnostics. This is still Linux overlay/bootstrap diagnostics only; Windows DirectX code remains untouched.
- Historical limitation after the animation-payload diagnostics chunk, superseded by the next CPU animation-skinning preview chunk: the bootstrap preview then proved real geometry, UVs, material-indexed diffuse DDS binding, extracted normals, simple preview lighting, parsed skinning streams, validated `SkeletonData`, and validated resolved `.anim` payloads for tested heroes, but it still drew the bind-pose mesh and did not yet build skinning matrices or deform vertices.
- Verification after the animation-payload diagnostics chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.13.11/linux-client-shell.log`, selected Plane/Aviator, and reported `Selected hero scene asset: resolved=yes dbid=Heroes/Plane/_.SOBJ.xdb components=60 animated=1 skinParts=4 animations=17 payloads=57/97 bounds=yes skeleton=yes joints=53 animation=yes anims=17/17 animJoints=53 maxDur=4.00`, `selectedHeroSceneAssetAnimationReady=yes`, `selectedHeroSceneAssetAnimationPayloads=17/17`, `selectedHeroSceneAssetAnimationSignature=ANIM`, `selectedHeroSceneAssetAnimationDurationMin=0.333333`, `selectedHeroSceneAssetAnimationDurationMax=4`, `selectedHeroSceneAssetAnimationPositionKeys=3455`, `selectedHeroSceneAssetAnimationRotationKeys=20423`, `selectedHeroSceneAssetAnimationScaleKeys=2401`, and `selectedHeroSceneAssetAnimationMaxTrackKeys=121`, while keeping `uv=9566`, `normals=9566`, `skin=9566`, `texture=yes`, and `textures=1`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.13.24/linux-client-shell.log`, and reported `skeleton=yes joints=55 animation=yes anims=21/21 animJoints=55 maxDur=4.67`, `selectedHeroSceneAssetAnimations=22` with `selectedHeroSceneAssetAnimationFiles=21` unique resolved payloads, `selectedHeroSceneAssetAnimationReady=yes`, `selectedHeroSceneAssetAnimationPayloads=21/21`, `selectedHeroSceneAssetAnimationSignature=ANIM`, `selectedHeroSceneAssetAnimationDurationMin=0.5`, `selectedHeroSceneAssetAnimationDurationMax=4.66667`, `selectedHeroSceneAssetAnimationPositionKeys=5969`, `selectedHeroSceneAssetAnimationRotationKeys=51250`, `selectedHeroSceneAssetAnimationScaleKeys=3300`, and `selectedHeroSceneAssetAnimationMaxTrackKeys=141`, plus `uv=5784`, `normals=5784`, `skin=5784`, `texture=yes`, and `textures=1`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.13.41/linux-client-shell.log`, kept the same Shadow skeleton/mesh/animation proof, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Historical selected-hero CPU animation-skinning preview chunk: `PW_Client/Game.cpp` mapped H6/XKIN fragment-local `BLENDINDICES0` through the payload `GeometryReindex` table to real skeleton joint indices, recorded reindexed/invalid influence diagnostics, kept the first selected animation and skeleton payloads resident in the Linux preview, sampled `SkeletalAnimationData` tracks at the same 30 FPS convention as `Render::SkeletalAnimationSampler`, composed parent bone matrices like `Render::SkeletonWrapper::Update`, multiplied by `SkeletonData::invertedBindPoseBones`, and CPU-deformed the selected hero preview vertices/normals every OpenGL overlay frame. This remains the local fallback, but it is now superseded by the maintained Render geometry/sampler bridge below.
- Historical limitation after the CPU animation-skinning preview chunk, superseded by the maintained Render bridge: the visible selected hero was already a native OpenGL textured/lit mesh deformed by a sampled real `.anim` payload, but it was still driven entirely by bootstrap-local mesh/animation math rather than the maintained Render mesh loader and sampler/wrapper objects.
- Verification after the CPU animation-skinning preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.33.10/linux-client-shell.log`, selected Plane/Aviator, and reported `animation=yes anims=17/17 animJoints=53 maxDur=4.00 animSkin=yes`, `skinReindex=yes`, `hero3DPreviewSource=linux-opengl-hero-skeletal-animation-cpu-preview`, `selectedHeroSceneAssetMeshSkinningReindexedVertices=28698`, `selectedHeroSceneAssetMeshSkinningInvalidInfluences=0`, `selectedHeroSceneAssetAnimationSkinningReady=yes`, `selectedHeroSceneAssetAnimationSkinningJoints=53`, `selectedHeroSceneAssetAnimationSkinningTriangles=9566`, `selectedHeroSceneAssetAnimationSkinningVertices=28698`, `selectedHeroSceneAssetAnimationSkinningDuration=1.5`, and first sampled animation file `Data/Heroes/Plane/attack1.anim`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0` in the same log directory and reported `animation=yes anims=21/21 animJoints=55 maxDur=4.67 animSkin=yes`, `skinReindex=yes`, `selectedHeroSceneAssetMeshSkinningReindexedVertices=17352`, `selectedHeroSceneAssetMeshSkinningInvalidInfluences=0`, `selectedHeroSceneAssetAnimationSkinningReady=yes`, `selectedHeroSceneAssetAnimationSkinningJoints=55`, `selectedHeroSceneAssetAnimationSkinningTriangles=5784`, `selectedHeroSceneAssetAnimationSkinningVertices=17352`, `selectedHeroSceneAssetAnimationSkinningDuration=2`, and first sampled animation file `Data/Heroes/Shadow/idle1.anim`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0` in the same log directory, kept `animSkin=yes` and the CPU-skinned Shadow hero preview source, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero maintained Render geometry/sampler bridge chunk: `Render/renderresourcemanager.cpp` now gives the `PW_LINUX_NULL_RENDER` branch a real memory-stream `RenderResourceManager::LoadMeshGeometry(...)` implementation instead of returning null. The Linux null-render loader now builds maintained `Render::MeshGeometry` objects from H6/XKIN memory payloads, creates null-render vertex/index buffers, fills primitive DIP/material IDs, copies skeletal `GeometryReindex` tables into null-render-owned storage, and releases those copied reindex blocks from `RenderResourceManager::Release()`. This is Linux/null-render-only; the Windows/full-render implementation remains under its existing `#else`.
- `PW_Client/Game.cpp` now probes every selected hero `.skin` payload through that maintained Render mesh loader and logs `selectedHeroSceneAssetMeshRendererGeometryReady`, payload counts, renderer fragment/material/triangle counts, and renderer reindex readiness. The same selected-hero CPU preview now drives animation skin matrices through real `Render::SkeletalAnimationSampler` and `Render::SkeletonWrapper` first, copying renderer-owned skin matrices for the OpenGL CPU-deformed preview and keeping the local sampler/composition code only as fallback. The active preview source now reports `linux-opengl-hero-render-sampler-cpu-skin-preview`; Windows DirectX code remains untouched.
- Historical limitation after the maintained Render bridge, superseded by the renderer-owned SkeletalMesh queue chunk below: the selected hero preview then proved selected `.skin` payloads were accepted by maintained Render mesh geometry loading and that animation skin matrices came from maintained Render sampler/wrapper code first, but renderer-owned `SkeletalMesh` construction, skin-part lifecycle, and batch queue submission were still inert.
- Verification after the maintained Render geometry/sampler bridge: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.53.48/linux-client-shell.log`, selected Plane/Aviator, and reported `renderGeom=yes/4`, `renderReindex=yes`, `selectedHeroSceneAssetMeshRendererGeometryPayloads=4/4`, `selectedHeroSceneAssetMeshRendererGeometryFragments=5`, `selectedHeroSceneAssetMeshRendererGeometryTriangles=9566`, `selectedHeroSceneAssetAnimationSkinningRendererSamplerReady=yes`, `selectedHeroSceneAssetAnimationSkinningRendererSkeletonReady=yes`, `hero3DPreviewSource=linux-opengl-hero-render-sampler-cpu-skin-preview`, `hero3DPreviewAnimationSkinningRenderer=yes`, `selectedHeroSceneAssetMeshSkinningReindexedVertices=28698`, and `selectedHeroSceneAssetMeshSkinningInvalidInfluences=0`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.54.05/linux-client-shell.log`, and reported `renderGeom=yes/1`, `renderReindex=yes`, `selectedHeroSceneAssetMeshRendererGeometryPayloads=1/1`, `selectedHeroSceneAssetMeshRendererGeometryTriangles=5784`, `selectedHeroSceneAssetAnimationSkinningRendererSamplerReady=yes`, `selectedHeroSceneAssetAnimationSkinningRendererSkeletonReady=yes`, `hero3DPreviewAnimationSkinningRenderer=yes`, `selectedHeroSceneAssetMeshSkinningReindexedVertices=17352`, and `selectedHeroSceneAssetMeshSkinningInvalidInfluences=0`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-09.54.20/linux-client-shell.log`, kept the same Render geometry/reindex and Render animation-skinning proof, and still reached loading screen, map-loading job, scheduler, transceiver/PFWorld, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero renderer-owned SkeletalMesh queue chunk: the Linux null-render `RenderResourceManager` now file-loads and caches real skeleton, skeletal animation, static mesh, and skeletal mesh resources through `RootFileSystem`, with separate static-mesh cache keys for appended-color and plain geometry and owned raw/reindex block cleanup in `Release()`. `Render/SkeletalMesh.cpp` now does real Linux-null `Initialize(...)`, `AddSkinPart(...)`, skeleton-wrapper creation, geometry-backed slot setup, `UpdateReindexMap()`, `RenderToQueue(...)`, enable/disable, remove, material traversal, and slot/material cleanup instead of stopping at no-op renderer ownership. `Render/smartrenderer.cpp` now counts null-render draw DIPs/triangles when queued primitives are rendered, so `BatchQueue::Render()` has observable maintained-render output on Linux. Windows/full-render code remains under the existing `#else` path.
- `PW_Client/Game.cpp` now probes the selected hero through the renderer-owned `Render::SkeletalMesh` path in addition to the visible OpenGL CPU-skinned preview. For each selected animated scene component, it loads the real skeleton and skin parts through `RenderResourceManager`, toggles all skin slots off to prove disabled slots do not queue, toggles them back on, submits the `SkeletalMesh` to a real `Render::BatchQueue`, renders the queue through null `SmartRenderer`, records queued batch/DIP/triangle/material-switch counts, removes the skin parts, and verifies the removed mesh queues nothing. The animation-skinning preview also keeps RootFileSystem-relative skeleton/animation resource paths so the maintained `RenderResourceManager::LoadSkeletonData(...)` and `LoadSkeletalAnimation(...)` wrappers are used first during both setup and per-frame skin-matrix sampling; the absolute file paths remain only for direct diagnostic payload reads.
- Current limitation after the renderer-owned SkeletalMesh queue chunk: this is still not production character rendering. The selected hero now reaches renderer-owned `SkeletalMesh` object construction, skin-part reindexing, batch queue submission, and null-render DIP/triangle accounting, but the visible hero remains the bootstrap OpenGL CPU-deformed triangle preview. The next rendering wall is replacing that preview path with renderer-owned OpenGL batch/material drawing: real material instances/textures/shader pins, vertex declarations/streams, skeleton constants or CPU-skinned vertex buffers in the renderer path, animation graph/state selection, attachments, particles/FX, alpha sorting, production lighting/camera/scene ownership, terrain/map-object rendering, and loaded-PFWorld scene rendering.
- Verification after the renderer-owned SkeletalMesh queue chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The default selected-hero diagnostics smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-10.20.10/linux-client-shell.log`, selected Plane/Aviator, and reported `selectedHeroSceneAssetRendererSkeletalMeshReady=yes`, `selectedHeroSceneAssetRendererSkeletalMeshSkinParts=4/4`, `selectedHeroSceneAssetRendererSkeletalMeshSlots=5`, `selectedHeroSceneAssetRendererSkeletalMeshQueuedBatches=5`, `selectedHeroSceneAssetRendererSkeletalMeshRenderedDips=5`, `selectedHeroSceneAssetRendererSkeletalMeshRenderedTriangles=9566`, `selectedHeroSceneAssetRendererSkeletalMeshDisableCullReady=yes`, `selectedHeroSceneAssetRendererSkeletalMeshRemoveReady=yes`, `selectedHeroSceneAssetAnimationSkinningRendererResourceSkeletonReady=yes`, `selectedHeroSceneAssetAnimationSkinningRendererResourceAnimationReady=yes`, and `hero3DPreviewAnimationSkinningRenderer=yes`. The Shadow/Kara regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8 --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-10.20.24/linux-client-shell.log`, and reported `selectedHeroSceneAssetRendererSkeletalMeshReady=yes`, `selectedHeroSceneAssetRendererSkeletalMeshSkinParts=1/1`, `selectedHeroSceneAssetRendererSkeletalMeshQueuedBatches=1`, `selectedHeroSceneAssetRendererSkeletalMeshRenderedDips=1`, `selectedHeroSceneAssetRendererSkeletalMeshRenderedTriangles=5784`, `selectedHeroSceneAssetAnimationSkinningRendererResourceSkeletonReady=yes`, and `selectedHeroSceneAssetAnimationSkinningRendererResourceAnimationReady=yes`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game --diagnostics-overlay` exited `0`, wrote `Bin/logs/2026.05.30-10.20.39/linux-client-shell.log`, kept the same Shadow renderer-owned SkeletalMesh proof, and still reached `loadingRuntimeReady=yes`, `bootstrap3DPreviewActive=yes`, `uiRootRuntimeGameTransceiverReady=yes`, `uiRootRuntimeGameTransceiverWorldAttached=yes`, `fog=130x130`, and `uiRootRuntimeGameWorldMapObjects=2468`.
- Latest selected-hero OpenGL renderer-owned SkeletalMesh draw chunk, superseding the "visible hero remains CPU-deformed" limitation above: the runnable Linux client now draws the selected hero preview through renderer-owned `Render::SkeletalMesh` batches before falling back to the old CPU-deformed OpenGL preview. The Linux null-render `IDirect3DVertexDeclaration9` now preserves H6 vertex-element metadata, `Render/smartrenderer.cpp` tracks the currently bound vertex/index buffers, vertex declaration, stride, and stream offset, and executable-only `PW_LINUX_OPENGL_BOOTSTRAP` helpers expose immediate-mesh enable/preview-transform/skeletal-matrix state. When `Primitive::Draw()` reaches `SmartRenderer::DrawIndexedPrimitive(...)`, the bootstrap path now replays triangle-list H6 vertex/index data through OpenGL immediate mode, reads `POSITION0`, `NORMAL0`, `TEXCOORD0`, `BLENDWEIGHT0`, and `BLENDINDICES0`, applies the current fragment's skin matrices, maps the model into the existing character-preview camera, and emits textured/lit GL vertices. All of this is guarded behind `PW_LINUX_NULL_RENDER` plus `PW_LINUX_OPENGL_BOOTSTRAP`; the Windows Direct3D renderer remains on its existing path.
- `Render/SkeletalMesh.cpp` now passes per-fragment skin matrices from `SkeletalMesh::PrepareRendererAfterMaterial(...)` into the OpenGL bootstrap draw helper. `PW_Client/Game.cpp` now builds a visible-preview `Render::SkeletalMesh` from RootFileSystem-relative skeleton/skin paths, samples animation through `Render::SkeletalAnimationSampler`, updates the real `SkeletonWrapper`, queues the mesh into `Render::BatchQueue`, binds the best resolved diffuse texture for each queued slot, and calls each batch's canonical `Prepare()` / `Draw()` path. `LinuxBootstrapScreenRuntime` and the end-of-run `input-summary` log now record final render-loop `finalHero3DPreviewRendererMeshDrawn`, batch count, and triangle count, because the older startup `hero3DPreviewRendererMesh*` lines are written before any OpenGL frames are drawn. Diagnostics-overlay-only runs still do not draw the character-preview panel, so use normal UI smokes for final render-loop mesh proof.
- Current limitation after the OpenGL renderer-owned SkeletalMesh draw chunk: this is still a bootstrap immediate-OpenGL bridge, not the production renderer. It recreates a local `SkeletalMesh` for the visible hero every frame, binds textures with a simple payload/material-slot heuristic, bypasses real shader/material pin evaluation, and does not yet render attachments, alpha-sorted fragments, particle/FX children, scene lighting, production camera ownership, terrain/map objects, or the loaded `PFWorld` scene. The important milestone is that the visible starting UI can now show the real selected hero via maintained `SkeletalMesh` -> `BatchQueue` -> `Primitive::Draw()` data flow instead of only the CPU preview mesh.
- Verification after the OpenGL renderer-owned SkeletalMesh draw chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal default UI smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-10.49.16/linux-client-shell.log`, and its final render-loop summary reports `finalHero3DPreviewRendererMeshDrawn=yes`, `finalHero3DPreviewRendererMeshBatches=5`, and `finalHero3DPreviewRendererMeshTriangles=9566` for Plane/Aviator. The normal Shadow/Kara smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-10.49.37/linux-client-shell.log`, and reports `finalHero3DPreviewRendererMeshDrawn=yes`, `finalHero3DPreviewRendererMeshBatches=1`, and `finalHero3DPreviewRendererMeshTriangles=5784`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game` exited `0`, wrote `Bin/logs/2026.05.30-10.49.54/linux-client-shell.log`, kept `finalHero3DPreviewRendererMeshDrawn=yes`, `finalHero3DPreviewRendererMeshBatches=1`, and `finalHero3DPreviewRendererMeshTriangles=5784`, and still reached `loadingRuntimeReady=yes`, `uiRootRuntimeGameTransceiverReady=yes`, `uiRootRuntimeGameTransceiverWorldAttached=yes`, `fog=130x130`, and `uiRootRuntimeGameWorldMapObjects=2468`.
- Latest selected-hero renderer-owned static scene-component mesh chunk: the visible hero preview now records static scene-component geometry payloads while walking the selected hero `DBSceneObject` tree, including each attached component placement, locator metadata, and material diffuse-texture mapping. The Linux null-render `Render::StaticMesh` path now loads real static mesh geometry through `RenderResourceManager::LoadStaticMeshGeometry(...)`, owns null materials, queues primitives through `BatchQueue`, and passes an object matrix into the OpenGL immediate bridge; `SmartRenderer` now applies that object matrix for non-skinned static vertices while clearing it for skeletal draws. `PW_Client/Game.cpp` draws those static payloads immediately after the renderer-owned skeletal hero batches, using `Render::StaticMesh -> BatchQueue -> Primitive::Draw()` and the same diffuse texture cache. For locator-backed static children, non-joint locators now apply the DB locator offset during scene probing and joint-backed locators resolve against the live `Render::SkeletonWrapper` bone matrices during the preview draw, so attachments such as Shadow's `Body` / `Back` locators are no longer only using raw parent DB placement. Windows/full-render code remains behind existing non-Linux paths.
- Historical limitation after the static scene-component chunk, superseded by the attach-flag placement parity chunk below: this brought renderer-owned static attachments/components into the visible selected-hero preview, including simple DB locator/bone placement, but the first version still used a simplified attached-component/local-placement composition instead of the real `SceneComponent::ConstructWorldPlacement(...)` flag flow.
- Verification after the renderer-owned static scene-component chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal default UI smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-11.14.40/linux-client-shell.log`, selected Plane/Aviator, reported `selectedHeroSceneAssetStaticGeometryPayloads=24`, and its final render-loop summary reports `finalHero3DPreviewRendererMeshDrawn=yes`, `finalHero3DPreviewRendererMeshBatches=29`, `finalHero3DPreviewRendererMeshTriangles=11932`, `finalHero3DPreviewRendererStaticMeshDrawn=yes`, `finalHero3DPreviewRendererStaticMeshPayloads=24`, `finalHero3DPreviewRendererStaticMeshBatches=24`, and `finalHero3DPreviewRendererStaticMeshTriangles=2366`. The normal Shadow/Kara smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-11.15.08/linux-client-shell.log`, reported `selectedHeroSceneAssetStaticGeometryPayloads=14`, `finalHero3DPreviewRendererMeshBatches=15`, `finalHero3DPreviewRendererMeshTriangles=7054`, `finalHero3DPreviewRendererStaticMeshPayloads=14`, `finalHero3DPreviewRendererStaticMeshBatches=14`, and `finalHero3DPreviewRendererStaticMeshTriangles=1270`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game` exited `0`, wrote `Bin/logs/2026.05.30-11.15.30/linux-client-shell.log`, kept the same Shadow static preview counters, and still reached `loadingRuntimeReady=yes`, `uiRootRuntimeGameTransceiverReady=yes`, `uiRootRuntimeGameWorldMapObjects=2468`, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-hero attach-flag static placement parity chunk: `PW_Client/Game.cpp` now stores each static scene-component payload's original placement, parent fallback placement, `AttachedSceneComponent::attachFlags`, locator flags, and component orientation instead of only storing one precomposed placement. The Linux preview helper mirrors the real `SceneComponent::ConstructWorldPlacement(...)` default-orientation path for parent rotation/scale, `ATTACHFLAGS_CANCELOFFSETSCALING`, `LOCATORATTACHFLAGS_CANCELNORMALLEVELLING`, locator/base/original composition, and live skeleton-wrapper joint fallback when a locator disables joint position/rotation/scale. Joint-backed static payloads are now re-resolved through that helper at draw time after `Render::SkeletonWrapper` updates; non-joint payloads use the same helper during scene probing. The remaining intentional gaps are camera-facing `SCORIENTATION_CAMERA` / `SCORIENTATION_CAMERAFIXEDZ`, animated time controllers, `STAYINWORLD` history, nested dynamic locator chains, production material/shader pins, alpha sorting, particles/FX, production scene lighting/camera ownership, terrain/map-object rendering, and the loaded `PFWorld` scene. Windows/full-render code remains untouched.
- Verification after the attach-flag static placement parity chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`. The normal default UI smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-11.26.14/linux-client-shell.log`, selected Plane/Aviator, reported `selectedHeroSceneAssetStaticGeometryPayloads=24`, and its final render-loop summary reports `finalHero3DPreviewRendererMeshDrawn=yes`, `finalHero3DPreviewRendererMeshBatches=29`, `finalHero3DPreviewRendererMeshTriangles=11932`, `finalHero3DPreviewRendererStaticMeshDrawn=yes`, `finalHero3DPreviewRendererStaticMeshPayloads=24`, `finalHero3DPreviewRendererStaticMeshBatches=24`, and `finalHero3DPreviewRendererStaticMeshTriangles=2366`. The final normal Shadow/Kara smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-11.30.35/linux-client-shell.log`, reported `selectedHeroSceneAssetStaticGeometryPayloads=14`, `finalHero3DPreviewRendererMeshBatches=15`, `finalHero3DPreviewRendererMeshTriangles=7054`, `finalHero3DPreviewRendererStaticMeshPayloads=14`, `finalHero3DPreviewRendererStaticMeshBatches=14`, and `finalHero3DPreviewRendererStaticMeshTriangles=1270`. The deeper create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 6 --bootstrap-create-game` exited `0`, wrote `Bin/logs/2026.05.30-11.26.51/linux-client-shell.log`, kept the same Shadow static preview counters, and still reached `loadingRuntimeReady=yes`, `uiRootRuntimeGameTransceiverReady=yes`, `uiRootRuntimeGameWorldMapObjects=2468`, `fog=130x130`, and `mapObjects=2468`.
- Latest selected-map static scene-object 3D preview chunk: the create-game/loading OpenGL 3D preview now draws a capped set of real selected-map static scene-object payloads, not only tactical marker boxes. `PW_Client/Game.cpp` resolves the selected `NDb::AdvMapDescription` / `NDb::AdvMap`, walks `SingleStateObject` and first normal-state `MultiStateObject` / `TreeObject` `DBSceneObject` candidates, extracts nested `DBStaticSceneComponent` geometry/material diffuse references, stores map-object root placement plus attached-component/locator placement through the same helper used by hero static attachments, and renders those payloads through the maintained `Render::StaticMesh -> BatchQueue -> Primitive::Draw()` path. The existing Linux OpenGL `SmartRenderer` immediate bridge is reused with a selected-map center/scale transform, map diffuse textures get their own overlay cache, and runtime/log counters now report collected map static scene objects/components/payloads/textures plus final rendered payload/batch/triangle counts. The current cap is intentional (`384` map objects, `128` static payloads, `24` diffuse textures) to keep bootstrap loading responsive; this is still Linux overlay/bootstrap-only and does not touch the Windows DirectX path.
- Verification after the selected-map static scene-object 3D preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --bootstrap-create-game --seconds 6` exited `0`, wrote `Bin/logs/2026.05.30-11.43.53/linux-client-shell.log`, collected `mapStaticSceneObjectCandidates=29`, `mapStaticSceneComponents=145`, `mapStaticSceneGeometryPayloads=128`, `mapStaticSceneDiffuseTextures=24`, `mapStaticScenePayloadLimitHit=yes`, and the final render-loop summary reports `finalMap3DPreviewRendererStaticMeshDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=128`, `finalMap3DPreviewRendererStaticMeshBatches=129`, and `finalMap3DPreviewRendererStaticMeshTriangles=19008` while still reaching `uiRootRuntimeGameTransceiverReady=yes` and `uiRootRuntimeGameWorldMapObjects=7458` on the default MOBA map. The normal menu smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --seconds 4` exited `0`, wrote `Bin/logs/2026.05.30-11.44.16/linux-client-shell.log`, kept the default visible lobby path active, collected the same capped map static payload preview metadata, and correctly left `bootstrap3DPreviewActive=no` outside the loading/create-game path.
- Latest selected-map terrain/animated scene-object 3D preview chunk: `PW_Client/Game.cpp` now adds a real selected-map terrain-height preview and capped animated scene-component mesh drawing to the create-game/loading OpenGL scene. The terrain path resolves `NDb::AdvMap::terrain`, parses the selected map `.heightmap` binary payload, records source dimensions/range, samples it down to a capped 96x96 OpenGL surface, and draws it under the map-object preview. The animated object path records capped `DBAnimatedSceneComponent` payloads from selected-map scene objects, resolves skeleton, first animation, skin geometry, and material diffuse mappings, samples the first animation through `Render::SkeletalAnimationSampler` / `Render::SkeletonWrapper`, and draws them through `Render::SkeletalMesh -> BatchQueue -> Primitive::Draw()` using the same Linux OpenGL immediate bridge and map diffuse cache. Caps are intentional (`96x96` terrain samples, `128` static payloads, `24` animated payloads, `24` diffuse textures); Windows/full-render code remains untouched.
- Verification after the selected-map terrain/animated scene-object 3D preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --bootstrap-create-game --seconds 6` exited `0`, wrote `Bin/logs/2026.05.30-15.51.25/linux-client-shell.log`, collected `mapTerrainHeightmapReady=yes`, `mapTerrainHeightmapSourceSize=820x820`, `mapTerrainHeightmapSampledSize=96x96`, `mapTerrainHeightmapSampledTriangles=18050`, `mapTerrainHeightmapRange=-32 -> 28.5193`, `mapAnimatedSceneComponents=46`, `mapAnimatedSceneSkinParts=46`, `mapAnimatedSceneGeometryPayloads=24`, `mapAnimatedScenePayloadsResolved=72`, and `mapAnimatedScenePayloadLimitHit=yes`. The final render-loop summary reports `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewTerrainSurfaceVertices=9216`, `finalMap3DPreviewTerrainSurfaceTriangles=18050`, `finalMap3DPreviewRendererStaticMeshDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=128`, `finalMap3DPreviewRendererStaticMeshTriangles=19008`, `finalMap3DPreviewRendererAnimatedMeshDrawn=yes`, `finalMap3DPreviewRendererAnimatedMeshPayloads=24`, `finalMap3DPreviewRendererAnimatedMeshBatches=25`, and `finalMap3DPreviewRendererAnimatedMeshTriangles=70149` while still reaching `uiRootRuntimeGameTransceiverReady=yes` and `uiRootRuntimeGameWorldMapObjects=7458` on the default MOBA map.
- Latest selected-map terrain-element mesh preview chunk: `PW_Client/Game.cpp` now also resolves real `NDb::Terrain::geometry.terrainElements` from the selected map terrain descriptor, records capped terrain-object static mesh payloads separately from gameplay map-object meshes, carries terrain-element placement/angle/scale, extracts `TerrainMaterial` `N_DiffuseMap`/`A_DiffuseMap`/`B_DiffuseMap` and terrain-object material diffuse mappings into the map diffuse cache, and draws those payloads through the same maintained `Render::StaticMesh -> BatchQueue -> Primitive::Draw()` OpenGL bridge. Caps are intentional (`192` terrain-element payloads, map diffuse texture cache raised to `32`); Windows/full-render code remains untouched.
- Verification after the selected-map terrain-element mesh preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --bootstrap-create-game --seconds 6` exited `0`, wrote `Bin/logs/2026.05.30-16.02.38/linux-client-shell.log`, collected `mapTerrainElementCount=1681`, `mapTerrainElementGeometryPayloads=192`, `mapTerrainElementPayloadReferences=192`, `mapTerrainElementPayloadsResolved=192`, and `mapTerrainElementPayloadLimitHit=yes`. The final render-loop summary reports `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewTerrainSurfaceTriangles=18050`, `finalMap3DPreviewTerrainElementMeshDrawn=yes`, `finalMap3DPreviewTerrainElementMeshPayloads=192`, `finalMap3DPreviewTerrainElementMeshBatches=192`, `finalMap3DPreviewTerrainElementMeshTriangles=33202`, `finalMap3DPreviewRendererStaticMeshPayloads=128`, `finalMap3DPreviewRendererStaticMeshTriangles=19008`, `finalMap3DPreviewRendererAnimatedMeshPayloads=24`, and `finalMap3DPreviewRendererAnimatedMeshTriangles=70149` while still reaching `uiRootRuntimeGameTransceiverReady=yes` and `uiRootRuntimeGameWorldMapObjects=7458` on the default MOBA map.
- Latest selected-map terrain layer/road preview chunk: `PW_Client/Game.cpp` now samples the selected terrain's real `layerMaskFile`, resolves the `TerrainLayers -> TerrainTypeInfo -> TerrainMaterial` chain, extracts each terrain layer diffuse texture, averages loaded layer textures into per-layer OpenGL preview colors, colors the sampled heightmap surface by authored terrain layer instead of only height, and draws real `DBNatureMap` attack/logic road polylines over the terrain surface with terrain-height-following vertices. The shared map diffuse cache cap is now `64` so terrain-layer, terrain-element, static-object, and animated-object preview textures can coexist. Windows/full-render code remains untouched.
- Verification after the selected-map terrain layer/road preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The pinned create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 2` exited `0`, wrote `Bin/logs/2026.05.30-16.31.47/linux-client-shell.log`, and had `inputTotalEvents=0` with `mapSelectedDescriptor=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb`. It collected `mapTerrainLayerMaskReady=yes`, `mapTerrainLayerMaskSize=656x656`, `mapTerrainLayerMaskBytes=430336`, `mapTerrainLayers=11`, `mapTerrainLayerMaterials=11`, `mapTerrainLayerDiffuseTextures=11`, `mapTerrainLayerDiffuseTexturesLoaded=11`, `mapTerrainLayerSampledUnique=10`, `mapTerrainNatureRoads=6`, `mapTerrainNatureRoadNodes=51`, and `mapTerrainNatureRoadSegments=45`. The final render-loop summary reports `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewTerrainSurfaceTriangles=18050`, `finalMap3DPreviewTerrainRoadsDrawn=yes`, `finalMap3DPreviewTerrainRoads=6`, `finalMap3DPreviewTerrainRoadSegments=45`, `finalMap3DPreviewTerrainElementMeshPayloads=192`, `finalMap3DPreviewTerrainElementMeshTriangles=33202`, `finalMap3DPreviewRendererStaticMeshPayloads=128`, `finalMap3DPreviewRendererStaticMeshTriangles=19008`, `finalMap3DPreviewRendererAnimatedMeshPayloads=24`, and `finalMap3DPreviewRendererAnimatedMeshTriangles=70149` while still reaching `uiRootRuntimeGameTransceiverReady=yes` and `uiRootRuntimeGameWorldMapObjects=7458`.
- Latest selected-map water-zone preview chunk: `PW_Client/Game.cpp` now scans every selected-map `NDb::AdvMapObject` for authored `NDb::AdvMapWaterZone` objects independently of the static/animated mesh preview caps, expands circle and polygon `FlatSplineDesc` data into world-space preview polygons, and draws translucent native OpenGL water fans/outlines over sampled terrain before roads, terrain elements, and scene objects. The Linux diagnostics now log source and final draw counters for water-zone count, point count, and triangle count. This is Linux bootstrap/OpenGL-only code; Windows/full-render code remains untouched.
- Verification after the selected-map water-zone preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The pinned create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 2` exited `0`, wrote `Bin/logs/2026.05.30-16.44.51/linux-client-shell.log`, and had `inputTotalEvents=0` with `mapSelectedDescriptor=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb`. It collected `mapWaterZones=4`, `mapWaterZonePoints=96`, `mapWaterZoneTriangles=88`, `bootstrap3DPreviewWaterZoneTriangles=88`, `finalMap3DPreviewWaterZonesDrawn=yes`, `finalMap3DPreviewWaterZones=4`, and `finalMap3DPreviewWaterZoneTriangles=88`. Existing map-preview counters remained stable in the same run: `mapTerrainLayerMaskReady=yes`, `mapTerrainLayerDiffuseTexturesLoaded=11`, `mapTerrainNatureRoadSegments=45`, `finalMap3DPreviewTerrainSurfaceTriangles=18050`, `finalMap3DPreviewTerrainRoadSegments=45`, `finalMap3DPreviewTerrainElementMeshTriangles=33202`, `finalMap3DPreviewRendererStaticMeshTriangles=19008`, `finalMap3DPreviewRendererAnimatedMeshTriangles=70149`, and `uiRootRuntimeGameWorldMapObjects=7458`.
- Latest selected-map scene-payload widening chunk: `PW_Client/Game.cpp` now lets the create-game/loading OpenGL map preview scan up to `2048` selected-map objects, records the actual scanned object count and whether the hard object scan cap was hit, raises the static scene-object payload cap from `128` to `256`, raises the animated scene-component payload cap from `24` to `48`, and raises the shared map diffuse texture cache from `64` to `96`. The scan still stops as soon as both static and animated payload caps are full, so it remains a bounded bootstrap preview rather than an uncapped production scene renderer. Windows/full-render code remains untouched.
- Verification after the selected-map scene-payload widening chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The pinned create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 2` exited `0`, wrote `Bin/logs/2026.05.30-16.52.54/linux-client-shell.log`, and had `mapSelectedDescriptor=Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb`. It scanned `mapStaticSceneObjectsScanned=74` of `7478` map objects before both payload caps filled, with `mapStaticSceneObjectScanLimitHit=no`, `mapStaticSceneObjectCandidates=60`, `mapStaticSceneObjectsResolved=60`, `mapStaticSceneComponents=258`, `mapStaticSceneGeometryPayloads=256`, `mapStaticSceneDiffuseTextures=75`, `mapAnimatedSceneComponents=64`, `mapAnimatedSceneSkinParts=64`, and `mapAnimatedSceneGeometryPayloads=48`. The final render-loop summary reports `finalMap3DPreviewRendererStaticMeshPayloads=256`, `finalMap3DPreviewRendererStaticMeshTriangles=53964`, `finalMap3DPreviewRendererAnimatedMeshPayloads=48`, and `finalMap3DPreviewRendererAnimatedMeshTriangles=133867`, while still preserving `mapWaterZones=4`, `finalMap3DPreviewWaterZoneTriangles=88`, `mapTerrainLayerMaskReady=yes`, `mapTerrainLayerDiffuseTexturesLoaded=11`, `mapTerrainNatureRoadSegments=45`, `finalMap3DPreviewTerrainSurfaceTriangles=18050`, `finalMap3DPreviewTerrainRoadSegments=45`, `finalMap3DPreviewTerrainElementMeshTriangles=33202`, and `uiRootRuntimeGameWorldMapObjects=7458`.
- Latest selected-map authored-guide preview chunk: `PW_Client/Game.cpp` now scans selected-map authored gameplay/navigation guide records independently of the scene mesh payload caps: `AdvMap::scriptAreas`, `NDb::ScriptPath`, `NDb::ScriptPolygonArea`, and `NDb::AdvMapCameraSpline` objects. The loading OpenGL 3D preview renders script-area circles, script paths, script polygons, and camera splines as terrain-following translucent overlays after the mesh preview, and logs both probe counters and final draw counters. Camera splines use the same base-placement transform as the gameplay Lua camera-spline path; script paths/polygons preserve the PFWorld/AI-container raw point semantics. Windows/full-render code remains untouched.
- Verification after the selected-map authored-guide preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The pinned create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 2` exited `0`, wrote `Bin/logs/2026.05.30-17.05.25/linux-client-shell.log`, and reported `mapScriptAreas=24`, `mapScriptAreaSegments=768`, `mapScriptPaths=0`, `mapCameraSplines=4`, `mapCameraSplineSegments=27`, `mapScriptPolygons=0`, `finalMap3DPreviewScriptAreasDrawn=yes`, `finalMap3DPreviewScriptAreas=24`, `finalMap3DPreviewScriptAreaSegments=768`, `finalMap3DPreviewCameraSplinesDrawn=yes`, `finalMap3DPreviewCameraSplines=4`, and `finalMap3DPreviewCameraSplineSegments=27`. Existing map-preview counters remained stable in the same run: `finalMap3DPreviewRendererStaticMeshPayloads=256`, `finalMap3DPreviewRendererStaticMeshTriangles=53964`, `finalMap3DPreviewRendererAnimatedMeshPayloads=48`, `finalMap3DPreviewRendererAnimatedMeshTriangles=133867`, `finalMap3DPreviewTerrainSurfaceTriangles=18050`, `finalMap3DPreviewWaterZoneTriangles=88`, `finalMap3DPreviewTerrainRoadSegments=45`, `finalMap3DPreviewTerrainElementMeshTriangles=33202`, and `uiRootRuntimeGameWorldMapObjects=7458`.
- Latest selected-map lighting/point-light preview chunk: `PW_Client/Game.cpp` now resolves the selected map `NDb::AdvMap::lightEnvironment`, `nightLightEnvironment`, and `pointLights` records for the Linux OpenGL loading preview. The bootstrap preview records day/night environment DBIDs, classifies authored `NDb::PointLightInfo` entries as day/night/neutral, and draws each point light as a terrain-following native OpenGL range ring plus center marker using the authored location, range, intensity, color, and flags. The light markers are bounded diagnostics/preview geometry and leave the Windows DirectX/full-render path untouched.
- Verification after the selected-map lighting/point-light preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The pinned MOBA create-game smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 2` exited `0`, wrote `Bin/logs/2026.05.30-17.15.13/linux-client-shell.log`, resolved both day/night light environments, and correctly reported `mapPointLights=0`, `bootstrap3DPreviewPointLightMarkers=0`, `finalMap3DPreviewPointLightsDrawn=no`, `finalMap3DPreviewPointLights=0`, and `finalMap3DPreviewPointLightMarkers=0` while preserving the existing terrain/scene counters. The point-light regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map NightMap --hero plane --bootstrap-create-game --seconds 2` exited `0`, wrote `Bin/logs/2026.05.30-17.15.14/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/NightMap/_.ADMPDSCR.xdb`, reported `mapLightEnvironmentResolved=yes`, `mapNightLightEnvironmentResolved=yes`, `mapPointLights=75`, `mapPointLightsDay=8`, `mapPointLightsNight=73`, `mapPointLightsNeutral=0`, `bootstrap3DPreviewPointLightMarkers=75`, `finalMap3DPreviewPointLightsDrawn=yes`, `finalMap3DPreviewPointLights=75`, and `finalMap3DPreviewPointLightMarkers=75`, and kept `finalMap3DPreviewRendererStaticMeshPayloads=256` plus `finalMap3DPreviewTerrainSurfaceDrawn=yes`.
- Latest selected-map rotation/longer-visual chunk: `PW_Client/Game.cpp` now keeps independent Linux map-preview yaw state alongside the existing hero yaw state. The loading map preview auto-rotates slowly, exposes manual map rotation on `Q`/`E`, resets map yaw with `R`, and supports mouse-drag rotation on the loading map outside the hero-preview rectangle; `A`/`D` remain reserved for the selected-hero preview. The final logs now record `map3DPreviewBaseYaw`, `map3DPreviewRenderedYaw`, `map3DPreviewInputCount`, and `map3DPreviewLastAction` so automated smokes can prove the rendered map angle changes even without synthetic input. This is Linux bootstrap/OpenGL-only code and leaves Windows untouched.
- Verification after the selected-map rotation/longer-visual chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The longer pinned first-3D smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-17.24.10/linux-client-shell.log`, kept `inputTotalEvents=0`, selected `Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb`, and reported `finalMap3DPreviewBaseYaw=-36`, `finalMap3DPreviewRenderedYaw=0.352165`, `finalMap3DPreviewInputCount=0`, `finalMap3DPreviewLastAction=auto`, `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, `finalMap3DPreviewRendererAnimatedMeshPayloads=48`, and `finalMap3DPreviewPointLights=0`. The longer NightMap point-light smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map NightMap --hero plane --bootstrap-create-game --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-17.23.17/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/NightMap/_.ADMPDSCR.xdb`, and reported `mapPointLights=75`, `mapPointLightsDay=8`, `mapPointLightsNight=73`, `bootstrap3DPreviewPointLightMarkers=75`, `finalMap3DPreviewPointLightsDrawn=yes`, `finalMap3DPreviewPointLights=75`, `finalMap3DPreviewPointLightMarkers=75`, `finalMap3DPreviewBaseYaw=-36`, `finalMap3DPreviewRenderedYaw=0.531693`, and the same stable `finalMap3DPreviewRendererStaticMeshPayloads=256` / `finalMap3DPreviewTerrainSurfaceDrawn=yes` counters.
- Latest selected-map camera-control/input-lock chunk: `PW_Client/Game.cpp` now gives the Linux OpenGL loading map preview a small camera state instead of yaw-only control. The preview logs and applies map pitch, zoom, and pan; `Z`/`X` zoom out/in, `I`/`K` tilt the isometric view, right-mouse drag pans the map outside the hero-preview rectangle, and `R` now resets yaw/pitch/zoom/pan together. Hidden map-browser and lineup navigation are also locked while the bootstrap hero/loading runtime is active, so mouse-wheel noise cannot change the selected map during a loading smoke; the visible loading screen plus map/hero preview controls still run. This is Linux bootstrap/OpenGL-only and leaves the Windows DirectX/client path untouched.
- Verification after the selected-map camera-control/input-lock chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The longer pinned MOBA smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-17.35.44/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb` at startup and shutdown, and reported `map3DPreviewPitch=60`, `map3DPreviewRenderedPitch=60`, `map3DPreviewZoom=1`, `map3DPreviewPan=0,0`, `finalMap3DPreviewBaseYaw=-36`, `finalMap3DPreviewRenderedYaw=0.743034`, `finalMap3DPreviewPitch=60`, `finalMap3DPreviewRenderedPitch=60`, `finalMap3DPreviewZoom=1`, `finalMap3DPreviewPan=0,0`, `finalMap3DPreviewInputCount=0`, `finalMap3DPreviewLastAction=auto`, `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, `finalMap3DPreviewRendererAnimatedMeshPayloads=48`, and `finalMap3DPreviewPointLights=0`. The longer NightMap point-light smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map NightMap --hero plane --bootstrap-create-game --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-17.35.59/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/NightMap/_.ADMPDSCR.xdb` at startup and shutdown, reported `mapPointLights=75`, `mapPointLightsDay=8`, `mapPointLightsNight=73`, `finalMap3DPreviewPointLights=75`, `finalMap3DPreviewPointLightMarkers=75`, `finalMap3DPreviewBaseYaw=-36`, `finalMap3DPreviewRenderedYaw=0.552429`, `finalMap3DPreviewPitch=60`, `finalMap3DPreviewRenderedPitch=60`, `finalMap3DPreviewZoom=1`, `finalMap3DPreviewPan=0,0`, and kept `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, and `finalMap3DPreviewRendererAnimatedMeshPayloads=48`.
- Latest selected-map engine-start-slot preview chunk: `PW_Client/Game.cpp` now draws the real engine `MapStartInfo` start assignments inside the Linux OpenGL loading map preview. The preview consumes the same `FillMapStartInfo` result used by the loading/game-start path, terrain-follows each start slot, draws team-colored rings/columns, highlights the human slot separately from bots, and logs final draw/total/assigned/human/bot counters. Mouse wheel over the loading map now zooms the map preview directly while the existing hidden map-browser lock still prevents accidental selected-map changes during loading. This is Linux bootstrap/OpenGL-only and leaves the Windows DirectX/client path untouched.
- Verification after the selected-map engine-start-slot preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The longer pinned MOBA smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-17.46.59/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb` at startup and shutdown, and reported `bootstrap3DPreviewSource=selected-map-layered-terrain-heightmap-water-zones-roads-authored-guides-point-lights-engine-start-slots-terrain-elements-tactical-markers-and-scene-objects`, `finalMap3DPreviewEngineStartSlotsDrawn=yes`, `finalMap3DPreviewEngineStartSlots=10`, `finalMap3DPreviewEngineStartAssignedSlots=10`, `finalMap3DPreviewEngineStartHumanSlots=1`, `finalMap3DPreviewEngineStartBotSlots=9`, `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, `finalMap3DPreviewRendererAnimatedMeshPayloads=48`, and `finalMap3DPreviewPointLights=0`. The longer NightMap point-light/start-slot smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map NightMap --hero plane --bootstrap-create-game --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-17.48.10/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/NightMap/_.ADMPDSCR.xdb` at startup and shutdown, reported `mapPointLights=75`, `finalMap3DPreviewPointLights=75`, `finalMap3DPreviewPointLightMarkers=75`, `finalMap3DPreviewEngineStartSlotsDrawn=yes`, `finalMap3DPreviewEngineStartSlots=10`, `finalMap3DPreviewEngineStartAssignedSlots=10`, `finalMap3DPreviewEngineStartHumanSlots=1`, `finalMap3DPreviewEngineStartBotSlots=9`, and kept `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, and `finalMap3DPreviewRendererAnimatedMeshPayloads=48`.
- Latest selected-map team-route preview chunk: `PW_Client/Game.cpp` now derives stronger tactical marker teams for the loading map preview, including A/B tower/shop/fountain naming and lane-spawner labels from either `scriptName` or the object-ref stem (`BDown`, `BUp`, `B`, etc.). The Linux OpenGL loading preview now draws terrain-followed team route overlays from the real engine `MapStartInfo` start slots to allied lane/objective markers, plus lane-to-lane corridor lines pairing A/B lane spawners by lane key where possible. Human start-route lines are highlighted separately from bot routes, and the final diagnostics log route draw, line, segment, start-route, and lane-route counters. This remains Linux bootstrap/OpenGL-only and leaves Windows DirectX/client behavior untouched.
- Verification after the selected-map team-route preview chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and `git diff --check` exited `0`. The longer pinned MOBA smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-18.06.08/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb` at startup and shutdown, classified `BDown`/`BUp`/`B` lane markers as `team=2`, and reported `bootstrap3DPreviewSource=selected-map-layered-terrain-heightmap-water-zones-roads-authored-guides-point-lights-engine-start-slots-team-routes-terrain-elements-tactical-markers-and-scene-objects`, `finalMap3DPreviewTeamRoutesDrawn=yes`, `finalMap3DPreviewTeamRouteLines=13`, `finalMap3DPreviewTeamRouteSegments=78`, `finalMap3DPreviewTeamStartRouteLines=10`, `finalMap3DPreviewTeamLaneRouteLines=3`, `finalMap3DPreviewEngineStartSlotsDrawn=yes`, `finalMap3DPreviewEngineStartSlots=10`, `finalMap3DPreviewEngineStartAssignedSlots=10`, `finalMap3DPreviewEngineStartHumanSlots=1`, `finalMap3DPreviewEngineStartBotSlots=9`, `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, `finalMap3DPreviewRendererAnimatedMeshPayloads=48`, and `finalMap3DPreviewPointLights=0`. The longer NightMap point-light/team-route smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map NightMap --hero plane --bootstrap-create-game --seconds 8` exited `0`, wrote `Bin/logs/2026.05.30-18.06.35/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/NightMap/_.ADMPDSCR.xdb` at startup and shutdown, reported `mapPointLights=75`, `mapPointLightsDay=8`, `mapPointLightsNight=73`, `finalMap3DPreviewPointLights=75`, `finalMap3DPreviewPointLightMarkers=75`, the same `finalMap3DPreviewTeamRouteLines=13` / `finalMap3DPreviewTeamLaneRouteLines=3` route counters, the same `finalMap3DPreviewEngineStartSlots=10` / `finalMap3DPreviewEngineStartAssignedSlots=10` start-slot counters, and kept `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, and `finalMap3DPreviewRendererAnimatedMeshPayloads=48`.
- Latest live transceiver/PFWorld loading-runtime tick chunk: `PW_Client/Game.cpp` now advances the real Linux local scheduler/transceiver repeatedly during the create-game loading path instead of stopping at a single proof step. The bootstrap driver primes the scheduler buffer, then runs up to three fixed scheduler/transceiver ticks per loading frame until a bounded 96-step smoke cap is reached, while continuing normal scheduler ticking after the cap. Final diagnostics now record scheduler ticks/segment, transceiver step calls/processed steps/next step/world step/no-data state, command/status batch counts, and PFWorld player/fog/map-object class counters. This is Linux bootstrap/runtime integration only; Windows compatibility is untouched.
- Verification after the live transceiver/PFWorld loading-runtime tick chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both pinned create-game smokes exited `0`. The MOBA smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 8` wrote `Bin/logs/2026.05.30-18.17.49/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/_.ADMPDSCR.xdb`, and reports `finalGameSchedulerTicks=118`, `finalGameSchedulerSegmentStep=97`, `finalGameTransceiverStepCalls=96`, `finalGameTransceiverProcessedSteps=96`, `finalGameTransceiverNextStep=97`, `finalGameTransceiverWorldStep=95`, `finalGameTransceiverCommandBatches=96`, `finalGameTransceiverStatusUpdates=96`, `finalGameTransceiverNoData=no`, `finalGameWorldPlayers=10`, `finalGameWorldPresentPlayers=1`, `finalGameWorldFog=205x205`, `finalGameWorldMapObjects=7458`, `finalGameWorldSimpleObjects=862`, `finalGameWorldMultiStateObjects=3900`, `finalGameWorldTreeObjects=2519`, `finalGameWorldCreepSpawners=6`, `finalGameWorldNeutralSpawners=28`, `finalGameWorldTowers=24`, and `finalGameWorldMainBuildings=2`, while preserving `finalMap3DPreviewTeamRouteLines=13`, `finalMap3DPreviewTeamRouteSegments=78`, `finalMap3DPreviewTeamLaneRouteLines=3`, `finalMap3DPreviewEngineStartSlots=10`, `finalMap3DPreviewTerrainSurfaceDrawn=yes`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, `finalMap3DPreviewRendererAnimatedMeshPayloads=48`, and `finalMap3DPreviewPointLights=0`. The NightMap regression smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map NightMap --hero plane --bootstrap-create-game --seconds 8` wrote `Bin/logs/2026.05.30-18.18.03/linux-client-shell.log`, selected `Maps/Multiplayer/MOBA/NightMap/_.ADMPDSCR.xdb`, reports the same 96 processed transceiver/PFWorld steps with `finalGameWorldMapObjects=6682`, `finalGameWorldSimpleObjects=835`, `finalGameWorldMultiStateObjects=3453`, `finalGameWorldTreeObjects=2253`, `finalGameWorldNeutralSpawners=16`, and preserves `mapPointLights=75`, `finalMap3DPreviewPointLightsDrawn=yes`, `finalMap3DPreviewPointLights=75`, `finalMap3DPreviewTeamRouteLines=13`, `finalMap3DPreviewEngineStartSlots=10`, and the same terrain/static/animated preview counters.
- Latest large chunk: the Linux null-render `PFWorld::ExecuteCommands(...)` path is no longer an empty transceiver stub. It now deserializes real `NCore::PackedWorldCommand` payloads with `GetWorldCommand(GetPointerSerialization())`, routes them through `PF_Core::World::ExecuteCommand(...)`, and records Linux-only packed-command and command-body diagnostics. The bootstrap loading runtime now queues a serialized `LinuxBootstrapWorldRuntimeCommand` through `NCore::Transceiver::SendCommand(...)` after the initial `LocalCmdScheduler` buffer-priming step, proving the real command serializer, scheduler queue, transceiver segment delivery, PFWorld command dispatch, and command body execution without touching Windows DirectX/client behavior.
- Verification after the packed world-command execution chunk: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both pinned create-game smokes exited `0`. The MOBA smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 8` wrote `Bin/logs/2026.05.30-18.35.40/linux-client-shell.log` and reports `finalGameTransceiverCommands=1`, `finalGameTransceiverRuntimeCommandsSent=1`, `finalGameWorldExecutedPackedCommands=1`, `finalGameWorldBootstrapRuntimeCommands=1`, `finalGameWorldLastPackedCommandClientId=1984`, `finalGameWorldLastBootstrapCommandClientId=1984`, `finalGameWorldLastBootstrapCommandToken=20260530`, `finalGameWorldLastBootstrapCommandValue=1`, while preserving `finalGameTransceiverProcessedSteps=96`, `finalGameTransceiverWorldStep=95`, `finalGameWorldMapObjects=7458`, `finalMap3DPreviewTeamRouteLines=13`, and `finalMap3DPreviewPointLights=0`. The NightMap smoke `/tmp/primeworld-linux-bootstrap/LinuxBootstrap/PrimeWorldLinuxClient --map NightMap --hero plane --bootstrap-create-game --seconds 8` wrote `Bin/logs/2026.05.30-18.36.01/linux-client-shell.log` and reports the same packed-command counters with `finalGameWorldMapObjects=6682`, `mapPointLights=75`, `finalMap3DPreviewPointLights=75`, and `finalMap3DPreviewTeamRouteLines=13`.
- Latest large chunk: the Linux null-render target-selector runtime is now active instead of factory-only. `PFBaseUnit.cpp` keeps a Linux bootstrap unit registry and exposes faction-mask helpers, `PFAIWorld.cpp` and `PFVoxelMap.cpp` now register/unregister/select live units through Linux-safe unit/object filters, `PFTargetSelector.cpp` implements common selector behavior for unit enumeration, area, nearest, first/counting/list, relative-unit, point, condition, select-applicator, and applicator-recipient selectors, and `DBAbility.cpp` exposes Linux bootstrap factories for the corresponding generated selector DB resources. `PFUniTarget.cpp` also fixes Linux `Target::SetUnit(CPtr<PFBaseUnit>)` so CPtr-selected relative units stay typed as unit targets. Windows/full-render branches remain under their original paths.
- Latest large chunk: the Linux null-render target-selector runtime now promotes the next geometric/filter/single-selection selector slice. `PFTargetSelector.cpp` implements Linux bootstrap behavior for `PFDblPointTargetSelector`, `PFSectorTargetSelector`, `PFCapsuleTargetSelector`, `PFWallTargetSelector`, `PFFilterTargetSelector`, `PFWeightTargetSelector`, and `PFComparingTargetSelector`, including deterministic sector/capsule/wall geometry over the live unit registry, DBID-based suitable-unit filtering, and controlled first-best target selection/comparison semantics. `DBAbility.cpp` now instantiates the generated Linux bootstrap factories for `CapsuleTargetSelector`, `SectorTargetSelector`, `WallTargetSelector`, `FilterTargetSelector`, `WeightTargetSelector`, and `ComparingTargetSelector`. Windows/full-render branches remain under their original paths.
- Latest large chunk: the Linux null-render target-selector runtime now promotes the next wrapper/utility selector slice. `PFTargetSelector.cpp` implements Linux bootstrap behavior for `PFSummonEnumerator`, `PFHeroEnumerator`, `PFUnitPlaceCorrector`, `PFConvertTargetToLand`, `PFUnitShiftTarget`, `PFFixToCenterTargetSelector`, `PFBetweenUnitsTargetSelector`, `PFMainBuildingTargetSelector`, `PFFountainTargetSelector`, `PFShopTargetSelector`, and `PFAttackersTargetSelector`, using the live Linux unit registry for summon/hero/building/shop/fountain/attacker selection and deterministic point correction/projection for controlled geometry probes. `DBAbility.cpp` now instantiates the corresponding generated Linux bootstrap selector resources. Windows/full-render branches remain under their original paths.
- Latest large chunk: the Linux null-render target-selector runtime now promotes the AI selector slice in `PFAITargetSelectors.cpp`. `PFMaximizingTargetSelector`, `PFDelayTargetSelector`, `PFCheckConditionTargetSelector`, `PFNotTargetOfSameAbilitySelector`, and `PFDamagingLinksTargetSelector` now instantiate their child selectors and run deterministic Linux bootstrap behavior over the live unit/target registry, including delayed target exposure, condition-gated fanout, same-ability target exclusion, and best-position scans for maximizing/damaging-link selectors. `DBAbility.cpp` now instantiates the corresponding generated Linux bootstrap selector resources. Windows/full-render branches remain under their original paths.
- Latest large chunk: the Linux null-render target-selector/runtime now promotes the Highlander and selector-backed MicroAI slice. `PFHighlander.cpp` implements Linux bootstrap `PFHighlanderA1TargetSelector` over the live unit registry with hero-first/nearest ordering and target-count/range/filter checks; `PFMicroAI.cpp` implements Linux `PFBasicMicroAI`, `PFTargetSelectorMicroAI`, and `PFMultipleTargetSelectorMicroAI` with explicit ability/formula context, child selector instantiation, range/faction scans, unit-filter checks, and min-target-count handling. `PFAbilityData.cpp` now creates and uses MicroAI on Linux (`CreateMicroAI`, `FindMicroAITarget`, temp MicroAI lookup, and formula-backed `GetUseRange()`), `DBAbility.cpp` exposes the generated Linux bootstrap factories for `HighlanderA1TargetSelector`, `BasicMicroAI`, `TargetSelectorMicroAI`, and `MultipleTargetSelectorMicroAI`, and `runtime_stubs.cpp` drops the obsolete bootstrap `PFMicroAI` cast shim because the real translation unit now owns it. `PFHighlander.cpp` and `PFMicroAI.cpp` are now compiled into `PrimeWorldLinuxClient`, `PrimeWorldLinuxClientRuntimeProbe`, and `PrimeWorldLinuxGameplayProbe`; Windows/full-render branches remain behind their original paths.
- `PW_Client/Game.cpp` now carries a wider `Session effects target-selector runtime` probe. Both current smokes report `db=yes units=yes unitenum=yes/yes/2 area=yes/yes/1 sector=yes/yes/1 capsule=yes/yes/2 wall=yes/yes/1 hero=yes/yes/1 summon=yes/yes/1 nearest=yes/yes weight=yes/yes filter=yes/yes/1 counting=yes/yes/1 comparing=yes/yes list=yes/yes/2 relative=yes/yes shift=yes/yes/103.00 point=yes/yes/103.00 place=yes/yes/103.00 convert=yes/yes/103.50 fix=yes/yes/103.00 between=yes/yes/1/103.50 main=yes/yes fountain=yes/yes shop=yes/yes attackers=yes/yes/1 maximizing=yes/yes/100.87 delay=yes/yes/2 check=yes/yes/2 notsame=yes/yes/1 links=yes/yes/100.87 highlander=yes/yes/2 micro=yes/yes multimicro=yes/yes counter=yes/yes/2`, proving generated selector factories, faction/type filtering, area/range selection, sector/capsule/wall geometry, nearest and weight selection, DBID filter selection, list/count fanout, comparing selection, relative current-target lookup, point projection/correction/shift/fix/between selectors, hero/summon/main-building/fountain/shop lookup, attacker targeter enumeration, maximizing/delay/check-condition/not-same-ability/damaging-links selectors, HighlanderA1 selection, selector-backed single/multiple MicroAI through `PFAbilityData::FindMicroAITarget`, and selector-driven `TargetsCounterApplicator` behavior through real Linux unit/applicator paths.
- Verification after the Highlander/MicroAI target-selector promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -j4` exited `0`, `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal and `--bootstrap-create-game` smokes report the target-selector proof above while preserving the prior gameplay-effect/unit-mod proofs; the target-selector proof was reverified after the special-applicator promotion in `Bin/logs/2026.05.25-06.08.19/linux-client-shell.log`.
- Latest large chunk: the Linux null-render ability/applicator runtime now promotes real summon creation beyond registry-only summon enumeration. `DBAbility.cpp` exposes Linux bootstrap factories for `SummonApplicator`, `SummonBehaviourBase`, and `SummonBehaviourCommon`; `PFBaseUnit.cpp` now initializes Linux summon groups and supports summon iteration/removal plus top-behaviour apply/suspend/resume; `PFSummoned.cpp` now registers/removes common summon AI behaviours in the master's behaviour groups. `PW_Client/Game.cpp` proves a summon ability creates two `PFBaseSummonedUnit` instances through `PFBaseUnit::UseExternalAbility`, assigns master/faction/unit type, and applies child `ValueApplicator` state to both summoned units. Windows/full-render branches remain unchanged.
- `PW_Client/Game.cpp` now carries a `Session effects unit-summon runtime` probe. Both current smokes report `db=yes units=yes ability=yes factory=yes created=yes/2/2 mastered=yes child=yes/2/0.88`, proving generated summon factories, common summon behavior data, master summon-group registration, `ForAllSummons` enumeration, and child applicator activation on created summons through the Linux ability path.
- Verification after the summon-applicator promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal and `--bootstrap-create-game` smokes report the summon proof above while preserving the prior gameplay-effect/unit-mod/target-selector proofs; the summon proof was reverified after the special-applicator promotion in `Bin/logs/2026.05.25-06.08.19/linux-client-shell.log`.
- Latest large chunk: the Linux null-render special-applicator runtime now promotes the first safe `PFApplSpecial` slice. `DBAbility.cpp` exposes Linux bootstrap factories for `SpellSwitchApplicator`, `ProgramApplicator`, `ForAllTargetsApplicator`, `NaftaTransferApplicator`, and `DealedDamageConverterApplicator`; `PFApplicatorHistoryAnalysis.cpp` now scans Linux sent applicator history for `PFApplDamage` instead of returning zero; and `PFApplSpecial.cpp` lets Linux null-render `ProgramApplicator` and `ForAllTargetsApplicator` advance through zero-notification child/fanout programs in controlled bootstrap use. Windows/full-render branches remain unchanged.
- `PW_Client/Game.cpp` now carries a `Session effects special-applicator runtime` probe. Both current smokes report `db=yes units=yes factory=yes switch=yes/1/0.22 program=yes/2/0.63 forall=yes/2/0.16 nafta=yes/6->2/0->3 converter=yes/0.50->0.60/1.00->0.80`, proving generated special-applicator factories, switch-selected spell child activation, sequential program child activation, selector-backed `ForAllTargets` fanout, nafta transfer math, damage-history-driven `DealedDamageConverter` healing, and the Linux damage-history scan through the Linux ability path.
- Verification after the special-applicator promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal and `--bootstrap-create-game` smokes report the special-applicator proof above while preserving the prior gameplay-effect/unit-mod/summon/target-selector proofs; the latest create-game smoke log path is `Bin/logs/2026.05.25-06.08.19/linux-client-shell.log`.
- Latest large chunk: the Linux null-render special-control runtime now promotes the next controlled `PFApplSpecial` slice. `DBAbility.cpp` exposes Linux bootstrap factories for `InvalidAbilityTargetApplicator`, `MovementControlApplicator`, `ChangeAnimSetApplicator`, `ChangeAnimationApplicator`, `WatchApplicator`, `ScaleControlApplicator`, `DayNightTransitionApplicator`, and `SetTimescaleApplicator`; `PFApplSpecial.cpp` null-guards day/night and timescale world/screen lookups so bootstrap runs can exercise them without a full `AdventureScreen`; and `PW_Client/Game.cpp` applies the slice through real `PFBaseUnit::UseExternalAbility(...)` calls against synthetic moving-unit and creature targets. Windows/full-render branches remain unchanged except the shared `VISUAL_CUTTED` null-world guards, which only avoid null dereferences in non-visual bootstrap use.
- `PW_Client/Game.cpp` now carries a `Session effects special-control runtime` probe. Both current smokes report `db=yes units=yes factory=yes invalid=yes/0->1->0 movement=yes/1->0 anim=yes/1/1 watch=yes/1->0 scale=yes/1 daynight=yes timescale=yes`, proving invalid-target marker add/remove, movement-control add/remove, animation-set/animation applicator startup, watch add/remove, scale-control registration, and day/night/timescale bootstrap start paths through the Linux ability/applicator runtime.
- Verification after the special-control promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.25-06.25.11/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same special-control proof while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.25-06.25.21/linux-client-shell.log`.
- Latest large chunk: the Linux null-render special-object/channeling runtime now promotes another controlled `PFApplSpecial` slice. `DBAbility.cpp` exposes Linux bootstrap factories for `ChannellingApplicator`, `WaitForSpellApplicator`, `EyeApplicator`, `LockTilesApplicator`, `ModifyTerrainApplicator`, `RaiseFlagApplicator`, `PickupChannelingApplicator`, and the `DropTreeApplicator` factory; `PFApplSpecial.cpp` null-guards eye, terrain, tile-locking, and drop-tree world/map lookups so bootstrap runs can exercise the non-render state paths without a fully owned world/tile/fog scene; and `PFApplPickupChanneling` now advertises object targets, matching its existing `Start()` path that consumes `GetTarget().GetObject()`.
- `PW_Client/Game.cpp` now carries a `Session effects special-object runtime` probe. Both current smokes report `db=yes units=yes factory=yes channel=yes/1->0 wait=yes/1->0 eye=yes/1->0 lock=yes/1->0 terrain=yes raise=yes/1/1 pickup=yes/1/0`, proving generated special-object factories, channeling add/remove, wait-for-spell add/remove, eye add/remove with null fog/tile guards, tile-lock add/remove with null tile-map guards, terrain start in a no-nature-map bootstrap context, flag raising against a synthetic flagpole, and pickup-channeling object-target validation through the Linux ability/applicator runtime.
- Verification after the special-object/channeling promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke writes `Bin/logs/2026.05.25-06.40.58/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same special-object proof while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.25-06.42.10/linux-client-shell.log`.
- `PF_Core/DBEffect.h` deliberately exposes the base `EffectBase::Retrieve(...)` virtual under the same bootstrap macro so generated gameplay-effect translation units keep the same vtable layout as the callers; do not add more generated `Retrieve(...)` overrides under `VISUAL_CUTTED` without preserving that layout. `KillAllActiveEffects()` remains inert for DB-only Linux bootstrap tools, but the runnable `PW_LINUX_NULL_RENDER` client now owns the global active-effect kill traversal. The next effect boundary is the remaining real gameplay/applicator call sites (`PFAppl*` and dispatch/client paths), `PFClientApplicators::ApplyEffect`/`RestoreEffect` through real client-backed `PFLogicObject`/`PFBaseUnit` instances, gameplay-owned scene objects, and real renderer-backed effects rather than basic factory, standalone scene-object, attached-effect mechanics, client-object apply, helper-level prepare/kill/standalone retrieval, buff-owned prepare/restart/kill lifecycle, aura/minimap/play-animation/priestess/random/switcher/invisibility/unit-scene-modify mechanics, animated-scene/creature-death factory promotion, or global active-effect kill traversal.
- Verification after the scene/attached-effect promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -j4`, `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4`, and both stable non-redirected smoke paths all exited `0`. The smoke `timeout 20 /tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 0.2` reaches the current UI/runtime shell, reports `Session effects component runtime: retrieved=yes released=yes root=yes components=7 scene=yes added=yes updated=yes removed=yes objects=1/0 type=BasicEffectStandalone dbid=Effects/UI/PathDestination/_.EFFT.xdb` and `Session effects attached runtime: retrieved=yes released=yes root=yes components=3 scene=yes target=yes applied=yes updated=yes detached=yes objects=1 targetComponents=1/3/1 type=BasicEffectAttached dbid=Effects/UI/Selection/HeroSelection.EFFT.xdb`, and writes `Bin/logs/2026.05.19-21.46.13/linux-client-shell.log`. The deeper smoke `timeout 25 /tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --bootstrap-create-game --seconds 0.2` reaches `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, reports the same default/standalone/attached effect-pool proof plus `mapObjects=2468`, `simple=282`, `multi=740`, `trees=1395`, `glyphSpawners=7`, `obstacles=18`, `heroPlaceholders=10`, `creepSpawners=2`, `shops=2`, `quarters=2`, `towers=4`, `fountains=2`, `roadFlagpoles=2`, and `mainBuildings=2`, and writes `Bin/logs/2026.05.19-21.46.19/linux-client-shell.log` before `Prime World Linux client shell finished.`
- Verification after the client-object apply/global-kill effect promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -j4`, `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4`, and both stable smoke paths exited `0`. The normal smoke now reports `Session effects apply runtime: retrieved=yes released=yes root=yes components=3 scene=yes target=yes client=yes callback=yes/1 applied=yes updated=yes killed=yes detached=yes objects=1 targetComponents=1/3/1 type=BasicEffectAttached dbid=Effects/UI/Selection/HeroSelection.EFFT.xdb` and writes `Bin/logs/2026.05.19-21.57.55/linux-client-shell.log`. The `--bootstrap-create-game` smoke reports the same apply/global-kill proof while still reaching `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and writes `Bin/logs/2026.05.19-21.58.01/linux-client-shell.log`.
- Verification after the gameplay `RandomEffect` promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke reports `Session effects random runtime: db=yes retrieved=yes released=yes choices=1 scene=yes target=yes client=yes callback=yes/1 childApplied=yes updated=yes childDetached=yes objects=1 targetComponents=1/3/1 type=PFEffectRandom childDbid=Effects/UI/Selection/HeroSelection.EFFT.xdb` and writes `Bin/logs/2026.05.19-22.27.42/linux-client-shell.log`. The `--bootstrap-create-game` smoke reports the same `PFEffectRandom` child-selection proof while still reaching `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and writes `Bin/logs/2026.05.19-22.27.49/linux-client-shell.log`.
- Verification after the gameplay `EffectSwitcher` / `InvisibilityEffect` promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke reports `Session effects random runtime: db=yes retrieved=yes released=yes choices=1 scene=yes target=yes client=yes callback=yes/1 childApplied=yes updated=yes childDetached=yes objects=1 targetComponents=1/3/1 type=PFEffectRandom childDbid=Effects/UI/Selection/HeroSelection.EFFT.xdb`, `Session effects switcher runtime: db=yes retrieved=yes released=yes scene=yes target=yes client=yes callback=yes/1 childApplied=yes updated=yes childDetached=yes objects=1 targetComponents=1/3/1 type=PFEffectSwitcher childDbid=Effects/UI/Selection/HeroSelection.EFFT.xdb`, and `Session effects invisibility runtime: db=yes retrieved=yes released=yes scene=yes target=yes client=yes channel=yes/1 removed=yes/1 updated=yes opacityChanged=yes opacity=1.00->0.50 objects=1 targetComponents=1 type=InvisibilityEffect`, and writes `Bin/logs/2026.05.19-23.02.17/linux-client-shell.log`. The `--bootstrap-create-game` smoke reports the same Random/Switcher/Invisibility proof while still reaching `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and writes `Bin/logs/2026.05.19-23.02.26/linux-client-shell.log`.
- Verification after the gameplay `UnitSceneObjectModify` promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke reports `Session effects unit-scene-modify runtime: db=yes retrieved=yes released=yes scene=yes target=yes client=yes applied=yes updated=yes detached=yes objects=1/2/1 targetComponents=1 type=PFUnitSceneObjectModify` and writes `Bin/logs/2026.05.19-23.21.03/linux-client-shell.log`. The `--bootstrap-create-game` smoke reports the same unit-scene-modify proof while still reaching `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and writes `Bin/logs/2026.05.19-23.21.09/linux-client-shell.log`.
- Verification after the gameplay `AuraEffect` / `MinimapEffect` / `PlayAnimationEffect` promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke reports `Session effects aura runtime: db=yes retrieved=yes applied=yes released=yes changes=2 type=PFAuraEffect`, `Session effects minimap runtime: db=yes retrieved=yes applied=yes updated=yes released=yes updates=1 type=PFMinimapEffect`, and `Session effects play-anim runtime: db=yes retrieved=yes applied=yes sceneUpdated=yes released=yes type=PFPlayAnimEffect`, and writes `Bin/logs/2026.05.19-23.41.43/linux-client-shell.log`. The `--bootstrap-create-game` smoke reports the same Aura/Minimap/PlayAnimation proof while still reaching `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and writes `Bin/logs/2026.05.19-23.41.50/linux-client-shell.log`.
- Verification after the gameplay `PriestessSignEffect` promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. The normal smoke reports `Session effects priestess runtime: db=yes retrieved=yes init=yes scene=yes target=yes client=yes applied=yes soul=yes/yes fly=yes/yes completed=yes released=yes updates=3 objects=1/2/1 type=PriestessSignEffect` and writes `Bin/logs/2026.05.20-00.05.08/linux-client-shell.log`. The `--bootstrap-create-game` smoke reports the same PriestessSign proof while still reaching `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and writes `Bin/logs/2026.05.20-00.05.13/linux-client-shell.log`.
- Verification after the `PFApplBuff.cpp` Linux effect-lifecycle promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. Both smokes still report `Session effects client-applicators runtime: prepared=yes killed=yes standalone=yes scene=yes added=yes updated=yes released=yes collection=yes/1 objects=0/1/0 type=BasicEffectAttached standaloneType=BasicEffectStandalone` and now also report `Session effects buff-applicator runtime: db=yes init=yes running=yes enabled=yes prepared=1/1 restarted=1/1 stopped=yes killed=yes type=BasicEffectAttached`. The normal smoke writes `Bin/logs/2026.05.20-00.36.59/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same buff-owned lifecycle proof while still reaching `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and writes `Bin/logs/2026.05.20-00.37.06/linux-client-shell.log`.
- Verification after the Linux aura/dispatch applicator promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. Both smokes still report the client-applicator and buff-applicator proofs, and now also report `Session effects aura-applicator runtime: db=yes units=yes ability=yes init=yes factory=yes/1 running=yes stepped=yes targets=1 child=yes/1/1/1 applied=0/1/0 stopped=yes removed=yes` plus `Session effects dispatch-applicator runtime: db=yes units=yes ability=yes spell=yes created=yes started=yes stepped=yes applied=yes/1/1 removed=yes/0/0`. The normal smoke writes `Bin/logs/2026.05.20-00.57.13/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same aura/dispatch applicator proof while still reaching `Game::LoadingScreen::Init/Step/Draw`, `NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare`, `Game::LocalCmdScheduler::StartGame/Step/GetSyncSegment`, and `NCore::Transceiver::Transceiver/SetWorld(PFWorld)/Step`, and writes `Bin/logs/2026.05.20-00.57.59/linux-client-shell.log`.
- Verification after the Linux ability-layer promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. Both smokes now also report `Session effects ability-applicator runtime: db=yes units=yes passive=yes/yes/1/1/1 removed=yes/0/0 active=yes/yes/yes/1/1/1 removed=yes/0/0`, proving `PFAbilityData::ApplyPassivePart(...)` and `PFAbilityData::ApplyToTarget(...)` can create/remove concrete `AbilityUpgradeApplicator` instances through the higher-level ability API. The normal smoke writes `Bin/logs/2026.05.20-01.09.30/linux-client-shell.log`; the `--bootstrap-create-game` smoke reports the same ability/applicator proof while still reaching the loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics, and writes `Bin/logs/2026.05.20-01.09.36/linux-client-shell.log`.
- Latest gameplay chunk: the Linux null-render gameplay path is now past direct `PFAbilityData` calls and into the real unit/base-attack entry points. Under `PW_LINUX_NULL_RENDER`, `PFUnitAbilities.cpp` now creates and updates concrete `PFAbilityData` slot abilities from unit DB data, honors global cooldown/readiness in `IsAbilityAvailable(...)`, cancels/removes passive parts on destroy, upgrades ability slots, and lets `PFBaseUnitUseAbilityState::OnStep(...)` call `PFBaseUnit::UseAbility(...)`. `PFBaseUnit.cpp` now owns Linux implementations for target/range checks, ability instance creation, unit-slot `UseAbility(...)`, external ability use, external passive registration, base-attack replacement, active ability-instance cleanup from `Step(...)`, ability cancellation, attack readiness, `DoAttack(...)`, and ability-forbid checks. `PFBaseAttackData.cpp` now creates base-attack dispatches through `CreateDispatch(...)`, starts the dispatch holder, cancels live dispatches, maintains attack instances during update/cancel, and extracts damage type from `DamageApplicator` DB data when present; bootstrap units without DB descriptors default safely instead of calling `PFBaseUnit::GetNativeDamageType()` on null metadata. Windows/full-render branches remain under their original `#else` paths.
- Verification after the unit/base-attack promotion: `cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient PrimeWorldLinuxClientRuntimeProbe PrimeWorldLinuxGameplayProbe PrimeWorldLinuxRenderProbe -j4` exited `0`, and both stable smoke paths exited `0`. Both smokes now report `Session effects unit-combat runtime: db=yes units=yes slot=yes/yes/yes/1/1 removed=yes/0/0 external=yes/yes/1/1 removed=yes/0/0 baseattack=yes/yes/yes/yes/1/1 removed=yes/0/0`, proving unit-slot `PFBaseUnit::UseAbility(...)`, `PFBaseUnit::UseExternalAbility(...)`, and `PFBaseUnit::DoAttack(...)` all create dispatch-backed `AbilityUpgradeApplicator` instances and remove them cleanly through the promoted gameplay API. The normal smoke writes `Bin/logs/2026.05.20-01.26.42/linux-client-shell.log`; the `--bootstrap-create-game` smoke writes `Bin/logs/2026.05.20-01.26.50/linux-client-shell.log` while still reaching loading screen, map-loading job, scheduler segment, transceiver, PFWorld, TileMap, and ARAM object-count diagnostics.
- `PrimeWorldLinuxClient` linkability remains stable after the executable grew far enough to include the real main-loop shell slice (`Client/MainLoop.cpp`, `Client/ScreenCommands.cpp`, `Client/CommandBinds.cpp`, `Client/Console.cpp`, `Client/ConsoleLogic.cpp`, `Client/DebugTextDraw.cpp`, `Client/TooltipsScreen.cpp`, `Client/TooltipsScreenLogic.cpp`, and `UI/DebugDraw.cpp`), the first real FSM/update slice (`Core/BaseFSM.cpp`, `Core/CoreFSM.cpp`, `System/UpdateProcessor.cpp`, and `System/Updater.cpp`), `PW_Client/LocalGameContext.cpp`, `PW_Client/LocalCmdScheduler.cpp`, `PW_Client/CpuTopology.cpp`, `PW_Client/DebugVarsSender.cpp`, `PW_Client/GameStatistics.cpp`, `PW_Client/GameStatisticsWrapper.cpp`, `PW_Client/LobbyConnection.cpp`, `PW_Client/NetworkStatusScreen.cpp`, `PW_Client/ReplayRunner.cpp`, `PW_Client/ReplayTransceiver.cpp`, `Core/CommandSerializer.cpp`, `Core/GameCommand.cpp`, `Core/Transceiver.cpp`, `System/MemoryStream.cpp`, `PF_Core/AnimatedTexture.cpp`, `PF_Core/CameraShakeController.cpp`, `PF_Core/ChangeMaterialEffect.cpp`, `PF_Core/DBEffect.cpp`, `PF_Core/EnableSCEffect.cpp`, `PF_Core/EffectsPool.cpp`, `PF_Core/GhostEffect.cpp`, `PF_Core/IUpdateable.cpp`, `PF_Core/ObjectsPool.cpp`, `PF_Core/RecolorableTexture.cpp`, `PF_Core/ScaleColorEffect.cpp`, `PF_Core/SpectatorEffectsPool.cpp`, `PF_Core/World.cpp`, `PF_Core/WorldObject.cpp`, `PF_GameLogic/FPUState.cpp`, `PF_GameLogic/MapStartup.cpp`, `PF_GameLogic/PFStatistics.cpp`, `PF_GameLogic/TriggerMarkerHandler.cpp`, `PF_GameLogic/CollisionResolver.cpp`, `PF_GameLogic/PFAIContainer.cpp`, `PF_GameLogic/PFAIWorld.cpp`, `PF_GameLogic/PFVoxelMap.cpp`, `PF_GameLogic/PointersHolder.cpp`, `PF_GameLogic/SmartRandom.cpp`, and the executable UI diagnostics/debug-draw/font/script/preferences slice (`UI/Alphabet.cpp`, `UI/DebugDraw.cpp`, `UI/DebugVarsRender.cpp`, `UI/FrameTimeRender.cpp`, `UI/FontRender.cpp`, `UI/FontStyle.cpp`, `UI/PreferencesProcessor.cpp`, and `UI/Scripts.cpp`). The earlier post-run crash seen while testing that command-serialization trio was not a serialization-link blocker; it was the Linux DB/UI shutdown lifetime bug fixed by releasing bootstrap-owned `DBUIData` / `UIRoot` pointers before `NDb::SetResourceCache(0)`.
- `PW_Client/LocalCmdScheduler.cpp` now compiles and exercises the replay command-serialization branch in the Linux executable instead of guarding it out under `PW_LINUX_DB_BOOTSTRAP`. The Linux bootstrap creates a guarded `NCore::ReplayWriter`, attaches it to the live `LocalCmdScheduler`, and captures the scheduler's serialized command blocks into `logs/linux-bootstrap-replay.pwrp`. This is a native Linux bootstrap command-capture stream with magic `PWLXREPLAY1`, not yet the production Windows `.pwrp` replay format or a replay playback path. Windows behavior is unchanged because the new diagnostics/state live under `PW_LINUX_DB_BOOTSTRAP`, while the real Windows replay writer implementation remains in its original branch.
- The live scheduler/transceiver path now also sends real hero movement/control/ability/interaction/minigame/pickup orders after `MapLoadingJob -> PFWorld::LoadMap(...)` has created player hero objects. `PW_Client/Game.cpp` finds the first attached human/fallback hero in the loaded Linux `PFWorld`, queues the command stream through the same `NCore::Transceiver::SendCommand(...)` path, waits for each predecessor to become the last executed packed command, and records source/target/player diagnostics for seventeen Linux bootstrap production command implementations; current smokes prove sixteen command ids on both MOBA/NightMap and the pickup command on MOBA content. `Src/LinuxBootstrap/hero_actions_bootstrap.cpp` owns Linux bootstrap `CmdCombatMoveHero`, `CmdMoveHero`, `CmdStopHero`, `CmdAttackTarget`, `CmdFollowUnit`, `CmdHold`, `CmdCancelChannelling`, `CmdMinimapSignal`, `CmdUseUnit`, `CmdActivateTalent`, `CmdUseTalent`, `CmdUsePortal`, `CmdUseConsumable`, `CmdBuyConsumable`, `CmdRaiseFlag`, `CmdInitMinigame`, and `CmdPickupObject` implementations with the Windows command type ids `0x2C5B9CC0`, `0x2C59C380`, `0x2C5B9481`, `0x2C5B9480`, `0x2C6A2BC0`, `0x2C6BBB40`, `0xE78854C0`, `0x0B622CC0`, `0xE78B5B00`, `0x0B695200`, `0x0B695201`, `0x6294CD01`, `0x0F5CC401`, `0x2C61F340`, `0x0B76AAC0`, `0x9D62D440`, and `0xA05CCB40`; these call the maintained Linux null-render movement/hero-state/signal/ability/shop/flag/minigame/pickup surface (`PFBaseUnitCombatMoveState`, `PFBaseHero::Move(target)`, `PFBaseHero::DoStop()`, `PFBaseHero::OnTarget(enemyHero, true)`, `PFBaseHero::MoveTo(enemyHero, followRange, 0)`, `PFHeroHoldState`, `PFBaseHero::CancelChannelling()` when channeling is active, `PFAIContainer::OnMinimapSignal(...)`, `PFHeroUseUnitState`, `PFBaseMaleHero::ActivateTalent(...)`, `PFBaseMaleHero::UseTalent(...)`, `PFBaseMaleHero::UseTalent(GetPortal(), ...)`, `PFHeroUseConsumableState`, `PFShop::CanBuyConsumable(...)` plus `PFBaseHero::TakeConsumable(...)`/`TakeGold(...)`, `PFCreatureRaiseFlagState`, loaded `PFMinigamePlace` lookup with battle-start-delay gating on the runnable null-render path, a `PFBaseUnit` fallback for non-null-render gameplay-probe compilation where `PFMinigamePlace` is intentionally incomplete, and `PFHeroPickupObjectState` for loaded `PFPickupableObjectBase` objects). The Linux minimap-signal and buy-consumable commands deliberately avoid the Windows `AdventureScreen`/Flash/minimap/stat-event UI side effects, cancel-channelling deliberately avoids the Windows local completion callback, `CmdInitMinigame` preserves the production availability/control-delay checks, and the current `CmdUseUnit` smoke targets an enemy hero so serialization/execution is proven even if the gameplay side effect is gated by `CanBeUsedBy(...)`. `PFWorld` now exposes Linux-only helpers to find the first loaded shop/consumable, raisable flagpole, available minigame place, and pickupable object for the controlled hero so the smoke uses objects from the loaded map when content exists. The full Windows `HeroActions.cpp` path is unchanged. This is a native Linux bootstrap execution proof for seventeen production hero/control/ability/interaction command ids, not yet the full Windows `HeroActions` command surface with all AdventureScreen/control/sound side effects.
- The maintained `PrimeWorldLinuxClientRuntimeProbe` and the runnable `PrimeWorldLinuxClient` now compile/link the canonical command serialization surface (`Core/CommandSerializer.cpp`, `Core/GameCommand.cpp`, and `System/MemoryStream.cpp`), the next scheduler transport surface (`Core/Transceiver.cpp`), the Linux-safe client helper/statistics slice (`PW_Client/CpuTopology.cpp`, `PW_Client/DebugVarsSender.cpp`, `PW_Client/GameStatistics.cpp`, and `PW_Client/GameStatisticsWrapper.cpp`), the bootstrap network-status overlay slice (`PW_Client/NetworkStatusScreen.cpp`), the bootstrap replay runner/transceiver slice (`PW_Client/ReplayRunner.cpp` and `PW_Client/ReplayTransceiver.cpp`), the generated-effect/effect-pool/texture/camera-shake-controller/update-ring/base-world/FPU/gameplay-helper slice (`PF_Core/AnimatedTexture.cpp`, `PF_Core/CameraShakeController.cpp`, `PF_Core/ChangeMaterialEffect.cpp`, `PF_Core/DBEffect.cpp`, `PF_Core/EnableSCEffect.cpp`, `PF_Core/EffectsPool.cpp`, `PF_Core/GhostEffect.cpp`, `PF_Core/IUpdateable.cpp`, `PF_Core/ObjectsPool.cpp`, `PF_Core/RecolorableTexture.cpp`, `PF_Core/ScaleColorEffect.cpp`, `PF_Core/SpectatorEffectsPool.cpp`, `PF_Core/World.cpp`, `PF_Core/WorldObject.cpp`, `PF_GameLogic/FPUState.cpp`, `PF_GameLogic/PointersHolder.cpp`, and `PF_GameLogic/SmartRandom.cpp`), and the active-effect implementation slice (`PF_Core/BasicEffect.cpp`, `PF_Core/ControlledStatusEffect.cpp`, `PF_Core/CameraShakeSceneComponent.cpp`, `PF_Core/SpringSceneComponent.cpp`, `PF_Core/EffectGenSceneComponent.cpp`, `PF_Core/ParticleSceneComponentWithEffect.cpp`, `PF_Core/TraceGenSceneComponent.cpp`, and `PF_Core/LightningEffect.cpp`) under the Linux bootstrap/null-render definitions. The executable no longer carries the old `BasicEffect` / `BasicEffectStandalone::SwitchEffect(...)` shims, and it now verifies the generated default-effect factory path at runtime. A case-safe `Core/StdAfx.h` alias was added for legacy `#include "StdAfx.h"` usage, `System/platform_types.h` now exposes narrow Linux `OVERLAPPED`/`LPOVERLAPPED` and `BADNODENAME` compatibility shims, `PF_GameLogic/PointersHolder.h` now includes `../PF_Core/WorldObject.h` directly to remove a Linux include-order dependency without changing behavior, `Render/OcclusionQueries.h/.cpp` now route `PW_LINUX_NULL_RENDER` through the existing Linux occlusion-query stub, `Core/Transceiver.cpp` uses Linux bootstrap-only tick-count/FPU compatibility glue while keeping the Windows path intact, and `runtime_stubs.cpp` now carries the narrow Linux-only pointer/refcount specializations needed by the replay bootstrap headers without enabling active replay playback.
- The Linux executable shutdown path now explicitly releases its cached `DBUIData` and runtime `UIRoot` pointers before clearing the DB resource cache. This fixes the post-run Linux-only crash in `NDb::DbResourceCache::ReleaseResourceRef()` / `DBUIData::~DBUIData()` without changing Windows shutdown behavior.
- The native Linux client shell runs, opens a native window, mounts real game content, loads config, input, localization, session data, DB roots, and real map/hero/loading assets.
- `PrimeWorldLinuxClient` now exercises a narrow real client-runtime seam in the executable itself: `PW_Client/LoadingFlashInterface.cpp` and `PW_Client/LoadingStatusHandler.cpp` are linked into the Linux client target, and the Linux `Game.cpp` shell now drives loading-status events through the real runtime handler instead of only using duplicated shell-side status mapping.
- `PrimeWorldLinuxClient` now also links `PW_Client/LoadingHeroes.cpp` directly into the Linux executable. The bootstrap `LoadingFlashInterface` now captures the real loading-lineup state produced by `LoadingHeroes`, so the Linux shell can log and display runtime hero slots, player names, hero titles, and loading progress instead of only the loading-status text path.
- The Linux `Game.cpp` shell now injects synthetic human metadata into the real `MapStartInfo` loading path before `LoadingHeroes` consumes it. The current verified Linux-local-match path carries the human locale through to both `engineStartSlot[...]` and the real loading runtime hero list (`Loading hero metadata: Linux Player / en-US`, `Loading runtime heroes: ... 1 locales ...`) without changing the Windows client path.
- The Linux shell now presents through a real native OpenGL path instead of only X11 pixmap blitting. `System/MainFrame_linux.cpp` now creates a GLX-capable window/context, and `PW_Client/Game.cpp` uploads the existing loading artwork as an OpenGL texture and draws the shell header/panels/text/artwork through OpenGL. The verified smoke run now reports `Overlay backend: OpenGL` in stdout and `overlayBackend=OpenGL` in `linux-client-shell.log`.
- `Render/RenderInterface.cpp` now also has a Linux executable-only OpenGL bootstrap branch behind `PW_LINUX_OPENGL_BOOTSTRAP`. On the Linux client target, the engine-facing render interface can now bind the GLX context through `NMainFrame`, apply the viewport from `RenderMode`, clear through OpenGL, and swap buffers without touching the non-UTF8 legacy `renderer.cpp`. The maintained render probe still uses the pure null-render path, so probe coverage and the Windows renderer path stay isolated from this executable-only bootstrap behavior.
- The Linux shell now also owns a real `Render::DeviceLostWrapper<PF_Render::Interface>` bootstrap object in the executable path. `PW_Client/Game.cpp` now starts that render interface with a Linux `RenderMode`, keeps it sized to the shell window, and routes the frame begin/end path through `PF_Render::Interface::Render()` / `Present()` on top of the OpenGL backend instead of doing shell-side clear/present calls directly. The current verified smoke run reports `Render bootstrap: PF_Render::Interface via OpenGL` plus `Render frame path: PF_Render::Interface::Render/Present` in stdout and records `renderBootstrapReady=yes`, `renderBootstrapFramePath=PF_Render::Interface::Render/Present`, and the active render mode in `linux-client-shell.log`.
- The Linux shell UI now also renders through the canonical `PF_Render::Interface::RenderUI()` path in `PF_GameLogic/PFRenderInterface.cpp` instead of drawing beside the render interface. `PW_Client/Game.cpp` registers a Linux-only UI callback on the bootstrap render interface, the shell frame now reports `Render UI path: PF_Render::Interface::RenderUI callback` in stdout, and the startup log records `renderBootstrapUiPath=PF_Render::Interface::RenderUI callback`.
- The Linux OpenGL bootstrap now also initializes the canonical `Render::IUIRenderer` surface through `PF_Render::Interface::Start()`, releases it through `Stop()`, and keeps the canonical UI resolution state in sync through `UI::UpdateScreenResolution()`. The current verified shell run reports `Render UI resolution: 1820x1024 on 1280x720`, and the startup log records `renderBootstrapUiResolution=1820x1024` plus `renderBootstrapScreenResolution=1280x720`.
- The Linux executable now adopts a narrow canonical `UI::Initialize(...)` / `UI::Release()` lifecycle after the OpenGL render bootstrap is live. `PW_Client/Game.cpp` resolves the real `UI/UIRoot`, calls `UI::Initialize(...)`, and logs `UI runtime: init=yes path=UI::Initialize`. On Linux bootstrap, `UI/Root.cpp` intentionally registers root/screens/content/constants/substitutes while keeping Flash, scripts, cursor creation, font renderer startup, debug UI, and `UI::User` creation out of the executable path until the real client loop needs them. The latest startup log records `uiRootRuntimeInit=yes` and `uiRootRuntimePath=UI::Initialize`.
- The Linux executable now also drives the canonical UI frame lifecycle each frame through `UI::ApplyNewParams(...)`, `UI::NewFrame(...)`, and `UI::PresentFrame(...)` in the OpenGL shell render loop. The verified runtime path now resolves canonical screen and constant lookups through `UI::GetScreenLayout(...)` and `UI::GetConstant(...)`, and the latest smoke run reports `UI frame loop: yes path=UI::ApplyNewParams/NewFrame/PresentFrame`, `UI runtime screen: ClinicResultsScreen -> <unnamed>`, `UI runtime content: test/<empty-group> [empty-group]`, and `UI runtime constant: test = 123`. The same run also proves that the remaining content seam is data-driven rather than a Linux registration bug: `Data/UI/UIRoot.xdb` currently defines one content group, `test`, with an empty `<resources />` list, so the Linux runtime now reports that state explicitly as `empty-group` instead of treating it as a failed content lookup.
- The Linux executable now also exercises the canonical `UI::User` runtime path. `UI/Root.cpp` creates a real `UI::User` on the Linux bootstrap path, `PW_Client/Game.cpp` now drives `UI::User::StartEvent(...)`, `EndEvent(...)`, and `Step(...)`, and the verified runtime now reports `UI user: yes events=0 path=UI::User::StartEvent/EndEvent/Step`.
- The Linux executable now also exercises the canonical cursor bootstrap path without pulling the full Windows texture/material stack into the runnable target. `UI/Root.cpp` now calls `NCursor::Init()`, registers real cursor DB entries on Linux bootstrap, and no longer suppresses `UI::SetCursor(...)` or the frame-end cursor update in `UI::PresentFrame(...)` on Linux bootstrap. `Render/TextureManager.cpp` now provides the maintained Linux null-render texture-manager/default-texture surface needed by that path instead of an executable-local runtime stub. The current verified runtime reports `UI cursor: ready=yes registered=yes id=ui_normal size=29x48 hotspot=1,1 path=UI::SetCursor/NCursor::Update`.
- The Linux executable now also instantiates, steps, and draws the real `Game::NetworkStatusScreen` overlay on the bootstrap UI path. `PW_Client/Game.cpp` creates the screen after `UI::Initialize(...)`, injects a controlled async status through `SetClientAsync()`, and reports the overlay layout/window state in stdout and the startup log. The latest verified runs record `UI network status runtime: ready=yes window=<unnamed:1-children> visible=yes path=Game::NetworkStatusScreen::Init/SetClientAsync/Step/Draw` and `uiRootRuntimeNetworkStatusReady=yes`, proving the `NetworkStatus` layout is reachable in the runnable Linux client rather than only link-clean.
- The Linux executable now also links and exercises the real `Game::DebugVarsSender` command-registration/polling surface. `PW_Client/Game.cpp` creates a bootstrap sender and calls `PollSession(...)`; `PW_Client/DebugVarsSender.cpp` keeps the actual statistic RPC send branch behind a Linux bootstrap guard so the runnable client does not drag `GameStatClient::Message<DebugVarMessage>` / statistic-writer symbols into the executable before real statistics transport is active. The latest verified runs record `Debug vars runtime: ready=yes polled=yes path=Game::DebugVarsSender::DebugVarsSender/PollSession(null-statistics)` and `uiRootRuntimeDebugVarsReady=yes`.
- The Linux executable now directly links `PW_Client/LobbyConnection.cpp`; the target also carries `${Src}/Server/Chat` in its include roots so the chat RPC generated headers resolve the same way the maintained runtime probe already did. `PW_Client/LocalGameContext.cpp` is now also executable-linked with a `PW_LINUX_DB_BOOTSTRAP` path that keeps command registration, map/hero setup, loading-screen handoff, scheduler creation, transceiver creation, and state transitions live while deferring the real `ChatUiController`, `IgnoreListStorage`, threaded map-loading job, and `AdventureScreen` calls to the Windows/non-bootstrap path. The remaining wider lobby/client-context files, especially `GameContext.cpp`, `NewLobbyClientPW.cpp`, and `NewLobbyGameClientPW.cpp`, still require real transport/RPC gatekeeper, persistent events, game chat, world serialization, statistic RPC writers, and `lobby::ClientBase` / `lobby::GameClient` implementations before they can be executable-linked.
- The Linux executable now also links the real screen-command/main-loop/font/script/preferences UI surface: `Client/ScreenCommands.cpp`, `Client/MainLoop.cpp`, `Client/CommandBinds.cpp`, `Client/Console.cpp`, `Client/ConsoleLogic.cpp`, `Client/DebugTextDraw.cpp`, `Client/TooltipsScreen.cpp`, `Client/TooltipsScreenLogic.cpp`, `UI/DebugDraw.cpp`, `UI/Scripts.cpp`, `UI/Alphabet.cpp`, `UI/FontRender.cpp`, `UI/FontStyle.cpp`, and `UI/PreferencesProcessor.cpp`, on top of the diagnostics slice (`UI/DebugVarsRender.cpp` and `UI/FrameTimeRender.cpp`). The old executable-only screen-command, UI script, font-renderer, tooltip, and `NMainLoop::GetScreens()` shims were removed from `runtime_stubs.cpp`; the executable now uses the real `MainLoop.cpp` screen list, real command-bind dispatch, real console screen creation, and real tooltip screen creation. `Client/MainLoop.cpp` now steps the real `UI::Debug::Step(...)` path too, so there is no remaining Linux-bootstrap guard in `Client/MainLoop.cpp`. `CoreFSM.cpp` is now executable-linked with `BaseFSM.cpp`, `Updater.cpp`, and `UpdateProcessor.cpp`, so `NMainLoop::Step()` calls the real global FSM step on Linux; `runtime_stubs.cpp` only provides the narrow invalid `IBaseFSMState` sentinel needed because `BaseState.cpp` itself is still not GCC-clean. `PreferencesProcessor.cpp` now has a `PW_LINUX_DB_BOOTSTRAP` implementation that preserves the object/lifecycle surface without touching `FlashContainer2` / `FlashInterface`, so Windows stays on the original Flash-backed implementation path. Diagnostic line/text rendering now comes from the maintained Linux `debugrenderer.cpp` and `smartrenderer.cpp` null-render translation units; `runtime_stubs.cpp` still only carries unrelated narrow helpers such as `utils::GetMemoryStatus` and `NDebug::GetProductNameW`. `FontStyle.cpp` skips D3D material creation only under `PW_LINUX_NULL_RENDER`, and `Alphabet.cpp` uses Unicode escapes for its Cyrillic metric-sampling glyphs so GCC no longer depends on the source file's legacy byte encoding.
- The Linux executable now also takes the first real screen-layout step instead of stopping before layout initialization. `PW_Client/SelectGameModeScreen.cpp` now lets the Linux bootstrap call `LoadScreenLayout("Lobby_SelectGameMode")`, and `PW_Client/Game.cpp` now reports the resulting runtime window state clearly. The current verified runtime reports `UI screen runtime: ready=yes window=<unnamed:15-children> events=0 path=NGameX::SelectGameModeScreen::Init/LoadScreenLayout/Step/Draw`, which confirms that the canonical `SelectGameModeScreen` now owns a live main window on Linux even though the layout still has no explicit window name.
- The Linux executable now also reaches the first live custom-lobby hero-selection screen on the canonical UI path. `PW_Client/Game.cpp` now accepts `--bootstrap-create-game`, requests the narrow custom-lobby flow through the existing Linux bootstrap context, and then drives `NGameX::SelectHeroScreen::Init/CommonStep/Draw` directly. The current verified runtime reports `UI hero runtime: ready=yes window=<unnamed:13-children> players=6 path=NGameX::SelectHeroScreen::Init/CommonStep/Draw`, which confirms that Linux now gets past the game-mode list and into the first live hero-selection screen without touching the Windows transport path.
- The Linux executable now also keeps the live custom-lobby hero-selection state synchronized with the canonical bootstrap game context instead of only drawing the screen. `PW_Client/Game.cpp` now pushes the selected team/hero from the Linux local-match preview into `LinuxBootstrapGameContextUi`, keeps faction aligned on the bootstrap path when appropriate, and reports the resulting state from the live `SelectHeroScreen` runtime. The current verified custom-lobby run reports `UI hero state: team=2 faction=2 ready=no hero=shadow`, which proves that the live hero-selection screen is now reading the canonical game-context state on Linux instead of only showing a static preview.
- The Linux executable now also crosses the first real custom-lobby game-start boundary and instantiates a canonical `Game::LoadingScreen` on Linux through the existing `MapStartInfo` / `LoadingGameContext` / `LoadingScreenLogic` path. `PW_Client/Game.cpp` now auto-readies the bootstrap custom-lobby flow, builds a real loading context from the selected map/lineup, and drives `Game::LoadingScreen::Init/Step/Draw` directly in the executable path. The current verified runtime reports `UI loading runtime: ready=yes window=<unnamed:2-children> players=7 status=Загрузка path=Game::LoadingScreen::Init/Step/Draw`, which confirms that Linux now moves past the live `SelectHeroScreen` path and into the first real loading-screen transition without touching the Windows transport path.
- The Linux loading runtime seam now also resolves the localized status text correctly on the canonical loading-screen path instead of surfacing a Linux-specific fallback key. `PW_Client/LoadingStatusHandler.cpp` now uses explicit Unicode conversion for the non-localized fallback path, and `PW_Client/Game.cpp` normalizes brace-wrapped loading-status ids back through the already-decoded loading-status table before reporting them. The latest verified deeper log at `Bin/logs/2026.04.27-09.21.04/linux-client-shell.log` records `uiRootRuntimeLoadingScreenReady=yes`, `uiRootRuntimeLoadingScreenPath=Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap`, `uiRootRuntimeLoadingPlayerEntries=7`, `uiRootRuntimeLoadingStatusReady=yes`, and `uiRootRuntimeLoadingStatusText=Загрузка`.
- The Linux loading runtime seam now also drives real `LoadingProgress` and per-player progress through the canonical `Game::LoadingScreen` path instead of holding the Linux bootstrap branch at `0.0f`. `PW_Client/Game.cpp` now uses `NWorld::MapLoadingJob`'s progress object when the bootstrap create-game path is active, seeds per-player loading progress for the real lineup, and reports the resulting progress/disconnect state in the Linux overlay/stdout/log. `PW_Client/LoadingScreen.cpp` now uses `progress->GetTotalProgress()` on the Linux bootstrap path too, so the canonical screen logic consumes the same progress source on both Windows and Linux. The latest verified deeper log at `Bin/logs/2026.04.27-09.21.04/linux-client-shell.log` records `uiRootRuntimeLoadingScreenReady=yes`, `uiRootRuntimeLoadingScreenPath=Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap`, `uiRootRuntimeLoadingPlayerEntries=7`, `uiRootRuntimeLoadingProgress=1`, `uiRootRuntimeLoadingDisconnected=0`, and `uiRootRuntimeLoadingStatusText=Загрузка`.
- The deeper `--bootstrap-create-game` smoke path now also verifies repeated local scheduler and transceiver/PFWorld execution in the executable while the replay serialization code is compiled into `LocalCmdScheduler`, and it now proves one real serialized packed world command through the same boundary. `PW_Client/Game.cpp` primes the bootstrap `Game::LocalCmdScheduler`, creates a real `NWorld::PFWorld` object through `PFWorld::CreatePFWorld()`, attaches it to a real `NCore::Transceiver`, queues a `LinuxBootstrapWorldRuntimeCommand` after the priming step, and drives bounded repeated `Transceiver::Step(...)` calls during loading instead of consuming only the first local scheduler segment. The latest verified deeper runs record scheduler start/segment readiness, `Transceiver::SetWorld(PFWorld)`, packed-command serialization and execution (`finalGameTransceiverRuntimeCommandsSent=1`, `finalGameWorldExecutedPackedCommands=1`, `finalGameWorldBootstrapRuntimeCommands=1`, `finalGameWorldLastBootstrapCommandToken=20260530`), `finalGameTransceiverStepCalls=96`, `finalGameTransceiverProcessedSteps=96`, `finalGameTransceiverWorldStep=95`, `finalGameTransceiverCommandBatches=96`, `finalGameTransceiverStatusUpdates=96`, `finalGameTransceiverNoData=no`, `finalGameWorldPlayers=10`, `finalGameWorldPresentPlayers=1`, `finalGameWorldFog=205x205`, and loaded-world map-object counters in `Bin/logs/2026.05.30-18.35.40/linux-client-shell.log` for MOBA and `Bin/logs/2026.05.30-18.36.01/linux-client-shell.log` for NightMap.
- `PF_GameLogic/MapStartup.cpp` now compiles and links inside the runnable executable, and `PF_GameLogic/AsyncMapStartup.cpp` is maintained by `PrimeWorldLinuxClientRuntimeProbe`. The Linux bootstrap branch keeps the real `NWorld::MapLoadingJob` object/progress/camera surface and makes `MapLoader::IsThreaded()` return false. Under `PW_LINUX_NULL_RENDER`, `MapLoadingJob` now keeps the attached `PFWorld` pointer, runs the bootstrap `InitializeEnvironment()` seam, and calls `PFWorld::LoadMap(...)` with the real map descriptor, camera settings, player start info, reconnect flag, and an empty bootstrap talent map. The deeper smoke now constructs `MapLoadingJob`, calls `NWorld::MapLoadingJob::DoTheJob()`, feeds its `LoadingProgress` into the canonical loading screen, and records `UI loading runtime: ... progress=1.00 ... path=Game::LoadingScreen::Init/Step/Draw + NWorld::MapLoadingJob::DoTheJob/PFWorld::LoadMap` in `Bin/logs/2026.04.27-09.21.04/linux-client-shell.log`.
- `PF_GameLogic/TileMap.cpp`, `PF_GameLogic/DI/DI.cpp`, `PF_GameLogic/PFPlayer.cpp`, and `PF_GameLogic/WarFog.cpp` are now promoted into the runnable Linux client and the maintained client-runtime probe. The Linux null-render `PFWorld::LoadMap(...)` now creates a real `TileMap`, calls `TileMap::Prepare(...)`, sizes the map from the real terrain descriptor, builds the real `vector<CObj<PFPlayer> > players` from the canonical `NCore::TPlayersStartInfo`, then creates and resets a real `FogOfWar` from the prepared tile-map dimensions. `PFPlayer.cpp` keeps the original Windows/full-gameplay implementation outside `PW_LINUX_NULL_RENDER`; the Linux null-render branch preserves the object, namemap, team/faction, disconnect, player-id/team/user-id, and lightweight step/lifecycle surface. Hero attachment is no longer fully deferred: the current Linux null-render `PFWorld::LoadMap(...)` resolves player hero IDs from `SessionRoot.logicRoot.heroes`, creates `PFBaseHero` objects through the Linux `CreateHero` factory at authored `HeroPlaceHolder` placements, and the Linux `PFBaseHero::AttachToPlayer(...)` branch now mirrors the essential Windows attach state by setting player/faction/gender IDs, calling `PFPlayer::AttachHero(...)`, and running `ClientAttachToPlayer()`. `PlayerBehaviourTracker` remains deferred. `WarFog.cpp` keeps its debug-frustum draw path out of `PW_LINUX_NULL_RENDER`, while the executable now gets the no-op debug line surface from the maintained Linux `debugrenderer.cpp` translation unit. The latest verified `--bootstrap-create-game` smokes at `Bin/logs/2026.05.31-05.08.59/linux-client-shell.log` and `Bin/logs/2026.05.31-05.09.11/linux-client-shell.log` record `spawnedHeroes=10` / `playerHeroes=10` for MOBA and NightMap, with a 19-command cross-map stream through init-minigame and a 20-command MOBA stream through pickup-object when loaded pickupable content exists.
- `PF_GameLogic/PFWorldProtection.cpp` is now also promoted into the runnable Linux client and the maintained client-runtime probe. `PFWorld::LoadMap(...)` creates the real `PFWorldProtection::Create(...)` object, which selects the existing dummy protection implementation in the non-`STARFORCE_PROTECTED` build, and the Linux null-render `PFWorld::Step(...)`, `PollProtectionResult(...)`, and `SetProtectionUpdateFrequency(...)` now delegate to the real protection object instead of staying as standalone PFWorld stubs. The `NWorld::PFWorldProtection` bootstrap object-cast shim was removed from `runtime_stubs.cpp`; Windows and protected builds stay on the original source path.
- `PF_GameLogic/DayNightController.cpp` is now also promoted into the runnable Linux client and the maintained client-runtime probe. `PFWorld::LoadMap(...)` creates and initializes the real `DayNightController`, `PFWorld::Step(...)` updates it, and `PFWorld::IsDay()` / `IsNight()` now query it instead of returning fixed bootstrap values. Under `PW_LINUX_NULL_RENDER`, `DayNightController::Initialize()` still reads real map day/night settings and initializes the controller state, but deliberately leaves scene lighting, render light filters, and AI object day/night update traversal disabled until the scene/AI object stacks are executable-linked. The `NWorld::DayNightController` bootstrap object-cast shim was removed from `runtime_stubs.cpp`; Windows/full-render behavior stays on the original implementation path.
- `PF_GameLogic/PFWorldNatureMap.cpp` is now also promoted into the runnable Linux client and the maintained client-runtime probe. Linux `PFWorld::LoadMap(...)` creates a real `PFWorldNatureMap`, loads the DB terrain/nature-map descriptor through `OnLoaded(...)`, and steps it from `PFWorld::Step(...)`; under executable `PW_LINUX_NULL_RENDER`, the scene-backed `Terrain::NatureMap*` stays absent and terrain mutation/AI traversal methods compile to inert returns until the real scene terrain and AI object stacks are executable-safe. The runtime/gameplay probes still compile the real terrain-backed branch, `PFWorldNatureMap.h` no longer leaks `PFAIWorld.h` transitively, the direct `PFAIWorld.h` users now include it explicitly, and the `NWorld::PFWorldNatureMap` bootstrap object-cast shim was removed from `runtime_stubs.cpp`. Windows/full-render behavior stays on the original implementation path.
- `PF_GameLogic/PFStatistics.cpp` is now also promoted into the runnable Linux client while staying maintained by the client-runtime and gameplay probes. Linux `PFWorld::LoadMap(...)` creates the real `PFStatistics` object through an executable-only helper and `PFWorld::Step(...)` steps that object; under executable `PW_LINUX_OPENGL_BOOTSTRAP` + `PW_LINUX_NULL_RENDER`, the statistics shell owns the real scoring table reference and UI-event DB reset state but leaves hero iteration, achievement updates, AdventureScreen UI dispatch, and stat event mutation inert until `PFAIWorld`, heroes/units, and the full UI/gameplay event stack are executable-safe. The executable now links the `PFStatistics` translation unit directly; `runtime_stubs.cpp` keeps bootstrap cast fallbacks only for the heavier still-unlinked logic/unit/AI classes referenced by statistics headers. Windows/full-gameplay behavior stays on the original implementation path.
- `PF_GameLogic/TriggerMarkerHandler.cpp` is now also promoted into the runnable Linux client and the maintained client-runtime/gameplay probes. Linux `PFWorld::LoadMap(...)` resolves the real map settings, constructs a real `TriggerMarkerHandler`, owns the real trigger-marker DB binding state, and steps the handler from `PFWorld::Step(...)`; under executable `PW_LINUX_OPENGL_BOOTSTRAP` + `PW_LINUX_NULL_RENDER`, marker stepping is intentionally inert until `PFAIContainer`, `PFAIWorld`, units, and active script-area traversal are executable-safe. `TriggerMarkerHandler.h` no longer drags `PFLogicObject.h` into every includer; the functor body lives in the `.cpp`, preserving the Windows/full-gameplay implementation path. The `NWorld::TriggerMarkerHandler` bootstrap object-cast shim was removed from `runtime_stubs.cpp`.
- `PF_GameLogic/CollisionResolver.cpp` is now also promoted into the runnable Linux client and the maintained client-runtime/gameplay probes. Linux `PFWorld::LoadMap(...)` now creates a real `CollisionResolver` object and owns it through the real `PFWorld` member/save-load surface; under executable `PW_LINUX_OPENGL_BOOTSTRAP` + `PW_LINUX_NULL_RENDER`, `Resolve(...)` now performs the bootstrap DB-sized moving-unit overlap separation described above instead of staying inert. The full collision prediction/resolution implementation remains compiled by `PrimeWorldLinuxGameplayProbe` and the Windows/full-gameplay path. The `NWorld::CollisionResolver` bootstrap object-cast shim was removed from `runtime_stubs.cpp`.
- `PF_GameLogic/PFAIContainer.cpp` is now also promoted into the runnable Linux client and the maintained client-runtime/gameplay probes. Linux `PFWorld::LoadMap(...)` now creates a real `PFAIContainer` object and `PFWorld::Step(...)` steps it through the real PFWorld-owned member; under `PW_LINUX_NULL_RENDER`, the AI container preserves object/save-load, script-area/path/minimap-icon, and script command lookup surfaces while keeping controller creation, Lua script execution, unit/object traversal, effects, minimap updates, and script-triggered world mutation inert until `PFAIWorld`, units/heroes, Lua script binding, and active AI controller execution are executable-safe. The full AI container/controller/script implementation remains compiled by `PrimeWorldLinuxGameplayProbe` and the Windows/full-gameplay path. The `NWorld::PFAIContainer` bootstrap object-cast shim was removed from `runtime_stubs.cpp`; the Linux branch owns the narrow inert serializer/cast endpoints needed by its still-inert member containers.
- `PF_GameLogic/PFAIWorld.cpp` and `PF_GameLogic/PFVoxelMap.cpp` are now also promoted into the runnable Linux client and the maintained client-runtime probe. Linux `PFWorld::LoadMap(...)` now creates a real `PFAIWorld`, sizes its `PFVoxelMap` from the prepared `TileMap`, passes the resolved map description/settings into `PFAIWorld::SetMapData(...)`, and `PFWorld::Step(...)` updates it. Under `PW_LINUX_NULL_RENDER`, `PFAIWorld` preserves object/save-load, DB parameter ownership, route/spawner bookkeeping, faction color/name lookup, force/level/surrender/glyph query surfaces, and AI-world update ownership while keeping voxel insertion, hero/unit registration, assist/killer analysis, voting UI, glyph manager behavior, unit traversal, and active route/tower mutation inert until `PFBaseUnit`, heroes, glyphs, and moving-unit traversal are executable-safe. `PFVoxelMap` keeps its object and size surface but leaves ring mutation/traversal inert in the executable. The full AI-world/voxel implementation remains compiled by `PrimeWorldLinuxGameplayProbe` and the Windows/full-gameplay path. The `NWorld::PFAIWorld` bootstrap object-cast shim was removed from `runtime_stubs.cpp`.
- `PF_GameLogic/PFLogicObject.cpp` is now also promoted into the runnable Linux client and the maintained client-runtime/gameplay probes. Under `PW_LINUX_NULL_RENDER`, it preserves the real world-object/client-object registration surface, formula-parameter base, faction/team/type/position/size/range/nature helpers, hidden counter, and AI-world registration/unregistration calls while keeping terrain height sampling and real client visibility callbacks inert until scene/client objects and units are executable-safe. The `NWorld::PFLogicObject` bootstrap object-cast shim was removed from `runtime_stubs.cpp`; Windows/full-gameplay stays on the original implementation.
- `PF_GameLogic/PFAdvMapObject.cpp`, `PF_GameLogic/PFSimpleObject.cpp`, `PF_GameLogic/PFTree.cpp`, and `PF_GameLogic/PFGlyph.cpp` are now also promoted into the runnable Linux client and the maintained client-runtime probe. `PFAdvMapObject.cpp` now includes `DBAdvMap.h` explicitly instead of relying on precompiled include state, and the Linux null-render `PFWorld::LoadSceneMapObjects(...)` now constructs the real `PFWarFogUnblock`, `PFSimpleObject` (`SimpleObject`, base `GameObject`, and `Road` DB types), `PFMultiStateObject`, `PFTree`, `PFGlyphSpawner`, and `PFAdvMapObstacle` objects from the real map object list, marks them as map objects, and hands them to the AI-container script registration seam. `PFMultiStateObject` now has a Linux null-render constructor path that keeps its `PFLogicObject` initialization, nature/day-night state, map-object tile/fog marking, and coarse object-size surface while leaving client scene state selection and DB scene-component AABB probing to the Windows/full-render path. `PFTree` keeps real `PFLogicObject` initialization, nature/day-night state, tree tile/fog obstacle marking, visibility queries, grow/drop state bookkeeping, and object-size updates while guarding client-tree animation calls and scene-component AABB probing under `PW_LINUX_NULL_RENDER`. `PFGlyph.cpp` now has a Linux null-render branch that keeps `PFGlyphSpawner` object/save-load/script-name/visibility state in the executable while leaving active glyph pickup creation, spawned `PFGlyph` objects, and nature glyph behavior inert until `PFPickupableObjectBase`, `PFBaseUnit`, and glyph-manager behavior are executable-safe. The executable now reports Linux-loaded map-object counters in stdout/logs; the latest ARAM create-game smoke constructs `2442` static map objects (`282` simple, `740` multi-state, `1395` trees, `7` glyph spawners, `18` obstacles, `0` war-fog unblockers). The still-heavy map-object constructors (towers, shops, glyph pickup/building/spawner/flagpole objects, minigame objects, and unit/hero-backed objects) remain deferred until their scene/client/unit dependencies are executable-safe. The old `NWorld::PFSimpleObject` bootstrap object-cast shim was removed from `PFAIContainer.cpp`; Windows/full-gameplay stays on the original implementation.
- `PF_GameLogic/PFWorld.cpp` now compiles and links inside the runnable executable and `PrimeWorldLinuxClientRuntimeProbe` through a `PW_LINUX_NULL_RENDER` bootstrap branch, while `PrimeWorldLinuxGameplayProbe` still compiles the real non-null-render branch. The bootstrap branch keeps PFWorld's object/refcount/save-load surface, command/status entry points, simple step counter, progress/camera compatibility, lightweight protection/day-night/player methods, and now the first real `LoadMap(...)` data handoff alive without pulling the full scene/terrain/map-object construction stack into the Linux client too early. The Linux `LoadMap(...)` branch records the incoming map descriptor, counts human start slots, derives a coarse map size from the terrain descriptor when present, drives the MapObjects/Heroes/HeightMap progress stages through `LoadSceneMapObjects(...)`, and preserves Windows on the original implementation path. The old local fake transceiver world in `PW_Client/Game.cpp` was removed; the Linux transceiver now attaches `NWorld::PFWorld::CreatePFWorld()` directly. The next PFWorld work is to replace these bootstrap PFWorld methods with the real scene/terrain/map-object loading path in a controlled chunk.
- `PW_Client/LocalGameContext.cpp` now compiles and links inside both `PrimeWorldLinuxClient` and `PrimeWorldLinuxClientRuntimeProbe`. The Linux bootstrap branch deliberately skips only the still-unlinked presentation/runtime edges (`PFResourcesCollection::CollectConsumables`, chat UI storage construction, `JobThread` control, and `AdventureScreen` activation), while leaving Windows behavior on the original code path. The next retained step should replace the remaining bootstrap world/presentation shortcuts with the real `AdventureScreen`/PFWorld seam in one deliberate scene/runtime chunk.
- `PF_Core/AnimatedTexture.cpp`, `PF_Core/CameraShakeController.cpp`, `PF_Core/ChangeMaterialEffect.cpp`, `PF_Core/EffectsPool.cpp`, `PF_Core/EnableSCEffect.cpp`, `PF_Core/GhostEffect.cpp`, `PF_Core/IUpdateable.cpp`, `PF_Core/ObjectsPool.cpp`, `PF_Core/RecolorableTexture.cpp`, `PF_Core/ScaleColorEffect.cpp`, `PF_Core/SpectatorEffectsPool.cpp`, `PF_Core/BasicEffect.cpp`, `PF_Core/ControlledStatusEffect.cpp`, `PF_Core/CameraShakeSceneComponent.cpp`, `PF_Core/SpringSceneComponent.cpp`, `PF_Core/EffectGenSceneComponent.cpp`, `PF_Core/ParticleSceneComponentWithEffect.cpp`, `PF_Core/TraceGenSceneComponent.cpp`, and `PF_Core/LightningEffect.cpp` are now compiled into both the runnable Linux client and the maintained client runtime probe.
  The Linux bootstrap path keeps the real pool bookkeeping, `NDb::EffectsPool` mapping, singleton wiring, retrieve entry points, the canonical update rings, the pool placeholder translation unit, spectator-effect-switcher bookkeeping, animated texture update/load entry points, recolorable texture loader/cache shell, the change-material effect lifecycle/fade shell, the scale-color effect lifecycle/color/scale shell, the ghost-effect target/prototype shell, the camera-shake-controller translation unit, the first narrow component-enable effect, the real `BasicEffect` lifecycle/update/color/interruption implementation, controlled-status effect event/synchronization translation unit, camera-shake/spring scene-component helpers, effect-generator/particle/trace component helpers, and the remaining active-effect implementation helpers in the executable. `EffectsPool::RetrieveFactory(...)` now calls generated `NDb::EffectBase::Retrieve(...)` for Linux bootstrap builds, and the Linux shell verifies the no-component default effect (`Tech/Default/Default.EFFT.xdb`), standalone component-backed `ClickOnTerrain` (`Effects/UI/PathDestination/_.EFFT.xdb`, `root=yes`, `components=7`, scene objects `1/0` after add/release), attached `SelectionAuraSelf` (`Effects/UI/Selection/HeroSelection.EFFT.xdb`, `BasicEffectAttached`, `components=3`, target components `1/3/1`) through real effect-pool retrieval/release and scene-object attach/detach mechanics, and the canonical `BasicEffectAttached::Apply(CPtr<ClientObjectBase>)` path through a Linux probe client object (`callback=yes/1`, `applied=yes`, `updated=yes`, `killed=yes`, `detached=yes`). `KillAllActiveEffects()` remains inert for DB-only Linux bootstrap tools but now traverses active effects in the runnable null-render client, and gameplay-owned scene objects/gameplay-spawned effect ownership remain future work. Texture-manager/default-texture helpers now come from the maintained Linux `Render/TextureManager.cpp` path. `PF_Core/BasicEffect.h` now includes `StringExecutorBootstrap.h` before `DBStats.h` under `PW_LINUX_DB_BOOTSTRAP`, includes the generated `NDb::EFaction` definition explicitly, and has a GCC-safe `this->pDBObj` dependent-base lookup fix in `EffectDBLinker::GetDBEffect()`, `PF_Core/EffectsPool.h` avoids pulling `ControlledStatusEffect.h` too early on the bootstrap path, `PF_Core/AnimatedTexture.h` and `PF_Core/RecolorableTexture.h` now use the repo-correct `Render/texture.h` include casing for Linux filesystems, `RecolorableTexture.cpp` keeps its Win32 debug thread check Windows-only and no-ops the immediate-render `FillTexture(...)` plus scene recolor traversal helpers only under `PW_LINUX_NULL_RENDER`, `PF_Core/ChangeMaterialEffect.cpp` no-ops material traversal/replacement plus unsupported material pin/blend/emissive calls under `PW_LINUX_NULL_RENDER`, `PF_Core/GhostEffect.cpp` no-ops scene-object clone/update work under `PW_LINUX_NULL_RENDER` while preserving the original Windows scene clone path, `PF_Core/CameraShakeController.cpp` now pass-throughs camera update under `PW_LINUX_NULL_RENDER` while preserving the original scene-component-driven shake path for Windows and non-null render builds, and `PF_Core/LightningEffect.h` now has a Linux-safe local 16-byte alignment macro while preserving the Windows `__declspec(align(16))` path. Windows behavior stays on the original implementation path.
- `PF_Core/StatusEffect.cpp` was tested as the next effect candidate and deliberately left out of the maintained runtime probe: this branch's generated `PF_Core/DBEffect.h` does not define `NDb::StatusEffect` or `NDb::EStatusEffectSlot`, and the main `Src/CMakeLists.txt` already removes `StatusEffect.cpp`. Do not retry it as a simple probe addition unless the generated DB layer is restored or the obsolete source is reconciled with the current `ControlledStatusEffect` model.
- The Linux executable and the maintained runtime probe now both compile against the canonical `PF_GameLogic/PFRenderInterface.cpp`. The temporary Linux-only sidecar bootstrap translation unit is gone, and the Linux bootstrap implementation now lives directly in the real source file behind the Linux render guards while the original Windows Direct3D body remains intact under `#else`.
- The maintained Linux gameplay probe currently compiles the broad gameplay `.cpp` slice, now including `PF_GameLogic/PFAuraEffect.cpp`, `PF_GameLogic/PFMinimapEffect.cpp`, `PF_GameLogic/PFPlayAnimEffect.cpp`, `PF_GameLogic/PFPriestessSignEffect.cpp`, `PF_GameLogic/PFEffectRandom.cpp`, `PF_GameLogic/PFEffectSwitcher.cpp`, `PF_GameLogic/PFInvisibilityEffect.cpp`, and `PF_GameLogic/PFUnitSceneObjectModify.cpp` for the generated gameplay-effect runtime path.
- The maintained Linux client runtime probe currently compiles the broad client/runtime/UI/render-bridge/map-startup/PFWorld/generated-effect/effect-pool/texture/camera-shake/spring/effect-generator/active-effect/update-ring/base-world/gameplay-helper `.cpp` slice, with `PF_GameLogic/AsyncMapStartup.cpp`, `PF_GameLogic/MapStartup.cpp`, `PF_GameLogic/TriggerMarkerHandler.cpp`, `PF_GameLogic/CollisionResolver.cpp`, `PF_GameLogic/PFAIContainer.cpp`, `PF_GameLogic/PFAIWorld.cpp`, `PF_GameLogic/PFVoxelMap.cpp`, `PF_GameLogic/PFLogicObject.cpp`, `PF_GameLogic/PFAdvMapObject.cpp`, `PF_GameLogic/PFSimpleObject.cpp`, `PF_GameLogic/PFTree.cpp`, `PF_GameLogic/PFGlyph.cpp`, `PF_GameLogic/PFPickupable.cpp`, `PF_GameLogic/PFBaseSpawner.cpp`, `PF_GameLogic/PFBaseUnit.cpp`, `PF_GameLogic/PFBattleBuilding.cpp`, `PF_GameLogic/PFBuildings.cpp`, `PF_GameLogic/PFCommonCreep.cpp`, `PF_GameLogic/PFFlagpole.cpp`, `PF_GameLogic/PFMainBuilding.cpp`, `PF_GameLogic/PFMinigamePlace.cpp`, `PF_GameLogic/PFNeutralCreep.cpp`, `PF_GameLogic/PFRoadFlagpole.cpp`, `PF_GameLogic/PFScriptedFlagpole.cpp`, `PF_GameLogic/PFTower.cpp`, `PF_GameLogic/PFAuraEffect.cpp`, `PF_GameLogic/PFMinimapEffect.cpp`, `PF_GameLogic/PFPlayAnimEffect.cpp`, `PF_GameLogic/PFPriestessSignEffect.cpp`, `PF_GameLogic/PFEffectRandom.cpp`, `PF_GameLogic/PFEffectSwitcher.cpp`, `PF_GameLogic/PFInvisibilityEffect.cpp`, `PF_GameLogic/PFUnitSceneObjectModify.cpp`, and the Linux null-render branch of `PF_GameLogic/PFWorld.cpp` now included alongside the existing client runtime slice.
- The Linux UI bootstrap seam is narrower now. `UI/Cursor.cpp`, `UI/FontRender.cpp`, and `UI/FontStyle.cpp` all build under the maintained Linux runtime path, `UI/Root.cpp` now lets the executable adopt `UI::Initialize(...)`, and the Linux executable now also reaches the canonical `UI::User` and cursor bootstrap paths without pulling the full Flash/script/window/material stack into the runnable client too early.
- The maintained Linux render probe now compiles 165 render/scene/terrain `.cpp` files, including the real render-runtime path pieces `batch.cpp`, `DXManager.cpp`, `OcclusionQueries.cpp`, `DBRender.cpp`, `DBRenderResources.cpp`, `shaderdefinestable.cpp`, `DeviceLost.cpp`, `ReadDDS.cpp`, `SHCoeffs.cpp`, `VertexColorStream.cpp`, `shaderconstantsbinder.cpp`, `Blur.cpp`, `Bloom.cpp`, `ConstantProtection.cpp`, `FullScreenFX.cpp`, `AOERenderer.cpp`, `WaterConvexes.cpp`, `CleanGeometry.cpp`, `fxresource.cpp`, `skeletonanimation.cpp`, `SkeletalAnimationBlender.cpp`, `SkeletalAnimationSampler.cpp`, `SkeletonWrapper.cpp`, `Stretcher.cpp`, `dipdescriptor.cpp`, `rendermode.cpp`, `DebugMaterial.cpp`, `GrassMaterial.cpp`, `MaterialResourceInterface.cpp`, `facefxsystem.cpp`, `ModelLoader.cpp`, `TimeCtrlSceneComponent.cpp`, and the now-maintained full `DiAnGr` / terrain-runtime/core slice on top of the earlier null-render mesh, scene, UI, and utility work.
- Recent additions to the maintained client runtime probe now cover the deeper lobby/runtime slice and the first real client screen/UI layer. Linux bootstrap now keeps `GameContext.cpp` inside the maintained runtime target by cutting the transport/ACE initialization path behind `PW_LINUX_DB_BOOTSTRAP`, sharing `EGameStatStatus` through `GameStatStatus.h`, adding a Linux `__time32_t` alias in `System/ported/types.h`, fixing the lobby status include-order seam around `LobbyClientBase.h`, and providing the missing `LobbyUserProxy` / `Peered` declarations for the bootstrap path. On top of that, the lobby layer itself is no longer outside the maintained target: `PW_Game/server_ip.h` now exists in the repo, `Shared/WebRequests.h` has a Linux bootstrap stub path, `LobbyConnection.cpp` has a Linux dummy castle-link path instead of pulling WinSock, `NewLobbyClientPW.cpp` was converted to UTF-8 and now uses `StringExecutorBootstrap.h`, `NewLobbyGameClientPW.cpp` no longer drags the generated `HybridServer/RPeered.auto.h` wrapper into the bootstrap path, `DebugVarsSender.cpp` and `LocalCmdScheduler.cpp` are maintained, and `CpuTopology.cpp` now has a Linux bootstrap implementation instead of depending on `<intrin.h>`. The runnable Linux client now also links the Linux-safe CPU topology/statistics helper trio directly (`PW_Client/CpuTopology.cpp`, `PW_Client/GameStatistics.cpp`, and `PW_Client/GameStatisticsWrapper.cpp`), the command/polling side of `DebugVarsSender.cpp`, the `LobbyConnection.cpp` translation unit, the network-status overlay (`PW_Client/NetworkStatusScreen.cpp`), and the real main-loop/screen-command/console/font/script/preferences UI slice (`Client/MainLoop.cpp`, `Client/ScreenCommands.cpp`, `Client/CommandBinds.cpp`, `Client/Console.cpp`, `Client/ConsoleLogic.cpp`, `Client/DebugTextDraw.cpp`, `UI/Alphabet.cpp`, `UI/DebugDraw.cpp`, `UI/DebugVarsRender.cpp`, `UI/FrameTimeRender.cpp`, `UI/FontRender.cpp`, `UI/FontStyle.cpp`, `UI/PreferencesProcessor.cpp`, and `UI/Scripts.cpp`). The maintained target now also includes `Client/DefaultScreenBase.cpp`, `PW_Client/SelectGameModeScreen.cpp`, `PW_Client/SelectHeroScreen.cpp`, and the bootstrap-safe UI stack around `UI/Root.cpp`, `UI/User.cpp`, `UI/Cursor.cpp`, `UI/FontRender.cpp`, `UI/FontStyle.cpp`, `UI/ImageComponent.cpp`, `UI/ImageLabel.cpp`, `UI/NameMappedWindow.cpp`, `UI/PreferencesProcessor.cpp`, `UI/ScreenLogicBase.cpp`, `UI/SkinStyles.cpp`, `UI/Scripts.cpp`, `UI/TextComponent.cpp`, `UI/TextComponentBasic.cpp`, `UI/TextMarkupLexems.cpp`, `UI/TextMarkupParser.cpp`, `UI/View.cpp`, and `UI/WindowPointJob.cpp`. The intentionally excluded executable UI slice is now focused on heavier Flash/container/vendor paths: `UI/FlashContainer2.cpp`, `UI/FlashFontsRender.cpp`, `UI/FlashInterface.cpp`, `UI/Window.cpp`, and `UI/TextMarkup.cpp`.
- The Linux executable no longer tries to link the entire `PrimeWorldLinuxClientRuntimeProbe` object library. That experiment was intentionally backed out because it exploded into unrelated transport/UI/link dependencies. Instead, the executable links the runtime pieces it needs as they become bootstrap-safe: the loading UI/status/hero path, the real screen-command queue (`Client/ScreenCommands.cpp`), `PW_Client/LocalGameContext.cpp`, the local scheduler, the canonical command serialization trio (`Core/CommandSerializer.cpp`, `Core/GameCommand.cpp`, `System/MemoryStream.cpp`), `Core/Transceiver.cpp`, the Linux-safe CPU topology/statistics/debug-vars/lobby-connection helper slice (`PW_Client/CpuTopology.cpp`, `PW_Client/DebugVarsSender.cpp`, `PW_Client/GameStatistics.cpp`, `PW_Client/GameStatisticsWrapper.cpp`, and `PW_Client/LobbyConnection.cpp`), the network-status overlay slice (`PW_Client/NetworkStatusScreen.cpp`), the bootstrap replay runner/transceiver slice (`PW_Client/ReplayRunner.cpp`, `PW_Client/ReplayTransceiver.cpp`), the executable UI diagnostics/font/script slice (`UI/Alphabet.cpp`, `UI/DebugDraw.cpp`, `UI/DebugVarsRender.cpp`, `UI/FrameTimeRender.cpp`, `UI/FontRender.cpp`, `UI/FontStyle.cpp`, and `UI/Scripts.cpp`), and now the generated-effect/effect-pool/update-ring/animated-texture/recolorable-texture/change-material-effect/scale-color-effect/ghost-effect/camera-shake-controller/base-world/FPU/gameplay-helper slice (`PF_Core/AnimatedTexture.cpp`, `PF_Core/CameraShakeController.cpp`, `PF_Core/ChangeMaterialEffect.cpp`, `PF_Core/DBEffect.cpp`, `PF_Core/EnableSCEffect.cpp`, `PF_Core/EffectsPool.cpp`, `PF_Core/GhostEffect.cpp`, `PF_Core/IUpdateable.cpp`, `PF_Core/ObjectsPool.cpp`, `PF_Core/RecolorableTexture.cpp`, `PF_Core/ScaleColorEffect.cpp`, `PF_Core/SpectatorEffectsPool.cpp`, `PF_Core/World.cpp`, `PF_Core/WorldObject.cpp`, `PF_GameLogic/FPUState.cpp`, `PF_GameLogic/PointersHolder.cpp`, `PF_GameLogic/SmartRandom.cpp`). The Linux `Game.cpp` shell builds a bootstrap-safe `LoadingFlashInterface`, dispatches real loading events through `LoadingStatusHandler`, records the real runtime status key, and resolves the displayed localized status text through the already-loaded `DBUIData` preview table.
- `Core/Transceiver.cpp` is now executable-linked under `PW_LINUX_DB_BOOTSTRAP`, and the runnable Linux client now uses the real `Core/BinStatsCollector.cpp` / `System/FileSystem/TinyFileWriteStream.cpp` implementation instead of an executable-local collector stub. Linux bootstrap still guards inactive replay-storage and debug file-writer branches until active replay output/playback is intentionally enabled, but the currently linked transceiver/CRC template surface no longer depends on fake BinStats definitions. Full active replay storage and CRC dump behavior are still future work.
- `Game::LocalCmdScheduler` now preserves every ready local `SyncSegment` in a FIFO queue instead of overwriting a single pending segment when the producer gets ahead of the transceiver consumer. The Linux create-game path queues the synthetic bootstrap world command plus two production `HeroActions` commands (`CmdKeepAlive` id `0x229AD400` and `CmdSetTimescale` id `0x228DA404`) before the first transceiver priming steps, proving the queued segment path and a real state-mutating command. `LinuxBootstrap/hero_actions_bootstrap.cpp` registers those command ids for the bootstrap executable without compiling the full UI-heavy `PF_GameLogic/HeroActions.cpp`; its Linux `CmdSetTimescale::Execute(...)` updates `PFWorld::timeScale` directly instead of touching `AdventureScreen`, while the original Windows/DirectX `HeroActions.cpp` path remains intact. `PFWorld` now records the last executed packed world-command type id and exposes `GetTimeScale()` in logs so smokes can prove the production command crossed and mutated the serialized transceiver boundary.
- The latest executable frontier moved from a fake bootstrap world to a real `PFWorld.cpp` executable/runtime-probe link with a Linux null-render branch and an actual `MapLoadingJob -> PFWorld::LoadMap(...)` handoff, crossed the first unit-backed object wall, and now also links the active PF_Core effect implementation plus generated default/component/attached effect factory paths, eight PF_GameLogic gameplay-effect factories (`PFAuraEffect`, `PFMinimapEffect`, `PFPlayAnimEffect`, `PFPriestessSignEffect`, `PFEffectRandom`, `PFEffectSwitcher`, `PFInvisibilityEffect`, and `PFUnitSceneObjectModify`), the native Linux `PFClientApplicators` helper surface, and the first buff-applicator-owned effect lifecycle. The current create-game smoke proves `PFWorld::LoadMap/TileMap::Prepare`, a real PFWorld player vector (`players=10`, `present=1`), a real fog grid (`fog=130x130`), bounded real `LoadSceneMapObjects(...)` construction over the ARAM object list, default effect pool retrieval/release (`BasicEffectStandalone`, `Tech/Default/Default.EFFT.xdb`), standalone component-backed scene add/update/remove (`BasicEffectStandalone`, `Effects/UI/PathDestination/_.EFFT.xdb`, `root=yes`, `components=7`, scene objects `1/0`), attached target effect attach/update/detach (`BasicEffectAttached`, `Effects/UI/Selection/HeroSelection.EFFT.xdb`, `root=yes`, `components=3`, target components `1/3/1`), canonical client-object effect application plus global active-effect removal (`callback=yes/1`, `updated=yes`, `killed=yes`, `target components 1/3/1`), helper-level applicator prepare/kill/standalone retrieval, `PFApplBuff` prepare/restart/kill ownership (`prepared=1/1`, `restarted=1/1`, `killed=yes`), synthetic `AuraEffect` state changes (`changes=2`), `MinimapEffect` update tracking (`updates=1`), `PlayAnimationEffect` scene update, `PriestessSignEffect` generic target/soul/fly lifecycle (`soul=yes/yes`, `fly=yes/yes`, `completed=yes`, `updates=3`, scene objects `1/2/1`), `RandomEffect` child selection/ownership, `EffectSwitcher` visible-branch child ownership, `InvisibilityEffect` color-channel update/removal (`opacity=1.00->0.50`), `UnitSceneObjectModify` temporary scene-object lifecycle (`objects=1/2/1`), direct Linux `MoveTo` stepping (`moveto=yes/1->0/269.0->264.0`), live target tracking (`track=yes/285.0`), follow-distance movement (`follow=yes/308.0->302.0`), mounted child movement mirroring (`attach=yes/318.0->310.0/310.0`), simple push/free-place/global movement ticking (`push=yes/332.0->336.0/332.0`, `find=yes/346.0`, `tick=yes/354.0->350.0`), straight movement tracing (`trace=yes/10.0->2.0`), blocked-step collision stopping (`block=yes/380.0->382.0`), bootstrap resolver overlap separation (`resolve=yes/2.0->4.0`), high-speed tunnel stopping (`tunnel=yes/440.0->442.0`), resolver side-placement search (`search=yes/2.0->4.0/36.0`), and fixed area/AI selector defaults (`forall=yes/2/0.16`, `maximizing=yes/yes/100.87`, `links=yes/yes/100.87`). The smoke reports `mapObjects=2468`, `simple=282`, `multi=740`, `trees=1395`, `glyphSpawners=7`, `obstacles=18`, `heroPlaceholders=10`, `creepSpawners=2`, `shops=2`, `quarters=2`, `towers=4`, `fountains=2`, `roadFlagpoles=2`, and `mainBuildings=2`. The concrete wall is now full production pathfinding/voxel steering and production collision-resolver behavior beyond the current overlap separation and side-placement search, content-driven movement trajectories, broader active gameplay behavior, gameplay-owned scene-object/effect ownership beyond the proven buff prepare/restart/kill lifecycle, remaining gameplay/applicator-spawned effects, and the real renderer backend rather than concrete map-object identity, active-effect compile/link ownership, generated factory null returns, standalone scene-component construction, basic attached-effect mechanics, client-object apply, aura/minimap/play-animation/priestess/random/switcher/invisibility/unit-scene-modify mechanics, helper-level applicator ownership, buff-owned effect lifecycle, animated-scene/creature-death factory promotion, direct/live-target/follow/attach null-render movement stepping, DB-sized unit geometry, simple bootstrap push/free-place checks, Linux straight-move tracing, blocked-step stopping, high-speed tunnel stopping, bootstrap resolver overlap separation, resolver side-placement search, or global active-effect kill traversal.
- Superseding verification for the prior "missing X display" note: the current sandbox smoke did create the native window and both normal/create-game runs exited `0` after the Scene/Terrain promotion. The create-game run reports the current ARAM counters as `mapObjects=2468`, `simple=282`, `multi=740`, `trees=1395`, `glyphSpawners=7`, `obstacles=18`, `shops=2`, `towers=4`, `fountains=2`, `roadFlagpoles=2`, and `mainBuildings=2`.
- Superseding the older active-effect note in the executable frontier paragraph: the direct `BasicEffect.cpp` / scene-effect promotion now succeeds after the broader Scene/Terrain promotion and duplicate cast-shim cleanup. The active effect implementation is executable-linked, the old executable-only `BasicEffect` shims are gone, `EffectsPool::RetrieveFactory(...)` now calls generated `NDb::EffectBase::Retrieve(...)`, and the Linux shell verifies default no-component, standalone scene-component-backed, attached target-effect creation/update/release, canonical `BasicEffectAttached::Apply(CPtr<ClientObjectBase>)`, global update-ring stepping, `KillAllActiveEffects()` removal, native `PFClientApplicators` helper ownership, `PFApplBuff` prepare/restart/kill ownership, and generated gameplay-effect behavior for `PFAuraEffect`, `PFMinimapEffect`, `PFPlayAnimEffect`, `PFPriestessSignEffect`, `PFEffectRandom`, `PFEffectSwitcher`, `PFInvisibilityEffect`, and `PFUnitSceneObjectModify` in the runnable null-render client. The next risky boundary is gameplay-owned effect lifetime through real unit/client objects and the remaining gameplay/applicator-spawned ownership, not compile/link ownership, null factory returns, basic scene-component creation, basic target attachment, helper-level applicator ownership, buff-owned effect lifecycle, aura/minimap/play-animation/priestess/random/switcher/invisibility/unit-scene-modify effect mechanics, or probe-owned global effect killing.
- Superseding the older "remaining gameplay/applicator-spawned ownership" note: the Linux executable now proves two concrete gameplay-spawned applicator paths. A real `PFApplAura` bootstrap instance creates an `AbilityUpgradeApplicator` child through the central factory and removes it through parent-owned child cleanup, and a real `PFDispatch` created from a synthetic `NDb::Spell` applies and removes an `AbilityUpgradeApplicator` through the dispatch applicator list. The next risky boundary is broader real unit/client-backed applicator behavior and active gameplay spell/attack flows, not central applicator factory null returns, aura child ownership, or dispatch list delivery.
- Superseding the older active ability stub note: the Linux executable now proves passive and active ability APIs above the raw dispatch/applicator layer. `PFAbilityData::ApplyPassivePart(...)` creates/removes a passive `AbilityUpgradeApplicator` on the owner, and `PFAbilityData::ApplyToTarget(...)` creates an active `PFAbilityInstance`, dispatches its `NDb::Ability::applicators`, applies the child to the receiver, and removes it through ability-owned cleanup. The next risky boundary is real content-driven ability/attack behavior with formula/cast-limitation/stat side effects, not Linux ability instance/data methods returning null.
- Superseding the older registry-only summon note: the Linux executable now proves controlled real summon allocation through `SummonApplicator`. A synthetic ability creates two `PFBaseSummonedUnit` instances, attaches `SummonBehaviourCommon`, registers both in the master's primary summon group, enumerates them through `ForAllSummons`, mirrors master faction/unit type, and activates a child `ValueApplicator` on each summon. The next risky summon boundary is content-driven clone/template behavior, full summon AI, pet escort behavior, and client-backed summon rendering, not generated summon factory null returns or master summon-group bookkeeping.
- Superseding the older special-applicator side-effect note: the Linux executable now proves controlled `PFApplSpecial` gameplay/control/object slices through `SpellSwitch`, `Program`, `ForAllTargets`, `NaftaTransfer`, `DealedDamageConverter`, `InvalidAbilityTarget`, `MovementControl`, `ChangeAnimSet`, `ChangeAnimation`, `Watch`, `ScaleControl`, `DayNightTransition`, `SetTimescale`, `Channelling`, `WaitForSpell`, `Eye`, `LockTiles`, `ModifyTerrain`, `RaiseFlag`, and `PickupChanneling`, and it exposes the `DropTree` factory. The next risky boundary is full real terrain/tree/tile/fog side effects, mounted hero, and full client/scene ownership, not generated special factory null returns or controlled channeling/flag/pickup/tile/fog bootstrap state.
- Superseding the older movement-applicator factory note: the Linux executable now proves the maintained `PFApplMove` factory slice and controlled runtime behavior for `Teleport`, `KickAway`, `Attract`, `Shift`, `MoveTo`, `GhostMove`, `Fly`, `Throw`, and `AttackTarget`. `MoveTo` is no longer only an add/remove ownership proof: the Linux null-render `MovingUnit::Step(...)` advances a direct movement applicator from `269.0` into the target band at `264.0`, tracks a moved unit target into its finish band at `285.0`, preserves follow spacing from `308.0` to `302.0` after a leader move, and mirrors a mounted child with its parent from `318.0` to `310.0`. Linux bootstrap units now also carry DB-derived collision size/targeting/attack-sector setup, `PlaceUnitWithPush(...)` pushes an overlapping blocker from `332.0` to `336.0`, `FindFreePlace(...)` finds `346.0` around an occupied point, `MovingUnit::UpdateMovements(...)` globally ticks a registered unit from `354.0` to `350.0`, `CheckStraightMove(...)` traces a blocked segment from `10.0` to `2.0`, normal movement stops at the blocker edge from `380.0` to `382.0`, high-speed movement stops at the traced blocker edge from `440.0` to `442.0`, and the Linux bootstrap `CollisionResolver::Resolve(...)` separates an overlapping pair from `2.0` to `4.0` or side-places it at `36.0` when the direct separation point is occupied. `ThrowApplicator` now proves selector-backed `THROWTYPE_THROW` displacement from `273.0` to `276.0`, and `AttackTargetApplicator` now bridges the null-render FSM gap by driving `PFBaseUnit::DoAttack(...)` directly and proving target health change from `1.00` to `0.82`. `PFWorld::Step(...)` now drives the Linux resolver before `MovingUnit::UpdateMovements(...)` for loaded Linux worlds. The next risky movement boundary is full production pathfinding/voxel steering and production collision-resolver behavior beyond the current overlap separation and side-placement search, content-driven throw/fly trajectories in real world-owned units, and production attack-state/FSM integration beyond the current Linux direct-attack bridge, not generated movement factory null returns, controlled add/remove, immediate relocation behavior, direct/live-target/follow/attach stepping, simple bootstrap push/free-place checks, DB-sized unit geometry, straight-move tracing, blocked-step stopping, tunnel stopping, resolver overlap/search behavior, selector-backed throw displacement, direct attack-target damage, or loaded-world movement ticking.
- Recent additions to the maintained render probe are the animation-graph/time-control slice (`AnimGraphApplicator.cpp`, `AnimGraphBlender.cpp`, `AnimGraphController.cpp`, `Animators.cpp`, `BitMap.cpp`, `HeightsController.cpp`, `TimeMutator.cpp`), the next scene/terrain runtime slice (`CollisionHull.cpp`, `DBSceneBase.cpp`, `DiAnGrAPI.cpp`, `DiAnGrCl.cpp`, `DiAnGrNLinker.cpp`, `DiAnGrSStorage.cpp`, `DiAnGrUp.cpp`, `DiAnGrUtils.cpp`, `DiGraph.cpp`, `LandPlacementMutator.cpp`, `DBTerrain.cpp`, `GrassLayersManager.cpp`, `GrassRegion.cpp`, `GrassRenderManager.cpp`, `NatureMap.cpp`, `SpeedGrass.cpp`, `TerrainElementManager.cpp`), the terrain cache/runtime layer (`TerrainHeightManager.cpp`, `TerrainLayerManager.cpp`, `TerrainMaterialCache.cpp`, `TerrainTextureCache.cpp`), the remaining maintained terrain core (`NatureMapVisual.cpp`, `Terrain.cpp`, `TerrainCollision.cpp`, `TerrainElement.cpp`, `TerrainGeometryManager.cpp`, `BezierSurface.cpp`, `NatureAttackSpace.cpp`), and now the rest of the legacy animation-graph core (`DiAnGr.cpp`, `DiAnGrDb.cpp`, `DiAnGrEditor.cpp`, `DiAnGrExtPars.cpp`, `DiAnGrExtParsAd.cpp`, `DiAnGrG.cpp`, `DiAnGrIo2.cpp`, `DiAnGrMarker.cpp`). Those files now build in the maintained Linux render probe instead of only passing ad-hoc syntax checks.
- Standalone Linux null-render syntax probes for `AnimatedSplitSceneComponent.cpp`, `AttacherSceneComponent.cpp`, `Camera.cpp`, `CameraControllersContainer.cpp`, `CameraInputModifier.cpp`, and `FreeCameraController.cpp` are now green or superseded by maintained render-probe builds.
- Latest verified runtime command:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8
```

The current deeper custom-lobby verification command is:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map MOBA --bootstrap-create-game --seconds 90
```

The current first-3D/create-game visual verification command is:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --map MOBA --hero plane --bootstrap-create-game --seconds 8
```

The current selected-map point-light verification command is:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --map NightMap --hero plane --bootstrap-create-game --seconds 8
```

The current selected-hero 3D visual verification command is:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map Aram --hero shadow --seconds 8
```

- Latest verified runtime log:

```text
/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/2026.06.30-05.00.34/linux-client-shell.log
```

- Latest verified Linux-generated replay-input log:

```text
/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/2026.06.30-05.00.03/linux-client-shell.log
```

- Latest verified gameplay-effect runtime log:

```text
/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/2026.05.30-11.15.08/linux-client-shell.log
```

- Latest verified deeper custom-lobby/loading log:

```text
/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/2026.06.30-04.56.31/linux-client-shell.log
```

- Latest verified first-3D visual run log:

```text
/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/2026.05.31-05.09.11/linux-client-shell.log
```

- Latest selected-map point-light verification log:

```text
/home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/2026.05.30-19.17.42/linux-client-shell.log
```

- Latest selected-hero real mesh screenshot:

```text
/tmp/primeworld-hero-real-skin-mesh-preview.png
```

## What Is Already Done

### Linux bootstrap and shell

- Native X11 window/bootstrap path exists and now publishes enough X11/EWMH metadata to appear as a visible, correctly sized Linux game window on common window managers.
- Linux build path is wired through `Src/CMakeLists.txt` and `Src/LinuxBootstrap/CMakeLists.txt`.
- Native shell handles command line, filesystem root detection, profile/log setup, content mounting, localization fallback, launcher/session import, and input diagnostics.
- Default Linux startup now shows a visible OpenGL `Lobby_SelectGameMode` bridge using the real lobby background, Windows XDB layout coordinates, the same `\Tech\Default\_.MAPLST.xdb` custom map list as the Windows-authored screen, XDB-sized map/session slots, shipped button/panel/frame/radio/scrollbar material textures, bundled PrimeWorld FreeType text, the real `Lobby_SelectGameMode`/`SessionSlot` UTF-16 textrefs for authored captions, XDB child text boxes for map/session row internals, context-owned cached real `lobby::SDevGameInfo` session-row names/ids/player counts/map titles, absolute-row session-list scrolling, and mouse hit-testing for map rows, game rows, scrollbars, join-mode radios, the developer-sex selector, the player-count slider, and create/start/refresh controls instead of covering the first screen with diagnostics; dense diagnostics are opt-in via `--diagnostics-overlay` or `F10`, and the native window is forced to a sane bootstrap size/sticky foreground state so it is not hidden by the window manager. The lobby/loading bridge can now also show a rotatable selected-hero OpenGL preview backed by real selected hero `DBSceneObject` metadata/AABB, real `.skin` triangle payloads accepted by maintained `RenderResourceManager::LoadMeshGeometry(...)` in the Linux null-render path, extracted UVs, material-indexed diffuse DDS textures, extracted normals, parsed blend weights/indices, validated resolved `SkeletonData` and `SkeletalAnimationData` payload diagnostics, `Render::SkeletalAnimationSampler` / `Render::SkeletonWrapper` animation sampling, renderer-owned `Render::SkeletalMesh` queue submission, and locator/attach-flag-aware renderer-owned static scene-component mesh payload drawing through `Render::StaticMesh`; the create-game/loading path can also show an auto-rotating, zoomable, tiltable, pannable, mouse/key/mouse-wheel-controlled native OpenGL 3D tactical preview derived from the selected map's real terrain `.heightmap`, authored terrain `layerMaskFile` plus terrain-layer material diffuse textures, authored `NDb::AdvMapWaterZone` water polygons, real `DBNatureMap` attack/logic road polylines, authored script/camera guide overlays, authored selected-map point-light range rings/markers, real `MapStartInfo` hero start-slot assignment rings/columns, terrain-followed team-route overlays from start slots to allied lanes/objectives plus A/B lane corridors, capped real `NDb::Terrain::geometry.terrainElements` static meshes drawn through `Render::StaticMesh -> BatchQueue -> Primitive::Draw()`, objective markers, capped selected-map static scene-object meshes drawn through `Render::StaticMesh -> BatchQueue -> Primitive::Draw()`, and capped selected-map animated scene-component meshes drawn through `Render::SkeletalMesh -> BatchQueue -> Primitive::Draw()`, but both remain bootstrap previews rather than final material/animation-graph/effect/full-terrain/game-scene rendering.

### Content and data bootstrap

- Real `RootFileSystem` mounting works.
- Real DB cache bootstrap works for the Linux shell.
- Real map catalog, Windows custom map-list ordering, map settings inheritance, loading screens, map art, hero portraits, and localized strings are resolved from game data.
- Real `SessionRoot`, `SessionLogicRoot`, `SessionVisualRoot`, `UIRoot`, and related DB roots are probed from the Linux shell.

### Gameplay portability groundwork

- A maintained Linux gameplay compile target exists: `PrimeWorldLinuxGameplayProbe`.
- A maintained Linux client runtime compile target exists: `PrimeWorldLinuxClientRuntimeProbe`.
- A maintained Linux render compile target exists: `PrimeWorldLinuxRenderProbe`.
- It now covers a broad slice of `PF_GameLogic`, including world, AI, applicators, statistics, base unit state, target selectors, buildings, glyphs, towers, and related gameplay objects.
- The Linux gameplay probe now also compiles the heavier ability timing/attack slice in `PFAbilityData.cpp` and `PFBaseAttackData.cpp` by cutting visual-only scene animation paths behind `VISUAL_CUTTED`.
- The Linux render probe now compiles `renderer.cpp`, `smartrenderer.cpp`, `dxutils.cpp`, `rendersurface.cpp`, `texture.cpp`, `TextureManager.cpp`, and `renderflagsconverter.cpp` through Linux/bootstrap-only null-render branches instead of failing immediately at the top-level Direct3D include wall.
- `System/Geom.h` now exposes a narrow Linux null-render D3DX math shim for `D3DXVECTOR3`, `D3DXMATRIX`, `D3DXPLANE`, `D3DXVec3Length`, `D3DXPlaneDotNormal`, `D3DXPlaneDotCoord`, and `D3DXVec3TransformCoord`, which is enough to compile the bounding-volume slice without pulling the Windows D3DX headers into the Linux path.
- The Linux render probe now also compiles `StaticMesh.cpp`, `SkeletalMesh.cpp`, `InstancedMesh.cpp`, and `WaterMesh.cpp` through Linux/bootstrap-only null-render branches, so the maintained render slice is past the old mesh implementation wall and through the first water mesh implementation seam as well.
- The Linux render probe now also compiles the real `batch.cpp` together with `BoundingPrimitives.cpp`, `ConvexVolume.cpp`, `FrustumCuller.cpp`, and `InstancedMeshResource.cpp`, so the maintained render slice is now past the old batch-adjacent bounding-volume seam without relying on `batch_linux_null.cpp`.
- The Linux render probe now also compiles `AnimatedSceneComponent.cpp`, `StaticSceneComponent.cpp`, and `WaterSceneComponent.cpp`, so the maintained scene slice is now past the first mesh-consuming scene component layer.
- The Linux render probe now also compiles `SceneComponent.cpp`, `SceneObject.cpp`, and `LightingScene.cpp`, so the maintained scene slice is now past the first core scene-object and lighting layer rather than stopping at mesh-consuming scene components.
- The Linux render probe now also compiles `Scene.cpp`, so the maintained scene slice is past the old full-scene `ShadowManager -> d3dx9.h -> d3d9.h` blocker and into the next runtime layer above core scene-object management.
- The Linux render probe now also compiles the lightning and trace effect families: `LightningFX.cpp`, `LightningSceneComponent.cpp`, `TraceFX.cpp`, `TraceFXGeo.cpp`, and `TraceSceneComponent.cpp`.
- The Linux render probe now also compiles the camera/runtime control slice: `Camera.cpp`, `CameraControllersContainer.cpp`, `CameraInputModifier.cpp`, `FreeCameraController.cpp`, `AnimatedSplitSceneComponent.cpp`, and `AttacherSceneComponent.cpp`.
- The Linux render probe now also compiles the scene DB/object utility slice above the old core scene wall: `DBScene.cpp`, `SceneObjectCreation.cpp`, `SceneObjectsPool.cpp`, `SceneObjectTrack.cpp`, `CollisionGeometry.cpp`, `CollisionGeometryManager.cpp`, `ScenePassabilityMask.cpp`, and `SoundItem.cpp`.
- The Linux render probe now also compiles the render/runtime utility slice above the old core scene wall: `ConfigManager.cpp`, `DxResourcesControl.cpp`, `ImmediateRenderer.cpp`, `MaterialSpec.cpp`, `RenderComponent.cpp`, `sceneconstants.cpp`, `SceneUtils.cpp`, `GeometryTracer.cpp`, `TerrainGrid.cpp`, `TimeController.cpp`, and `VertexColorManager.cpp`.
- The Linux render probe now also compiles `Material.cpp` and `LightsManager.cpp`: the Linux null-render path now sees the texture helper declarations used by materials, the old MSVC-only material registration block no longer stops GCC, and the null-render `LightsManager` header split is unified enough for the maintained probe to build the real translation unit instead of a duplicate class.
- The Linux render probe now also compiles `SHGrid.cpp`: the Linux null-render path now exposes the legacy Z-state enums used by the debug path and `SHGrid.cpp` now pulls the actual `RenderResourceManager` declaration it already relies on.
- The Linux render probe now also compiles the render-foundation slice `aabb.cpp`, `primitive.cpp`, `sampler.cpp`, and `vertexformatdescriptor.cpp`. The Linux path now pulls the real `NDb::AABB` definition into `aabb.cpp`, sampler binding sees the null-render `renderer.h` helpers, the vertex-format hash specialization is GCC-safe inside `nstl`, and the shared render headers have cleaner Linux/GCC behavior through fixes in `material.h` and `Scene/VoxelGrid.h`.
- The Linux render probe now also compiles `configdatabase.cpp`: the Linux null-render path now gets past the old `<tchar.h>` and Win32 adapter/file-version seam through `System/tchar_compat.h`, the `configdatabase.h -> dxutils.h` Linux include split, and the extra adapter/caps/file-version declarations in the Linux null-render `dxutils.h` surface.
- The Linux render probe now also compiles the debug/runtime utility slice `debugrenderer.cpp`, `debugmesh.cpp`, `debugstaticmesh.cpp`, and `renderstatesmanager.cpp`: Linux now has a no-op debug renderer translation unit under `PW_LINUX_NULL_RENDER`, and `renderstatesmanager.cpp` no longer pulls the real D3D state-manager body on the Linux null-render path because the maintained probe uses the existing header-level stub instead.
- The Linux render probe now also compiles `shadercompiler.cpp` and `multishader.cpp`: the Linux null-render path no longer pulls `DxIntrusivePtr.h` into `shadercompiler.h`, `shadercompiler.cpp` now provides a no-op bootstrap implementation under `PW_LINUX_NULL_RENDER`, and `multishader.cpp` now uses the proper renderer/aligned-allocation declarations and a cross-platform `snprintf` path.
- The Linux render probe now also compiles the UI renderer/cache slice: `uirenderer.cpp`, `FlashRenderer.cpp`, `UITextureAtlasItem.cpp`, and `UITextureCache.cpp`. The Linux null-render path now has no-op but linkable implementations for the UI renderer, Flash renderer, and bitmap/texture cache layer, and the shared UI texture-cache headers now pull the real `Texture2DRef` definition plus the texture-manager helpers they already rely on.
- The Linux render probe now also compiles the generated render DB glue slice: `DBRender.cpp`, `DBRenderResources.cpp`, and `shaderdefinestable.cpp`. That moves the maintained Linux path through the real render enum/resource registration layer instead of treating it as an offline-only generated dependency.
- The Linux render probe now also compiles the stale generated material/runtime slice `DebugMaterial.cpp`, `GrassMaterial.cpp`, and `MaterialResourceInterface.cpp` through Linux null-render/bootstrap-safe wrappers. On Linux, those files no longer try to instantiate the obsolete generated material API; they stay inert while the maintained probe keeps the current Windows-generated path intact under the existing `#else` branches.
- The Linux render probe now also compiles `facefxsystem.cpp` through a Linux bootstrap no-op path. That keeps the maintained render slice honest for this repo state, where the FaceFX vendor SDK headers are not present.
- `Scene/ModelLoader.cpp` is now inside the maintained Linux render probe. The Linux path no longer depends on the stale primitive/material API there: it now uses the current `CreateVB` / `CreateIB` helpers, current `SubMesh` / `Primitive` setup, current `CreateRenderMaterial` path, and 32-bit packed vertex colors so the legacy AIGeometry loader compiles correctly on 64-bit Linux.
- `Scene/TimeCtrlSceneComponent.cpp` is now also inside the maintained Linux render probe through a Linux null-render/bootstrap stub. The Windows code path is unchanged, but the Linux probe no longer treats this excluded legacy component as an unresolved scene portability hole.
- The Linux render probe now also compiles the shader/runtime utility slice `DeviceLost.cpp`, `ReadDDS.cpp`, `SHCoeffs.cpp`, `VertexColorStream.cpp`, and `shaderconstantsbinder.cpp` through Linux null-render/bootstrap-safe translation-unit branches.
- The Linux render probe now also compiles the first screen-space post-processing slice `Blur.cpp`, `Bloom.cpp`, `ConstantProtection.cpp`, and `FullScreenFX.cpp` through Linux null-render branches. That moves the maintained slice beyond pure renderer infrastructure and into real screen-space runtime code.
- The Linux render probe now also compiles `AOERenderer.cpp` and `WaterConvexes.cpp`. `AOERenderer.cpp` now gates one Windows-only material specialization call on the Linux null-render path, and `WaterConvexes.cpp` builds unchanged as a real water geometry utility translation unit.
- The Linux render probe now also compiles the first maintained animation-graph slice above the older scene/runtime wall: `AnimGraphApplicator.cpp`, `AnimGraphBlender.cpp`, `AnimGraphController.cpp`, `Animators.cpp`, `BitMap.cpp`, `HeightsController.cpp`, and `TimeMutator.cpp`. Linux now has a cross-platform `_alloca` path in `AnimGraphApplicator.cpp`, GCC-safe callback declarations and string-copy usage in `AnimGraphController.cpp`, and a bootstrap terrain height-version shim in `Terrain.h` so these files build inside the real maintained probe rather than staying as one-off probes.
- The Linux render probe now also compiles the next scene/terrain runtime slice: `CollisionHull.cpp`, `DBSceneBase.cpp`, `DiAnGrAPI.cpp`, `DiAnGrCl.cpp`, `DiAnGrNLinker.cpp`, `DiAnGrSStorage.cpp`, `DiAnGrUp.cpp`, `DiAnGrUtils.cpp`, `DiGraph.cpp`, `LandPlacementMutator.cpp`, `DBTerrain.cpp`, `GrassLayersManager.cpp`, `GrassRegion.cpp`, `GrassRenderManager.cpp`, `NatureMap.cpp`, `SpeedGrass.cpp`, and `TerrainElementManager.cpp`. That moves the maintained Linux path through more of the legacy animation-graph support layer and into the first wider terrain runtime/cache layer without reopening the old top-level Direct3D include wall.
- The Linux render probe now also compiles the next terrain cache/runtime layer: `TerrainHeightManager.cpp`, `TerrainLayerManager.cpp`, `TerrainMaterialCache.cpp`, and `TerrainTextureCache.cpp`. The Linux null-render material surface now exposes the terrain pins and frozen/burned texture samplers those files expect, `ArrayInVRAM.h` now sees the null-render texture/renderer declarations it uses, and `TerrainTextureCache.cpp` no longer calls the Windows-only `EvictManagedResources()` path on Linux.
- `dxutils.h` now exposes the extra Win/D3D stub surface needed by that slice on Linux, including `IUnknown`, `IDirect3DDevice9`, `IDirect3DResource9`, `HMONITOR`, `WCHAR`, and `S_OK`/`E_FAIL`.
- `System/tchar_compat.h` now provides a narrow Linux-safe `_tcscat_s`, `_stprintf_s`, and `sprintf_s` compatibility layer for legacy render code that still includes `<tchar.h>` and MSVC CRT helpers on the Linux null-render path.
- `TimedChannel.h` now has the GCC `typename` fixes needed for deeper animation/runtime probes, and the Linux null-render `RenderStatesManager` stub now lives in `renderstatesmanager.h` instead of colliding with `renderer.h`.
- `MemoryLib/UserMessage.h` no longer hard-depends on `Windows.h` for Linux bootstrap probes.
- The Linux null-render `dxutils` path is now self-contained and no longer depends on `d3d9.h`, `objbase.h`, or `DeviceLost.h` support glue during bootstrap probes.
- Linux null-render texture management now has a maintained bootstrap path in `TextureManager.cpp`, so the render probe no longer stops at the old texture/surface D3D smart-pointer seam. The runnable Linux client now links that maintained `TextureManager.cpp` path too, so default/empty/white texture handling is no longer duplicated in `runtime_stubs.cpp`.
- Linux case-sensitivity aliases were added for `materialspec.h`, `Material.h`, and `RenderResourceManager.h` so deeper render resource probes can get past Windows-only include casing.
- Linux case-sensitivity aliases now also cover `ConfigDatabase.h`, `Renderer.h`, `MultiShader.h`, and `System/FixedVector.h`, which moves the maintained render slice past more Windows-only include casing.
- Linux case-sensitivity aliases now also cover `DebugRenderer.h`, which removed one more Windows-only include-case seam in scene/runtime code.
- Linux case-sensitivity aliases now also cover legacy Windows PCH spellings via `Scene/StdAfx.h`, `Render/StdAfx.h`, and `Terrain/StdAfx.h`, which unblocks deeper scene/render/terrain files that still include `StdAfx.h`.
- `RenderComponent.h` now has a GCC-safe `DECLARE_INSTANCE_COUNTER`, and the shared mesh declaration layer in `MeshResource.h` is Linux-safe enough for the widened mesh probes.
- `Terrain.h` now splits its Linux bootstrap path correctly: scene/null-render probes use the real terrain manager headers, while gameplay bootstrap keeps lightweight terrain manager shims, so the maintained render and gameplay probes stay green at the same time.
- `AnimatedPlacement.h` and `SceneObjectTrack.cpp` now include `DBScene.h` directly, which removes one more generated-DB type visibility seam on Linux.
- `SceneObjectsPool.h` now forward-declares `NDb::DBSceneObject`, and `SceneObjectsPool.cpp` now includes `DBScene.h` plus `DxResourcesControl.h`, which gets the scene object pool layer past the old DB/resource-control split.
- The Linux bootstrap terrain shim now exposes `GetHeightsAsFloat()` for `ScenePassabilityMask.cpp`, and `ScenePassabilityMask.h` accessor returns were fixed.
- `Render/debugRenderer.h` was added as a case-safe alias for the legacy lowercase include used by deeper scene/runtime files.
- `PrimeWorldLinuxClient` linkability still depends on `runtime_stubs.cpp` for narrow Linux-only object-cast/refcount/logger/script/material-creation shims, but texture loading/default texture helpers, debug-renderer line/point helpers, SmartRenderer line helpers, render surfaces, render flags, shader-constant binding, shader compiler placeholders, DDS/SH utilities, device-lost placeholders, and render resource-manager placeholders now come from maintained Render translation units in the executable target.
- Several Windows-path, case-sensitivity, GCC, and DB/bootstrap blockers were already removed.
- The Linux client/runtime slice is now well past shell-only code. `MainLoop.cpp`, `GameContext.cpp`, `GameStatistics.cpp`, `GameStatisticsWrapper.cpp`, `LoadingFlashInterface.cpp`, `LoadingHeroes.cpp`, `LoadingScreen.cpp`, `LoadingScreenLogic.cpp`, `LoadingStatusHandler.cpp`, `LocalGameContext.cpp`, `NetworkStatusScreen.cpp`, `ReplayRunner.cpp`, `ReplayTransceiver.cpp`, `SelectGameModeLogic.cpp`, `SelectGameModeScreen.cpp`, `SelectHeroScreen.cpp`, and `SelectHeroScreenLogic.cpp` all compile under `PW_LINUX_NULL_RENDER`, `PW_LINUX_DB_BOOTSTRAP`, and `VISUAL_CUTTED`, and are maintained by the `PrimeWorldLinuxClientRuntimeProbe` target instead of ad-hoc compiler invocations.

## What Is Not Done Yet

- The real playable client path is still blocked by the Direct3D 9 renderer and scene/render coupling.
- The current Linux executable is still a native bootstrap shell, not the real in-game client loop.
- Real Linux gameplay rendering, production UI, audio, and final client runtime integration are not running yet. The visible starting UI is a Linux bootstrap presentation bridge that mirrors the Windows `Lobby_SelectGameMode` layout, and the first visible 3D is an auto-rotating, zoomable, tiltable, pannable, mouse/key-controlled selected-map tactical preview with real sampled terrain heightmap geometry, authored terrain-layer mask coloring, authored `NDb::AdvMapWaterZone` water overlays, real `DBNatureMap` road polylines, authored script/camera guide overlays, authored point-light range markers, real `MapStartInfo` hero start-slot assignment rings/columns, terrain-followed team-route overlays from start slots to allied lanes/objectives plus A/B lane corridors, capped real `NDb::Terrain::geometry.terrainElements` mesh payloads, capped real selected-map static and animated scene-object meshes, plus a textured, simply lit selected-hero preview that now flows through maintained `Render::SkeletalMesh` / `Render::StaticMesh` queue APIs for the tested heroes and map payloads. The loaded PFWorld create-game smoke now allocates real headless lane and neutral creep gameplay objects from DB-backed map spawners, proves lane common-creep waypoint movement through the Linux null-render movement path, and feeds the OpenGL tactical preview with live loaded-world markers for heroes, lane creeps, neutral creeps, and moving units; live hero markers now resolve back to engine start-slot lineup indices and draw 10 real per-lineup hero meshes with a source-file-keyed OpenGL diffuse texture cache instead of selected-hero replicas or per-index texture thrashing. The hero path includes real scene-asset metadata/AABB, selected payloads accepted by maintained Render mesh geometry loading, extracted UVs, material-indexed diffuse DDS textures, extracted normals, parsed skinning streams, validated resolved `SkeletonData`, validated resolved `.anim` payload diagnostics, Render-sampler/wrapper-driven first-animation deformation, and locator/attach-flag-aware static scene-component payload placement. It is still not the final production UI/renderer, full material/shader/animation-graph character rendering, production terrain material rendering, uncapped production map rendering, production hero/creep model rendering in the live world, or playable 3D scene.

## Main Remaining Milestones

### 1. Renderer escape hatch

Create a Linux-safe null-render or minimal renderer path that lets the real client startup move past the current Direct3D 9 wall.

Success condition:
- real client startup goes further than the current bootstrap shell without including `d3d9.h` on the Linux path

### 2. First real client startup on Linux

Move from shell/bootstrap behavior into more of the actual client runtime, even if visual output is incomplete.

Success condition:
- Linux client enters the real client loop with native startup, real runtime objects, and no D3D dependency on the Linux path

### 3. First in-game render

Bring up the first native Linux-rendered scene, even if minimal or visually wrong.

Success condition:
- map scene renders on Linux with enough runtime integration to observe live world state

### 4. First playable local match

Reach a playable local game on Linux with input, world simulation, basic UI visibility, and stable rendering.

Success condition:
- launch a local match and control a hero natively on Linux

### 5. Stabilization and parity

Fix correctness, UI gaps, audio, launcher integration, and wider gameplay/client regressions.

Success condition:
- Linux client is stable enough to ship publicly without breaking the Windows client

## Active Blockers

### Primary blocker

The real renderer/backend path is still centered on Direct3D 9 and Windows-specific render infrastructure, especially:

- `Src/Render/renderer.h`
- `Src/Render/smartrenderer.h`
- `Src/Render/DXManager.cpp`
- `Src/Render/RenderInterface.cpp`
- `Src/Render/DxIntrusivePtr.h`
- `Src/Render/dxutils.cpp`
- `Src/Render/texture.h`
- `Src/Render/rendersurface.h`

### Secondary blocker

Scene and client runtime code still pull render-side types too early. The Linux path needs a cleaner seam between gameplay/runtime code and scene/render code.

### Current client runtime frontier

The maintained client runtime probe is now past the earlier UI/status/replay wall, past the deeper lobby layer, past the remaining lightweight runtime helpers (`CpuTopology.cpp`, `DebugVarsSender.cpp`, `GameStatistics.cpp`, `GameStatisticsWrapper.cpp`, `LocalCmdScheduler.cpp`), and into the real client screen implementation layer through `Client/DefaultScreenBase.cpp`, `Client/TooltipsScreen.cpp`, `Client/TooltipsScreenLogic.cpp`, `UI/DebugDraw.cpp`, `PW_Client/SelectGameModeScreen.cpp`, `PW_Client/SelectHeroScreen.cpp`, and the wider UI class stack. The runnable executable is also now past the Linux-safe CPU/statistics/local-scheduler/debug-vars/lobby-connection helpers (`CpuTopology.cpp`, `DebugVarsSender.cpp`, `GameStatistics.cpp`, `GameStatisticsWrapper.cpp`, `LobbyConnection.cpp`, and `LocalCmdScheduler.cpp`) plus the network-status overlay (`NetworkStatusScreen.cpp`), bootstrap replay runner/transceiver translation units (`ReplayRunner.cpp` and `ReplayTransceiver.cpp`), the real screen-command/main-loop queue (`Client/ScreenCommands.cpp`, `Client/MainLoop.cpp`, `Client/CommandBinds.cpp`, `Client/Console.cpp`, `Client/ConsoleLogic.cpp`, `Client/DebugTextDraw.cpp`, `Client/TooltipsScreen.cpp`, `Client/TooltipsScreenLogic.cpp`, and `UI/DebugDraw.cpp`), the first real CoreFSM/update step (`Core/BaseFSM.cpp`, `Core/CoreFSM.cpp`, `System/UpdateProcessor.cpp`, and `System/Updater.cpp`), and the executable UI diagnostics/debug-draw/font/script/preferences slice (`UI/Alphabet.cpp`, `UI/DebugDraw.cpp`, `UI/DebugVarsRender.cpp`, `UI/FrameTimeRender.cpp`, `UI/FontRender.cpp`, `UI/FontStyle.cpp`, `UI/PreferencesProcessor.cpp`, and `UI/Scripts.cpp`).

`DebugVarsSender.cpp` is no longer probe-only: the Linux executable now constructs it and polls the null-statistics path, while its real statistic RPC send branch stays guarded under `PW_LINUX_DB_BOOTSTRAP` until active statistic transport is linked. `NetworkStatusScreen.cpp` is no longer just executable-linked: the Linux bootstrap now creates it, injects a controlled async state, steps/draws it, and verifies `uiRootRuntimeNetworkStatusReady=yes` / `uiRootRuntimeNetworkStatusVisible=yes`. Replay command serialization is no longer guarded out of the Linux executable, the Linux bootstrap replay runner/transceiver stubs now link in the executable, and the current bootstrap smoke actively writes a native Linux command-capture stream through an attached `ReplayWriter`. Linux-generated `.pwrp` files with the `PWLXREPLAYINFO1` sidecar can now be header-adopted at startup, played through real `ReplayStorage2`/`ReplayTransceiver`, validated against an isolated loaded `PFWorld` with 10 replay-spawned player heroes, and driven through the visible bootstrap PFWorld loop with the same 23/23 command and 5/5 status counts. The visible replay-input loop now also supports Linux-only pause/resume, single-step, speed, header-derived realtime pacing, and explicit millisecond pacing from command line and keyboard while preserving the normal local-scheduler create-game path, and replay-input runs draw a compact native OpenGL playback/status panel with progress, key hints, and pacing state. Full Windows `.pwrp` compatibility, production replay UI controls, and production camera/scene playback remain future work.

The latest larger executable-promotion attempt deliberately retained only the pieces that are stable at the current boundary. `LobbyConnection.cpp`, `LocalGameContext.cpp`, the bootstrap-safe `MapStartup.cpp` object, and a Linux null-render `PFWorld.cpp` branch now link in the runnable client; `AsyncMapStartup.cpp`, `MapStartup.cpp`, and the null-render `PFWorld.cpp` branch are maintained in the runtime probe, and the gameplay probe still compiles the real non-null-render `PFWorld.cpp` branch. `LocalGameContext.cpp` keeps command registration, map/hero setup, loading-screen transition, scheduler/transceiver setup, and state machine live under `PW_LINUX_DB_BOOTSTRAP`, while the map-loading object/progress/camera surface now calls from `NWorld::MapLoadingJob::DoTheJob()` into `PFWorld::LoadMap(...)` and feeds its progress to `Game::LoadingScreen`. Direct executable promotion of `GameContext.cpp`, `NewLobbyClientPW.cpp`, and `NewLobbyGameClientPW.cpp` is still deferred because those files require real transport/RPC/statistics/game-chat/world-serialization and `lobby::ClientBase` / `lobby::GameClient` implementations. The real screen-command queue, real `MainLoop.cpp` screen stack, real command-bind dispatch, real tooltip screen/logic, real CoreFSM/update step, real UI font/script stack, and bootstrap-safe `UI::PreferencesProcessor.cpp` are now executable-linked. There is no remaining `Client/MainLoop.cpp` Linux-bootstrap guard after promoting `Client/Console.cpp`, `Client/ConsoleLogic.cpp`, `Client/DebugTextDraw.cpp`, and `UI/DebugDraw.cpp`; bootstrap-local no-op logger dumper plus null-render debug-text/point/line shims keep the executable green. The active-effect executable promotion is now in place too: `PF_Core/BasicEffect.cpp`, `CameraShakeSceneComponent.cpp`, `ControlledStatusEffect.cpp`, `EffectGenSceneComponent.cpp`, `LightningEffect.cpp`, `ParticleSceneComponentWithEffect.cpp`, `SpringSceneComponent.cpp`, and `TraceGenSceneComponent.cpp` link into the executable, and the Linux shell verifies default, standalone scene-component-backed, attached target effects, canonical client-object `Apply(...)`, update-ring stepping, global active-effect kill, and generated PF_GameLogic effect behavior for `PFAuraEffect`, `PFMinimapEffect`, `PFPlayAnimEffect`, `PFPriestessSignEffect`, `PFEffectRandom`, `PFEffectSwitcher`, `PFInvisibilityEffect`, and `PFUnitSceneObjectModify` through the generated `EffectsPool` factory path. The next effect boundary is gameplay-owned effect lifetime plus applicator/gameplay-spawned ownership, not generated animated-scene/creature-death factory retrieval.

The user-visible first screen is no longer diagnostic-only or hidden behind a window-manager accident. By default the executable now renders a Linux-only OpenGL `Lobby_SelectGameMode` bridge over the real lobby background texture while the real `SelectGameModeScreen` remains initialized and drawn underneath; the current normal smoke reports `Visible menu runtime: ready=yes selected=1/4 action=Create custom game ... path=Linux visible lobby overlay + NGameX::SelectGameModeScreen`. The bridge follows the Windows XDB coordinates for the create/join panels, map/game lists, join-mode radios, player-count slider, and bottom buttons, resolves the shipped button/panel/frame/radio/scrollbar `.MTRL` assets into OpenGL textures, uses the real `MapSlot`, `SessionSlot`, list-scrollbar, and player-count scrollbar dimensions from the child XDB layouts, renders localized UTF-8 text through FreeType using the bundled PrimeWorld font, resolves the real `Lobby_SelectGameMode`/`SessionSlot` UTF-16 textrefs for authored captions, draws the map/session row internals inside the child XDB text boxes with UTF-8-safe truncation and wrapping, and now projects native X11 mouse coordinates into the 1280x1024 XDB layout space so map rows, game rows, scrollbars, join-mode radios, the developer-sex selector, the player-count slider, and create/start/refresh buttons are clickable. The map list now comes from the same `\Tech\Default\_.MAPLST.xdb` custom list as the Windows screen (`mapCatalogSource=Tech/Default/_.MAPLST.xdb`, `mapCatalogCount=41`, `mapCatalogScannedCount=112`, `mapCatalogCustomMatchedCount=41`), while command-line selection such as `--map Aram` still resolves inside that filtered list. The OpenGL session list is now backed by context-owned cached real `lobby::SDevGameInfo` rows from `RefreshGamesList()`/`PopGameList()`, so it draws actual session names, `gameId` values, player/max-player counts, and resolved map titles; game-row double-clicks call the existing bootstrap join/reconnect/spectate context methods with the actual cached `gameId`, refresh calls the real `RefreshGamesList()` hook, and longer session lists now hit-test/render/scroll by absolute row instead of resetting the visible rows to the top. The native X11 window advertises fixed bootstrap size hints, sticky/above state, and foreground activation, and the latest targeted capture `/tmp/primeworld-lobby-real-devgames-context-cache-window.png` shows the `wmctrl`-reported `1280x720` Windows-layout lobby bridge with XDB-sized rows, material-backed controls, actual XDB captions, join-control state, and real dev-game row data aligned to the `MapSlot`/`SessionSlot` layouts. This is an intentional bootstrap bridge so humans can see and operate a main-menu-like first screen while the full production UI/renderer path is still being ported.

The first visible 3D is now available in deliberately limited forms. The loaded-world map preview can now draw all 10 live hero markers as real per-lineup hero meshes, and the latest MOBA/NightMap create-game smokes report `finalMap3DPreviewDynamicHeroMeshes=10`, `finalMap3DPreviewDynamicHeroMeshTriangles=62625`, `finalMap3DPreviewHeroDiffuseTexturesCached=16`, and `finalMap3DPreviewHeroDiffuseTextureFailures=0`. On the create-game/loading path without the diagnostics overlay, the Linux OpenGL bridge draws an auto-rotating isometric tactical preview from the selected map's real sampled terrain heightmap colored by authored terrain layer mask/material diffuse textures, authored `NDb::AdvMapWaterZone` water polygons over sampled terrain, real `DBNatureMap` attack/logic road polylines over the terrain, authored script-area/camera-spline/path/polygon guide overlays, authored selected-map point-light range rings/markers, real `MapStartInfo` hero start-slot assignment rings/columns, terrain-followed team-route overlays from start slots to allied lanes/objectives plus A/B lane corridors, capped real `NDb::Terrain::geometry.terrainElements` meshes, objective markers, capped real selected-map static scene-object meshes, and capped real selected-map animated scene-component meshes (`bootstrap3DPreviewReady=yes`, `bootstrap3DPreviewActive=yes`, `bootstrap3DPreviewMarkers=102`, `mapTerrainHeightmapSampledTriangles=18050`, `mapTerrainLayerMaskReady=yes`, `mapTerrainLayerDiffuseTexturesLoaded=11`, `mapTerrainLayerSampledUnique=10`, `mapWaterZones=4`, `mapWaterZoneTriangles=88`, `bootstrap3DPreviewWaterZoneTriangles=88`, `finalMap3DPreviewWaterZoneTriangles=88`, `mapTerrainNatureRoadSegments=45`, `finalMap3DPreviewTerrainSurfaceTriangles=18050`, `finalMap3DPreviewTerrainRoadSegments=45`, `mapScriptAreas=24`, `finalMap3DPreviewScriptAreas=24`, `finalMap3DPreviewScriptAreaSegments=768`, `mapCameraSplines=4`, `finalMap3DPreviewCameraSplines=4`, `finalMap3DPreviewCameraSplineSegments=27`, `mapPointLights=75`, `bootstrap3DPreviewPointLightMarkers=75`, `finalMap3DPreviewPointLights=75`, `finalMap3DPreviewPointLightMarkers=75`, `finalMap3DPreviewEngineStartSlotsDrawn=yes`, `finalMap3DPreviewEngineStartSlots=10`, `finalMap3DPreviewEngineStartAssignedSlots=10`, `finalMap3DPreviewEngineStartHumanSlots=1`, `finalMap3DPreviewEngineStartBotSlots=9`, `finalMap3DPreviewTeamRoutesDrawn=yes`, `finalMap3DPreviewTeamRouteLines=13`, `finalMap3DPreviewTeamRouteSegments=78`, `finalMap3DPreviewTeamStartRouteLines=10`, `finalMap3DPreviewTeamLaneRouteLines=3`, `finalMap3DPreviewBaseYaw=-36`, `finalMap3DPreviewRenderedYaw=5.26978` on MOBA and `0.67728` on NightMap after the current longer 8-second smokes, `finalMap3DPreviewPitch=60`, `finalMap3DPreviewRenderedPitch=60`, `finalMap3DPreviewZoom=1`, `finalMap3DPreviewPan=0,0`, `mapTerrainElementGeometryPayloads=192`, `finalMap3DPreviewTerrainElementMeshTriangles=33202`, `mapStaticSceneObjectsScanned=74`, `mapStaticSceneGeometryPayloads=256`, `finalMap3DPreviewRendererStaticMeshPayloads=256`, `finalMap3DPreviewRendererStaticMeshTriangles=53964`, `mapAnimatedSceneGeometryPayloads=48`, `finalMap3DPreviewRendererAnimatedMeshPayloads=48`, and `finalMap3DPreviewRendererAnimatedMeshTriangles=133867` across the current default MOBA and NightMap verification runs). The map preview can also be rotated manually with `Q`/`E`, zoomed with `Z`/`X` or mouse wheel during loading, tilted with `I`/`K`, panned with right-mouse drag outside the hero-preview rectangle, reset with `R`, and mouse-dragged with left button for yaw outside the hero-preview rectangle. Hidden map-browser/lineup navigation is locked while the hero/loading runtime is active so the selected loading map stays stable during longer visual smokes. The default lobby/loading bridge also draws a selected-hero OpenGL preview backed by the resolved real hero scene asset. The main visible hero path now goes through maintained renderer ownership: it builds a `Render::SkeletalMesh`, samples the first resolved animation through `Render::SkeletalAnimationSampler` / `Render::SkeletonWrapper`, submits the mesh through `Render::BatchQueue`, and replays `Primitive::Draw()` through the Linux OpenGL `SmartRenderer` immediate bridge with real H6 `POSITION0`, `NORMAL0`, `TEXCOORD0`, `BLENDWEIGHT0`, and `BLENDINDICES0` streams. It also records and draws renderer-owned static scene-component payloads through `Render::StaticMesh -> BatchQueue -> Primitive::Draw()` using attached-component DB placement, DB locator offsets, attach flags, default-orientation `SceneComponent::ConstructWorldPlacement(...)` semantics, live skeleton-wrapper joint matrices for joint-backed locators, and material diffuse texture mappings. Final normal UI smokes now report Plane/Aviator with `selectedHeroSceneAssetStaticGeometryPayloads=24`, `finalHero3DPreviewRendererMeshDrawn=yes`, `finalHero3DPreviewRendererMeshBatches=29`, `finalHero3DPreviewRendererMeshTriangles=11932`, `finalHero3DPreviewRendererStaticMeshPayloads=24`, and `finalHero3DPreviewRendererStaticMeshTriangles=2366`; Shadow/Kara reports `selectedHeroSceneAssetStaticGeometryPayloads=14`, `finalHero3DPreviewRendererMeshBatches=15`, `finalHero3DPreviewRendererMeshTriangles=7054`, and `finalHero3DPreviewRendererStaticMeshTriangles=1270`. The hero preview still supports `A`/`D` rotation, `C` reset, mouse-drag rotation, and the real scene-object AABB; if renderer-owned drawing fails it falls back to the older CPU-deformed OpenGL mesh path or colored primitive path. This lets humans see a native OpenGL map/object preview during loading and rotate a real native OpenGL textured/lit selected hero through maintained mesh queue APIs now, including static component geometry placed with attach flags for the tested direct locator cases, but the full playable 3D game is still blocked by production material/shader pins, alpha sorting, particles/FX, camera-facing component orientation, time controllers, nested dynamic locator chains, animation graph/state selection, production lighting/camera/scene ownership, production terrain material/patch ownership, uncapped/all map-object scene ownership, and the loaded PFWorld scene.

The latest visual smoke extends the loaded-world preview beyond hero meshes: common/neutral creep markers now carry DB unit/scene-object identity from `PFWorld`, and all live unit markers now carry status/direction data. The OpenGL preview can render DB-backed creep mesh replicas at live terrain-followed positions, draw live health bars for all loaded heroes/creeps, draw hero energy bars from real Linux `STAT_LIFE` / `STAT_ENERGY` values instead of the old `1/1` placeholders, draw movement-direction arrows for moving common creeps, and face live hero/creep mesh replicas from real `PFBaseMovingUnit::GetMoveDirection()` when available. The current MOBA/NightMap create-game runs report `Final live HUD state` hero values of `hp=810/810/100% energy=725/725/100% damage=49.5-60.5 aps=0.68 move=6.76 regen=1.22/2.61` and selected creep values of `hp=300/300/100% energy=0/0/0% damage=9.0-11.0 aps=0.71 move=5.66 regen=0.00/0.00`; the same runs preserve live marker/mesh coverage with `finalMap3DPreviewDynamicHeroMeshes=10`, `heroMeshes=yes/10/62625`, `creepMeshes=yes/48/181650`, and currently report `unitMeshAssets=4/4` on both MOBA and NightMap after the derivative-stat chunk. The preview is still a bootstrap tactical visualization, not production in-world creature scene ownership.

The loaded-world preview is now also a first native command surface instead of only a tactical visualization. Right-click on the map converts the OpenGL screen point through the current preview camera into a PFWorld map target and queues a real `CmdMoveHero`; if the projected click is close enough to a live enemy hero, the same right-click queues `CmdAttackTarget` instead. Right-drag still pans the map, with click-vs-drag separated by a small movement threshold. Middle-click queues `CmdMinimapSignal`, and keyboard `S`/`A`/`F`/`H`/`C` queues stop, attack-nearest, follow-nearest, hold, and cancel-channelling commands through the same transceiver path. Move commands draw the existing source-to-target marker, and non-move live/scripted commands now draw a separate last-action source/target marker plus `finalMap3DPreviewCommandAction*` diagnostics. The automated proof move sends one extra map-preview `CmdMoveHero` after the command chain, so the current MOBA/NightMap 30-second smokes end with two hero move commands total, one visible `bootstrap-map-click-proof` move marker, and seven recorded scripted action-marker updates ending at minimap signal. This is still Linux bootstrap interaction, not the final production camera/input/game scene.

The next meaningful client-runtime work is no longer those old lobby blockers, and it is no longer only the loading-status seam either. The executable now exercises a real runtime status path through `LoadingStatusHandler` and a bootstrap `LoadingFlashInterface`, real `LoadingHeroes` lineup data, canonical `UI::Initialize(...)` / `UI::ApplyNewParams(...)` / `UI::NewFrame(...)` / `UI::PresentFrame(...)`, and the first real `Game::LoadingScreen::Init/Step/Draw` plus `MapLoadingJob::DoTheJob/PFWorld::LoadMap/TileMap::Prepare` transition.

The real command serialization/memory-stream surface (`Core/CommandSerializer.cpp`, `Core/GameCommand.cpp`, `System/MemoryStream.cpp`) is linked into `PrimeWorldLinuxClient`, `Core/Transceiver.cpp` is compile-maintained by `PrimeWorldLinuxClientRuntimeProbe` and linked into the runnable client, and the deeper smoke constructs a real `NCore::Transceiver`, attaches a real `NWorld::PFWorld`, advances the local scheduler/transceiver/PFWorld boundary during loading, attaches a Linux bootstrap `ReplayWriter`, creates real Linux null-render `PFBaseHero` / `PFBaseMaleHero` objects for the loaded players, and now queues hero move, hero stop, hero attack-target, hero follow-unit, hero combat-move, hero hold, hero cancel-channelling, hero minimap-signal, use-unit, activate-talent, use-talent, use-portal, use-consumable, buy-consumable, raise-flag, init-minigame, and content-available pickup-object commands through the same boundary.

Older 30-second create-game command smokes before the bot-driver chunk superseded the earlier command-only scheduler proof for the interactive-map boundary. MOBA reported the 20-command loaded-world baseline plus one additional map-preview proof move (`finalGameTransceiverCommands=21`, `finalGameTransceiverHeroMoveCommandsSent=2`, `finalMap3DPreviewCommandMoveCommands=1`, `finalMap3DPreviewCommandMoveTargetDrawn=yes`, `finalMap3DPreviewCommandMoveInputSource=bootstrap-map-click-proof`, `finalGameReplayWriterCommandWrites=21`, and `finalGameReplayWriterBytesWritten=964`) while proving live hero displacement through the replayed command (`Final hero move command execution: ... accepted=2 executed=2 ... resolved=1 clientMatch=1`, `Final map command move: ... moved=yes reached=yes ... distance=31.62 maxDistance=31.62 remaining=0.00`) and preserving real loaded common-creep movement (`registeredCreeps=60`, `movingCommonCreeps=60`, `movedCommonCreeps=60`, `commonCreepDistance=1371.90`, `creepWaves=12`, and `neutralWaves=22`). The same MOBA run also reported `Final map command action: targetDrawn=yes commands=7 kind=signal input=bootstrap-command-chain`, proving the shared non-move action marker/log path. NightMap reported the 19-command baseline plus the same additional map-preview proof move (`finalGameTransceiverCommands=20`, `finalGameTransceiverHeroMoveCommandsSent=2`, `finalMap3DPreviewCommandMoveCommands=1`, `finalMap3DPreviewCommandMoveTargetDrawn=yes`, `finalMap3DPreviewCommandMoveInputSource=bootstrap-map-click-proof`, `finalGameReplayWriterCommandWrites=20`, and `finalGameReplayWriterBytesWritten=926`) with the same moved/reached proof, the same seven recorded action-marker updates ending at minimap signal, and NightMap point-light proof (`finalMap3DPreviewPointLights=75`). The longer 65-second spawner/creep-distance proofs remain useful for wave-depth regression checks, but the latest command frontier is now beyond dispatch: the right-click/map-surface move path reaches actual live hero movement, and the loaded preview can issue stop/attack/follow/hold/cancel/signal commands from native Linux input.

The current runtime proof also reaches the real UI root lookup APIs: `UI::GetScreenLayout(...)` and `UI::GetConstant(...)` both resolve in the Linux executable, while `UI::GetContentResource(...)` is now known to be blocked by data shape rather than Linux registration, because `Data/UI/UIRoot.xdb` currently exposes a single empty content group (`test`) with no resources. The next client-runtime seam is therefore deeper real game-start/runtime adoption above the first live `Game::LoadingScreen`/`MapLoadingJob`/PFWorld/TileMap/player/FogOfWar/hero-command transition, full replay loader/playback compatibility, replacing the current scripted bot driver with production AI/player command sources, and broader production command classes beyond the current manual hero/control/ability/interaction stream. The non-maintained client files left in `PW_Client/` are:

- `PW_Client/FullScreenTest.cpp`, which is a Windows-only test utility and still stops at `windows.h`
- `PW_Client/RegistryToolbox.cpp`, which is still tied to Win32 registry types through `RegistryStorage.h`
- `PW_Client/Game.cpp`, which is already part of the Linux client executable, but is still the next real integration seam because moving toward a playable client now means pushing the Linux path deeper into the actual game loop instead of only widening the maintained bootstrap/runtime slice

For first playable Linux work, `FullScreenTest.cpp` and `RegistryToolbox.cpp` are not on the critical path. The next practical frontier is deeper `Game.cpp` runtime integration on top of the maintained lobby/runtime slice.

Historical wave-depth note: the 65-second scheduler/spawner paragraphs below predate the map-click proof command and are retained because they cover longer creep-wave progression. Their 20/19 MOBA/NightMap command counts are expected to be one lower than the current 25-second map-command smokes.

Superseding the preceding scheduler note: the latest MOBA and NightMap 65-second create-game smokes still queue nineteen serialized commands cross-map, execute all nineteen through the live local scheduler/transceiver/PFWorld boundary, and capture the same scheduler command segment through the Linux bootstrap `ReplayWriter`; MOBA queues and executes a twentieth command because that map currently exposes a pickupable object to the Linux helper. They now keep running long enough to reach transceiver step `699`, step real loaded `PFBaseSpawner` objects, allocate DB-driven lane/neutral creeps, and move DB-waypoint common creeps inside the same loaded PFWorld. The first three commands remain the bootstrap runtime marker, keep-alive, and timescale command. Commands 4-11 remain Linux bootstrap `CmdMoveHero` (`0x2C59C380`), `CmdStopHero` (`0x2C5B9481`), `CmdAttackTarget` (`0x2C5B9480`), `CmdFollowUnit` (`0x2C6A2BC0`), `CmdCombatMoveHero` (`0x2C5B9CC0`), `CmdHold` (`0x2C6BBB40`), `CmdCancelChannelling` (`0xE78854C0`), and `CmdMinimapSignal` (`0x0B622CC0`), each queued only after the previous production command has executed. Commands 12-16 remain Linux bootstrap `CmdUseUnit` (`0xE78B5B00`), `CmdActivateTalent` (`0x0B695200`), `CmdUseTalent` (`0x0B695201`), `CmdUsePortal` (`0x6294CD01`), and `CmdUseConsumable` (`0x0F5CC401`). Commands 17-19 are Linux bootstrap `CmdBuyConsumable` (`0x2C61F340`), `CmdRaiseFlag` (`0x0B76AAC0`), and `CmdInitMinigame` (`0x9D62D440`), chained after use-consumable, buy-consumable, and raise-flag respectively. Command 20 is Linux bootstrap `CmdPickupObject` (`0xA05CCB40`), chained after init-minigame when a loaded pickupable exists; MOBA sends it, NightMap currently does not. `CmdUseUnit` currently targets an enemy hero to prove the serialized command path; its actual state transition remains gated by `CanBeUsedBy(...)`, and `CmdInitMinigame` respects the production battle-start delay gate even though the command boundary is serialized/executed.

The older map-click command counters before the bot-driver chunk were MOBA `finalGameTransceiverCommands=21`, `finalGameTransceiverHeroMoveCommandsSent=2`, `finalMap3DPreviewCommandMoveCommands=1`, `finalMap3DPreviewCommandMoveTargetDrawn=yes`, `finalMap3DPreviewCommandMoveInputSource=bootstrap-map-click-proof`, `finalMap3DPreviewCommandMoveHeroMoved=yes`, `finalMap3DPreviewCommandMoveHeroReached=yes`, `finalMap3DPreviewCommandMoveHeroDistance=31.62`, `finalMap3DPreviewCommandActionTargetDrawn=yes`, `finalMap3DPreviewCommandActionCommands=7`, `finalMap3DPreviewCommandActionKind=signal`, `finalMap3DPreviewCommandActionInputSource=bootstrap-command-chain`, `finalGameReplayWriterCommandWrites=21`, and `finalGameReplayWriterBytesWritten=964`. NightMap had the same map-click/action proof shape with `finalGameTransceiverCommands=20`, `finalGameTransceiverHeroMoveCommandsSent=2`, `finalMap3DPreviewCommandMoveCommands=1`, `finalMap3DPreviewCommandMoveTargetDrawn=yes`, `finalMap3DPreviewCommandMoveInputSource=bootstrap-map-click-proof`, `finalMap3DPreviewCommandMoveHeroMoved=yes`, `finalMap3DPreviewCommandMoveHeroReached=yes`, `finalMap3DPreviewCommandMoveHeroDistance=31.62`, `finalMap3DPreviewCommandActionTargetDrawn=yes`, `finalMap3DPreviewCommandActionCommands=7`, `finalMap3DPreviewCommandActionKind=signal`, `finalMap3DPreviewCommandActionInputSource=bootstrap-command-chain`, `finalGameReplayWriterCommandWrites=20`, and `finalGameReplayWriterBytesWritten=926`. The scripted baseline still sends all prior production command classes through init-minigame, and MOBA still sends the pickup-object command when that content is present.

The latest replay-capture counters still use `finalGameReplayWriterReady=yes`, `finalGameReplayWriterOpen=yes`, `finalGameReplayWriterStartWrites=1`, `finalGameReplayWriterStepWrites=1`, `finalGameReplayWriterStatusWrites=0`, `finalGameReplayWriterFailures=0`, and `finalGameReplayWriterPath=logs/linux-bootstrap-replay.pwrp`; after the latest bot command-driver chunk, MOBA writes `finalGameReplayWriterCommandWrites=26` / `finalGameReplayWriterBytesWritten=1260` and the live replay-input path consumes the same generated file with `commands=26/26`. This writes the native Linux command-capture file at `Bin/logs/linux-bootstrap-replay.pwrp`. The latest verified bot command-driver smoke log is `Bin/logs/2026.06.25-04.42.27/linux-client-shell.log`; older live-map-control smoke logs remain `Bin/logs/2026.05.31-18.09.35/linux-client-shell.log` for MOBA and `Bin/logs/2026.05.31-18.10.15/linux-client-shell.log` for NightMap, and the older 65-second loaded-creep movement logs remain `Bin/logs/2026.05.31-06.41.00/linux-client-shell.log` for MOBA and `Bin/logs/2026.05.31-06.42.13/linux-client-shell.log` for NightMap. The latest map visual defaults remain `baseYaw=-42`, `pitch=56`, and startup `zoom=1.18`; longer runs may end at a higher zoom because the loading preview input/demo path continues to run. The next scheduler/runtime step is therefore replacing the current scripted bot driver with production AI/player command generation, feeding replay loader/playback data and broader production command classes through this now-live packed-command/spawner/creep-behavior/map-command boundary, adding content-driven creep/AI decisions, and replacing the remaining bootstrap PFWorld/game-scene methods with real scene/terrain/map-object ownership.

The intentionally unmaintained UI/runtime files are now mostly the Flash/vendor and encoding-heavy seam rather than basic runtime code:

- `UI/ActionContext.cpp`, which still depends on the stale missing `UIBase.h`
- `UI/FlashContainer2.cpp`, `UI/FlashFontsRender.cpp`, and `UI/FlashInterface.cpp`, which still pull the Tamarin/Flash vendor path and `windows.h`
- `UI/Window.cpp` and `UI/TextMarkup.cpp`, which are still non-UTF8 legacy sources and should stay out until they are worth converting rather than patching around

### Current gameplay frontier

The maintained gameplay probe is now past the old include/case-sensitivity wall, past the previous heavy ability timing slice, and past the latest `BADNODENAME` include-order seam by exposing the existing `"badNodeName"` sentinel through the Linux platform shim under `PW_LINUX_DB_BOOTSTRAP`. The runnable executable is now also past the gameplay-probe source coverage boundary: it links every gameplay-probe PF_GameLogic translation unit except `PFAbilityDataNamemaps.cpp`, which is intentionally left probe-only because the Linux `PFAbilityData.cpp` executable branch already defines the same namemap/constant-resolver functions.

The executable is therefore past basic unit identity, hero object identity, hero factory registration, corpse object registration, movement/attack state registration, unit event vtables, base/concrete AI vtables, target-selector registration, ability/talent metadata identity, dispatch object/factory identity, first dispatch-strike identity, base applicator identity, basic applicator registration identity, force/glyph/predefined-variable helper identity, synchronous async-map-startup identity, generated gameplay-effect identity for `PFAuraEffect`, `PFMinimapEffect`, `PFPlayAnimEffect`, `PFPriestessSignEffect`, `PFEffectRandom`, `PFEffectSwitcher`, `PFInvisibilityEffect`, and `PFUnitSceneObjectModify`, the first real Linux unit ability/base-attack API surface (`PFUnitAbilities`, `PFBaseUnit::UseAbility`, `PFBaseUnit::UseExternalAbility`, and `PFBaseUnit::DoAttack`), controlled spell/status/variable/cooldown applicator chains (`SpellApplicator`, `VariableProxyApplicator`, `StatusApplicator` plus `DispellApplicator`, `RefreshCooldownApplicator`, `AbilityEndApplicator`, `SpellPeriodicallyApplicator`, and `SpellProbabilityApplicator`), controlled fanout applicator behavior (`BounceApplicator`, `ChainLightningApplicator`, and `DelegateDamageApplicator`), controlled modifier/status behavior (`StatModApplicator`, `PermanentStatModApplicator`, `FlagsApplicator`, `ValueApplicator`, `MarkerApplicator`, `TargetsCounterApplicator`, `StatusApplicator`, `InvisibilityApplicator`, `AbilityModApplicator`, and `TauntApplicator`), controlled unit utility/mod/fx applicator behavior (`AbilityUpgradeTechApplicator`, `AddApplicatorDuration`, `ChangeBaseAttackApplicator`, `ChangeHeroStateApplicator`, `ChangeStateApplicator`, `CreateGlyphApplicator`, `CreepBehaviourChangeApplicator`, `DamageReflectApplicator`, `FXApplicator`, `GiveConsumable`, `OnDamageApplicator`, `ResurrectApplicator` factory exposure, `SceneObjectChangeApplicator`, `UIMessageApplicator`, and `VictoryApplicator`), controlled movement-applicator behavior (`TeleportApplicator`, `KickAwayApplicator`, `AttractApplicator`, `ShiftApplicator`, `MoveToApplicator`, `GhostMoveApplicator`, `FlyApplicator`, `ThrowApplicator`, `AttackTargetApplicator`, live unit-target tracking, follow-distance movement, attached/mounted child mirroring, registered moving-unit enumeration, simple pushable/range filtering, straight-segment tracing, blocked-step stopping, high-speed tunnel stopping, bootstrap resolver overlap separation/search, and loaded-world `PFWorld::Step(...)` movement ticking), controlled summon-applicator behavior (`SummonApplicator`, `SummonBehaviourCommon`, master summon-group registration/enumeration/removal, and child applicator activation on created summons), controlled special-applicator behavior (`SpellSwitchApplicator`, `ProgramApplicator`, `ForAllTargetsApplicator`, `NaftaTransferApplicator`, `DealedDamageConverterApplicator`, and Linux sent-damage history scanning), controlled special-control behavior (`InvalidAbilityTargetApplicator`, `MovementControlApplicator`, `ChangeAnimSetApplicator`, `ChangeAnimationApplicator`, `WatchApplicator`, `ScaleControlApplicator`, `DayNightTransitionApplicator`, and `SetTimescaleApplicator`), controlled special object/channeling behavior (`ChannellingApplicator`, `WaitForSpellApplicator`, `EyeApplicator`, `LockTilesApplicator`, `ModifyTerrainApplicator`, `RaiseFlagApplicator`, `PickupChannelingApplicator`, and `DropTreeApplicator` factory exposure), and controlled target-selector/MicroAI behavior (`UnitEnumerator`, area/sector/capsule/wall/nearest/weight/filter/comparing/first/counting/list selectors, relative-unit, point, summon/hero enumeration, unit-place correction, convert-to-land, unit-shift, fix-to-center, between-units, main-building/fountain/shop lookup, attacker targeter enumeration, maximizing/delay/check-condition/not-same-ability/damaging-links selectors, HighlanderA1 selection, selector-backed `BasicMicroAI`/`TargetSelectorMicroAI`/`MultipleTargetSelectorMicroAI`, and selector-driven `TargetsCounterApplicator`) through dispatch-backed/runtime probes.

Loaded-map creep runtime is now past the old inert-spawner boundary too: the Linux executable steps real `PFBaseSpawner` instances from the loaded PFWorld, creates DB-backed lane/neutral creep objects during long create-game smokes, executes enabled `PFCommonCreepBehaviour` from `PFBaseUnit::Step(...)`, scans same-world bootstrap enemy targets, and moves lane common creeps along their DB waypoint lists through `PFBaseMovingUnit::MoveTo(...)`. That proof is still headless and null-render-only; production pathfinding/steering, chase/evade AI parity, combat/state-machine parity, and visible creep client objects remain open.

Linux AI controller runtime is now past the pure scripted-bot-driver boundary for initial combat, talent activation, first controller-driven talent use, first consumable/healing/shop decision wiring, first Linux flag/pickup interaction state wiring, the Linux lane-walk portal hook, first controller-originated live portal/flag/pickup command proof, the former empty Linux tower/war-front methods, and the old simplified Linux route-index helper. Controller-backed bots can acquire nearby enemy units, push `AIAttackUnitState`, send production attack commands, scan real talents, send delayed activation commands, resolve Linux fallback targets for active talents, send production `CmdUseTalent` commands through the same transceiver/replay path, replay those commands deterministically, scan real consumable slots, enter healing/shop states, attempt configured potion purchases through real shop data, search only nearby flagpoles/pickups for interaction states, run the same tower-targeted `TryTeleport()` decision hook as the Windows lane-walk state, use Windows-like route segment progression for lane walking and war-front comparisons, send staged AI-originated `CmdUsePortal`, `CmdRaiseFlag`, and `CmdPickupObject` commands through `PFAIHelper`/the native transceiver path, push `ATTACKINGTOWER` combat moves toward visible enemy structures, retreat from tower danger, and return toward the route war front when too far behind. The remaining AI gap is full Windows-parity decision quality and organic proof coverage: natural live portal/flag/pickup/tower hits from normal controller decisions, live low-health/potion/shop command execution from organic controller decisions, lane/chase/evade strategy beyond current route-following parity, broader state transitions, richer content-driven MicroAI target quality across hero/talent setups, and client-backed visual/effect parity.

The hard work is enabling full pathfinding/voxel behavior beyond controlled registry/selection, production steering/collision resolver behavior beyond the current direct/live-target/follow/attach, segment-trace, blocked-step, tunnel-stop, resolver-overlap/search, and simple push/free-place/tick null-render movement proofs, content-driven summon cloning/template behavior and full summon/pet AI beyond the current controlled `SummonApplicator` creation proof, remaining special-applicator side effects for full real terrain/tree/tile/fog mutations, mounted-hero gameplay beyond the current attached-unit mirror, and full-client-scene paths beyond the controlled eye/lock/terrain/flag/pickup/channeling probes, full aura scans/child applicators beyond controlled probes, content-driven bounce target-selector retargeting beyond the current same-target dispatch proof, chain-lightning AI range scans/visual effects beyond the current first-hit dispatch proof, delegate-damage selector fanout beyond the current owner-fallback split proof, content-driven stat/status/taunt/modifier stacking and interactions beyond controlled probes, real resurrect hero/death-state transitions, content-driven base-attack swapping, scene-object visual mutation, FX/effect ownership, production creep lane-path/evade/chase/AI behavior and rendered creep client objects beyond the current first headless waypoint/target proof, trigger/shield/event processors, content-driven throw/fly trajectories beyond the current selector-backed throw displacement proof, production attack-state/FSM behavior beyond the current Linux direct-attack bridge, production content-driven Highlander/MicroAI/selector scans and conditions beyond controlled bootstrap probes, content-driven damage-history selector/converter semantics beyond the current controlled sent-history scan, active base-attack damage/stat application beyond the current dispatch/applicator proof, client-backed hero/creature/summon/pet rendering, full content-driven consumable/interact behavior beyond the current AI slot scan/shop-state wiring and scripted command proof, production bot AI parity beyond the current controller attack/talent/use-talent/consumable scaffolding, active hero leveling/statistics/respawn/session-event behavior, active minigame/easel behavior, active ability formula/cast-limitation evaluation beyond constant bootstrap formulas, active threaded map loading, and gameplay/applicator-spawned effect ownership without pulling the Windows Direct3D scene path into Linux.

As of the 2026-06-02 chunks, the specific Linux loaded-world enemy-hero damage, death, respawn-timer, resurrection, local kill-award, and first Prime-funded talent level-up proof is closed for the bootstrap/null-render path. Remaining hero respawn/progression references above mean full parity work around session events, statistics, broader talent/consumable interactions, visual state, and production FSM/client-object behavior.

### Current render frontier

The maintained render probe is now past the old mesh implementation wall, the first core scene-object layer, the camera/runtime control slice above full-scene orchestration, the scene DB/object utility slice above that, the render/runtime utility layer around config, immediate rendering, DX resource bookkeeping, the batch-adjacent bounding-volume slice, the generated render DB/resource layer, the first screen-space post-processing slice, the first AOE/water utility slice, the remaining non-test scene slice, and the broad maintained scene/terrain runtime slice. The runnable Linux client now links much of that maintained probe directly: texture management, render surfaces, dxutils buffer helpers, render flag conversion, config manager, device-lost placeholders, immediate renderer, debug renderer, SmartRenderer, DDS/shader/SH utilities, shader constants binder, render resource manager, occlusion-query placeholders, material/runtime foundations, post-processing (`AOERenderer`, blur, bloom, fullscreen FX), mesh/resource wrappers (`mesh`, static/skeletal/instanced mesh, skeleton animation/samplers/wrappers), UI texture cache/atlas, particle FX, water manager/math/mesh/convex utilities, shadow manager/map stubs, debug mesh utilities, FaceFX bootstrap no-op, render-state manager, scene constants, generated shader-definition tables, and now the real Scene/Terrain runtime files around scene objects, camera/control, animation graph, collision, lighting, lightning/trace FX, sound-scene components, terrain grid, grass, nature maps, roads, and terrain cache/core. `Render/configdatabase.cpp` remains probe-only because it still pulls Win32 version/file/device configuration APIs that are not needed by the current Linux executable path.

The next deeper practical blockers are no longer `DiAnGr`, `ModelLoader.cpp`, `TimeCtrlSceneComponent.cpp`, scene/terrain compile coverage, active-effect translation-unit link coverage, the default `EffectsPool::RetrieveFactory(...)` path, standalone scene-component-backed effect construction, basic attached-effect mechanics, probe-owned global active-effect killing, or the verified Aura/Minimap/PlayAnimation/PriestessSign/Random/Switcher/Invisibility/UnitSceneObjectModify gameplay-effect probes. The next frontier is replacing the current null-render/bootstrap maintenance surface with a real Linux renderer backend, expanding from verified probe-owned apply/update/kill/aura/minimap/play-animation/priestess/random/switcher/invisibility/unit-scene-modify mechanics to gameplay-owned active effect spawning, and pushing the actual client loop past compile/link coverage into runtime behavior.

Current exact blockers:

- The generated material/runtime slice (`Render/DebugMaterial.cpp`, `Render/GrassMaterial.cpp`, `Render/MaterialResourceInterface.cpp`) is no longer outside the maintained probe. On Linux it now stays behind null-render/bootstrap-safe wrappers because the generated API in those files has drifted from the current DB/material surface.
- `Render/facefxsystem.cpp` is also now inside the maintained probe. The Linux path uses a no-op bootstrap implementation because this repository does not ship the FaceFX SDK headers (`FxSDK.h`, `FxActor.h`, `FxActorInstance.h`), so the next work should not assume those vendor headers exist locally.
- `Render/mesh.cpp` is no longer outside the maintained probe. It now builds on Linux through the current compatibility layer, so it is no longer an active compile blocker even though it remains legacy render code.
- Outside the probe, the remaining obvious Windows-only render helpers are `Render/VidMemViaDDraw.cpp` and `Render/VidMemViaWMI.cpp`. Those are not meaningful Linux runtime wins; they should stay deprioritized behind the real renderer/runtime path unless a build-system reason forces them earlier.
- `Render/batch_linux_null.cpp` is now legacy scaffolding rather than the active seam. The maintained render probe already compiles the real `batch.cpp`, so future work should prioritize the remaining real runtime files still outside the probe instead of extending the older null batch shim.
- `Render/RenderInterface.cpp` is no longer only a no-op on the Linux executable path, and the Linux shell now constructs and runs a real `Render::DeviceLostWrapper<PF_Render::Interface>` through the canonical `PF_GameLogic/PFRenderInterface.cpp` file. The Linux bootstrap branch there now owns constrained frame setup plus `Render()`, `RenderUI()`, and `Present()` for the shell, and the executable now links maintained null-render infrastructure around it rather than runtime-only shims. It is still a narrow OpenGL bootstrap implementation: the next real render/backend seam is deeper inside that same file and the material/scene stack, where the full Windows body still assumes the broader Direct3D 9 renderer/runtime stack.
- `Scene/DiAnGr.cpp`, `Scene/DiAnGrDb.cpp`, `Scene/DiAnGrEditor.cpp`, `Scene/DiAnGrExtPars.cpp`, `Scene/DiAnGrExtParsAd.cpp`, `Scene/DiAnGrG.cpp`, `Scene/DiAnGrIo2.cpp`, and `Scene/DiAnGrMarker.cpp` are no longer outside the maintained probe. The Linux path now has the missing `DiMath` include alias, safe non-MSVC `strcpy_s` / `sprintf_s` compatibility shims, direct DB type visibility for `DiAnGrExtPars`, and a manual marker-ring teardown in `DiAnGrG.cpp` that avoids `ring::Clear()` ambiguity for refcounted ring elements.
- `Scene/ModelLoader.cpp` is no longer an active blocker. The local `AIGeometry.h` compatibility definition remains in-tree, but the loader now builds through the current mesh/material/runtime API and is maintained as part of the render probe.
- The next scene/runtime frontier is no longer `DiAnGr`, `ModelLoader.cpp`, or `TimeCtrlSceneComponent.cpp`. The maintained probe now covers every non-test, non-`stdafx`, non-deprecated scene translation unit. The remaining work is no longer scene compile coverage; it is moving from the null-render/bootstrap path toward real client runtime integration and a real Linux renderer backend.
- `Terrain/TerrainMaterialCache.cpp`, `Terrain/TerrainHeightManager.cpp`, `Terrain/TerrainLayerManager.cpp`, `Terrain/TerrainTextureCache.cpp`, `Terrain/NatureMapVisual.cpp`, `Terrain/TerrainElement.cpp`, `Terrain/Terrain.cpp`, `Terrain/TerrainCollision.cpp`, `Terrain/TerrainGeometryManager.cpp`, `Terrain/BezierSurface.cpp`, and `Terrain/NatureAttackSpace.cpp` are no longer outside the maintained probe. The render-probe-specific `PW_LINUX_TERRAIN_RUNTIME_PROBE` split in `Terrain.h` now lets the real terrain runtime compile on Linux while gameplay bootstrap keeps the lightweight terrain surface.
- `Terrain/UberElement.cpp` is still outside the maintained probe, but it is explicitly deprecated in-tree (`#error "This file was deprecated 28.05.2008"`) and depends on missing retired headers (`DummyElement.h`), so it should stay out unless some content unexpectedly revives it.
- `Scene/TimeCtrlSceneComponent.cpp` still targets removed DB/runtime APIs on Windows, which is why it remains excluded from the normal source list. For Linux maintenance it now has a probe-only null-render stub, so it is no longer an open portability blocker.

## Current Effort Estimate

Roughly `60-70%` of the hard work still remains before a truly playable native Linux client exists.

Reason:
- bootstrap/startup/content/input groundwork is largely in place
- the renderer/scene/client runtime conversion is still the hardest remaining block

## Maintained Verification Commands

Build the gameplay portability slice:

```bash
cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxGameplayProbe -j4
```

Build the client runtime portability slice:

```bash
cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClientRuntimeProbe -j4
```

Build the native Linux client shell:

```bash
cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxClient -j4
```

Build the maintained render portability slice:

```bash
cmake --build /tmp/primeworld-linux-bootstrap --target PrimeWorldLinuxRenderProbe -j4
```

Run the latest verified Linux shell smoke test:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --seconds 3 --diagnostics-overlay
```

Run the latest verified deeper custom-lobby/loading live-map hero death/respawn/progression smoke tests from `pw/branches/r1117/Bin` so the native replay-capture file is written under `Bin/logs`:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map MOBA --bootstrap-create-game --seconds 90
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --map NightMap --hero plane --bootstrap-create-game --seconds 90
```

Run the latest verified starting UI smoke:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8
```

Run the latest verified synthetic Start Session click smoke from `pw/branches/r1117/Bin`; it should finish with `lastHit=start-session-button` and `lastAction=join-game`:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --bootstrap-click-base 340,965
```

Run the latest verified synthetic starting-lobby multi-click smoke from `pw/branches/r1117/Bin`; it should select Spectate, reduce the custom-game player count to `2`, and then route Start Session to `spectate-game`:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --seconds 8 --bootstrap-click-script '1090,425;69,899;340,965'
```

Run the latest verified Linux-generated replay-input smoke after creating `Bin/logs/linux-bootstrap-replay.pwrp` with a create-game run. Use an absolute replay path unless the current working directory is `pw/branches/r1117/Bin`, because replay detection ignores positional paths that do not exist relative to the process cwd:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --replay-speed 2 --seconds 12 /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp
```

Run the latest verified Linux replay-input realtime playback smoke; this should derive the effective pace from the replay header `stepLength` and finish with `Final live replay input ... valid=yes ... pacing=yes/100 ... error=none` for the current generated replay:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --replay-realtime --seconds 12 /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp
```

Run the latest verified Linux replay-input explicit pacing smoke; this intentionally uses a short runtime so it should finish with `Final live replay input ... valid=no ... pacing=yes/500 ... error=incomplete` while still drawing the replay-control panel:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --replay-pace-ms 500 --seconds 4 /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp
```

Run the latest verified Linux replay-input pause/manual-step smoke; this should intentionally finish with `Final live replay input ... valid=no ... error=paused` because only five replay segments are allowed through:

```bash
/tmp/primeworld-linux-bootstrap/PrimeWorldLinuxClient --root /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117 --locale en-US --replay-paused --replay-steps 5 --seconds 6 /home/vitaly/p/Prime-World--codex-of-andrei/pw/branches/r1117/Bin/logs/linux-bootstrap-replay.pwrp
```

For longer creep-wave regression checks, keep the same commands but use `--seconds 65`.

## Working Rules For Future Updates

- Update this file after each substantial chunk, not after every tiny edit.
- Record the latest verified build/run commands and latest verified log path.
- When a blocker changes, move it here immediately so the next session does not need to reconstruct context from chat.
- Prefer moving the maintained Linux gameplay probe deeper before touching risky shared code blindly.
- Use this document as the source of truth for port status, not chat memory.
