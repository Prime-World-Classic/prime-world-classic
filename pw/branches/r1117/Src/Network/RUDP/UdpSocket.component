sources = [
  'UdpAddr.h',
  'UdpAddr.cpp',
  'UdpSocket.h',
  'UdpSocket.cpp',
]

components = [ 
  'Vendor/Libc',
  'System/Pointers',
]

# Linux doesn't need ws2_32 (sockets are in libc)
if platform == 'win32':
  libDependencies = [ 'ws2_32.lib' ]
